{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"cycquery","text":"<p><code>cycquery</code> is a tool for querying relational databases using a simple Python API. It is specifically developed to query Electronic Health Record (EHR) databases. The tool is a wrapper around SQLAlchemy and can be used to write SQL-like queries in Python, including joins, conditions, groupby aggregation and many more.</p>"},{"location":"#getting-started","title":"Getting Started","text":""},{"location":"#installing-cycquery-using-pip","title":"Installing cycquery using pip","text":"<pre><code>python3 -m pip install cycquery\n</code></pre>"},{"location":"#query-postgresql-database","title":"Query postgresql database","text":"<pre><code>from cycquery import DatasetQuerier\nimport cycquery.ops as qo\n\n\nquerier = DatasetQuerier(\n    dbms=\"postgresql\",\n    port=5432,\n    host=\"localhost\",\n    database=\"dbname\",\n    user=\"usename\",\n    password=\"password\",\n)\n# List all tables.\nquerier.list_tables()\n\n# Get some table.\ntable = querier.schema.sometable()\n# Filter based on some condition (e.g. substring match).\ntable = table.ops(qo.ConditionSubstring(\"col1\", \"substr\"))\n# Run query to get data as a pandas dataframe.\ndf = table.run()\n\n# Create a sequential list of operations to perform on the query.\nops = qo.Sequential(\n    qo.ConditionIn(\"col2\", [1, 2]),\n    qo.DropNulls(\"col3\"),\n    qo.Distinct(\"col1\")\n)\ntable = table.ops(ops)\n# Run query to get data as a pandas dataframe.\ndf = table.run()\n</code></pre> <p>Please check out the user guide for more detailed information on setting up a development environment, building documentation, and contributing. For API reference, see the API Reference page.</p>"},{"location":"api/","title":"API Reference","text":""},{"location":"api/#top-level-module","title":"Top Level Module","text":""},{"location":"api/#cycquery","title":"cycquery","text":"<p>The <code>query</code> API provides classes to query EHR databases.</p>"},{"location":"api/#cycquery.DatasetQuerier","title":"DatasetQuerier","text":"<p>Base class to query EHR datasets.</p> <p>Attributes:</p> Name Type Description <code>db</code> <p>ORM Database used to run queries.</p> <p>Parameters:</p> Name Type Description Default <code>dbms</code> <code>str</code> <p>The database management system type (e.g., 'postgresql', 'mysql', 'sqlite').</p> required <code>user</code> <code>str</code> <p>The username for the database, by default empty. Not used for SQLite.</p> <code>''</code> <code>password</code> <code>str</code> <p>The password for the database, by default empty. Not used for SQLite.</p> <code>''</code> <code>host</code> <code>str</code> <p>The host address of the database, by default empty. Not used for SQLite.</p> <code>''</code> <code>port</code> <code>int</code> <p>The port number for the database, by default None. Not used for SQLite.</p> <code>None</code> <code>database</code> <code>str</code> <p>The name of the database or the path to the database file (for SQLite), by default empty.</p> <code>''</code> <code>schemas</code> <code>Union[str, List[str]]</code> <p>The schema(s) to query, by default None.</p> <code>None</code> Notes <p>This class is intended to be subclassed to provide methods for querying tables in the database. This class automatically creates methods for querying tables in the database. The methods are named after the schema and table name, i.e. <code>self.schema_name.table_name()</code>. The methods are created when the class is instantiated. The subclass can provide custom methods for querying tables in the database which can build on the methods created by this class.</p> Source code in <code>cycquery/base.py</code> <pre><code>class DatasetQuerier:\n    \"\"\"Base class to query EHR datasets.\n\n    Attributes\n    ----------\n    db\n        ORM Database used to run queries.\n\n    Parameters\n    ----------\n    dbms : str\n        The database management system type (e.g., 'postgresql', 'mysql', 'sqlite').\n    user : str, optional\n        The username for the database, by default empty. Not used for SQLite.\n    password : str, optional\n        The password for the database, by default empty. Not used for SQLite.\n    host : str, optional\n        The host address of the database, by default empty. Not used for SQLite.\n    port : int, optional\n        The port number for the database, by default None. Not used for SQLite.\n    database : str, optional\n        The name of the database or the path to the database file (for SQLite),\n        by default empty.\n    schemas : Union[str, List[str]], optional\n        The schema(s) to query, by default None.\n\n    Notes\n    -----\n    This class is intended to be subclassed to provide methods for querying tables in\n    the database. This class automatically creates methods for querying tables in the\n    database. The methods are named after the schema and table name, i.e.\n    `self.schema_name.table_name()`. The methods are created when the class is\n    instantiated. The subclass can provide custom methods for querying tables in the\n    database which can build on the methods created by this class.\n\n    \"\"\"\n\n    def __init__(\n        self,\n        dbms: str,\n        user: str = \"\",\n        password: str = \"\",\n        host: str = \"\",\n        port: int | None = None,\n        database: str = \"\",\n        schemas: list[str] | None = None,\n    ) -&gt; None:\n        \"\"\"Initialize the querier.\"\"\"\n        config = DatasetQuerierConfig(\n            database=database,\n            user=user,\n            password=password,\n            dbms=dbms,\n            host=host,\n            port=port,\n            schemas=schemas,\n        )\n        self.db = Database(config)\n        if not self.db.is_connected:\n            LOGGER.error(\"Database is not connected, cannot run queries.\")\n            return\n        self._setup_table_methods()\n\n    def list_schemas(self) -&gt; list[str]:\n        \"\"\"List schemas in the database to query.\n\n        Returns\n        -------\n        List[str]\n            List of schema names.\n\n        \"\"\"\n        all_schemas = list(self.db.inspector.get_schema_names())\n        if self.db.config.schemas is None:\n            return all_schemas\n        if isinstance(self.db.config.schemas, str):\n            schemas_to_use = [self.db.config.schemas]\n        else:\n            schemas_to_use = self.db.config.schemas\n\n        return [schema for schema in all_schemas if schema in schemas_to_use]\n\n    def list_tables(self, schema_name: str | None = None) -&gt; list[str]:\n        \"\"\"List table methods that can be queried using the database.\n\n        Parameters\n        ----------\n        schema_name\n            Name of schema in the database.\n\n        Returns\n        -------\n        List[str]\n            List of table names.\n\n        \"\"\"\n        if schema_name:\n            table_names = []\n            for table in self.db.list_tables():\n                schema_name_, _ = table.split(\".\")\n                if schema_name_ == schema_name:\n                    table_names.append(table)\n        else:\n            table_names = self.db.list_tables()\n\n        return table_names\n\n    def list_columns(self, schema_name: str, table_name: str) -&gt; list[str]:\n        \"\"\"List columns in a table.\n\n        Parameters\n        ----------\n        schema_name\n            Name of schema in the database.\n        table_name\n            Name of table in the database.\n\n        Returns\n        -------\n        List[str]\n            List of column names.\n\n        \"\"\"\n        return list(\n            getattr(getattr(self.db, schema_name), table_name).data_.columns.keys(),\n        )\n\n    def list_custom_tables(self) -&gt; list[str]:\n        \"\"\"List custom tables methods provided by the dataset API.\n\n        Returns\n        -------\n        List[str]\n            List of custom table names.\n\n        \"\"\"\n        method_list = dir(self)\n        custom_tables = []\n        for method in method_list:\n            if (\n                not method.startswith(\n                    \"__\",\n                )\n                and not method.startswith(\"_\")\n                and method not in self.list_schemas()\n                and not method.startswith(\"list_\")\n                and not method.startswith(\"get_\")\n                and method not in [\"db\"]\n            ):\n                custom_tables.append(method)\n\n        return custom_tables\n\n    def get_table(\n        self,\n        schema_name: str,\n        table_name: str,\n        cast_timestamp_cols: bool = True,\n    ) -&gt; Subquery:\n        \"\"\"Get a table and possibly map columns to have standard names.\n\n        Standardizing column names allows for columns to be\n        recognized in downstream processing.\n\n        Parameters\n        ----------\n        schema_name\n            Name of schema in the database.\n        table_name\n            Name of table in the database.\n        cast_timestamp_cols\n            Whether to cast timestamp columns to datetime.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Table with mapped columns.\n\n        \"\"\"\n        table = _create_get_table_lambdafn(schema_name, table_name)(self.db).data_\n\n        if cast_timestamp_cols:\n            table = _cast_timestamp_cols(table)\n\n        return _to_subquery(table)\n\n    def _template_table_method(\n        self,\n        schema_name: str,\n        table_name: str,\n    ) -&gt; QueryInterface:\n        \"\"\"Template method for table methods.\n\n        Parameters\n        ----------\n        schema_name\n            Name of schema in the database.\n        table_name\n            Name of table in the database.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            A query interface object.\n\n        \"\"\"\n        table = getattr(getattr(self.db, schema_name), table_name).data_\n        table = _to_subquery(table)\n\n        return QueryInterface(self.db, table)\n\n    def _setup_table_methods(self) -&gt; None:\n        \"\"\"Add table methods.\n\n        This method adds methods to the querier class that allow querying of tables in\n        the database. The methods are named after the table names.\n\n        \"\"\"\n        schemas = self.list_schemas()\n        meta: dict[str, MetaData] = {}\n        for schema_name in schemas:\n            metadata = MetaData(schema=schema_name)\n            with self.db.engine.connect() as conn:\n                metadata.reflect(bind=conn)\n            meta[schema_name] = metadata\n            schema = DBSchema(schema_name, meta[schema_name])\n            for table_name in meta[schema_name].tables:\n                setattr(\n                    schema,\n                    get_attr_name(table_name),\n                    partial(\n                        self._template_table_method,\n                        schema_name=schema_name,\n                        table_name=get_attr_name(table_name),\n                    ),\n                )\n            setattr(self, schema_name, schema)\n</code></pre>"},{"location":"api/#cycquery.DatasetQuerier.__init__","title":"__init__","text":"<pre><code>__init__(\n    dbms,\n    user=\"\",\n    password=\"\",\n    host=\"\",\n    port=None,\n    database=\"\",\n    schemas=None,\n)\n</code></pre> <p>Initialize the querier.</p> Source code in <code>cycquery/base.py</code> <pre><code>def __init__(\n    self,\n    dbms: str,\n    user: str = \"\",\n    password: str = \"\",\n    host: str = \"\",\n    port: int | None = None,\n    database: str = \"\",\n    schemas: list[str] | None = None,\n) -&gt; None:\n    \"\"\"Initialize the querier.\"\"\"\n    config = DatasetQuerierConfig(\n        database=database,\n        user=user,\n        password=password,\n        dbms=dbms,\n        host=host,\n        port=port,\n        schemas=schemas,\n    )\n    self.db = Database(config)\n    if not self.db.is_connected:\n        LOGGER.error(\"Database is not connected, cannot run queries.\")\n        return\n    self._setup_table_methods()\n</code></pre>"},{"location":"api/#cycquery.DatasetQuerier.list_schemas","title":"list_schemas","text":"<pre><code>list_schemas()\n</code></pre> <p>List schemas in the database to query.</p> <p>Returns:</p> Type Description <code>List[str]</code> <p>List of schema names.</p> Source code in <code>cycquery/base.py</code> <pre><code>def list_schemas(self) -&gt; list[str]:\n    \"\"\"List schemas in the database to query.\n\n    Returns\n    -------\n    List[str]\n        List of schema names.\n\n    \"\"\"\n    all_schemas = list(self.db.inspector.get_schema_names())\n    if self.db.config.schemas is None:\n        return all_schemas\n    if isinstance(self.db.config.schemas, str):\n        schemas_to_use = [self.db.config.schemas]\n    else:\n        schemas_to_use = self.db.config.schemas\n\n    return [schema for schema in all_schemas if schema in schemas_to_use]\n</code></pre>"},{"location":"api/#cycquery.DatasetQuerier.list_tables","title":"list_tables","text":"<pre><code>list_tables(schema_name=None)\n</code></pre> <p>List table methods that can be queried using the database.</p> <p>Parameters:</p> Name Type Description Default <code>schema_name</code> <code>str | None</code> <p>Name of schema in the database.</p> <code>None</code> <p>Returns:</p> Type Description <code>List[str]</code> <p>List of table names.</p> Source code in <code>cycquery/base.py</code> <pre><code>def list_tables(self, schema_name: str | None = None) -&gt; list[str]:\n    \"\"\"List table methods that can be queried using the database.\n\n    Parameters\n    ----------\n    schema_name\n        Name of schema in the database.\n\n    Returns\n    -------\n    List[str]\n        List of table names.\n\n    \"\"\"\n    if schema_name:\n        table_names = []\n        for table in self.db.list_tables():\n            schema_name_, _ = table.split(\".\")\n            if schema_name_ == schema_name:\n                table_names.append(table)\n    else:\n        table_names = self.db.list_tables()\n\n    return table_names\n</code></pre>"},{"location":"api/#cycquery.DatasetQuerier.list_columns","title":"list_columns","text":"<pre><code>list_columns(schema_name, table_name)\n</code></pre> <p>List columns in a table.</p> <p>Parameters:</p> Name Type Description Default <code>schema_name</code> <code>str</code> <p>Name of schema in the database.</p> required <code>table_name</code> <code>str</code> <p>Name of table in the database.</p> required <p>Returns:</p> Type Description <code>List[str]</code> <p>List of column names.</p> Source code in <code>cycquery/base.py</code> <pre><code>def list_columns(self, schema_name: str, table_name: str) -&gt; list[str]:\n    \"\"\"List columns in a table.\n\n    Parameters\n    ----------\n    schema_name\n        Name of schema in the database.\n    table_name\n        Name of table in the database.\n\n    Returns\n    -------\n    List[str]\n        List of column names.\n\n    \"\"\"\n    return list(\n        getattr(getattr(self.db, schema_name), table_name).data_.columns.keys(),\n    )\n</code></pre>"},{"location":"api/#cycquery.DatasetQuerier.list_custom_tables","title":"list_custom_tables","text":"<pre><code>list_custom_tables()\n</code></pre> <p>List custom tables methods provided by the dataset API.</p> <p>Returns:</p> Type Description <code>List[str]</code> <p>List of custom table names.</p> Source code in <code>cycquery/base.py</code> <pre><code>def list_custom_tables(self) -&gt; list[str]:\n    \"\"\"List custom tables methods provided by the dataset API.\n\n    Returns\n    -------\n    List[str]\n        List of custom table names.\n\n    \"\"\"\n    method_list = dir(self)\n    custom_tables = []\n    for method in method_list:\n        if (\n            not method.startswith(\n                \"__\",\n            )\n            and not method.startswith(\"_\")\n            and method not in self.list_schemas()\n            and not method.startswith(\"list_\")\n            and not method.startswith(\"get_\")\n            and method not in [\"db\"]\n        ):\n            custom_tables.append(method)\n\n    return custom_tables\n</code></pre>"},{"location":"api/#cycquery.DatasetQuerier.get_table","title":"get_table","text":"<pre><code>get_table(\n    schema_name, table_name, cast_timestamp_cols=True\n)\n</code></pre> <p>Get a table and possibly map columns to have standard names.</p> <p>Standardizing column names allows for columns to be recognized in downstream processing.</p> <p>Parameters:</p> Name Type Description Default <code>schema_name</code> <code>str</code> <p>Name of schema in the database.</p> required <code>table_name</code> <code>str</code> <p>Name of table in the database.</p> required <code>cast_timestamp_cols</code> <code>bool</code> <p>Whether to cast timestamp columns to datetime.</p> <code>True</code> <p>Returns:</p> Type Description <code>Subquery</code> <p>Table with mapped columns.</p> Source code in <code>cycquery/base.py</code> <pre><code>def get_table(\n    self,\n    schema_name: str,\n    table_name: str,\n    cast_timestamp_cols: bool = True,\n) -&gt; Subquery:\n    \"\"\"Get a table and possibly map columns to have standard names.\n\n    Standardizing column names allows for columns to be\n    recognized in downstream processing.\n\n    Parameters\n    ----------\n    schema_name\n        Name of schema in the database.\n    table_name\n        Name of table in the database.\n    cast_timestamp_cols\n        Whether to cast timestamp columns to datetime.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Table with mapped columns.\n\n    \"\"\"\n    table = _create_get_table_lambdafn(schema_name, table_name)(self.db).data_\n\n    if cast_timestamp_cols:\n        table = _cast_timestamp_cols(table)\n\n    return _to_subquery(table)\n</code></pre>"},{"location":"api/#cycquery.EICUQuerier","title":"EICUQuerier","text":"<p>               Bases: <code>DatasetQuerier</code></p> <p>EICU dataset querier.</p> Source code in <code>cycquery/eicu.py</code> <pre><code>class EICUQuerier(DatasetQuerier):\n    \"\"\"EICU dataset querier.\"\"\"\n</code></pre>"},{"location":"api/#cycquery.GEMINIQuerier","title":"GEMINIQuerier","text":"<p>               Bases: <code>DatasetQuerier</code></p> <p>GEMINI dataset querier.</p> Source code in <code>cycquery/gemini.py</code> <pre><code>class GEMINIQuerier(DatasetQuerier):\n    \"\"\"GEMINI dataset querier.\"\"\"\n\n    def ip_admin(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query GEMINI patient encounters.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed query, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(\"public\", \"ip_administrative\")\n\n        # Possibly cast string representations to timestamps\n        table = qo.Cast([\"admit_date_time\", \"discharge_date_time\"], \"timestamp\")(table)\n\n        # Get the discharge disposition code descriptions\n        lookup_table = self.get_table(\"public\", \"lookup_ip_administrative\")\n        lookup_table = qo.ConditionEquals(\"variable\", \"discharge_disposition\")(\n            lookup_table,\n        )\n        table = qo.Join(\n            lookup_table,\n            on=(\"discharge_disposition\", \"value\"),\n            on_to_type=\"int\",\n            join_table_cols=\"description\",\n            isouter=True,\n        )(table)\n        table = qo.Rename({\"description\": \"discharge_description\"})(table)\n        table = qo.Drop(\"value\")(table)\n\n        return QueryInterface(self.db, table)\n\n    def diagnoses(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query diagnosis data.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed table, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(\"public\", \"diagnosis\")\n\n        lookup_table = self.get_table(\"public\", \"lookup_diagnosis\")\n        lookup_table = qo.ConditionEquals(\"variable\", \"diagnosis_type\")(lookup_table)\n        table = qo.Join(\n            lookup_table,\n            on=(\"diagnosis_type\", \"value\"),\n            join_table_cols=\"description\",\n            isouter=True,\n        )(table)\n        table = qo.Drop(\"value\")(table)\n        table = qo.Rename({\"description\": \"diagnosis_type_description\"})(table)\n        table = qo.ReorderAfter(\"diagnosis_type_description\", \"diagnosis_type\")(table)\n\n        # Trim whitespace from ICD codes.\n        table = qo.Trim(\"diagnosis_code\")(table)\n\n        return QueryInterface(self.db, table)\n\n    def room_transfer(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query room transfer data.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed table, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(\"public\", \"room_transfer\")\n\n        # Join with lookup to get transfer description.\n        lookup_table = self.get_table(\"public\", \"lookup_room_transfer\")\n        lookup_table = qo.ConditionEquals(\"variable\", \"medical_service\")(lookup_table)\n\n        table = qo.Join(\n            lookup_table,\n            on=(\"medical_service\", \"value\"),\n            join_table_cols=\"description\",\n            isouter=True,\n        )(table)\n        table = qo.Rename({\"description\": \"transfer_description\"})(table)\n\n        return QueryInterface(self.db, table)\n\n    def care_units(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query care unit data, fetches transfer info from multiple tables.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed table, wrapped in an interface object.\n\n        \"\"\"\n        filter_care_unit_cols = qo.Keep(\n            [\n                \"genc_id\",\n                \"admit\",\n                \"discharge\",\n                CARE_UNIT,\n            ],\n        )\n\n        # In-patient table.\n        ip_table = self.get_table(\"public\", \"ip_administrative\")\n        ip_table = qo.Rename(\n            {\n                \"admit_date_time\": \"admit\",\n                \"discharge_date_time\": \"discharge\",\n            },\n        )(ip_table)\n        ip_table = qo.Literal(\"IP\", CARE_UNIT)(ip_table)\n        ip_table = filter_care_unit_cols(ip_table)\n\n        # Special care unit table.\n        scu_table = self.get_table(\"public\", \"ip_scu\")\n        scu_table = qo.Rename(\n            {\n                \"scu_admit_date_time\": \"admit\",\n                \"scu_discharge_date_time\": \"discharge\",\n            },\n        )(scu_table)\n        scu_table = qo.Literal(\"SCU\", CARE_UNIT)(scu_table)\n        scu_table = filter_care_unit_cols(scu_table)\n\n        # Emergency room/department table.\n        er_table = self.get_table(\"public\", \"er_administrative\")\n        er_table = qo.Rename(\n            {\n                \"er_admit_timestamp\": \"admit\",\n                \"er_discharge_timestamp\": \"discharge\",\n            },\n        )(er_table)\n        er_table = qo.Literal(\"ER\", CARE_UNIT)(er_table)\n        er_table = filter_care_unit_cols(er_table)\n\n        # Room transfer table.\n        rt_table = self.get_table(\"public\", \"room_transfer\")\n        rt_table = qo.Rename(\n            {\n                \"checkin_date_time\": \"admit\",\n                \"checkout_date_time\": \"discharge\",\n            },\n        )(rt_table)\n        rt_table = qo.Rename({\"transfer_description\": CARE_UNIT})(rt_table)\n        rt_table = filter_care_unit_cols(rt_table)\n\n        # Combine.\n        table = union_all(\n            select(er_table),\n            select(scu_table),\n            select(ip_table),\n            select(rt_table),\n        ).subquery()\n\n        return QueryInterface(self.db, table)\n\n    def imaging(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query imaging reports data.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed table, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(\"public\", \"imaging\")\n\n        # Get imaging test description\n        lookup_table = self.get_table(\"public\", \"lookup_imaging\")\n        lookup_table = qo.ConditionEquals(\"variable\", \"imaging_test_name_mapped\")(\n            lookup_table,\n        )\n\n        table = qo.Join(\n            lookup_table,\n            on=(\"imaging_test_name_mapped\", \"value\"),\n            on_to_type=\"str\",\n            join_table_cols=\"description\",\n        )(table)\n        table = qo.Drop(\"value\")(table)\n        table = qo.Rename({\"description\": \"imaging_test_description\"})(table)\n        table = qo.ReorderAfter(\"imaging_test_description\", \"imaging_test_name_mapped\")(\n            table,\n        )\n\n        return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.GEMINIQuerier.ip_admin","title":"ip_admin","text":"<pre><code>ip_admin()\n</code></pre> <p>Query GEMINI patient encounters.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed query, wrapped in an interface object.</p> Source code in <code>cycquery/gemini.py</code> <pre><code>def ip_admin(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query GEMINI patient encounters.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed query, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(\"public\", \"ip_administrative\")\n\n    # Possibly cast string representations to timestamps\n    table = qo.Cast([\"admit_date_time\", \"discharge_date_time\"], \"timestamp\")(table)\n\n    # Get the discharge disposition code descriptions\n    lookup_table = self.get_table(\"public\", \"lookup_ip_administrative\")\n    lookup_table = qo.ConditionEquals(\"variable\", \"discharge_disposition\")(\n        lookup_table,\n    )\n    table = qo.Join(\n        lookup_table,\n        on=(\"discharge_disposition\", \"value\"),\n        on_to_type=\"int\",\n        join_table_cols=\"description\",\n        isouter=True,\n    )(table)\n    table = qo.Rename({\"description\": \"discharge_description\"})(table)\n    table = qo.Drop(\"value\")(table)\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.GEMINIQuerier.diagnoses","title":"diagnoses","text":"<pre><code>diagnoses()\n</code></pre> <p>Query diagnosis data.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed table, wrapped in an interface object.</p> Source code in <code>cycquery/gemini.py</code> <pre><code>def diagnoses(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query diagnosis data.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed table, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(\"public\", \"diagnosis\")\n\n    lookup_table = self.get_table(\"public\", \"lookup_diagnosis\")\n    lookup_table = qo.ConditionEquals(\"variable\", \"diagnosis_type\")(lookup_table)\n    table = qo.Join(\n        lookup_table,\n        on=(\"diagnosis_type\", \"value\"),\n        join_table_cols=\"description\",\n        isouter=True,\n    )(table)\n    table = qo.Drop(\"value\")(table)\n    table = qo.Rename({\"description\": \"diagnosis_type_description\"})(table)\n    table = qo.ReorderAfter(\"diagnosis_type_description\", \"diagnosis_type\")(table)\n\n    # Trim whitespace from ICD codes.\n    table = qo.Trim(\"diagnosis_code\")(table)\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.GEMINIQuerier.room_transfer","title":"room_transfer","text":"<pre><code>room_transfer()\n</code></pre> <p>Query room transfer data.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed table, wrapped in an interface object.</p> Source code in <code>cycquery/gemini.py</code> <pre><code>def room_transfer(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query room transfer data.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed table, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(\"public\", \"room_transfer\")\n\n    # Join with lookup to get transfer description.\n    lookup_table = self.get_table(\"public\", \"lookup_room_transfer\")\n    lookup_table = qo.ConditionEquals(\"variable\", \"medical_service\")(lookup_table)\n\n    table = qo.Join(\n        lookup_table,\n        on=(\"medical_service\", \"value\"),\n        join_table_cols=\"description\",\n        isouter=True,\n    )(table)\n    table = qo.Rename({\"description\": \"transfer_description\"})(table)\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.GEMINIQuerier.care_units","title":"care_units","text":"<pre><code>care_units()\n</code></pre> <p>Query care unit data, fetches transfer info from multiple tables.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed table, wrapped in an interface object.</p> Source code in <code>cycquery/gemini.py</code> <pre><code>def care_units(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query care unit data, fetches transfer info from multiple tables.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed table, wrapped in an interface object.\n\n    \"\"\"\n    filter_care_unit_cols = qo.Keep(\n        [\n            \"genc_id\",\n            \"admit\",\n            \"discharge\",\n            CARE_UNIT,\n        ],\n    )\n\n    # In-patient table.\n    ip_table = self.get_table(\"public\", \"ip_administrative\")\n    ip_table = qo.Rename(\n        {\n            \"admit_date_time\": \"admit\",\n            \"discharge_date_time\": \"discharge\",\n        },\n    )(ip_table)\n    ip_table = qo.Literal(\"IP\", CARE_UNIT)(ip_table)\n    ip_table = filter_care_unit_cols(ip_table)\n\n    # Special care unit table.\n    scu_table = self.get_table(\"public\", \"ip_scu\")\n    scu_table = qo.Rename(\n        {\n            \"scu_admit_date_time\": \"admit\",\n            \"scu_discharge_date_time\": \"discharge\",\n        },\n    )(scu_table)\n    scu_table = qo.Literal(\"SCU\", CARE_UNIT)(scu_table)\n    scu_table = filter_care_unit_cols(scu_table)\n\n    # Emergency room/department table.\n    er_table = self.get_table(\"public\", \"er_administrative\")\n    er_table = qo.Rename(\n        {\n            \"er_admit_timestamp\": \"admit\",\n            \"er_discharge_timestamp\": \"discharge\",\n        },\n    )(er_table)\n    er_table = qo.Literal(\"ER\", CARE_UNIT)(er_table)\n    er_table = filter_care_unit_cols(er_table)\n\n    # Room transfer table.\n    rt_table = self.get_table(\"public\", \"room_transfer\")\n    rt_table = qo.Rename(\n        {\n            \"checkin_date_time\": \"admit\",\n            \"checkout_date_time\": \"discharge\",\n        },\n    )(rt_table)\n    rt_table = qo.Rename({\"transfer_description\": CARE_UNIT})(rt_table)\n    rt_table = filter_care_unit_cols(rt_table)\n\n    # Combine.\n    table = union_all(\n        select(er_table),\n        select(scu_table),\n        select(ip_table),\n        select(rt_table),\n    ).subquery()\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.GEMINIQuerier.imaging","title":"imaging","text":"<pre><code>imaging()\n</code></pre> <p>Query imaging reports data.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed table, wrapped in an interface object.</p> Source code in <code>cycquery/gemini.py</code> <pre><code>def imaging(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query imaging reports data.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed table, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(\"public\", \"imaging\")\n\n    # Get imaging test description\n    lookup_table = self.get_table(\"public\", \"lookup_imaging\")\n    lookup_table = qo.ConditionEquals(\"variable\", \"imaging_test_name_mapped\")(\n        lookup_table,\n    )\n\n    table = qo.Join(\n        lookup_table,\n        on=(\"imaging_test_name_mapped\", \"value\"),\n        on_to_type=\"str\",\n        join_table_cols=\"description\",\n    )(table)\n    table = qo.Drop(\"value\")(table)\n    table = qo.Rename({\"description\": \"imaging_test_description\"})(table)\n    table = qo.ReorderAfter(\"imaging_test_description\", \"imaging_test_name_mapped\")(\n        table,\n    )\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.MIMICIIIQuerier","title":"MIMICIIIQuerier","text":"<p>               Bases: <code>DatasetQuerier</code></p> <p>MIMIC-III dataset querier.</p> Source code in <code>cycquery/mimiciii.py</code> <pre><code>class MIMICIIIQuerier(DatasetQuerier):\n    \"\"\"MIMIC-III dataset querier.\"\"\"\n\n    def diagnoses(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query MIMICIII diagnosis data.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed query, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(\"mimiciii\", \"diagnoses_icd\")\n\n        # Join with diagnoses dimension table.\n        table = qo.Join(\n            join_table=self.get_table(\"mimiciii\", \"d_icd_diagnoses\"),\n            on=[\"icd9_code\"],\n            on_to_type=[\"str\"],\n        )(table)\n\n        return QueryInterface(self.db, table)\n\n    def labevents(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query MIMICIII labevents data.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed query, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(\"mimiciii\", \"labevents\")\n\n        # Join with lab dimension table.\n        table = qo.Join(\n            join_table=self.get_table(\"mimiciii\", \"d_labitems\"),\n            on=[\"itemid\"],\n            on_to_type=[\"str\"],\n        )(table)\n\n        return QueryInterface(self.db, table)\n\n    def chartevents(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query MIMICIII chartevents data.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed query, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(\"mimiciii\", \"chartevents\")\n\n        # Join with dimension table.\n        table = qo.Join(\n            join_table=self.get_table(\"mimiciii\", \"d_items\"),\n            on=[\"itemid\"],\n            on_to_type=[\"str\"],\n        )(table)\n\n        return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.MIMICIIIQuerier.diagnoses","title":"diagnoses","text":"<pre><code>diagnoses()\n</code></pre> <p>Query MIMICIII diagnosis data.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed query, wrapped in an interface object.</p> Source code in <code>cycquery/mimiciii.py</code> <pre><code>def diagnoses(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query MIMICIII diagnosis data.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed query, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(\"mimiciii\", \"diagnoses_icd\")\n\n    # Join with diagnoses dimension table.\n    table = qo.Join(\n        join_table=self.get_table(\"mimiciii\", \"d_icd_diagnoses\"),\n        on=[\"icd9_code\"],\n        on_to_type=[\"str\"],\n    )(table)\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.MIMICIIIQuerier.labevents","title":"labevents","text":"<pre><code>labevents()\n</code></pre> <p>Query MIMICIII labevents data.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed query, wrapped in an interface object.</p> Source code in <code>cycquery/mimiciii.py</code> <pre><code>def labevents(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query MIMICIII labevents data.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed query, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(\"mimiciii\", \"labevents\")\n\n    # Join with lab dimension table.\n    table = qo.Join(\n        join_table=self.get_table(\"mimiciii\", \"d_labitems\"),\n        on=[\"itemid\"],\n        on_to_type=[\"str\"],\n    )(table)\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.MIMICIIIQuerier.chartevents","title":"chartevents","text":"<pre><code>chartevents()\n</code></pre> <p>Query MIMICIII chartevents data.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed query, wrapped in an interface object.</p> Source code in <code>cycquery/mimiciii.py</code> <pre><code>def chartevents(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query MIMICIII chartevents data.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed query, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(\"mimiciii\", \"chartevents\")\n\n    # Join with dimension table.\n    table = qo.Join(\n        join_table=self.get_table(\"mimiciii\", \"d_items\"),\n        on=[\"itemid\"],\n        on_to_type=[\"str\"],\n    )(table)\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.MIMICIVQuerier","title":"MIMICIVQuerier","text":"<p>               Bases: <code>DatasetQuerier</code></p> <p>MIMICIV dataset querier.</p> Source code in <code>cycquery/mimiciv.py</code> <pre><code>class MIMICIVQuerier(DatasetQuerier):\n    \"\"\"MIMICIV dataset querier.\"\"\"\n\n    def patients(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query MIMIC patient data.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed query, wrapped in an interface object.\n\n        Notes\n        -----\n        The function infers the approximate year a patient received care, using the\n        `anchor_year` and `anchor_year_group` columns. The `join` and `ops` supplied\n        are applied after the approximate year is inferred. `dod` is\n        adjusted based on the inferred approximate year of care.\n\n        \"\"\"\n        table = self.get_table(\"mimiciv_hosp\", \"patients\")\n\n        # Process and include patient's anchor year.\n        table = select(\n            *list(table.columns),\n            (\n                func.substr(get_column(table, \"anchor_year_group\"), 1, 4).cast(Integer)\n            ).label(\"anchor_year_group_start\"),\n            (\n                func.substr(get_column(table, \"anchor_year_group\"), 8, 12).cast(Integer)\n            ).label(\"anchor_year_group_end\"),\n        ).subquery()\n\n        # Select the middle of the anchor year group as the anchor year\n        table = select(\n            *list(table.c),\n            (\n                get_column(table, \"anchor_year_group_start\")\n                + (\n                    get_column(table, \"anchor_year_group_end\")\n                    - get_column(table, \"anchor_year_group_start\")\n                )\n                / 2\n            ).label(\"anchor_year_group_middle\"),\n        ).subquery()\n\n        table = select(\n            *list(table.c),\n            (\n                get_column(table, \"anchor_year_group_middle\")\n                - get_column(table, \"anchor_year\")\n            ).label(\"anchor_year_difference\"),\n        ).subquery()\n\n        # Shift relevant columns by anchor year difference\n        table = qo.AddDeltaColumn(\"dod\", years=\"anchor_year_difference\")(table)\n        table = qo.Drop(\n            [\n                \"anchor_year_group_start\",\n                \"anchor_year_group_end\",\n                \"anchor_year_group_middle\",\n            ],\n        )(table)\n\n        return QueryInterface(self.db, table)\n\n    def diagnoses_icd(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query MIMIC diagnosis data.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed query, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(\"mimiciv_hosp\", \"diagnoses_icd\")\n\n        # Join with diagnoses dimension table.\n        table = qo.Join(\n            join_table=self.get_table(\"mimiciv_hosp\", \"d_icd_diagnoses\"),\n            on=[\"icd_code\", \"icd_version\"],\n            on_to_type=[\"str\", \"int\"],\n        )(table)\n\n        return QueryInterface(self.db, table)\n\n    def procedures_icd(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query MIMIC procedures data.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed query, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(\"mimiciv_hosp\", \"procedures_icd\")\n\n        # Join with procedures dimension table.\n        table = qo.Join(\n            join_table=self.get_table(\"mimiciv_hosp\", \"d_icd_procedures\"),\n            on=[\"icd_code\", \"icd_version\"],\n            on_to_type=[\"str\", \"int\"],\n        )(table)\n\n        return QueryInterface(self.db, table)\n\n    def labevents(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query lab events from the hospital module.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed query, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(\"mimiciv_hosp\", \"labevents\")\n        dim_items_table = self.get_table(\"mimiciv_hosp\", \"d_labitems\")\n\n        # Join with lab items dimension table.\n        table = qo.Join(\n            join_table=dim_items_table,\n            on=[\"itemid\"],\n        )(table)\n\n        return QueryInterface(self.db, table)\n\n    def chartevents(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query ICU chart events from the ICU module.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed table, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(\"mimiciv_icu\", \"chartevents\")\n        dim_items_table = self.get_table(\"mimiciv_icu\", \"d_items\")\n\n        # Join with items dimension table.\n        table = qo.Join(\n            dim_items_table,\n            on=\"itemid\",\n        )(table)\n\n        return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.MIMICIVQuerier.patients","title":"patients","text":"<pre><code>patients()\n</code></pre> <p>Query MIMIC patient data.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed query, wrapped in an interface object.</p> Notes <p>The function infers the approximate year a patient received care, using the <code>anchor_year</code> and <code>anchor_year_group</code> columns. The <code>join</code> and <code>ops</code> supplied are applied after the approximate year is inferred. <code>dod</code> is adjusted based on the inferred approximate year of care.</p> Source code in <code>cycquery/mimiciv.py</code> <pre><code>def patients(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query MIMIC patient data.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed query, wrapped in an interface object.\n\n    Notes\n    -----\n    The function infers the approximate year a patient received care, using the\n    `anchor_year` and `anchor_year_group` columns. The `join` and `ops` supplied\n    are applied after the approximate year is inferred. `dod` is\n    adjusted based on the inferred approximate year of care.\n\n    \"\"\"\n    table = self.get_table(\"mimiciv_hosp\", \"patients\")\n\n    # Process and include patient's anchor year.\n    table = select(\n        *list(table.columns),\n        (\n            func.substr(get_column(table, \"anchor_year_group\"), 1, 4).cast(Integer)\n        ).label(\"anchor_year_group_start\"),\n        (\n            func.substr(get_column(table, \"anchor_year_group\"), 8, 12).cast(Integer)\n        ).label(\"anchor_year_group_end\"),\n    ).subquery()\n\n    # Select the middle of the anchor year group as the anchor year\n    table = select(\n        *list(table.c),\n        (\n            get_column(table, \"anchor_year_group_start\")\n            + (\n                get_column(table, \"anchor_year_group_end\")\n                - get_column(table, \"anchor_year_group_start\")\n            )\n            / 2\n        ).label(\"anchor_year_group_middle\"),\n    ).subquery()\n\n    table = select(\n        *list(table.c),\n        (\n            get_column(table, \"anchor_year_group_middle\")\n            - get_column(table, \"anchor_year\")\n        ).label(\"anchor_year_difference\"),\n    ).subquery()\n\n    # Shift relevant columns by anchor year difference\n    table = qo.AddDeltaColumn(\"dod\", years=\"anchor_year_difference\")(table)\n    table = qo.Drop(\n        [\n            \"anchor_year_group_start\",\n            \"anchor_year_group_end\",\n            \"anchor_year_group_middle\",\n        ],\n    )(table)\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.MIMICIVQuerier.diagnoses_icd","title":"diagnoses_icd","text":"<pre><code>diagnoses_icd()\n</code></pre> <p>Query MIMIC diagnosis data.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed query, wrapped in an interface object.</p> Source code in <code>cycquery/mimiciv.py</code> <pre><code>def diagnoses_icd(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query MIMIC diagnosis data.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed query, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(\"mimiciv_hosp\", \"diagnoses_icd\")\n\n    # Join with diagnoses dimension table.\n    table = qo.Join(\n        join_table=self.get_table(\"mimiciv_hosp\", \"d_icd_diagnoses\"),\n        on=[\"icd_code\", \"icd_version\"],\n        on_to_type=[\"str\", \"int\"],\n    )(table)\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.MIMICIVQuerier.procedures_icd","title":"procedures_icd","text":"<pre><code>procedures_icd()\n</code></pre> <p>Query MIMIC procedures data.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed query, wrapped in an interface object.</p> Source code in <code>cycquery/mimiciv.py</code> <pre><code>def procedures_icd(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query MIMIC procedures data.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed query, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(\"mimiciv_hosp\", \"procedures_icd\")\n\n    # Join with procedures dimension table.\n    table = qo.Join(\n        join_table=self.get_table(\"mimiciv_hosp\", \"d_icd_procedures\"),\n        on=[\"icd_code\", \"icd_version\"],\n        on_to_type=[\"str\", \"int\"],\n    )(table)\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.MIMICIVQuerier.labevents","title":"labevents","text":"<pre><code>labevents()\n</code></pre> <p>Query lab events from the hospital module.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed query, wrapped in an interface object.</p> Source code in <code>cycquery/mimiciv.py</code> <pre><code>def labevents(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query lab events from the hospital module.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed query, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(\"mimiciv_hosp\", \"labevents\")\n    dim_items_table = self.get_table(\"mimiciv_hosp\", \"d_labitems\")\n\n    # Join with lab items dimension table.\n    table = qo.Join(\n        join_table=dim_items_table,\n        on=[\"itemid\"],\n    )(table)\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.MIMICIVQuerier.chartevents","title":"chartevents","text":"<pre><code>chartevents()\n</code></pre> <p>Query ICU chart events from the ICU module.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed table, wrapped in an interface object.</p> Source code in <code>cycquery/mimiciv.py</code> <pre><code>def chartevents(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query ICU chart events from the ICU module.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed table, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(\"mimiciv_icu\", \"chartevents\")\n    dim_items_table = self.get_table(\"mimiciv_icu\", \"d_items\")\n\n    # Join with items dimension table.\n    table = qo.Join(\n        dim_items_table,\n        on=\"itemid\",\n    )(table)\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.OMOPQuerier","title":"OMOPQuerier","text":"<p>               Bases: <code>DatasetQuerier</code></p> <p>OMOP querier.</p> Source code in <code>cycquery/omop.py</code> <pre><code>class OMOPQuerier(DatasetQuerier):\n    \"\"\"OMOP querier.\"\"\"\n\n    def __init__(\n        self,\n        database: str,\n        user: str,\n        password: str,\n        dbms: str = \"postgresql\",\n        host: str = \"localhost\",\n        port: int = 5432,\n        schema_name: str = \"omop\",\n    ) -&gt; None:\n        \"\"\"Initialize OMOP querier.\"\"\"\n        super().__init__(\n            database=database,\n            user=user,\n            password=password,\n            dbms=dbms,\n            host=host,\n            port=port,\n        )\n        self.schema_name = schema_name\n\n    def map_concept_ids_to_name(\n        self,\n        src_table: Union[Subquery, QueryInterface],\n        src_cols: Union[str, List[str]],\n        dst_cols: Optional[Union[str, List[str]]] = None,\n    ) -&gt; QueryInterface:\n        \"\"\"Map concept IDs in a source table to concept names from concept table.\n\n        For each concept ID column with a name like `somecol_concept_ID`, the mapped\n        concept name column will be named `somecol_concept_name`. If `dst_cols` is\n        specified, the mapped concept name column will be named according to the\n        corresponding name in `dst_cols`.\n\n        Parameters\n        ----------\n        src_table\n            Source table with concept IDs.\n        src_cols\n            Column name(s) to consider as concept IDs for mapping.\n        dst_cols\n            Column name(s) to assign for the mapped concept name columns.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Query with mapped columns from concept table.\n\n        \"\"\"\n        if isinstance(src_table, QueryInterface):\n            src_table = src_table.query\n        concept_table = self.get_table(self.schema_name, \"concept\")\n        src_cols = to_list(src_cols)\n        if dst_cols:\n            dst_cols = to_list(dst_cols)\n            if len(dst_cols) != len(src_cols):\n                raise ValueError(\"dst_cols must be same length as src_cols\")\n\n        for i, col in enumerate(src_cols):\n            if ID not in col:\n                raise ValueError(\"Specified column not a concept ID column!\")\n            src_table = qo.Join(\n                concept_table,\n                on=(col, CONCEPT_ID),\n                join_table_cols=[CONCEPT_NAME],\n                isouter=True,\n            )(src_table)\n            dst_col_name = dst_cols[i] if dst_cols else col.replace(ID, NAME)\n            src_table = qo.Rename({CONCEPT_NAME: dst_col_name})(src_table)  # type: ignore[arg-type]\n\n        return QueryInterface(self.db, src_table)\n\n    def _map_care_site_id(\n        self,\n        source_table: Union[Subquery, QueryInterface],\n    ) -&gt; QueryInterface:\n        \"\"\"Map care_site_id in a source table to care_site table.\n\n        Parameters\n        ----------\n        source_table\n            Source table with care_site_id.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Query with mapped columns from care_site table.\n\n        \"\"\"\n        if isinstance(source_table, QueryInterface):\n            source_table = source_table.query\n        care_site_table = self.get_table(self.schema_name, \"care_site\")\n        table = qo.Join(\n            care_site_table,\n            on=CARE_SITE_ID,\n            join_table_cols=[CARE_SITE_NAME, CARE_SITE_SOURCE_VALUE],\n            isouter=True,\n        )(source_table)\n\n        return QueryInterface(self.db, table)\n\n    def visit_occurrence(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query OMOP visit_occurrence table.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed query, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(self.schema_name, \"visit_occurrence\")\n        table = self.map_concept_ids_to_name(  # type: ignore[assignment]\n            table,\n            [\n                \"visit_concept_id\",\n                \"visit_type_concept_id\",\n            ],\n        )\n        table = self._map_care_site_id(table)  # type: ignore[assignment]\n\n        return QueryInterface(self.db, table)\n\n    def visit_detail(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query OMOP visit_detail table.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed query, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(self.schema_name, \"visit_detail\")\n        table = self.map_concept_ids_to_name(  # type: ignore[assignment]\n            table,\n            [\"visit_detail_concept_id\", \"visit_detail_type_concept_id\"],\n        )\n\n        return QueryInterface(self.db, table)\n\n    def person(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query OMOP person table.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed query, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(self.schema_name, \"person\")\n        table = self.map_concept_ids_to_name(  # type: ignore[assignment]\n            table,\n            [\"gender_concept_id\", \"race_concept_id\", \"ethnicity_concept_id\"],\n        )\n\n        return QueryInterface(self.db, table)\n\n    def observation(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query OMOP observation table.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed query, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(self.schema_name, \"observation\")\n        table = self.map_concept_ids_to_name(  # type: ignore[assignment]\n            table,\n            [OBSERVATION_CONCEPT_ID, OBSERVATION_TYPE_CONCEPT_ID],\n        )\n\n        return QueryInterface(self.db, table)\n\n    def measurement(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query OMOP measurement table.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed query, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(self.schema_name, \"measurement\")\n        # Cast value_as_concept_id to int.\n        table = qo.Cast([VALUE_AS_CONCEPT_ID], \"int\")(table)\n        table = self.map_concept_ids_to_name(  # type: ignore[assignment]\n            table,\n            [MEASUREMENT_CONCEPT_ID, MEASUREMENT_TYPE_CONCEPT_ID, UNIT_CONCEPT_ID],\n        )\n\n        return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.OMOPQuerier.__init__","title":"__init__","text":"<pre><code>__init__(\n    database,\n    user,\n    password,\n    dbms=\"postgresql\",\n    host=\"localhost\",\n    port=5432,\n    schema_name=\"omop\",\n)\n</code></pre> <p>Initialize OMOP querier.</p> Source code in <code>cycquery/omop.py</code> <pre><code>def __init__(\n    self,\n    database: str,\n    user: str,\n    password: str,\n    dbms: str = \"postgresql\",\n    host: str = \"localhost\",\n    port: int = 5432,\n    schema_name: str = \"omop\",\n) -&gt; None:\n    \"\"\"Initialize OMOP querier.\"\"\"\n    super().__init__(\n        database=database,\n        user=user,\n        password=password,\n        dbms=dbms,\n        host=host,\n        port=port,\n    )\n    self.schema_name = schema_name\n</code></pre>"},{"location":"api/#cycquery.OMOPQuerier.map_concept_ids_to_name","title":"map_concept_ids_to_name","text":"<pre><code>map_concept_ids_to_name(src_table, src_cols, dst_cols=None)\n</code></pre> <p>Map concept IDs in a source table to concept names from concept table.</p> <p>For each concept ID column with a name like <code>somecol_concept_ID</code>, the mapped concept name column will be named <code>somecol_concept_name</code>. If <code>dst_cols</code> is specified, the mapped concept name column will be named according to the corresponding name in <code>dst_cols</code>.</p> <p>Parameters:</p> Name Type Description Default <code>src_table</code> <code>Union[Subquery, QueryInterface]</code> <p>Source table with concept IDs.</p> required <code>src_cols</code> <code>Union[str, List[str]]</code> <p>Column name(s) to consider as concept IDs for mapping.</p> required <code>dst_cols</code> <code>Optional[Union[str, List[str]]]</code> <p>Column name(s) to assign for the mapped concept name columns.</p> <code>None</code> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Query with mapped columns from concept table.</p> Source code in <code>cycquery/omop.py</code> <pre><code>def map_concept_ids_to_name(\n    self,\n    src_table: Union[Subquery, QueryInterface],\n    src_cols: Union[str, List[str]],\n    dst_cols: Optional[Union[str, List[str]]] = None,\n) -&gt; QueryInterface:\n    \"\"\"Map concept IDs in a source table to concept names from concept table.\n\n    For each concept ID column with a name like `somecol_concept_ID`, the mapped\n    concept name column will be named `somecol_concept_name`. If `dst_cols` is\n    specified, the mapped concept name column will be named according to the\n    corresponding name in `dst_cols`.\n\n    Parameters\n    ----------\n    src_table\n        Source table with concept IDs.\n    src_cols\n        Column name(s) to consider as concept IDs for mapping.\n    dst_cols\n        Column name(s) to assign for the mapped concept name columns.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Query with mapped columns from concept table.\n\n    \"\"\"\n    if isinstance(src_table, QueryInterface):\n        src_table = src_table.query\n    concept_table = self.get_table(self.schema_name, \"concept\")\n    src_cols = to_list(src_cols)\n    if dst_cols:\n        dst_cols = to_list(dst_cols)\n        if len(dst_cols) != len(src_cols):\n            raise ValueError(\"dst_cols must be same length as src_cols\")\n\n    for i, col in enumerate(src_cols):\n        if ID not in col:\n            raise ValueError(\"Specified column not a concept ID column!\")\n        src_table = qo.Join(\n            concept_table,\n            on=(col, CONCEPT_ID),\n            join_table_cols=[CONCEPT_NAME],\n            isouter=True,\n        )(src_table)\n        dst_col_name = dst_cols[i] if dst_cols else col.replace(ID, NAME)\n        src_table = qo.Rename({CONCEPT_NAME: dst_col_name})(src_table)  # type: ignore[arg-type]\n\n    return QueryInterface(self.db, src_table)\n</code></pre>"},{"location":"api/#cycquery.OMOPQuerier.visit_occurrence","title":"visit_occurrence","text":"<pre><code>visit_occurrence()\n</code></pre> <p>Query OMOP visit_occurrence table.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed query, wrapped in an interface object.</p> Source code in <code>cycquery/omop.py</code> <pre><code>def visit_occurrence(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query OMOP visit_occurrence table.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed query, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(self.schema_name, \"visit_occurrence\")\n    table = self.map_concept_ids_to_name(  # type: ignore[assignment]\n        table,\n        [\n            \"visit_concept_id\",\n            \"visit_type_concept_id\",\n        ],\n    )\n    table = self._map_care_site_id(table)  # type: ignore[assignment]\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.OMOPQuerier.visit_detail","title":"visit_detail","text":"<pre><code>visit_detail()\n</code></pre> <p>Query OMOP visit_detail table.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed query, wrapped in an interface object.</p> Source code in <code>cycquery/omop.py</code> <pre><code>def visit_detail(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query OMOP visit_detail table.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed query, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(self.schema_name, \"visit_detail\")\n    table = self.map_concept_ids_to_name(  # type: ignore[assignment]\n        table,\n        [\"visit_detail_concept_id\", \"visit_detail_type_concept_id\"],\n    )\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.OMOPQuerier.person","title":"person","text":"<pre><code>person()\n</code></pre> <p>Query OMOP person table.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed query, wrapped in an interface object.</p> Source code in <code>cycquery/omop.py</code> <pre><code>def person(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query OMOP person table.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed query, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(self.schema_name, \"person\")\n    table = self.map_concept_ids_to_name(  # type: ignore[assignment]\n        table,\n        [\"gender_concept_id\", \"race_concept_id\", \"ethnicity_concept_id\"],\n    )\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.OMOPQuerier.observation","title":"observation","text":"<pre><code>observation()\n</code></pre> <p>Query OMOP observation table.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed query, wrapped in an interface object.</p> Source code in <code>cycquery/omop.py</code> <pre><code>def observation(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query OMOP observation table.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed query, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(self.schema_name, \"observation\")\n    table = self.map_concept_ids_to_name(  # type: ignore[assignment]\n        table,\n        [OBSERVATION_CONCEPT_ID, OBSERVATION_TYPE_CONCEPT_ID],\n    )\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.OMOPQuerier.measurement","title":"measurement","text":"<pre><code>measurement()\n</code></pre> <p>Query OMOP measurement table.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed query, wrapped in an interface object.</p> Source code in <code>cycquery/omop.py</code> <pre><code>def measurement(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query OMOP measurement table.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed query, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(self.schema_name, \"measurement\")\n    # Cast value_as_concept_id to int.\n    table = qo.Cast([VALUE_AS_CONCEPT_ID], \"int\")(table)\n    table = self.map_concept_ids_to_name(  # type: ignore[assignment]\n        table,\n        [MEASUREMENT_CONCEPT_ID, MEASUREMENT_TYPE_CONCEPT_ID, UNIT_CONCEPT_ID],\n    )\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.base","title":"base","text":"<p>Base querier class.</p>"},{"location":"api/#cycquery.base.DatasetQuerier","title":"DatasetQuerier","text":"<p>Base class to query EHR datasets.</p> <p>Attributes:</p> Name Type Description <code>db</code> <p>ORM Database used to run queries.</p> <p>Parameters:</p> Name Type Description Default <code>dbms</code> <code>str</code> <p>The database management system type (e.g., 'postgresql', 'mysql', 'sqlite').</p> required <code>user</code> <code>str</code> <p>The username for the database, by default empty. Not used for SQLite.</p> <code>''</code> <code>password</code> <code>str</code> <p>The password for the database, by default empty. Not used for SQLite.</p> <code>''</code> <code>host</code> <code>str</code> <p>The host address of the database, by default empty. Not used for SQLite.</p> <code>''</code> <code>port</code> <code>int</code> <p>The port number for the database, by default None. Not used for SQLite.</p> <code>None</code> <code>database</code> <code>str</code> <p>The name of the database or the path to the database file (for SQLite), by default empty.</p> <code>''</code> <code>schemas</code> <code>Union[str, List[str]]</code> <p>The schema(s) to query, by default None.</p> <code>None</code> Notes <p>This class is intended to be subclassed to provide methods for querying tables in the database. This class automatically creates methods for querying tables in the database. The methods are named after the schema and table name, i.e. <code>self.schema_name.table_name()</code>. The methods are created when the class is instantiated. The subclass can provide custom methods for querying tables in the database which can build on the methods created by this class.</p> Source code in <code>cycquery/base.py</code> <pre><code>class DatasetQuerier:\n    \"\"\"Base class to query EHR datasets.\n\n    Attributes\n    ----------\n    db\n        ORM Database used to run queries.\n\n    Parameters\n    ----------\n    dbms : str\n        The database management system type (e.g., 'postgresql', 'mysql', 'sqlite').\n    user : str, optional\n        The username for the database, by default empty. Not used for SQLite.\n    password : str, optional\n        The password for the database, by default empty. Not used for SQLite.\n    host : str, optional\n        The host address of the database, by default empty. Not used for SQLite.\n    port : int, optional\n        The port number for the database, by default None. Not used for SQLite.\n    database : str, optional\n        The name of the database or the path to the database file (for SQLite),\n        by default empty.\n    schemas : Union[str, List[str]], optional\n        The schema(s) to query, by default None.\n\n    Notes\n    -----\n    This class is intended to be subclassed to provide methods for querying tables in\n    the database. This class automatically creates methods for querying tables in the\n    database. The methods are named after the schema and table name, i.e.\n    `self.schema_name.table_name()`. The methods are created when the class is\n    instantiated. The subclass can provide custom methods for querying tables in the\n    database which can build on the methods created by this class.\n\n    \"\"\"\n\n    def __init__(\n        self,\n        dbms: str,\n        user: str = \"\",\n        password: str = \"\",\n        host: str = \"\",\n        port: int | None = None,\n        database: str = \"\",\n        schemas: list[str] | None = None,\n    ) -&gt; None:\n        \"\"\"Initialize the querier.\"\"\"\n        config = DatasetQuerierConfig(\n            database=database,\n            user=user,\n            password=password,\n            dbms=dbms,\n            host=host,\n            port=port,\n            schemas=schemas,\n        )\n        self.db = Database(config)\n        if not self.db.is_connected:\n            LOGGER.error(\"Database is not connected, cannot run queries.\")\n            return\n        self._setup_table_methods()\n\n    def list_schemas(self) -&gt; list[str]:\n        \"\"\"List schemas in the database to query.\n\n        Returns\n        -------\n        List[str]\n            List of schema names.\n\n        \"\"\"\n        all_schemas = list(self.db.inspector.get_schema_names())\n        if self.db.config.schemas is None:\n            return all_schemas\n        if isinstance(self.db.config.schemas, str):\n            schemas_to_use = [self.db.config.schemas]\n        else:\n            schemas_to_use = self.db.config.schemas\n\n        return [schema for schema in all_schemas if schema in schemas_to_use]\n\n    def list_tables(self, schema_name: str | None = None) -&gt; list[str]:\n        \"\"\"List table methods that can be queried using the database.\n\n        Parameters\n        ----------\n        schema_name\n            Name of schema in the database.\n\n        Returns\n        -------\n        List[str]\n            List of table names.\n\n        \"\"\"\n        if schema_name:\n            table_names = []\n            for table in self.db.list_tables():\n                schema_name_, _ = table.split(\".\")\n                if schema_name_ == schema_name:\n                    table_names.append(table)\n        else:\n            table_names = self.db.list_tables()\n\n        return table_names\n\n    def list_columns(self, schema_name: str, table_name: str) -&gt; list[str]:\n        \"\"\"List columns in a table.\n\n        Parameters\n        ----------\n        schema_name\n            Name of schema in the database.\n        table_name\n            Name of table in the database.\n\n        Returns\n        -------\n        List[str]\n            List of column names.\n\n        \"\"\"\n        return list(\n            getattr(getattr(self.db, schema_name), table_name).data_.columns.keys(),\n        )\n\n    def list_custom_tables(self) -&gt; list[str]:\n        \"\"\"List custom tables methods provided by the dataset API.\n\n        Returns\n        -------\n        List[str]\n            List of custom table names.\n\n        \"\"\"\n        method_list = dir(self)\n        custom_tables = []\n        for method in method_list:\n            if (\n                not method.startswith(\n                    \"__\",\n                )\n                and not method.startswith(\"_\")\n                and method not in self.list_schemas()\n                and not method.startswith(\"list_\")\n                and not method.startswith(\"get_\")\n                and method not in [\"db\"]\n            ):\n                custom_tables.append(method)\n\n        return custom_tables\n\n    def get_table(\n        self,\n        schema_name: str,\n        table_name: str,\n        cast_timestamp_cols: bool = True,\n    ) -&gt; Subquery:\n        \"\"\"Get a table and possibly map columns to have standard names.\n\n        Standardizing column names allows for columns to be\n        recognized in downstream processing.\n\n        Parameters\n        ----------\n        schema_name\n            Name of schema in the database.\n        table_name\n            Name of table in the database.\n        cast_timestamp_cols\n            Whether to cast timestamp columns to datetime.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Table with mapped columns.\n\n        \"\"\"\n        table = _create_get_table_lambdafn(schema_name, table_name)(self.db).data_\n\n        if cast_timestamp_cols:\n            table = _cast_timestamp_cols(table)\n\n        return _to_subquery(table)\n\n    def _template_table_method(\n        self,\n        schema_name: str,\n        table_name: str,\n    ) -&gt; QueryInterface:\n        \"\"\"Template method for table methods.\n\n        Parameters\n        ----------\n        schema_name\n            Name of schema in the database.\n        table_name\n            Name of table in the database.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            A query interface object.\n\n        \"\"\"\n        table = getattr(getattr(self.db, schema_name), table_name).data_\n        table = _to_subquery(table)\n\n        return QueryInterface(self.db, table)\n\n    def _setup_table_methods(self) -&gt; None:\n        \"\"\"Add table methods.\n\n        This method adds methods to the querier class that allow querying of tables in\n        the database. The methods are named after the table names.\n\n        \"\"\"\n        schemas = self.list_schemas()\n        meta: dict[str, MetaData] = {}\n        for schema_name in schemas:\n            metadata = MetaData(schema=schema_name)\n            with self.db.engine.connect() as conn:\n                metadata.reflect(bind=conn)\n            meta[schema_name] = metadata\n            schema = DBSchema(schema_name, meta[schema_name])\n            for table_name in meta[schema_name].tables:\n                setattr(\n                    schema,\n                    get_attr_name(table_name),\n                    partial(\n                        self._template_table_method,\n                        schema_name=schema_name,\n                        table_name=get_attr_name(table_name),\n                    ),\n                )\n            setattr(self, schema_name, schema)\n</code></pre>"},{"location":"api/#cycquery.base.DatasetQuerier.__init__","title":"__init__","text":"<pre><code>__init__(\n    dbms,\n    user=\"\",\n    password=\"\",\n    host=\"\",\n    port=None,\n    database=\"\",\n    schemas=None,\n)\n</code></pre> <p>Initialize the querier.</p> Source code in <code>cycquery/base.py</code> <pre><code>def __init__(\n    self,\n    dbms: str,\n    user: str = \"\",\n    password: str = \"\",\n    host: str = \"\",\n    port: int | None = None,\n    database: str = \"\",\n    schemas: list[str] | None = None,\n) -&gt; None:\n    \"\"\"Initialize the querier.\"\"\"\n    config = DatasetQuerierConfig(\n        database=database,\n        user=user,\n        password=password,\n        dbms=dbms,\n        host=host,\n        port=port,\n        schemas=schemas,\n    )\n    self.db = Database(config)\n    if not self.db.is_connected:\n        LOGGER.error(\"Database is not connected, cannot run queries.\")\n        return\n    self._setup_table_methods()\n</code></pre>"},{"location":"api/#cycquery.base.DatasetQuerier.list_schemas","title":"list_schemas","text":"<pre><code>list_schemas()\n</code></pre> <p>List schemas in the database to query.</p> <p>Returns:</p> Type Description <code>List[str]</code> <p>List of schema names.</p> Source code in <code>cycquery/base.py</code> <pre><code>def list_schemas(self) -&gt; list[str]:\n    \"\"\"List schemas in the database to query.\n\n    Returns\n    -------\n    List[str]\n        List of schema names.\n\n    \"\"\"\n    all_schemas = list(self.db.inspector.get_schema_names())\n    if self.db.config.schemas is None:\n        return all_schemas\n    if isinstance(self.db.config.schemas, str):\n        schemas_to_use = [self.db.config.schemas]\n    else:\n        schemas_to_use = self.db.config.schemas\n\n    return [schema for schema in all_schemas if schema in schemas_to_use]\n</code></pre>"},{"location":"api/#cycquery.base.DatasetQuerier.list_tables","title":"list_tables","text":"<pre><code>list_tables(schema_name=None)\n</code></pre> <p>List table methods that can be queried using the database.</p> <p>Parameters:</p> Name Type Description Default <code>schema_name</code> <code>str | None</code> <p>Name of schema in the database.</p> <code>None</code> <p>Returns:</p> Type Description <code>List[str]</code> <p>List of table names.</p> Source code in <code>cycquery/base.py</code> <pre><code>def list_tables(self, schema_name: str | None = None) -&gt; list[str]:\n    \"\"\"List table methods that can be queried using the database.\n\n    Parameters\n    ----------\n    schema_name\n        Name of schema in the database.\n\n    Returns\n    -------\n    List[str]\n        List of table names.\n\n    \"\"\"\n    if schema_name:\n        table_names = []\n        for table in self.db.list_tables():\n            schema_name_, _ = table.split(\".\")\n            if schema_name_ == schema_name:\n                table_names.append(table)\n    else:\n        table_names = self.db.list_tables()\n\n    return table_names\n</code></pre>"},{"location":"api/#cycquery.base.DatasetQuerier.list_columns","title":"list_columns","text":"<pre><code>list_columns(schema_name, table_name)\n</code></pre> <p>List columns in a table.</p> <p>Parameters:</p> Name Type Description Default <code>schema_name</code> <code>str</code> <p>Name of schema in the database.</p> required <code>table_name</code> <code>str</code> <p>Name of table in the database.</p> required <p>Returns:</p> Type Description <code>List[str]</code> <p>List of column names.</p> Source code in <code>cycquery/base.py</code> <pre><code>def list_columns(self, schema_name: str, table_name: str) -&gt; list[str]:\n    \"\"\"List columns in a table.\n\n    Parameters\n    ----------\n    schema_name\n        Name of schema in the database.\n    table_name\n        Name of table in the database.\n\n    Returns\n    -------\n    List[str]\n        List of column names.\n\n    \"\"\"\n    return list(\n        getattr(getattr(self.db, schema_name), table_name).data_.columns.keys(),\n    )\n</code></pre>"},{"location":"api/#cycquery.base.DatasetQuerier.list_custom_tables","title":"list_custom_tables","text":"<pre><code>list_custom_tables()\n</code></pre> <p>List custom tables methods provided by the dataset API.</p> <p>Returns:</p> Type Description <code>List[str]</code> <p>List of custom table names.</p> Source code in <code>cycquery/base.py</code> <pre><code>def list_custom_tables(self) -&gt; list[str]:\n    \"\"\"List custom tables methods provided by the dataset API.\n\n    Returns\n    -------\n    List[str]\n        List of custom table names.\n\n    \"\"\"\n    method_list = dir(self)\n    custom_tables = []\n    for method in method_list:\n        if (\n            not method.startswith(\n                \"__\",\n            )\n            and not method.startswith(\"_\")\n            and method not in self.list_schemas()\n            and not method.startswith(\"list_\")\n            and not method.startswith(\"get_\")\n            and method not in [\"db\"]\n        ):\n            custom_tables.append(method)\n\n    return custom_tables\n</code></pre>"},{"location":"api/#cycquery.base.DatasetQuerier.get_table","title":"get_table","text":"<pre><code>get_table(\n    schema_name, table_name, cast_timestamp_cols=True\n)\n</code></pre> <p>Get a table and possibly map columns to have standard names.</p> <p>Standardizing column names allows for columns to be recognized in downstream processing.</p> <p>Parameters:</p> Name Type Description Default <code>schema_name</code> <code>str</code> <p>Name of schema in the database.</p> required <code>table_name</code> <code>str</code> <p>Name of table in the database.</p> required <code>cast_timestamp_cols</code> <code>bool</code> <p>Whether to cast timestamp columns to datetime.</p> <code>True</code> <p>Returns:</p> Type Description <code>Subquery</code> <p>Table with mapped columns.</p> Source code in <code>cycquery/base.py</code> <pre><code>def get_table(\n    self,\n    schema_name: str,\n    table_name: str,\n    cast_timestamp_cols: bool = True,\n) -&gt; Subquery:\n    \"\"\"Get a table and possibly map columns to have standard names.\n\n    Standardizing column names allows for columns to be\n    recognized in downstream processing.\n\n    Parameters\n    ----------\n    schema_name\n        Name of schema in the database.\n    table_name\n        Name of table in the database.\n    cast_timestamp_cols\n        Whether to cast timestamp columns to datetime.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Table with mapped columns.\n\n    \"\"\"\n    table = _create_get_table_lambdafn(schema_name, table_name)(self.db).data_\n\n    if cast_timestamp_cols:\n        table = _cast_timestamp_cols(table)\n\n    return _to_subquery(table)\n</code></pre>"},{"location":"api/#cycquery.eicu","title":"eicu","text":"<p>EICU-CRD query module.</p> <p>Supports querying of eICU.</p>"},{"location":"api/#cycquery.eicu.EICUQuerier","title":"EICUQuerier","text":"<p>               Bases: <code>DatasetQuerier</code></p> <p>EICU dataset querier.</p> Source code in <code>cycquery/eicu.py</code> <pre><code>class EICUQuerier(DatasetQuerier):\n    \"\"\"EICU dataset querier.\"\"\"\n</code></pre>"},{"location":"api/#cycquery.gemini","title":"gemini","text":"<p>GEMINI query module.</p>"},{"location":"api/#cycquery.gemini.GEMINIQuerier","title":"GEMINIQuerier","text":"<p>               Bases: <code>DatasetQuerier</code></p> <p>GEMINI dataset querier.</p> Source code in <code>cycquery/gemini.py</code> <pre><code>class GEMINIQuerier(DatasetQuerier):\n    \"\"\"GEMINI dataset querier.\"\"\"\n\n    def ip_admin(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query GEMINI patient encounters.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed query, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(\"public\", \"ip_administrative\")\n\n        # Possibly cast string representations to timestamps\n        table = qo.Cast([\"admit_date_time\", \"discharge_date_time\"], \"timestamp\")(table)\n\n        # Get the discharge disposition code descriptions\n        lookup_table = self.get_table(\"public\", \"lookup_ip_administrative\")\n        lookup_table = qo.ConditionEquals(\"variable\", \"discharge_disposition\")(\n            lookup_table,\n        )\n        table = qo.Join(\n            lookup_table,\n            on=(\"discharge_disposition\", \"value\"),\n            on_to_type=\"int\",\n            join_table_cols=\"description\",\n            isouter=True,\n        )(table)\n        table = qo.Rename({\"description\": \"discharge_description\"})(table)\n        table = qo.Drop(\"value\")(table)\n\n        return QueryInterface(self.db, table)\n\n    def diagnoses(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query diagnosis data.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed table, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(\"public\", \"diagnosis\")\n\n        lookup_table = self.get_table(\"public\", \"lookup_diagnosis\")\n        lookup_table = qo.ConditionEquals(\"variable\", \"diagnosis_type\")(lookup_table)\n        table = qo.Join(\n            lookup_table,\n            on=(\"diagnosis_type\", \"value\"),\n            join_table_cols=\"description\",\n            isouter=True,\n        )(table)\n        table = qo.Drop(\"value\")(table)\n        table = qo.Rename({\"description\": \"diagnosis_type_description\"})(table)\n        table = qo.ReorderAfter(\"diagnosis_type_description\", \"diagnosis_type\")(table)\n\n        # Trim whitespace from ICD codes.\n        table = qo.Trim(\"diagnosis_code\")(table)\n\n        return QueryInterface(self.db, table)\n\n    def room_transfer(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query room transfer data.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed table, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(\"public\", \"room_transfer\")\n\n        # Join with lookup to get transfer description.\n        lookup_table = self.get_table(\"public\", \"lookup_room_transfer\")\n        lookup_table = qo.ConditionEquals(\"variable\", \"medical_service\")(lookup_table)\n\n        table = qo.Join(\n            lookup_table,\n            on=(\"medical_service\", \"value\"),\n            join_table_cols=\"description\",\n            isouter=True,\n        )(table)\n        table = qo.Rename({\"description\": \"transfer_description\"})(table)\n\n        return QueryInterface(self.db, table)\n\n    def care_units(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query care unit data, fetches transfer info from multiple tables.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed table, wrapped in an interface object.\n\n        \"\"\"\n        filter_care_unit_cols = qo.Keep(\n            [\n                \"genc_id\",\n                \"admit\",\n                \"discharge\",\n                CARE_UNIT,\n            ],\n        )\n\n        # In-patient table.\n        ip_table = self.get_table(\"public\", \"ip_administrative\")\n        ip_table = qo.Rename(\n            {\n                \"admit_date_time\": \"admit\",\n                \"discharge_date_time\": \"discharge\",\n            },\n        )(ip_table)\n        ip_table = qo.Literal(\"IP\", CARE_UNIT)(ip_table)\n        ip_table = filter_care_unit_cols(ip_table)\n\n        # Special care unit table.\n        scu_table = self.get_table(\"public\", \"ip_scu\")\n        scu_table = qo.Rename(\n            {\n                \"scu_admit_date_time\": \"admit\",\n                \"scu_discharge_date_time\": \"discharge\",\n            },\n        )(scu_table)\n        scu_table = qo.Literal(\"SCU\", CARE_UNIT)(scu_table)\n        scu_table = filter_care_unit_cols(scu_table)\n\n        # Emergency room/department table.\n        er_table = self.get_table(\"public\", \"er_administrative\")\n        er_table = qo.Rename(\n            {\n                \"er_admit_timestamp\": \"admit\",\n                \"er_discharge_timestamp\": \"discharge\",\n            },\n        )(er_table)\n        er_table = qo.Literal(\"ER\", CARE_UNIT)(er_table)\n        er_table = filter_care_unit_cols(er_table)\n\n        # Room transfer table.\n        rt_table = self.get_table(\"public\", \"room_transfer\")\n        rt_table = qo.Rename(\n            {\n                \"checkin_date_time\": \"admit\",\n                \"checkout_date_time\": \"discharge\",\n            },\n        )(rt_table)\n        rt_table = qo.Rename({\"transfer_description\": CARE_UNIT})(rt_table)\n        rt_table = filter_care_unit_cols(rt_table)\n\n        # Combine.\n        table = union_all(\n            select(er_table),\n            select(scu_table),\n            select(ip_table),\n            select(rt_table),\n        ).subquery()\n\n        return QueryInterface(self.db, table)\n\n    def imaging(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query imaging reports data.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed table, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(\"public\", \"imaging\")\n\n        # Get imaging test description\n        lookup_table = self.get_table(\"public\", \"lookup_imaging\")\n        lookup_table = qo.ConditionEquals(\"variable\", \"imaging_test_name_mapped\")(\n            lookup_table,\n        )\n\n        table = qo.Join(\n            lookup_table,\n            on=(\"imaging_test_name_mapped\", \"value\"),\n            on_to_type=\"str\",\n            join_table_cols=\"description\",\n        )(table)\n        table = qo.Drop(\"value\")(table)\n        table = qo.Rename({\"description\": \"imaging_test_description\"})(table)\n        table = qo.ReorderAfter(\"imaging_test_description\", \"imaging_test_name_mapped\")(\n            table,\n        )\n\n        return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.gemini.GEMINIQuerier.ip_admin","title":"ip_admin","text":"<pre><code>ip_admin()\n</code></pre> <p>Query GEMINI patient encounters.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed query, wrapped in an interface object.</p> Source code in <code>cycquery/gemini.py</code> <pre><code>def ip_admin(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query GEMINI patient encounters.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed query, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(\"public\", \"ip_administrative\")\n\n    # Possibly cast string representations to timestamps\n    table = qo.Cast([\"admit_date_time\", \"discharge_date_time\"], \"timestamp\")(table)\n\n    # Get the discharge disposition code descriptions\n    lookup_table = self.get_table(\"public\", \"lookup_ip_administrative\")\n    lookup_table = qo.ConditionEquals(\"variable\", \"discharge_disposition\")(\n        lookup_table,\n    )\n    table = qo.Join(\n        lookup_table,\n        on=(\"discharge_disposition\", \"value\"),\n        on_to_type=\"int\",\n        join_table_cols=\"description\",\n        isouter=True,\n    )(table)\n    table = qo.Rename({\"description\": \"discharge_description\"})(table)\n    table = qo.Drop(\"value\")(table)\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.gemini.GEMINIQuerier.diagnoses","title":"diagnoses","text":"<pre><code>diagnoses()\n</code></pre> <p>Query diagnosis data.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed table, wrapped in an interface object.</p> Source code in <code>cycquery/gemini.py</code> <pre><code>def diagnoses(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query diagnosis data.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed table, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(\"public\", \"diagnosis\")\n\n    lookup_table = self.get_table(\"public\", \"lookup_diagnosis\")\n    lookup_table = qo.ConditionEquals(\"variable\", \"diagnosis_type\")(lookup_table)\n    table = qo.Join(\n        lookup_table,\n        on=(\"diagnosis_type\", \"value\"),\n        join_table_cols=\"description\",\n        isouter=True,\n    )(table)\n    table = qo.Drop(\"value\")(table)\n    table = qo.Rename({\"description\": \"diagnosis_type_description\"})(table)\n    table = qo.ReorderAfter(\"diagnosis_type_description\", \"diagnosis_type\")(table)\n\n    # Trim whitespace from ICD codes.\n    table = qo.Trim(\"diagnosis_code\")(table)\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.gemini.GEMINIQuerier.room_transfer","title":"room_transfer","text":"<pre><code>room_transfer()\n</code></pre> <p>Query room transfer data.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed table, wrapped in an interface object.</p> Source code in <code>cycquery/gemini.py</code> <pre><code>def room_transfer(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query room transfer data.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed table, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(\"public\", \"room_transfer\")\n\n    # Join with lookup to get transfer description.\n    lookup_table = self.get_table(\"public\", \"lookup_room_transfer\")\n    lookup_table = qo.ConditionEquals(\"variable\", \"medical_service\")(lookup_table)\n\n    table = qo.Join(\n        lookup_table,\n        on=(\"medical_service\", \"value\"),\n        join_table_cols=\"description\",\n        isouter=True,\n    )(table)\n    table = qo.Rename({\"description\": \"transfer_description\"})(table)\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.gemini.GEMINIQuerier.care_units","title":"care_units","text":"<pre><code>care_units()\n</code></pre> <p>Query care unit data, fetches transfer info from multiple tables.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed table, wrapped in an interface object.</p> Source code in <code>cycquery/gemini.py</code> <pre><code>def care_units(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query care unit data, fetches transfer info from multiple tables.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed table, wrapped in an interface object.\n\n    \"\"\"\n    filter_care_unit_cols = qo.Keep(\n        [\n            \"genc_id\",\n            \"admit\",\n            \"discharge\",\n            CARE_UNIT,\n        ],\n    )\n\n    # In-patient table.\n    ip_table = self.get_table(\"public\", \"ip_administrative\")\n    ip_table = qo.Rename(\n        {\n            \"admit_date_time\": \"admit\",\n            \"discharge_date_time\": \"discharge\",\n        },\n    )(ip_table)\n    ip_table = qo.Literal(\"IP\", CARE_UNIT)(ip_table)\n    ip_table = filter_care_unit_cols(ip_table)\n\n    # Special care unit table.\n    scu_table = self.get_table(\"public\", \"ip_scu\")\n    scu_table = qo.Rename(\n        {\n            \"scu_admit_date_time\": \"admit\",\n            \"scu_discharge_date_time\": \"discharge\",\n        },\n    )(scu_table)\n    scu_table = qo.Literal(\"SCU\", CARE_UNIT)(scu_table)\n    scu_table = filter_care_unit_cols(scu_table)\n\n    # Emergency room/department table.\n    er_table = self.get_table(\"public\", \"er_administrative\")\n    er_table = qo.Rename(\n        {\n            \"er_admit_timestamp\": \"admit\",\n            \"er_discharge_timestamp\": \"discharge\",\n        },\n    )(er_table)\n    er_table = qo.Literal(\"ER\", CARE_UNIT)(er_table)\n    er_table = filter_care_unit_cols(er_table)\n\n    # Room transfer table.\n    rt_table = self.get_table(\"public\", \"room_transfer\")\n    rt_table = qo.Rename(\n        {\n            \"checkin_date_time\": \"admit\",\n            \"checkout_date_time\": \"discharge\",\n        },\n    )(rt_table)\n    rt_table = qo.Rename({\"transfer_description\": CARE_UNIT})(rt_table)\n    rt_table = filter_care_unit_cols(rt_table)\n\n    # Combine.\n    table = union_all(\n        select(er_table),\n        select(scu_table),\n        select(ip_table),\n        select(rt_table),\n    ).subquery()\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.gemini.GEMINIQuerier.imaging","title":"imaging","text":"<pre><code>imaging()\n</code></pre> <p>Query imaging reports data.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed table, wrapped in an interface object.</p> Source code in <code>cycquery/gemini.py</code> <pre><code>def imaging(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query imaging reports data.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed table, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(\"public\", \"imaging\")\n\n    # Get imaging test description\n    lookup_table = self.get_table(\"public\", \"lookup_imaging\")\n    lookup_table = qo.ConditionEquals(\"variable\", \"imaging_test_name_mapped\")(\n        lookup_table,\n    )\n\n    table = qo.Join(\n        lookup_table,\n        on=(\"imaging_test_name_mapped\", \"value\"),\n        on_to_type=\"str\",\n        join_table_cols=\"description\",\n    )(table)\n    table = qo.Drop(\"value\")(table)\n    table = qo.Rename({\"description\": \"imaging_test_description\"})(table)\n    table = qo.ReorderAfter(\"imaging_test_description\", \"imaging_test_name_mapped\")(\n        table,\n    )\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.interface","title":"interface","text":"<p>A query interface to wrap database objects and queries.</p>"},{"location":"api/#cycquery.interface.QueryInterface","title":"QueryInterface","text":"<p>An interface dataclass to wrap queries, and run them.</p> <p>Parameters:</p> Name Type Description Default <code>database</code> <code>Database</code> <p>Database object to create ORM, and query data.</p> required <code>query</code> <code>TableTypes | 'QueryInterface'</code> <p>The query.</p> required Source code in <code>cycquery/interface.py</code> <pre><code>class QueryInterface:\n    \"\"\"An interface dataclass to wrap queries, and run them.\n\n    Parameters\n    ----------\n    database\n        Database object to create ORM, and query data.\n    query\n        The query.\n\n    \"\"\"\n\n    def __init__(\n        self,\n        database: Database,\n        query: TableTypes | \"QueryInterface\",\n    ) -&gt; None:\n        \"\"\"Initialize the QueryInterface object, join and chain operations.\"\"\"\n        self.database = database\n        if isinstance(query, QueryInterface):\n            self.query = query.query  # type: ignore\n        else:\n            self.query = query\n        self._data = None\n\n    @property\n    def data(self) -&gt; pd.DataFrame | None:\n        \"\"\"Get data.\"\"\"\n        return self._data\n\n    def join(\n        self,\n        join_table: TableTypes | \"QueryInterface\",\n        on: str | list[str] | Tuple[str] | list[Tuple[str, str]] | None = None,\n        on_to_type: type | list[type] | None = None,\n        cond: BinaryExpression | None = None,\n        table_cols: str | list[str] | None = None,\n        join_table_cols: str | list[str] | None = None,\n        isouter: bool | None = False,\n    ) -&gt; \"QueryInterface\":\n        \"\"\"Join the query with another table.\n\n        Parameters\n        ----------\n        join_table\n            Table to join with.\n        on\n            Column(s) to join on.\n        on_to_type\n            Type(s) to cast the column(s) to join on.\n        cond\n            Condition to join on.\n        table_cols\n            Columns to select from the original table.\n        join_table_cols\n            Columns to select from the joined table.\n        isouter\n            Whether to perform an outer join.\n\n        Returns\n        -------\n        QueryInterface\n            QueryInterface object with the join operation added.\n\n        \"\"\"\n        on = to_list_optional(on)\n        on_to_type = to_list_optional(on_to_type)\n        table_cols = to_list_optional(table_cols)\n        join_table_cols = to_list_optional(join_table_cols)\n        if isinstance(join_table, QueryInterface):\n            join_table = join_table.query\n        query = qo.Join(\n            join_table=join_table,\n            on=on,\n            on_to_type=on_to_type,\n            cond=cond,\n            table_cols=table_cols,\n            join_table_cols=join_table_cols,\n            isouter=isouter,\n        )(self.query)\n\n        return QueryInterface(self.database, query)\n\n    def ops(\n        self,\n        ops: qo.QueryOp | qo.Sequential,\n    ) -&gt; \"QueryInterface\":\n        \"\"\"Chain operations with the query.\n\n        Parameters\n        ----------\n        ops\n            Operations to perform on the query.\n\n        Returns\n        -------\n        QueryInterface\n            QueryInterface object with the operations added.\n\n        \"\"\"\n        query = ops(self.query)\n\n        return QueryInterface(self.database, query)\n\n    def union(\n        self,\n        other: \"QueryInterface\",\n    ) -&gt; \"QueryInterface\":\n        \"\"\"Union the query with another query.\n\n        Parameters\n        ----------\n        other\n            The other query to union with.\n\n        Returns\n        -------\n        QueryInterface\n            QueryInterface object with the union operation added.\n\n        \"\"\"\n        query = qo.Union(other.query)(self.query)\n\n        return QueryInterface(self.database, query)\n\n    def union_all(\n        self,\n        other: \"QueryInterface\",\n    ) -&gt; \"QueryInterface\":\n        \"\"\"Union all the query with another query.\n\n        Parameters\n        ----------\n        other\n            The other query to union all with.\n\n        Returns\n        -------\n        QueryInterface\n            QueryInterface object with the union all operation added.\n\n        \"\"\"\n        query = qo.Union(other.query, union_all=True)(self.query)\n\n        return QueryInterface(self.database, query)\n\n    def run(\n        self,\n        limit: int | None = None,\n        index_col: str | None = None,\n        batch_mode: bool = False,\n        batch_size: int = 1000000,\n        dtype_backend: str = \"pyarrow\",\n    ) -&gt; pd.DataFrame | Generator[pd.DataFrame, None, None]:\n        \"\"\"Run the query, and fetch data.\n\n        Parameters\n        ----------\n        limit\n            No. of rows to limit the query return.\n        index_col\n            Column which becomes the index, and defines the partitioning.\n            Should be a indexed column in the SQL server, and any orderable type.\n        batch_mode\n            Whether to run the query in batch mode. A generator is returned if True.\n        batch_size\n            Batch size for the query, default 1 million rows.\n        dtype_backend\n            Data type to use for the backend, default pyarrow.\n\n        Returns\n        -------\n        pandas.DataFrame or Generator[pandas.DataFrame, None, None]\n            Query result.\n\n        \"\"\"\n        if not batch_mode:\n            self._data = self.database.run_query(\n                self.query,\n                limit=limit,\n                index_col=index_col,\n                dtype_backend=dtype_backend,\n            )\n        else:\n            self._data = self.database.run_query_batch(\n                self.query,\n                index_col=index_col,\n                batch_size=batch_size,\n                dtype_backend=dtype_backend,\n            )\n\n        return self._data\n\n    def save(\n        self,\n        path: str,\n        file_format: Literal[\"parquet\", \"csv\"] = \"parquet\",\n    ) -&gt; str:\n        \"\"\"Save the query.\n\n        Parameters\n        ----------\n        path\n            Path where the file will be saved.\n        file_format\n            File format of the file to save.\n\n        Returns\n        -------\n        str\n            Processed save path for upstream use.\n\n        \"\"\"\n        # If the query was already run.\n        if self._data is not None:\n            if isinstance(self._data, pd.DataFrame):\n                return save_dataframe(self._data, path, file_format=file_format)\n            if isinstance(self._data, Generator):\n                for i, df in enumerate(self._data):\n                    save_dataframe(df, f\"{path}/batch-{i:03d}\", file_format=file_format)\n        # Save without running.\n        if file_format == \"csv\":\n            path = self.database.save_query_to_csv(self.query, path)\n        elif file_format == \"parquet\":\n            path = self.database.save_query_to_parquet(self.query, path)\n        else:\n            raise ValueError(\n                f\"Invalid file format '{file_format}'. Use 'parquet' or 'csv'.\"\n            )\n\n        return path\n\n    def clear_data(self) -&gt; None:\n        \"\"\"Clear data container.\n\n        Sets the data attribute to None, thus clearing the dataframe contained.\n\n        \"\"\"\n        self._data = None\n</code></pre>"},{"location":"api/#cycquery.interface.QueryInterface.data","title":"data  <code>property</code>","text":"<pre><code>data\n</code></pre> <p>Get data.</p>"},{"location":"api/#cycquery.interface.QueryInterface.__init__","title":"__init__","text":"<pre><code>__init__(database, query)\n</code></pre> <p>Initialize the QueryInterface object, join and chain operations.</p> Source code in <code>cycquery/interface.py</code> <pre><code>def __init__(\n    self,\n    database: Database,\n    query: TableTypes | \"QueryInterface\",\n) -&gt; None:\n    \"\"\"Initialize the QueryInterface object, join and chain operations.\"\"\"\n    self.database = database\n    if isinstance(query, QueryInterface):\n        self.query = query.query  # type: ignore\n    else:\n        self.query = query\n    self._data = None\n</code></pre>"},{"location":"api/#cycquery.interface.QueryInterface.join","title":"join","text":"<pre><code>join(\n    join_table,\n    on=None,\n    on_to_type=None,\n    cond=None,\n    table_cols=None,\n    join_table_cols=None,\n    isouter=False,\n)\n</code></pre> <p>Join the query with another table.</p> <p>Parameters:</p> Name Type Description Default <code>join_table</code> <code>TableTypes | 'QueryInterface'</code> <p>Table to join with.</p> required <code>on</code> <code>str | list[str] | Tuple[str] | list[Tuple[str, str]] | None</code> <p>Column(s) to join on.</p> <code>None</code> <code>on_to_type</code> <code>type | list[type] | None</code> <p>Type(s) to cast the column(s) to join on.</p> <code>None</code> <code>cond</code> <code>BinaryExpression | None</code> <p>Condition to join on.</p> <code>None</code> <code>table_cols</code> <code>str | list[str] | None</code> <p>Columns to select from the original table.</p> <code>None</code> <code>join_table_cols</code> <code>str | list[str] | None</code> <p>Columns to select from the joined table.</p> <code>None</code> <code>isouter</code> <code>bool | None</code> <p>Whether to perform an outer join.</p> <code>False</code> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>QueryInterface object with the join operation added.</p> Source code in <code>cycquery/interface.py</code> <pre><code>def join(\n    self,\n    join_table: TableTypes | \"QueryInterface\",\n    on: str | list[str] | Tuple[str] | list[Tuple[str, str]] | None = None,\n    on_to_type: type | list[type] | None = None,\n    cond: BinaryExpression | None = None,\n    table_cols: str | list[str] | None = None,\n    join_table_cols: str | list[str] | None = None,\n    isouter: bool | None = False,\n) -&gt; \"QueryInterface\":\n    \"\"\"Join the query with another table.\n\n    Parameters\n    ----------\n    join_table\n        Table to join with.\n    on\n        Column(s) to join on.\n    on_to_type\n        Type(s) to cast the column(s) to join on.\n    cond\n        Condition to join on.\n    table_cols\n        Columns to select from the original table.\n    join_table_cols\n        Columns to select from the joined table.\n    isouter\n        Whether to perform an outer join.\n\n    Returns\n    -------\n    QueryInterface\n        QueryInterface object with the join operation added.\n\n    \"\"\"\n    on = to_list_optional(on)\n    on_to_type = to_list_optional(on_to_type)\n    table_cols = to_list_optional(table_cols)\n    join_table_cols = to_list_optional(join_table_cols)\n    if isinstance(join_table, QueryInterface):\n        join_table = join_table.query\n    query = qo.Join(\n        join_table=join_table,\n        on=on,\n        on_to_type=on_to_type,\n        cond=cond,\n        table_cols=table_cols,\n        join_table_cols=join_table_cols,\n        isouter=isouter,\n    )(self.query)\n\n    return QueryInterface(self.database, query)\n</code></pre>"},{"location":"api/#cycquery.interface.QueryInterface.ops","title":"ops","text":"<pre><code>ops(ops)\n</code></pre> <p>Chain operations with the query.</p> <p>Parameters:</p> Name Type Description Default <code>ops</code> <code>QueryOp | Sequential</code> <p>Operations to perform on the query.</p> required <p>Returns:</p> Type Description <code>QueryInterface</code> <p>QueryInterface object with the operations added.</p> Source code in <code>cycquery/interface.py</code> <pre><code>def ops(\n    self,\n    ops: qo.QueryOp | qo.Sequential,\n) -&gt; \"QueryInterface\":\n    \"\"\"Chain operations with the query.\n\n    Parameters\n    ----------\n    ops\n        Operations to perform on the query.\n\n    Returns\n    -------\n    QueryInterface\n        QueryInterface object with the operations added.\n\n    \"\"\"\n    query = ops(self.query)\n\n    return QueryInterface(self.database, query)\n</code></pre>"},{"location":"api/#cycquery.interface.QueryInterface.union","title":"union","text":"<pre><code>union(other)\n</code></pre> <p>Union the query with another query.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>'QueryInterface'</code> <p>The other query to union with.</p> required <p>Returns:</p> Type Description <code>QueryInterface</code> <p>QueryInterface object with the union operation added.</p> Source code in <code>cycquery/interface.py</code> <pre><code>def union(\n    self,\n    other: \"QueryInterface\",\n) -&gt; \"QueryInterface\":\n    \"\"\"Union the query with another query.\n\n    Parameters\n    ----------\n    other\n        The other query to union with.\n\n    Returns\n    -------\n    QueryInterface\n        QueryInterface object with the union operation added.\n\n    \"\"\"\n    query = qo.Union(other.query)(self.query)\n\n    return QueryInterface(self.database, query)\n</code></pre>"},{"location":"api/#cycquery.interface.QueryInterface.union_all","title":"union_all","text":"<pre><code>union_all(other)\n</code></pre> <p>Union all the query with another query.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>'QueryInterface'</code> <p>The other query to union all with.</p> required <p>Returns:</p> Type Description <code>QueryInterface</code> <p>QueryInterface object with the union all operation added.</p> Source code in <code>cycquery/interface.py</code> <pre><code>def union_all(\n    self,\n    other: \"QueryInterface\",\n) -&gt; \"QueryInterface\":\n    \"\"\"Union all the query with another query.\n\n    Parameters\n    ----------\n    other\n        The other query to union all with.\n\n    Returns\n    -------\n    QueryInterface\n        QueryInterface object with the union all operation added.\n\n    \"\"\"\n    query = qo.Union(other.query, union_all=True)(self.query)\n\n    return QueryInterface(self.database, query)\n</code></pre>"},{"location":"api/#cycquery.interface.QueryInterface.run","title":"run","text":"<pre><code>run(\n    limit=None,\n    index_col=None,\n    batch_mode=False,\n    batch_size=1000000,\n    dtype_backend=\"pyarrow\",\n)\n</code></pre> <p>Run the query, and fetch data.</p> <p>Parameters:</p> Name Type Description Default <code>limit</code> <code>int | None</code> <p>No. of rows to limit the query return.</p> <code>None</code> <code>index_col</code> <code>str | None</code> <p>Column which becomes the index, and defines the partitioning. Should be a indexed column in the SQL server, and any orderable type.</p> <code>None</code> <code>batch_mode</code> <code>bool</code> <p>Whether to run the query in batch mode. A generator is returned if True.</p> <code>False</code> <code>batch_size</code> <code>int</code> <p>Batch size for the query, default 1 million rows.</p> <code>1000000</code> <code>dtype_backend</code> <code>str</code> <p>Data type to use for the backend, default pyarrow.</p> <code>'pyarrow'</code> <p>Returns:</p> Type Description <code>DataFrame or Generator[DataFrame, None, None]</code> <p>Query result.</p> Source code in <code>cycquery/interface.py</code> <pre><code>def run(\n    self,\n    limit: int | None = None,\n    index_col: str | None = None,\n    batch_mode: bool = False,\n    batch_size: int = 1000000,\n    dtype_backend: str = \"pyarrow\",\n) -&gt; pd.DataFrame | Generator[pd.DataFrame, None, None]:\n    \"\"\"Run the query, and fetch data.\n\n    Parameters\n    ----------\n    limit\n        No. of rows to limit the query return.\n    index_col\n        Column which becomes the index, and defines the partitioning.\n        Should be a indexed column in the SQL server, and any orderable type.\n    batch_mode\n        Whether to run the query in batch mode. A generator is returned if True.\n    batch_size\n        Batch size for the query, default 1 million rows.\n    dtype_backend\n        Data type to use for the backend, default pyarrow.\n\n    Returns\n    -------\n    pandas.DataFrame or Generator[pandas.DataFrame, None, None]\n        Query result.\n\n    \"\"\"\n    if not batch_mode:\n        self._data = self.database.run_query(\n            self.query,\n            limit=limit,\n            index_col=index_col,\n            dtype_backend=dtype_backend,\n        )\n    else:\n        self._data = self.database.run_query_batch(\n            self.query,\n            index_col=index_col,\n            batch_size=batch_size,\n            dtype_backend=dtype_backend,\n        )\n\n    return self._data\n</code></pre>"},{"location":"api/#cycquery.interface.QueryInterface.save","title":"save","text":"<pre><code>save(path, file_format='parquet')\n</code></pre> <p>Save the query.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Path where the file will be saved.</p> required <code>file_format</code> <code>Literal['parquet', 'csv']</code> <p>File format of the file to save.</p> <code>'parquet'</code> <p>Returns:</p> Type Description <code>str</code> <p>Processed save path for upstream use.</p> Source code in <code>cycquery/interface.py</code> <pre><code>def save(\n    self,\n    path: str,\n    file_format: Literal[\"parquet\", \"csv\"] = \"parquet\",\n) -&gt; str:\n    \"\"\"Save the query.\n\n    Parameters\n    ----------\n    path\n        Path where the file will be saved.\n    file_format\n        File format of the file to save.\n\n    Returns\n    -------\n    str\n        Processed save path for upstream use.\n\n    \"\"\"\n    # If the query was already run.\n    if self._data is not None:\n        if isinstance(self._data, pd.DataFrame):\n            return save_dataframe(self._data, path, file_format=file_format)\n        if isinstance(self._data, Generator):\n            for i, df in enumerate(self._data):\n                save_dataframe(df, f\"{path}/batch-{i:03d}\", file_format=file_format)\n    # Save without running.\n    if file_format == \"csv\":\n        path = self.database.save_query_to_csv(self.query, path)\n    elif file_format == \"parquet\":\n        path = self.database.save_query_to_parquet(self.query, path)\n    else:\n        raise ValueError(\n            f\"Invalid file format '{file_format}'. Use 'parquet' or 'csv'.\"\n        )\n\n    return path\n</code></pre>"},{"location":"api/#cycquery.interface.QueryInterface.clear_data","title":"clear_data","text":"<pre><code>clear_data()\n</code></pre> <p>Clear data container.</p> <p>Sets the data attribute to None, thus clearing the dataframe contained.</p> Source code in <code>cycquery/interface.py</code> <pre><code>def clear_data(self) -&gt; None:\n    \"\"\"Clear data container.\n\n    Sets the data attribute to None, thus clearing the dataframe contained.\n\n    \"\"\"\n    self._data = None\n</code></pre>"},{"location":"api/#cycquery.mimiciii","title":"mimiciii","text":"<p>MIMIC-III query module.</p> <p>Supports querying of MIMIC-III.</p>"},{"location":"api/#cycquery.mimiciii.MIMICIIIQuerier","title":"MIMICIIIQuerier","text":"<p>               Bases: <code>DatasetQuerier</code></p> <p>MIMIC-III dataset querier.</p> Source code in <code>cycquery/mimiciii.py</code> <pre><code>class MIMICIIIQuerier(DatasetQuerier):\n    \"\"\"MIMIC-III dataset querier.\"\"\"\n\n    def diagnoses(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query MIMICIII diagnosis data.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed query, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(\"mimiciii\", \"diagnoses_icd\")\n\n        # Join with diagnoses dimension table.\n        table = qo.Join(\n            join_table=self.get_table(\"mimiciii\", \"d_icd_diagnoses\"),\n            on=[\"icd9_code\"],\n            on_to_type=[\"str\"],\n        )(table)\n\n        return QueryInterface(self.db, table)\n\n    def labevents(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query MIMICIII labevents data.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed query, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(\"mimiciii\", \"labevents\")\n\n        # Join with lab dimension table.\n        table = qo.Join(\n            join_table=self.get_table(\"mimiciii\", \"d_labitems\"),\n            on=[\"itemid\"],\n            on_to_type=[\"str\"],\n        )(table)\n\n        return QueryInterface(self.db, table)\n\n    def chartevents(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query MIMICIII chartevents data.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed query, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(\"mimiciii\", \"chartevents\")\n\n        # Join with dimension table.\n        table = qo.Join(\n            join_table=self.get_table(\"mimiciii\", \"d_items\"),\n            on=[\"itemid\"],\n            on_to_type=[\"str\"],\n        )(table)\n\n        return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.mimiciii.MIMICIIIQuerier.diagnoses","title":"diagnoses","text":"<pre><code>diagnoses()\n</code></pre> <p>Query MIMICIII diagnosis data.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed query, wrapped in an interface object.</p> Source code in <code>cycquery/mimiciii.py</code> <pre><code>def diagnoses(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query MIMICIII diagnosis data.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed query, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(\"mimiciii\", \"diagnoses_icd\")\n\n    # Join with diagnoses dimension table.\n    table = qo.Join(\n        join_table=self.get_table(\"mimiciii\", \"d_icd_diagnoses\"),\n        on=[\"icd9_code\"],\n        on_to_type=[\"str\"],\n    )(table)\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.mimiciii.MIMICIIIQuerier.labevents","title":"labevents","text":"<pre><code>labevents()\n</code></pre> <p>Query MIMICIII labevents data.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed query, wrapped in an interface object.</p> Source code in <code>cycquery/mimiciii.py</code> <pre><code>def labevents(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query MIMICIII labevents data.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed query, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(\"mimiciii\", \"labevents\")\n\n    # Join with lab dimension table.\n    table = qo.Join(\n        join_table=self.get_table(\"mimiciii\", \"d_labitems\"),\n        on=[\"itemid\"],\n        on_to_type=[\"str\"],\n    )(table)\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.mimiciii.MIMICIIIQuerier.chartevents","title":"chartevents","text":"<pre><code>chartevents()\n</code></pre> <p>Query MIMICIII chartevents data.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed query, wrapped in an interface object.</p> Source code in <code>cycquery/mimiciii.py</code> <pre><code>def chartevents(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query MIMICIII chartevents data.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed query, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(\"mimiciii\", \"chartevents\")\n\n    # Join with dimension table.\n    table = qo.Join(\n        join_table=self.get_table(\"mimiciii\", \"d_items\"),\n        on=[\"itemid\"],\n        on_to_type=[\"str\"],\n    )(table)\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.mimiciv","title":"mimiciv","text":"<p>MIMIC-IV query module.</p> <p>Supports querying of MIMICIV-2.0.</p>"},{"location":"api/#cycquery.mimiciv.MIMICIVQuerier","title":"MIMICIVQuerier","text":"<p>               Bases: <code>DatasetQuerier</code></p> <p>MIMICIV dataset querier.</p> Source code in <code>cycquery/mimiciv.py</code> <pre><code>class MIMICIVQuerier(DatasetQuerier):\n    \"\"\"MIMICIV dataset querier.\"\"\"\n\n    def patients(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query MIMIC patient data.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed query, wrapped in an interface object.\n\n        Notes\n        -----\n        The function infers the approximate year a patient received care, using the\n        `anchor_year` and `anchor_year_group` columns. The `join` and `ops` supplied\n        are applied after the approximate year is inferred. `dod` is\n        adjusted based on the inferred approximate year of care.\n\n        \"\"\"\n        table = self.get_table(\"mimiciv_hosp\", \"patients\")\n\n        # Process and include patient's anchor year.\n        table = select(\n            *list(table.columns),\n            (\n                func.substr(get_column(table, \"anchor_year_group\"), 1, 4).cast(Integer)\n            ).label(\"anchor_year_group_start\"),\n            (\n                func.substr(get_column(table, \"anchor_year_group\"), 8, 12).cast(Integer)\n            ).label(\"anchor_year_group_end\"),\n        ).subquery()\n\n        # Select the middle of the anchor year group as the anchor year\n        table = select(\n            *list(table.c),\n            (\n                get_column(table, \"anchor_year_group_start\")\n                + (\n                    get_column(table, \"anchor_year_group_end\")\n                    - get_column(table, \"anchor_year_group_start\")\n                )\n                / 2\n            ).label(\"anchor_year_group_middle\"),\n        ).subquery()\n\n        table = select(\n            *list(table.c),\n            (\n                get_column(table, \"anchor_year_group_middle\")\n                - get_column(table, \"anchor_year\")\n            ).label(\"anchor_year_difference\"),\n        ).subquery()\n\n        # Shift relevant columns by anchor year difference\n        table = qo.AddDeltaColumn(\"dod\", years=\"anchor_year_difference\")(table)\n        table = qo.Drop(\n            [\n                \"anchor_year_group_start\",\n                \"anchor_year_group_end\",\n                \"anchor_year_group_middle\",\n            ],\n        )(table)\n\n        return QueryInterface(self.db, table)\n\n    def diagnoses_icd(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query MIMIC diagnosis data.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed query, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(\"mimiciv_hosp\", \"diagnoses_icd\")\n\n        # Join with diagnoses dimension table.\n        table = qo.Join(\n            join_table=self.get_table(\"mimiciv_hosp\", \"d_icd_diagnoses\"),\n            on=[\"icd_code\", \"icd_version\"],\n            on_to_type=[\"str\", \"int\"],\n        )(table)\n\n        return QueryInterface(self.db, table)\n\n    def procedures_icd(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query MIMIC procedures data.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed query, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(\"mimiciv_hosp\", \"procedures_icd\")\n\n        # Join with procedures dimension table.\n        table = qo.Join(\n            join_table=self.get_table(\"mimiciv_hosp\", \"d_icd_procedures\"),\n            on=[\"icd_code\", \"icd_version\"],\n            on_to_type=[\"str\", \"int\"],\n        )(table)\n\n        return QueryInterface(self.db, table)\n\n    def labevents(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query lab events from the hospital module.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed query, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(\"mimiciv_hosp\", \"labevents\")\n        dim_items_table = self.get_table(\"mimiciv_hosp\", \"d_labitems\")\n\n        # Join with lab items dimension table.\n        table = qo.Join(\n            join_table=dim_items_table,\n            on=[\"itemid\"],\n        )(table)\n\n        return QueryInterface(self.db, table)\n\n    def chartevents(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query ICU chart events from the ICU module.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed table, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(\"mimiciv_icu\", \"chartevents\")\n        dim_items_table = self.get_table(\"mimiciv_icu\", \"d_items\")\n\n        # Join with items dimension table.\n        table = qo.Join(\n            dim_items_table,\n            on=\"itemid\",\n        )(table)\n\n        return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.mimiciv.MIMICIVQuerier.patients","title":"patients","text":"<pre><code>patients()\n</code></pre> <p>Query MIMIC patient data.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed query, wrapped in an interface object.</p> Notes <p>The function infers the approximate year a patient received care, using the <code>anchor_year</code> and <code>anchor_year_group</code> columns. The <code>join</code> and <code>ops</code> supplied are applied after the approximate year is inferred. <code>dod</code> is adjusted based on the inferred approximate year of care.</p> Source code in <code>cycquery/mimiciv.py</code> <pre><code>def patients(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query MIMIC patient data.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed query, wrapped in an interface object.\n\n    Notes\n    -----\n    The function infers the approximate year a patient received care, using the\n    `anchor_year` and `anchor_year_group` columns. The `join` and `ops` supplied\n    are applied after the approximate year is inferred. `dod` is\n    adjusted based on the inferred approximate year of care.\n\n    \"\"\"\n    table = self.get_table(\"mimiciv_hosp\", \"patients\")\n\n    # Process and include patient's anchor year.\n    table = select(\n        *list(table.columns),\n        (\n            func.substr(get_column(table, \"anchor_year_group\"), 1, 4).cast(Integer)\n        ).label(\"anchor_year_group_start\"),\n        (\n            func.substr(get_column(table, \"anchor_year_group\"), 8, 12).cast(Integer)\n        ).label(\"anchor_year_group_end\"),\n    ).subquery()\n\n    # Select the middle of the anchor year group as the anchor year\n    table = select(\n        *list(table.c),\n        (\n            get_column(table, \"anchor_year_group_start\")\n            + (\n                get_column(table, \"anchor_year_group_end\")\n                - get_column(table, \"anchor_year_group_start\")\n            )\n            / 2\n        ).label(\"anchor_year_group_middle\"),\n    ).subquery()\n\n    table = select(\n        *list(table.c),\n        (\n            get_column(table, \"anchor_year_group_middle\")\n            - get_column(table, \"anchor_year\")\n        ).label(\"anchor_year_difference\"),\n    ).subquery()\n\n    # Shift relevant columns by anchor year difference\n    table = qo.AddDeltaColumn(\"dod\", years=\"anchor_year_difference\")(table)\n    table = qo.Drop(\n        [\n            \"anchor_year_group_start\",\n            \"anchor_year_group_end\",\n            \"anchor_year_group_middle\",\n        ],\n    )(table)\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.mimiciv.MIMICIVQuerier.diagnoses_icd","title":"diagnoses_icd","text":"<pre><code>diagnoses_icd()\n</code></pre> <p>Query MIMIC diagnosis data.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed query, wrapped in an interface object.</p> Source code in <code>cycquery/mimiciv.py</code> <pre><code>def diagnoses_icd(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query MIMIC diagnosis data.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed query, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(\"mimiciv_hosp\", \"diagnoses_icd\")\n\n    # Join with diagnoses dimension table.\n    table = qo.Join(\n        join_table=self.get_table(\"mimiciv_hosp\", \"d_icd_diagnoses\"),\n        on=[\"icd_code\", \"icd_version\"],\n        on_to_type=[\"str\", \"int\"],\n    )(table)\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.mimiciv.MIMICIVQuerier.procedures_icd","title":"procedures_icd","text":"<pre><code>procedures_icd()\n</code></pre> <p>Query MIMIC procedures data.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed query, wrapped in an interface object.</p> Source code in <code>cycquery/mimiciv.py</code> <pre><code>def procedures_icd(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query MIMIC procedures data.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed query, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(\"mimiciv_hosp\", \"procedures_icd\")\n\n    # Join with procedures dimension table.\n    table = qo.Join(\n        join_table=self.get_table(\"mimiciv_hosp\", \"d_icd_procedures\"),\n        on=[\"icd_code\", \"icd_version\"],\n        on_to_type=[\"str\", \"int\"],\n    )(table)\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.mimiciv.MIMICIVQuerier.labevents","title":"labevents","text":"<pre><code>labevents()\n</code></pre> <p>Query lab events from the hospital module.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed query, wrapped in an interface object.</p> Source code in <code>cycquery/mimiciv.py</code> <pre><code>def labevents(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query lab events from the hospital module.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed query, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(\"mimiciv_hosp\", \"labevents\")\n    dim_items_table = self.get_table(\"mimiciv_hosp\", \"d_labitems\")\n\n    # Join with lab items dimension table.\n    table = qo.Join(\n        join_table=dim_items_table,\n        on=[\"itemid\"],\n    )(table)\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.mimiciv.MIMICIVQuerier.chartevents","title":"chartevents","text":"<pre><code>chartevents()\n</code></pre> <p>Query ICU chart events from the ICU module.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed table, wrapped in an interface object.</p> Source code in <code>cycquery/mimiciv.py</code> <pre><code>def chartevents(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query ICU chart events from the ICU module.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed table, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(\"mimiciv_icu\", \"chartevents\")\n    dim_items_table = self.get_table(\"mimiciv_icu\", \"d_items\")\n\n    # Join with items dimension table.\n    table = qo.Join(\n        dim_items_table,\n        on=\"itemid\",\n    )(table)\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.omop","title":"omop","text":"<p>OMOP query API.</p>"},{"location":"api/#cycquery.omop.OMOPQuerier","title":"OMOPQuerier","text":"<p>               Bases: <code>DatasetQuerier</code></p> <p>OMOP querier.</p> Source code in <code>cycquery/omop.py</code> <pre><code>class OMOPQuerier(DatasetQuerier):\n    \"\"\"OMOP querier.\"\"\"\n\n    def __init__(\n        self,\n        database: str,\n        user: str,\n        password: str,\n        dbms: str = \"postgresql\",\n        host: str = \"localhost\",\n        port: int = 5432,\n        schema_name: str = \"omop\",\n    ) -&gt; None:\n        \"\"\"Initialize OMOP querier.\"\"\"\n        super().__init__(\n            database=database,\n            user=user,\n            password=password,\n            dbms=dbms,\n            host=host,\n            port=port,\n        )\n        self.schema_name = schema_name\n\n    def map_concept_ids_to_name(\n        self,\n        src_table: Union[Subquery, QueryInterface],\n        src_cols: Union[str, List[str]],\n        dst_cols: Optional[Union[str, List[str]]] = None,\n    ) -&gt; QueryInterface:\n        \"\"\"Map concept IDs in a source table to concept names from concept table.\n\n        For each concept ID column with a name like `somecol_concept_ID`, the mapped\n        concept name column will be named `somecol_concept_name`. If `dst_cols` is\n        specified, the mapped concept name column will be named according to the\n        corresponding name in `dst_cols`.\n\n        Parameters\n        ----------\n        src_table\n            Source table with concept IDs.\n        src_cols\n            Column name(s) to consider as concept IDs for mapping.\n        dst_cols\n            Column name(s) to assign for the mapped concept name columns.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Query with mapped columns from concept table.\n\n        \"\"\"\n        if isinstance(src_table, QueryInterface):\n            src_table = src_table.query\n        concept_table = self.get_table(self.schema_name, \"concept\")\n        src_cols = to_list(src_cols)\n        if dst_cols:\n            dst_cols = to_list(dst_cols)\n            if len(dst_cols) != len(src_cols):\n                raise ValueError(\"dst_cols must be same length as src_cols\")\n\n        for i, col in enumerate(src_cols):\n            if ID not in col:\n                raise ValueError(\"Specified column not a concept ID column!\")\n            src_table = qo.Join(\n                concept_table,\n                on=(col, CONCEPT_ID),\n                join_table_cols=[CONCEPT_NAME],\n                isouter=True,\n            )(src_table)\n            dst_col_name = dst_cols[i] if dst_cols else col.replace(ID, NAME)\n            src_table = qo.Rename({CONCEPT_NAME: dst_col_name})(src_table)  # type: ignore[arg-type]\n\n        return QueryInterface(self.db, src_table)\n\n    def _map_care_site_id(\n        self,\n        source_table: Union[Subquery, QueryInterface],\n    ) -&gt; QueryInterface:\n        \"\"\"Map care_site_id in a source table to care_site table.\n\n        Parameters\n        ----------\n        source_table\n            Source table with care_site_id.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Query with mapped columns from care_site table.\n\n        \"\"\"\n        if isinstance(source_table, QueryInterface):\n            source_table = source_table.query\n        care_site_table = self.get_table(self.schema_name, \"care_site\")\n        table = qo.Join(\n            care_site_table,\n            on=CARE_SITE_ID,\n            join_table_cols=[CARE_SITE_NAME, CARE_SITE_SOURCE_VALUE],\n            isouter=True,\n        )(source_table)\n\n        return QueryInterface(self.db, table)\n\n    def visit_occurrence(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query OMOP visit_occurrence table.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed query, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(self.schema_name, \"visit_occurrence\")\n        table = self.map_concept_ids_to_name(  # type: ignore[assignment]\n            table,\n            [\n                \"visit_concept_id\",\n                \"visit_type_concept_id\",\n            ],\n        )\n        table = self._map_care_site_id(table)  # type: ignore[assignment]\n\n        return QueryInterface(self.db, table)\n\n    def visit_detail(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query OMOP visit_detail table.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed query, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(self.schema_name, \"visit_detail\")\n        table = self.map_concept_ids_to_name(  # type: ignore[assignment]\n            table,\n            [\"visit_detail_concept_id\", \"visit_detail_type_concept_id\"],\n        )\n\n        return QueryInterface(self.db, table)\n\n    def person(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query OMOP person table.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed query, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(self.schema_name, \"person\")\n        table = self.map_concept_ids_to_name(  # type: ignore[assignment]\n            table,\n            [\"gender_concept_id\", \"race_concept_id\", \"ethnicity_concept_id\"],\n        )\n\n        return QueryInterface(self.db, table)\n\n    def observation(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query OMOP observation table.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed query, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(self.schema_name, \"observation\")\n        table = self.map_concept_ids_to_name(  # type: ignore[assignment]\n            table,\n            [OBSERVATION_CONCEPT_ID, OBSERVATION_TYPE_CONCEPT_ID],\n        )\n\n        return QueryInterface(self.db, table)\n\n    def measurement(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query OMOP measurement table.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed query, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(self.schema_name, \"measurement\")\n        # Cast value_as_concept_id to int.\n        table = qo.Cast([VALUE_AS_CONCEPT_ID], \"int\")(table)\n        table = self.map_concept_ids_to_name(  # type: ignore[assignment]\n            table,\n            [MEASUREMENT_CONCEPT_ID, MEASUREMENT_TYPE_CONCEPT_ID, UNIT_CONCEPT_ID],\n        )\n\n        return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.omop.OMOPQuerier.__init__","title":"__init__","text":"<pre><code>__init__(\n    database,\n    user,\n    password,\n    dbms=\"postgresql\",\n    host=\"localhost\",\n    port=5432,\n    schema_name=\"omop\",\n)\n</code></pre> <p>Initialize OMOP querier.</p> Source code in <code>cycquery/omop.py</code> <pre><code>def __init__(\n    self,\n    database: str,\n    user: str,\n    password: str,\n    dbms: str = \"postgresql\",\n    host: str = \"localhost\",\n    port: int = 5432,\n    schema_name: str = \"omop\",\n) -&gt; None:\n    \"\"\"Initialize OMOP querier.\"\"\"\n    super().__init__(\n        database=database,\n        user=user,\n        password=password,\n        dbms=dbms,\n        host=host,\n        port=port,\n    )\n    self.schema_name = schema_name\n</code></pre>"},{"location":"api/#cycquery.omop.OMOPQuerier.map_concept_ids_to_name","title":"map_concept_ids_to_name","text":"<pre><code>map_concept_ids_to_name(src_table, src_cols, dst_cols=None)\n</code></pre> <p>Map concept IDs in a source table to concept names from concept table.</p> <p>For each concept ID column with a name like <code>somecol_concept_ID</code>, the mapped concept name column will be named <code>somecol_concept_name</code>. If <code>dst_cols</code> is specified, the mapped concept name column will be named according to the corresponding name in <code>dst_cols</code>.</p> <p>Parameters:</p> Name Type Description Default <code>src_table</code> <code>Union[Subquery, QueryInterface]</code> <p>Source table with concept IDs.</p> required <code>src_cols</code> <code>Union[str, List[str]]</code> <p>Column name(s) to consider as concept IDs for mapping.</p> required <code>dst_cols</code> <code>Optional[Union[str, List[str]]]</code> <p>Column name(s) to assign for the mapped concept name columns.</p> <code>None</code> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Query with mapped columns from concept table.</p> Source code in <code>cycquery/omop.py</code> <pre><code>def map_concept_ids_to_name(\n    self,\n    src_table: Union[Subquery, QueryInterface],\n    src_cols: Union[str, List[str]],\n    dst_cols: Optional[Union[str, List[str]]] = None,\n) -&gt; QueryInterface:\n    \"\"\"Map concept IDs in a source table to concept names from concept table.\n\n    For each concept ID column with a name like `somecol_concept_ID`, the mapped\n    concept name column will be named `somecol_concept_name`. If `dst_cols` is\n    specified, the mapped concept name column will be named according to the\n    corresponding name in `dst_cols`.\n\n    Parameters\n    ----------\n    src_table\n        Source table with concept IDs.\n    src_cols\n        Column name(s) to consider as concept IDs for mapping.\n    dst_cols\n        Column name(s) to assign for the mapped concept name columns.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Query with mapped columns from concept table.\n\n    \"\"\"\n    if isinstance(src_table, QueryInterface):\n        src_table = src_table.query\n    concept_table = self.get_table(self.schema_name, \"concept\")\n    src_cols = to_list(src_cols)\n    if dst_cols:\n        dst_cols = to_list(dst_cols)\n        if len(dst_cols) != len(src_cols):\n            raise ValueError(\"dst_cols must be same length as src_cols\")\n\n    for i, col in enumerate(src_cols):\n        if ID not in col:\n            raise ValueError(\"Specified column not a concept ID column!\")\n        src_table = qo.Join(\n            concept_table,\n            on=(col, CONCEPT_ID),\n            join_table_cols=[CONCEPT_NAME],\n            isouter=True,\n        )(src_table)\n        dst_col_name = dst_cols[i] if dst_cols else col.replace(ID, NAME)\n        src_table = qo.Rename({CONCEPT_NAME: dst_col_name})(src_table)  # type: ignore[arg-type]\n\n    return QueryInterface(self.db, src_table)\n</code></pre>"},{"location":"api/#cycquery.omop.OMOPQuerier.visit_occurrence","title":"visit_occurrence","text":"<pre><code>visit_occurrence()\n</code></pre> <p>Query OMOP visit_occurrence table.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed query, wrapped in an interface object.</p> Source code in <code>cycquery/omop.py</code> <pre><code>def visit_occurrence(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query OMOP visit_occurrence table.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed query, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(self.schema_name, \"visit_occurrence\")\n    table = self.map_concept_ids_to_name(  # type: ignore[assignment]\n        table,\n        [\n            \"visit_concept_id\",\n            \"visit_type_concept_id\",\n        ],\n    )\n    table = self._map_care_site_id(table)  # type: ignore[assignment]\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.omop.OMOPQuerier.visit_detail","title":"visit_detail","text":"<pre><code>visit_detail()\n</code></pre> <p>Query OMOP visit_detail table.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed query, wrapped in an interface object.</p> Source code in <code>cycquery/omop.py</code> <pre><code>def visit_detail(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query OMOP visit_detail table.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed query, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(self.schema_name, \"visit_detail\")\n    table = self.map_concept_ids_to_name(  # type: ignore[assignment]\n        table,\n        [\"visit_detail_concept_id\", \"visit_detail_type_concept_id\"],\n    )\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.omop.OMOPQuerier.person","title":"person","text":"<pre><code>person()\n</code></pre> <p>Query OMOP person table.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed query, wrapped in an interface object.</p> Source code in <code>cycquery/omop.py</code> <pre><code>def person(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query OMOP person table.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed query, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(self.schema_name, \"person\")\n    table = self.map_concept_ids_to_name(  # type: ignore[assignment]\n        table,\n        [\"gender_concept_id\", \"race_concept_id\", \"ethnicity_concept_id\"],\n    )\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.omop.OMOPQuerier.observation","title":"observation","text":"<pre><code>observation()\n</code></pre> <p>Query OMOP observation table.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed query, wrapped in an interface object.</p> Source code in <code>cycquery/omop.py</code> <pre><code>def observation(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query OMOP observation table.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed query, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(self.schema_name, \"observation\")\n    table = self.map_concept_ids_to_name(  # type: ignore[assignment]\n        table,\n        [OBSERVATION_CONCEPT_ID, OBSERVATION_TYPE_CONCEPT_ID],\n    )\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.omop.OMOPQuerier.measurement","title":"measurement","text":"<pre><code>measurement()\n</code></pre> <p>Query OMOP measurement table.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed query, wrapped in an interface object.</p> Source code in <code>cycquery/omop.py</code> <pre><code>def measurement(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query OMOP measurement table.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed query, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(self.schema_name, \"measurement\")\n    # Cast value_as_concept_id to int.\n    table = qo.Cast([VALUE_AS_CONCEPT_ID], \"int\")(table)\n    table = self.map_concept_ids_to_name(  # type: ignore[assignment]\n        table,\n        [MEASUREMENT_CONCEPT_ID, MEASUREMENT_TYPE_CONCEPT_ID, UNIT_CONCEPT_ID],\n    )\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.ops","title":"ops","text":"<p>Query operations.</p>"},{"location":"api/#cycquery.ops.QueryOp","title":"QueryOp","text":"<p>Base class for query operations.</p> Source code in <code>cycquery/ops.py</code> <pre><code>class QueryOp:\n    \"\"\"Base class for query operations.\"\"\"\n\n    _ops: typing.Dict[str, \"QueryOp\"]\n\n    def __init__(self, *args: typing.Any, **kwargs: typing.Any) -&gt; None:\n        \"\"\"Initialize the class.\"\"\"\n        super().__setattr__(\"_ops\", OrderedDict())\n\n    @abstractmethod\n    def __call__(self, *args: typing.Any, **kwargs: typing.Any) -&gt; Subquery:\n        \"\"\"Implement a calling function.\"\"\"\n        pass\n\n    def _add_op(self, name: str, op_: \"QueryOp\") -&gt; None:\n        \"\"\"Add a child operation to the current query operation.\n\n        The query op can be accessed as an attribute using the given name.\n\n        Parameters\n        ----------\n        name\n            Name of the child op. The child op can be accessed from this op using\n            the given name\n        op_\n            Child op to be added to the parent query op.\n\n        \"\"\"\n        if not isinstance(op_, QueryOp) and op_ is not None:\n            raise TypeError(\"{} is not a QueryOp subclass\".format(str(op_)))\n        if not isinstance(name, str):\n            raise TypeError(\"Query op name should be a string\")\n        if hasattr(self, name) and name not in self._ops:\n            raise KeyError(\"Attribute '{}' already exists\".format(name))\n        if \".\" in name:\n            raise KeyError('Query op name can\\'t contain \".\", got: {}'.format(name))\n        if name == \"\":\n            raise KeyError('Query op name can\\'t be empty string \"\"')\n        self._ops[name] = op_\n\n    def _get_ops(self) -&gt; typing.Iterator[\"QueryOp\"]:\n        \"\"\"Return an iterator over the child operations.\n\n        Returns\n        -------\n        typing.Iterator[QueryOp]\n            Iterator over the child operations.\n\n        \"\"\"\n        for _, op_ in self._ops.items():\n            yield op_\n\n    def _get_name(self) -&gt; str:\n        \"\"\"Get the name of the query op.\n\n        Returns\n        -------\n        str\n            Name of the query op.\n\n        \"\"\"\n        return self.__class__.__name__\n\n    def __setattr__(self, name: str, value: \"QueryOp\") -&gt; None:\n        \"\"\"Set an attribute.\n\n        Parameters\n        ----------\n        name\n            Name of the attribute.\n        value\n            Value of the attribute.\n\n        \"\"\"\n        ops = self.__dict__.get(\"_ops\")\n        if isinstance(value, QueryOp):\n            if ops is None:\n                raise AttributeError(\"Can't assign op before QueryOp.__init__() call\")\n            ops[name] = value\n        elif ops is not None and name in ops:\n            if value is not None:\n                raise TypeError(\n                    \"Cannot assign '{}' as child op '{}' (QueryOp or None expected)\",\n                )\n            ops[name] = value\n        else:\n            super().__setattr__(name, value)\n\n    def _extra_repr(self) -&gt; str:\n        \"\"\"Set the extra representation of the query op.\n\n        To print customized extra information, you should re-implement\n        this method in your own query ops. Both single-line and multi-line\n        strings are acceptable.\n\n        Returns\n        -------\n        str\n            Extra representation of the query op.\n\n        \"\"\"\n        return \"\"\n\n    def __repr__(self) -&gt; str:\n        \"\"\"Return the string representation of the query op.\n\n        Returns\n        -------\n        str\n            String representation of the query op.\n\n        \"\"\"\n        extra_lines = []\n        extra_repr = self._extra_repr()\n        if extra_repr:\n            extra_lines = extra_repr.split(\"\\n\")\n        child_lines = []\n        for key, op_ in self._ops.items():\n            mod_str = repr(op_)\n            mod_str = _addindent(mod_str, 2)\n            child_lines.append(\"(\" + key + \"): \" + mod_str)\n        lines = extra_lines + child_lines\n        main_str = self._get_name() + \"(\"\n        if lines:\n            if len(extra_lines) == 1 and not child_lines:\n                main_str += extra_lines[0]\n            else:\n                main_str += \"\\n  \" + \"\\n  \".join(lines) + \"\\n\"\n        main_str += \")\"\n\n        return main_str\n\n    def __getattr__(self, name: str) -&gt; \"QueryOp\":\n        \"\"\"Get an attribute.\n\n        Parameters\n        ----------\n        name\n            Name of the attribute.\n\n        Returns\n        -------\n        QueryOp\n            The child operation with the given name.\n\n        \"\"\"\n        if name in self._ops:\n            return self._ops[name]\n        raise AttributeError(\n            f\"'{self.__class__.__name__}' object has no attribute '{name}'\",\n        )\n</code></pre>"},{"location":"api/#cycquery.ops.QueryOp.__init__","title":"__init__","text":"<pre><code>__init__(*args, **kwargs)\n</code></pre> <p>Initialize the class.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(self, *args: typing.Any, **kwargs: typing.Any) -&gt; None:\n    \"\"\"Initialize the class.\"\"\"\n    super().__setattr__(\"_ops\", OrderedDict())\n</code></pre>"},{"location":"api/#cycquery.ops.QueryOp.__call__","title":"__call__  <code>abstractmethod</code>","text":"<pre><code>__call__(*args, **kwargs)\n</code></pre> <p>Implement a calling function.</p> Source code in <code>cycquery/ops.py</code> <pre><code>@abstractmethod\ndef __call__(self, *args: typing.Any, **kwargs: typing.Any) -&gt; Subquery:\n    \"\"\"Implement a calling function.\"\"\"\n    pass\n</code></pre>"},{"location":"api/#cycquery.ops.QueryOp.__setattr__","title":"__setattr__","text":"<pre><code>__setattr__(name, value)\n</code></pre> <p>Set an attribute.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of the attribute.</p> required <code>value</code> <code>'QueryOp'</code> <p>Value of the attribute.</p> required Source code in <code>cycquery/ops.py</code> <pre><code>def __setattr__(self, name: str, value: \"QueryOp\") -&gt; None:\n    \"\"\"Set an attribute.\n\n    Parameters\n    ----------\n    name\n        Name of the attribute.\n    value\n        Value of the attribute.\n\n    \"\"\"\n    ops = self.__dict__.get(\"_ops\")\n    if isinstance(value, QueryOp):\n        if ops is None:\n            raise AttributeError(\"Can't assign op before QueryOp.__init__() call\")\n        ops[name] = value\n    elif ops is not None and name in ops:\n        if value is not None:\n            raise TypeError(\n                \"Cannot assign '{}' as child op '{}' (QueryOp or None expected)\",\n            )\n        ops[name] = value\n    else:\n        super().__setattr__(name, value)\n</code></pre>"},{"location":"api/#cycquery.ops.QueryOp.__repr__","title":"__repr__","text":"<pre><code>__repr__()\n</code></pre> <p>Return the string representation of the query op.</p> <p>Returns:</p> Type Description <code>str</code> <p>String representation of the query op.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __repr__(self) -&gt; str:\n    \"\"\"Return the string representation of the query op.\n\n    Returns\n    -------\n    str\n        String representation of the query op.\n\n    \"\"\"\n    extra_lines = []\n    extra_repr = self._extra_repr()\n    if extra_repr:\n        extra_lines = extra_repr.split(\"\\n\")\n    child_lines = []\n    for key, op_ in self._ops.items():\n        mod_str = repr(op_)\n        mod_str = _addindent(mod_str, 2)\n        child_lines.append(\"(\" + key + \"): \" + mod_str)\n    lines = extra_lines + child_lines\n    main_str = self._get_name() + \"(\"\n    if lines:\n        if len(extra_lines) == 1 and not child_lines:\n            main_str += extra_lines[0]\n        else:\n            main_str += \"\\n  \" + \"\\n  \".join(lines) + \"\\n\"\n    main_str += \")\"\n\n    return main_str\n</code></pre>"},{"location":"api/#cycquery.ops.QueryOp.__getattr__","title":"__getattr__","text":"<pre><code>__getattr__(name)\n</code></pre> <p>Get an attribute.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of the attribute.</p> required <p>Returns:</p> Type Description <code>QueryOp</code> <p>The child operation with the given name.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __getattr__(self, name: str) -&gt; \"QueryOp\":\n    \"\"\"Get an attribute.\n\n    Parameters\n    ----------\n    name\n        Name of the attribute.\n\n    Returns\n    -------\n    QueryOp\n        The child operation with the given name.\n\n    \"\"\"\n    if name in self._ops:\n        return self._ops[name]\n    raise AttributeError(\n        f\"'{self.__class__.__name__}' object has no attribute '{name}'\",\n    )\n</code></pre>"},{"location":"api/#cycquery.ops.Sequential","title":"Sequential","text":"<p>               Bases: <code>QueryOp</code></p> <p>Sequential query operations class.</p> <p>Chains a sequence of query operations and executes the final query on a table.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; Sequential(Drop([\"col1\", \"col2\"]), ...)\n&gt;&gt;&gt; Sequential([Drop([\"col1\", \"col2\"]), ...])\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class Sequential(QueryOp):\n    \"\"\"Sequential query operations class.\n\n    Chains a sequence of query operations and executes the final query on a table.\n\n    Examples\n    --------\n    &gt;&gt;&gt; Sequential(Drop([\"col1\", \"col2\"]), ...)\n    &gt;&gt;&gt; Sequential([Drop([\"col1\", \"col2\"]), ...])\n\n    \"\"\"\n\n    @typing.overload\n    def __init__(self, *ops: QueryOp) -&gt; None: ...\n\n    @typing.overload\n    def __init__(self, ops: typing.List[QueryOp]) -&gt; None: ...\n\n    @typing.overload\n    def __init__(self, op: OrderedDict[str, QueryOp]) -&gt; None: ...\n\n    def __init__(self, *args: QueryOp) -&gt; None:  # type: ignore\n        \"\"\"Initialize the class.\n\n        Parameters\n        ----------\n        args\n            Query operations to be chained sequentially.\n\n        \"\"\"\n        super().__init__()\n        if len(args) == 1 and isinstance(args[0], OrderedDict):\n            for key, op_ in args[0].items():\n                self._add_op(key, op_)\n        elif len(args) == 1 and isinstance(args[0], list):\n            for idx, op_ in enumerate(args[0]):\n                self._add_op(str(idx), op_)\n        else:\n            for idx, op_ in enumerate(args):\n                self._add_op(str(idx), op_)\n\n    def __len__(self) -&gt; int:\n        \"\"\"Return the number of query ops in the Sequential.\n\n        Returns\n        -------\n        int\n            Number of query ops in the Sequential.\n\n        \"\"\"\n        return len(self._ops)\n\n    def __iter__(self) -&gt; typing.Iterator[QueryOp]:\n        \"\"\"Return an iterator over the query ops.\n\n        Returns\n        -------\n        typing.Iterator[QueryOp]\n            Iterator over the query ops.\n\n        \"\"\"\n        return iter(self._ops.values())\n\n    def __add__(self, other: \"Sequential\") -&gt; \"Sequential\":\n        \"\"\"Add two Sequential objects.\n\n        Parameters\n        ----------\n        other\n            Sequential object to be added.\n\n        Returns\n        -------\n        Sequential\n            Sequential object with the two Sequential objects chained.\n\n        \"\"\"\n        if isinstance(other, Sequential):\n            ret = Sequential()\n            for op_ in self:\n                ret.append(op_)\n            for op_ in other:\n                ret.append(op_)\n            return ret\n        raise ValueError(\n            \"Add operator supports only objects \"\n            \"of Sequential class, but {} is given.\".format(str(type(other))),\n        )\n\n    def __iadd__(self, other: \"Sequential\") -&gt; \"Sequential\":\n        \"\"\"Add two Sequential objects inplace.\n\n        Parameters\n        ----------\n        other\n            Sequential object to be added.\n\n        Returns\n        -------\n        Sequential\n            Sequential object with the two Sequential objects chained.\n\n        \"\"\"\n        if isinstance(other, Sequential):\n            offset = len(self)\n            for i, op_ in enumerate(other):\n                self._add_op(str(i + offset), op_)\n            return self\n        raise ValueError(\n            \"Add operator supports only objects \"\n            \"of Sequential class, but {} is given.\".format(str(type(other))),\n        )\n\n    def _get_item_by_idx(\n        self,\n        iterator: typing.Iterator[typing.Any],\n        idx: int,\n    ) -&gt; typing.Any:\n        \"\"\"Get the idx-th item of the iterator.\n\n        Parameters\n        ----------\n        iterator\n            Iterator to get the item from.\n        idx\n            Index of the item to get.\n\n        Returns\n        -------\n        QueryOp\n            The idx-th item of the iterator.\n\n        \"\"\"\n        size = len(self)\n        idx = operator.index(idx)\n        if not -size &lt;= idx &lt; size:\n            raise IndexError(\"index {} is out of range\".format(idx))\n        idx %= size\n\n        return next(islice(iterator, idx, None))\n\n    def __getitem__(\n        self,\n        idx: typing.Union[slice, int],\n    ) -&gt; typing.Any:\n        \"\"\"Get the idx-th item of the sequential query op.\n\n        Parameters\n        ----------\n        idx\n            Index of the item to get.\n\n        Returns\n        -------\n        Sequential or QueryOp\n            The idx-th item of the sequential query op.\n\n        \"\"\"\n        if isinstance(idx, slice):\n            return self.__class__(OrderedDict(list(self._ops.items())[idx]))\n\n        return self._get_item_by_idx(self._ops.values(), idx)  # type: ignore\n\n    def __setitem__(self, idx: int, op_: QueryOp) -&gt; None:\n        \"\"\"Set the idx-th item of the sequential query op.\n\n        Parameters\n        ----------\n        idx\n            Index of the item to set.\n        op_\n            Query op to set.\n\n        \"\"\"\n        key: str = self._get_item_by_idx(self._ops.keys(), idx)  # type: ignore\n        return setattr(self, key, op_)\n\n    def __delitem__(self, idx: typing.Union[slice, int]) -&gt; None:\n        \"\"\"Delete the idx-th item of the sequential query op.\n\n        Parameters\n        ----------\n        idx\n            Index of the item to delete.\n\n        \"\"\"\n        if isinstance(idx, slice):\n            for key in list(self._ops.keys())[idx]:\n                delattr(self, key)\n        else:\n            key = self._get_item_by_idx(self._ops.keys(), idx)  # type: ignore\n            delattr(self, key)\n        str_indices = [str(i) for i in range(len(self._ops))]\n        self._ops = OrderedDict(list(zip(str_indices, self._ops.values())))\n\n    def append(self, op_: QueryOp) -&gt; \"Sequential\":\n        \"\"\"Append a given query op to the end.\n\n        Parameters\n        ----------\n        op_\n            Query op to append.\n\n        Returns\n        -------\n        Sequential\n            Sequential object with the query op appended.\n\n        \"\"\"\n        self._add_op(str(len(self)), op_)\n        return self\n\n    def pop(self, key: typing.Union[int, slice]) -&gt; QueryOp:\n        \"\"\"Pop the query op at the given index.\n\n        Parameters\n        ----------\n        key\n            Index of the query op to pop.\n\n        Returns\n        -------\n        QueryOp\n            Popped query op.\n\n        \"\"\"\n        v = self[key]\n        del self[key]\n\n        return v\n\n    def insert(self, index: int, op_: QueryOp) -&gt; \"Sequential\":\n        \"\"\"Insert a given query op at the given index.\n\n        Parameters\n        ----------\n        index\n            Index to insert the query op at.\n        op_\n            Query op to insert.\n\n        Returns\n        -------\n        Sequential\n            Sequential object with the query op inserted.\n\n        \"\"\"\n        if not isinstance(op_, QueryOp):\n            raise AssertionError(\"Module should be of type: {}\".format(QueryOp))\n        n = len(self._ops)\n        if not (-n &lt;= index &lt;= n):\n            raise IndexError(\"Index out of range: {}\".format(index))\n        if index &lt; 0:\n            index += n\n        for i in range(n, index, -1):\n            self._ops[str(i)] = self._ops[str(i - 1)]\n        self._ops[str(index)] = op_\n\n        return self\n\n    def extend(self, sequential: \"Sequential\") -&gt; \"Sequential\":\n        \"\"\"Extend the sequential query op with another sequential query op.\n\n        Parameters\n        ----------\n        sequential\n            Sequential object to extend with.\n\n        Returns\n        -------\n        Sequential\n            Sequential object with the other sequential query op extended.\n\n        \"\"\"\n        for op_ in sequential:\n            self.append(op_)\n\n        return self\n\n    @table_params_to_type(Subquery)  # type: ignore[arg-type]\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Execute the query operations on the table.\n\n        Parameters\n        ----------\n        table\n            Table to be queried.\n\n        Returns\n        -------\n        Subquery\n            Query result after chaining the query operations.\n\n        \"\"\"\n        return _chain_ops(table, self._get_ops())  # type: ignore[arg-type]\n</code></pre>"},{"location":"api/#cycquery.ops.Sequential.__init__","title":"__init__","text":"<pre><code>__init__(*ops: QueryOp) -&gt; None\n</code></pre><pre><code>__init__(ops: List[QueryOp]) -&gt; None\n</code></pre><pre><code>__init__(op: OrderedDict[str, QueryOp]) -&gt; None\n</code></pre> <pre><code>__init__(*args)\n</code></pre> <p>Initialize the class.</p> <p>Parameters:</p> Name Type Description Default <code>args</code> <code>QueryOp</code> <p>Query operations to be chained sequentially.</p> <code>()</code> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(self, *args: QueryOp) -&gt; None:  # type: ignore\n    \"\"\"Initialize the class.\n\n    Parameters\n    ----------\n    args\n        Query operations to be chained sequentially.\n\n    \"\"\"\n    super().__init__()\n    if len(args) == 1 and isinstance(args[0], OrderedDict):\n        for key, op_ in args[0].items():\n            self._add_op(key, op_)\n    elif len(args) == 1 and isinstance(args[0], list):\n        for idx, op_ in enumerate(args[0]):\n            self._add_op(str(idx), op_)\n    else:\n        for idx, op_ in enumerate(args):\n            self._add_op(str(idx), op_)\n</code></pre>"},{"location":"api/#cycquery.ops.Sequential.__len__","title":"__len__","text":"<pre><code>__len__()\n</code></pre> <p>Return the number of query ops in the Sequential.</p> <p>Returns:</p> Type Description <code>int</code> <p>Number of query ops in the Sequential.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __len__(self) -&gt; int:\n    \"\"\"Return the number of query ops in the Sequential.\n\n    Returns\n    -------\n    int\n        Number of query ops in the Sequential.\n\n    \"\"\"\n    return len(self._ops)\n</code></pre>"},{"location":"api/#cycquery.ops.Sequential.__iter__","title":"__iter__","text":"<pre><code>__iter__()\n</code></pre> <p>Return an iterator over the query ops.</p> <p>Returns:</p> Type Description <code>Iterator[QueryOp]</code> <p>Iterator over the query ops.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __iter__(self) -&gt; typing.Iterator[QueryOp]:\n    \"\"\"Return an iterator over the query ops.\n\n    Returns\n    -------\n    typing.Iterator[QueryOp]\n        Iterator over the query ops.\n\n    \"\"\"\n    return iter(self._ops.values())\n</code></pre>"},{"location":"api/#cycquery.ops.Sequential.__add__","title":"__add__","text":"<pre><code>__add__(other)\n</code></pre> <p>Add two Sequential objects.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>'Sequential'</code> <p>Sequential object to be added.</p> required <p>Returns:</p> Type Description <code>Sequential</code> <p>Sequential object with the two Sequential objects chained.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __add__(self, other: \"Sequential\") -&gt; \"Sequential\":\n    \"\"\"Add two Sequential objects.\n\n    Parameters\n    ----------\n    other\n        Sequential object to be added.\n\n    Returns\n    -------\n    Sequential\n        Sequential object with the two Sequential objects chained.\n\n    \"\"\"\n    if isinstance(other, Sequential):\n        ret = Sequential()\n        for op_ in self:\n            ret.append(op_)\n        for op_ in other:\n            ret.append(op_)\n        return ret\n    raise ValueError(\n        \"Add operator supports only objects \"\n        \"of Sequential class, but {} is given.\".format(str(type(other))),\n    )\n</code></pre>"},{"location":"api/#cycquery.ops.Sequential.__iadd__","title":"__iadd__","text":"<pre><code>__iadd__(other)\n</code></pre> <p>Add two Sequential objects inplace.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>'Sequential'</code> <p>Sequential object to be added.</p> required <p>Returns:</p> Type Description <code>Sequential</code> <p>Sequential object with the two Sequential objects chained.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __iadd__(self, other: \"Sequential\") -&gt; \"Sequential\":\n    \"\"\"Add two Sequential objects inplace.\n\n    Parameters\n    ----------\n    other\n        Sequential object to be added.\n\n    Returns\n    -------\n    Sequential\n        Sequential object with the two Sequential objects chained.\n\n    \"\"\"\n    if isinstance(other, Sequential):\n        offset = len(self)\n        for i, op_ in enumerate(other):\n            self._add_op(str(i + offset), op_)\n        return self\n    raise ValueError(\n        \"Add operator supports only objects \"\n        \"of Sequential class, but {} is given.\".format(str(type(other))),\n    )\n</code></pre>"},{"location":"api/#cycquery.ops.Sequential.__getitem__","title":"__getitem__","text":"<pre><code>__getitem__(idx)\n</code></pre> <p>Get the idx-th item of the sequential query op.</p> <p>Parameters:</p> Name Type Description Default <code>idx</code> <code>Union[slice, int]</code> <p>Index of the item to get.</p> required <p>Returns:</p> Type Description <code>Sequential or QueryOp</code> <p>The idx-th item of the sequential query op.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __getitem__(\n    self,\n    idx: typing.Union[slice, int],\n) -&gt; typing.Any:\n    \"\"\"Get the idx-th item of the sequential query op.\n\n    Parameters\n    ----------\n    idx\n        Index of the item to get.\n\n    Returns\n    -------\n    Sequential or QueryOp\n        The idx-th item of the sequential query op.\n\n    \"\"\"\n    if isinstance(idx, slice):\n        return self.__class__(OrderedDict(list(self._ops.items())[idx]))\n\n    return self._get_item_by_idx(self._ops.values(), idx)  # type: ignore\n</code></pre>"},{"location":"api/#cycquery.ops.Sequential.__setitem__","title":"__setitem__","text":"<pre><code>__setitem__(idx, op_)\n</code></pre> <p>Set the idx-th item of the sequential query op.</p> <p>Parameters:</p> Name Type Description Default <code>idx</code> <code>int</code> <p>Index of the item to set.</p> required <code>op_</code> <code>QueryOp</code> <p>Query op to set.</p> required Source code in <code>cycquery/ops.py</code> <pre><code>def __setitem__(self, idx: int, op_: QueryOp) -&gt; None:\n    \"\"\"Set the idx-th item of the sequential query op.\n\n    Parameters\n    ----------\n    idx\n        Index of the item to set.\n    op_\n        Query op to set.\n\n    \"\"\"\n    key: str = self._get_item_by_idx(self._ops.keys(), idx)  # type: ignore\n    return setattr(self, key, op_)\n</code></pre>"},{"location":"api/#cycquery.ops.Sequential.__delitem__","title":"__delitem__","text":"<pre><code>__delitem__(idx)\n</code></pre> <p>Delete the idx-th item of the sequential query op.</p> <p>Parameters:</p> Name Type Description Default <code>idx</code> <code>Union[slice, int]</code> <p>Index of the item to delete.</p> required Source code in <code>cycquery/ops.py</code> <pre><code>def __delitem__(self, idx: typing.Union[slice, int]) -&gt; None:\n    \"\"\"Delete the idx-th item of the sequential query op.\n\n    Parameters\n    ----------\n    idx\n        Index of the item to delete.\n\n    \"\"\"\n    if isinstance(idx, slice):\n        for key in list(self._ops.keys())[idx]:\n            delattr(self, key)\n    else:\n        key = self._get_item_by_idx(self._ops.keys(), idx)  # type: ignore\n        delattr(self, key)\n    str_indices = [str(i) for i in range(len(self._ops))]\n    self._ops = OrderedDict(list(zip(str_indices, self._ops.values())))\n</code></pre>"},{"location":"api/#cycquery.ops.Sequential.append","title":"append","text":"<pre><code>append(op_)\n</code></pre> <p>Append a given query op to the end.</p> <p>Parameters:</p> Name Type Description Default <code>op_</code> <code>QueryOp</code> <p>Query op to append.</p> required <p>Returns:</p> Type Description <code>Sequential</code> <p>Sequential object with the query op appended.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def append(self, op_: QueryOp) -&gt; \"Sequential\":\n    \"\"\"Append a given query op to the end.\n\n    Parameters\n    ----------\n    op_\n        Query op to append.\n\n    Returns\n    -------\n    Sequential\n        Sequential object with the query op appended.\n\n    \"\"\"\n    self._add_op(str(len(self)), op_)\n    return self\n</code></pre>"},{"location":"api/#cycquery.ops.Sequential.pop","title":"pop","text":"<pre><code>pop(key)\n</code></pre> <p>Pop the query op at the given index.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>Union[int, slice]</code> <p>Index of the query op to pop.</p> required <p>Returns:</p> Type Description <code>QueryOp</code> <p>Popped query op.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def pop(self, key: typing.Union[int, slice]) -&gt; QueryOp:\n    \"\"\"Pop the query op at the given index.\n\n    Parameters\n    ----------\n    key\n        Index of the query op to pop.\n\n    Returns\n    -------\n    QueryOp\n        Popped query op.\n\n    \"\"\"\n    v = self[key]\n    del self[key]\n\n    return v\n</code></pre>"},{"location":"api/#cycquery.ops.Sequential.insert","title":"insert","text":"<pre><code>insert(index, op_)\n</code></pre> <p>Insert a given query op at the given index.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>int</code> <p>Index to insert the query op at.</p> required <code>op_</code> <code>QueryOp</code> <p>Query op to insert.</p> required <p>Returns:</p> Type Description <code>Sequential</code> <p>Sequential object with the query op inserted.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def insert(self, index: int, op_: QueryOp) -&gt; \"Sequential\":\n    \"\"\"Insert a given query op at the given index.\n\n    Parameters\n    ----------\n    index\n        Index to insert the query op at.\n    op_\n        Query op to insert.\n\n    Returns\n    -------\n    Sequential\n        Sequential object with the query op inserted.\n\n    \"\"\"\n    if not isinstance(op_, QueryOp):\n        raise AssertionError(\"Module should be of type: {}\".format(QueryOp))\n    n = len(self._ops)\n    if not (-n &lt;= index &lt;= n):\n        raise IndexError(\"Index out of range: {}\".format(index))\n    if index &lt; 0:\n        index += n\n    for i in range(n, index, -1):\n        self._ops[str(i)] = self._ops[str(i - 1)]\n    self._ops[str(index)] = op_\n\n    return self\n</code></pre>"},{"location":"api/#cycquery.ops.Sequential.extend","title":"extend","text":"<pre><code>extend(sequential)\n</code></pre> <p>Extend the sequential query op with another sequential query op.</p> <p>Parameters:</p> Name Type Description Default <code>sequential</code> <code>'Sequential'</code> <p>Sequential object to extend with.</p> required <p>Returns:</p> Type Description <code>Sequential</code> <p>Sequential object with the other sequential query op extended.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def extend(self, sequential: \"Sequential\") -&gt; \"Sequential\":\n    \"\"\"Extend the sequential query op with another sequential query op.\n\n    Parameters\n    ----------\n    sequential\n        Sequential object to extend with.\n\n    Returns\n    -------\n    Sequential\n        Sequential object with the other sequential query op extended.\n\n    \"\"\"\n    for op_ in sequential:\n        self.append(op_)\n\n    return self\n</code></pre>"},{"location":"api/#cycquery.ops.Sequential.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Execute the query operations on the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table to be queried.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Query result after chaining the query operations.</p> Source code in <code>cycquery/ops.py</code> <pre><code>@table_params_to_type(Subquery)  # type: ignore[arg-type]\ndef __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Execute the query operations on the table.\n\n    Parameters\n    ----------\n    table\n        Table to be queried.\n\n    Returns\n    -------\n    Subquery\n        Query result after chaining the query operations.\n\n    \"\"\"\n    return _chain_ops(table, self._get_ops())  # type: ignore[arg-type]\n</code></pre>"},{"location":"api/#cycquery.ops.FillNull","title":"FillNull","text":"<p>               Bases: <code>QueryOp</code></p> <p>Fill NULL values with a given value.</p> <p>Parameters:</p> Name Type Description Default <code>cols</code> <code>Union[str, List[str]]</code> <p>Columns to fill.</p> required <code>fill_values</code> <code>Union[Any, List[Any]]</code> <p>Value(s) to fill with.</p> required <code>new_col_names</code> <code>Optional[Union[str, List[str]]]</code> <p>New column name(s) for the filled columns. If not provided,</p> <code>None</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; FillNull(\"col1\", 0)(table)\n&gt;&gt;&gt; FillNull([\"col1\", \"col2\"], [0, 1])(table)\n&gt;&gt;&gt; FillNull([\"col1\", \"col2\"], [0, 1], [\"col1_new\", \"col2_new\"])(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class FillNull(QueryOp):\n    \"\"\"Fill NULL values with a given value.\n\n    Parameters\n    ----------\n    cols\n        Columns to fill.\n    fill_values\n        Value(s) to fill with.\n    new_col_names\n        New column name(s) for the filled columns. If not provided,\n\n    Examples\n    --------\n    &gt;&gt;&gt; FillNull(\"col1\", 0)(table)\n    &gt;&gt;&gt; FillNull([\"col1\", \"col2\"], [0, 1])(table)\n    &gt;&gt;&gt; FillNull([\"col1\", \"col2\"], [0, 1], [\"col1_new\", \"col2_new\"])(table)\n\n    \"\"\"\n\n    def __init__(\n        self,\n        cols: typing.Union[str, typing.List[str]],\n        fill_values: typing.Union[typing.Any, typing.List[typing.Any]],\n        new_col_names: typing.Optional[typing.Union[str, typing.List[str]]] = None,\n    ) -&gt; None:\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.cols = cols\n        self.fill_values = fill_values\n        self.new_col_names = new_col_names\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Fill NULL values with a given value.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        Subquery\n            Table with NULL values filled.\n\n        \"\"\"\n        cols = to_list(self.cols)\n        fill_values = to_list(self.fill_values)\n        new_col_names = to_list_optional(self.new_col_names)\n        if new_col_names and len(cols) != len(new_col_names):\n            raise ValueError(\n                \"\"\"Number of columns to fill and number of new column names\n                    must match.\"\"\",\n            )\n        table = _process_checks(table, cols=self.cols)\n        if len(fill_values) == 1:\n            fill_values = fill_values * len(cols)\n\n        # Build coalesced expressions map for all columns at once (avoids N nested subqueries)\n        coalesce_map = {\n            col: func.coalesce(table.c[col], fill)\n            for col, fill in zip(cols, fill_values)\n        }\n\n        if new_col_names:\n            # Keep all original columns and add new coalesced columns\n            new_col_map = dict(zip(cols, new_col_names))\n            result_cols = list(table.columns) + [\n                coalesce_map[col].label(new_col_map[col]) for col in cols\n            ]\n        else:\n            # Replace original columns with coalesced versions (same names)\n            result_cols = [\n                coalesce_map[c.name].label(c.name) if c.name in coalesce_map else c\n                for c in table.columns\n            ]\n\n        return select(*result_cols).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.FillNull.__init__","title":"__init__","text":"<pre><code>__init__(cols, fill_values, new_col_names=None)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    cols: typing.Union[str, typing.List[str]],\n    fill_values: typing.Union[typing.Any, typing.List[typing.Any]],\n    new_col_names: typing.Optional[typing.Union[str, typing.List[str]]] = None,\n) -&gt; None:\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.cols = cols\n    self.fill_values = fill_values\n    self.new_col_names = new_col_names\n</code></pre>"},{"location":"api/#cycquery.ops.FillNull.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Fill NULL values with a given value.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Table with NULL values filled.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Fill NULL values with a given value.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    Subquery\n        Table with NULL values filled.\n\n    \"\"\"\n    cols = to_list(self.cols)\n    fill_values = to_list(self.fill_values)\n    new_col_names = to_list_optional(self.new_col_names)\n    if new_col_names and len(cols) != len(new_col_names):\n        raise ValueError(\n            \"\"\"Number of columns to fill and number of new column names\n                must match.\"\"\",\n        )\n    table = _process_checks(table, cols=self.cols)\n    if len(fill_values) == 1:\n        fill_values = fill_values * len(cols)\n\n    # Build coalesced expressions map for all columns at once (avoids N nested subqueries)\n    coalesce_map = {\n        col: func.coalesce(table.c[col], fill)\n        for col, fill in zip(cols, fill_values)\n    }\n\n    if new_col_names:\n        # Keep all original columns and add new coalesced columns\n        new_col_map = dict(zip(cols, new_col_names))\n        result_cols = list(table.columns) + [\n            coalesce_map[col].label(new_col_map[col]) for col in cols\n        ]\n    else:\n        # Replace original columns with coalesced versions (same names)\n        result_cols = [\n            coalesce_map[c.name].label(c.name) if c.name in coalesce_map else c\n            for c in table.columns\n        ]\n\n    return select(*result_cols).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.Drop","title":"Drop","text":"<p>               Bases: <code>QueryOp</code></p> <p>Drop some columns.</p> <p>Parameters:</p> Name Type Description Default <code>cols</code> <code>Union[str, List[str]]</code> <p>Columns to drop.</p> required <p>Examples:</p> <pre><code>&gt;&gt;&gt; Drop(\"col1\")(table)\n&gt;&gt;&gt; Drop([\"col1\", \"col2\"])(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class Drop(QueryOp):\n    \"\"\"Drop some columns.\n\n    Parameters\n    ----------\n    cols\n        Columns to drop.\n\n    Examples\n    --------\n    &gt;&gt;&gt; Drop(\"col1\")(table)\n    &gt;&gt;&gt; Drop([\"col1\", \"col2\"])(table)\n\n    \"\"\"\n\n    def __init__(self, cols: typing.Union[str, typing.List[str]]) -&gt; None:\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.cols = cols\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        table = _process_checks(table, cols=self.cols)\n\n        return drop_columns(table, self.cols)\n</code></pre>"},{"location":"api/#cycquery.ops.Drop.__init__","title":"__init__","text":"<pre><code>__init__(cols)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(self, cols: typing.Union[str, typing.List[str]]) -&gt; None:\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.cols = cols\n</code></pre>"},{"location":"api/#cycquery.ops.Drop.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    table = _process_checks(table, cols=self.cols)\n\n    return drop_columns(table, self.cols)\n</code></pre>"},{"location":"api/#cycquery.ops.Rename","title":"Rename","text":"<p>               Bases: <code>QueryOp</code></p> <p>Rename some columns.</p> <p>Parameters:</p> Name Type Description Default <code>rename_map</code> <code>Dict[str, str]</code> <p>Map from an existing column name to another name.</p> required <code>check_exists</code> <code>bool</code> <p>Whether to check if all of the keys in the map exist as columns.</p> <code>True</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; Rename({\"col1\": \"col1_new\"})(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class Rename(QueryOp):\n    \"\"\"Rename some columns.\n\n    Parameters\n    ----------\n    rename_map\n        Map from an existing column name to another name.\n    check_exists\n        Whether to check if all of the keys in the map exist as columns.\n\n    Examples\n    --------\n    &gt;&gt;&gt; Rename({\"col1\": \"col1_new\"})(table)\n\n    \"\"\"\n\n    def __init__(self, rename_map: typing.Dict[str, str], check_exists: bool = True):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.rename_map = rename_map\n        self.check_exists = check_exists\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        if self.check_exists:\n            table = _process_checks(table, cols=list(self.rename_map.keys()))\n\n        return rename_columns(table, self.rename_map)\n</code></pre>"},{"location":"api/#cycquery.ops.Rename.__init__","title":"__init__","text":"<pre><code>__init__(rename_map, check_exists=True)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(self, rename_map: typing.Dict[str, str], check_exists: bool = True):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.rename_map = rename_map\n    self.check_exists = check_exists\n</code></pre>"},{"location":"api/#cycquery.ops.Rename.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    if self.check_exists:\n        table = _process_checks(table, cols=list(self.rename_map.keys()))\n\n    return rename_columns(table, self.rename_map)\n</code></pre>"},{"location":"api/#cycquery.ops.Substring","title":"Substring","text":"<p>               Bases: <code>QueryOp</code></p> <p>Get substring of a string column.</p> <p>Parameters:</p> Name Type Description Default <code>col</code> <code>str</code> <p>Name of column which has string, where substring needs to be extracted.</p> required <code>start_index</code> <code>int</code> <p>Start index of substring.</p> required <code>stop_index</code> <code>int</code> <p>Stop index of substring.</p> required <code>new_col_label</code> <code>Optional[str]</code> <p>Name of the new column with extracted substring.</p> <code>None</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; Substring(\"col1\", 0, 2, \"col1_substring\")(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class Substring(QueryOp):\n    \"\"\"Get substring of a string column.\n\n    Parameters\n    ----------\n    col\n        Name of column which has string, where substring needs\n        to be extracted.\n    start_index\n        Start index of substring.\n    stop_index\n        Stop index of substring.\n    new_col_label\n        Name of the new column with extracted substring.\n\n    Examples\n    --------\n    &gt;&gt;&gt; Substring(\"col1\", 0, 2, \"col1_substring\")(table)\n\n    \"\"\"\n\n    def __init__(\n        self,\n        col: str,\n        start_index: int,\n        stop_index: int,\n        new_col_label: typing.Optional[str] = None,\n    ):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.col = col\n        self.start_index = start_index\n        self.stop_index = stop_index\n        self.new_col_label = new_col_label\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        table = _process_checks(table, cols=self.col, cols_not_in=self.new_col_label)\n\n        return apply_to_columns(\n            table,\n            self.col,\n            lambda x: func.substr(\n                process_column(x, to_str=True),\n                self.start_index,\n                self.stop_index,\n            ),\n            new_col_labels=self.new_col_label,\n        )\n</code></pre>"},{"location":"api/#cycquery.ops.Substring.__init__","title":"__init__","text":"<pre><code>__init__(col, start_index, stop_index, new_col_label=None)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    col: str,\n    start_index: int,\n    stop_index: int,\n    new_col_label: typing.Optional[str] = None,\n):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.col = col\n    self.start_index = start_index\n    self.stop_index = stop_index\n    self.new_col_label = new_col_label\n</code></pre>"},{"location":"api/#cycquery.ops.Substring.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    table = _process_checks(table, cols=self.col, cols_not_in=self.new_col_label)\n\n    return apply_to_columns(\n        table,\n        self.col,\n        lambda x: func.substr(\n            process_column(x, to_str=True),\n            self.start_index,\n            self.stop_index,\n        ),\n        new_col_labels=self.new_col_label,\n    )\n</code></pre>"},{"location":"api/#cycquery.ops.Reorder","title":"Reorder","text":"<p>               Bases: <code>QueryOp</code></p> <p>Reorder the columns in a table.</p> <p>Parameters:</p> Name Type Description Default <code>cols</code> <code>List[str]</code> <p>Complete list of table column names in the new order.</p> required <p>Examples:</p> <pre><code>&gt;&gt;&gt; Reorder([\"col2\", \"col1\"])(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class Reorder(QueryOp):\n    \"\"\"Reorder the columns in a table.\n\n    Parameters\n    ----------\n    cols\n        Complete list of table column names in the new order.\n\n    Examples\n    --------\n    &gt;&gt;&gt; Reorder([\"col2\", \"col1\"])(table)\n\n    \"\"\"\n\n    def __init__(self, cols: typing.List[str]):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.cols = cols\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        table = _process_checks(table, cols=self.cols)\n\n        return reorder_columns(table, self.cols)\n</code></pre>"},{"location":"api/#cycquery.ops.Reorder.__init__","title":"__init__","text":"<pre><code>__init__(cols)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(self, cols: typing.List[str]):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.cols = cols\n</code></pre>"},{"location":"api/#cycquery.ops.Reorder.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    table = _process_checks(table, cols=self.cols)\n\n    return reorder_columns(table, self.cols)\n</code></pre>"},{"location":"api/#cycquery.ops.ReorderAfter","title":"ReorderAfter","text":"<p>               Bases: <code>QueryOp</code></p> <p>Reorder a number of columns to come after a specified column.</p> <p>Parameters:</p> Name Type Description Default <code>cols</code> <code>Union[str, List[str]]</code> <p>Ordered list of column names which will come after a specified column.</p> required <code>after</code> <code>str</code> <p>Column name for the column after which the other columns will follow.</p> required <p>Examples:</p> <pre><code>&gt;&gt;&gt; ReorderAfter([\"col2\", \"col1\"], \"col3\")(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class ReorderAfter(QueryOp):\n    \"\"\"Reorder a number of columns to come after a specified column.\n\n    Parameters\n    ----------\n    cols\n        Ordered list of column names which will come after a specified column.\n    after\n        Column name for the column after which the other columns will follow.\n\n    Examples\n    --------\n    &gt;&gt;&gt; ReorderAfter([\"col2\", \"col1\"], \"col3\")(table)\n\n    \"\"\"\n\n    def __init__(self, cols: typing.Union[str, typing.List[str]], after: str):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.cols = cols\n        self.after = after\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        self.cols = to_list(self.cols)\n        table = _process_checks(table, cols=self.cols + [self.after])\n        names = get_column_names(table)\n        names = [name for name in names if name not in self.cols]\n        name_after_ind = names.index(self.after) + 1\n        new_order = names[:name_after_ind] + self.cols + names[name_after_ind:]\n\n        return Reorder(new_order)(table)\n</code></pre>"},{"location":"api/#cycquery.ops.ReorderAfter.__init__","title":"__init__","text":"<pre><code>__init__(cols, after)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(self, cols: typing.Union[str, typing.List[str]], after: str):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.cols = cols\n    self.after = after\n</code></pre>"},{"location":"api/#cycquery.ops.ReorderAfter.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    self.cols = to_list(self.cols)\n    table = _process_checks(table, cols=self.cols + [self.after])\n    names = get_column_names(table)\n    names = [name for name in names if name not in self.cols]\n    name_after_ind = names.index(self.after) + 1\n    new_order = names[:name_after_ind] + self.cols + names[name_after_ind:]\n\n    return Reorder(new_order)(table)\n</code></pre>"},{"location":"api/#cycquery.ops.Keep","title":"Keep","text":"<p>               Bases: <code>QueryOp</code></p> <p>Keep only the specified columns in a table.</p> <p>Parameters:</p> Name Type Description Default <code>cols</code> <code>Union[str, List[str]]</code> <p>The columns to keep.</p> required <p>Examples:</p> <pre><code>&gt;&gt;&gt; Keep(\"col1\")(table)\n&gt;&gt;&gt; Keep([\"col1\", \"col2\"])(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class Keep(QueryOp):\n    \"\"\"Keep only the specified columns in a table.\n\n    Parameters\n    ----------\n    cols\n        The columns to keep.\n\n    Examples\n    --------\n    &gt;&gt;&gt; Keep(\"col1\")(table)\n    &gt;&gt;&gt; Keep([\"col1\", \"col2\"])(table)\n\n    \"\"\"\n\n    def __init__(self, cols: typing.Union[str, typing.List[str]]):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.cols = cols\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        table = _process_checks(table, cols=self.cols)\n\n        return filter_columns(table, self.cols)\n</code></pre>"},{"location":"api/#cycquery.ops.Keep.__init__","title":"__init__","text":"<pre><code>__init__(cols)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(self, cols: typing.Union[str, typing.List[str]]):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.cols = cols\n</code></pre>"},{"location":"api/#cycquery.ops.Keep.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    table = _process_checks(table, cols=self.cols)\n\n    return filter_columns(table, self.cols)\n</code></pre>"},{"location":"api/#cycquery.ops.Trim","title":"Trim","text":"<p>               Bases: <code>QueryOp</code></p> <p>Trim the whitespace from some string columns.</p> <p>Parameters:</p> Name Type Description Default <code>cols</code> <code>Union[str, List[str]]</code> <p>Columns to trim.</p> required <code>new_col_labels</code> <code>Optional[Union[str, List[str]]]</code> <p>If specified, create new columns with these labels. Otherwise, apply the function to the existing columns.</p> <code>None</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; Trim(\"col1\")(table)\n&gt;&gt;&gt; Trim([\"col1\", \"col2\"])(table)\n&gt;&gt;&gt; Trim(\"col1\", \"col1_trimmed\")(table)\n&gt;&gt;&gt; Trim([\"col1\", \"col2\"], [\"col1_trimmed\", \"col2_trimmed\"])(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class Trim(QueryOp):\n    \"\"\"Trim the whitespace from some string columns.\n\n    Parameters\n    ----------\n    cols\n        Columns to trim.\n    new_col_labels\n        If specified, create new columns with these labels. Otherwise,\n        apply the function to the existing columns.\n\n    Examples\n    --------\n    &gt;&gt;&gt; Trim(\"col1\")(table)\n    &gt;&gt;&gt; Trim([\"col1\", \"col2\"])(table)\n    &gt;&gt;&gt; Trim(\"col1\", \"col1_trimmed\")(table)\n    &gt;&gt;&gt; Trim([\"col1\", \"col2\"], [\"col1_trimmed\", \"col2_trimmed\"])(table)\n\n    \"\"\"\n\n    def __init__(\n        self,\n        cols: typing.Union[str, typing.List[str]],\n        new_col_labels: typing.Optional[typing.Union[str, typing.List[str]]] = None,\n    ):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.cols = cols\n        self.new_col_labels = new_col_labels\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        table = _process_checks(table, cols=self.cols)\n\n        return trim_columns(table, self.cols, new_col_labels=self.new_col_labels)\n</code></pre>"},{"location":"api/#cycquery.ops.Trim.__init__","title":"__init__","text":"<pre><code>__init__(cols, new_col_labels=None)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    cols: typing.Union[str, typing.List[str]],\n    new_col_labels: typing.Optional[typing.Union[str, typing.List[str]]] = None,\n):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.cols = cols\n    self.new_col_labels = new_col_labels\n</code></pre>"},{"location":"api/#cycquery.ops.Trim.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    table = _process_checks(table, cols=self.cols)\n\n    return trim_columns(table, self.cols, new_col_labels=self.new_col_labels)\n</code></pre>"},{"location":"api/#cycquery.ops.Literal","title":"Literal","text":"<p>               Bases: <code>QueryOp</code></p> <p>Add a literal column to a table.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>Any</code> <p>Value of the literal, e.g., a string or integer.</p> required <code>col</code> <code>str</code> <p>Label of the new literal column.</p> required <p>Examples:</p> <pre><code>&gt;&gt;&gt; Literal(1, \"col1\")(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class Literal(QueryOp):\n    \"\"\"Add a literal column to a table.\n\n    Parameters\n    ----------\n    value\n        Value of the literal, e.g., a string or integer.\n    col\n        Label of the new literal column.\n\n    Examples\n    --------\n    &gt;&gt;&gt; Literal(1, \"col1\")(table)\n\n    \"\"\"\n\n    def __init__(self, value: typing.Any, col: str):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.value = value\n        self.col = col\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        table = _process_checks(table, cols_not_in=self.col)\n\n        return select(table, literal(self.value).label(self.col)).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.Literal.__init__","title":"__init__","text":"<pre><code>__init__(value, col)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(self, value: typing.Any, col: str):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.value = value\n    self.col = col\n</code></pre>"},{"location":"api/#cycquery.ops.Literal.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    table = _process_checks(table, cols_not_in=self.col)\n\n    return select(table, literal(self.value).label(self.col)).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ExtractTimestampComponent","title":"ExtractTimestampComponent","text":"<p>               Bases: <code>QueryOp</code></p> <p>Extract a component such as year or month from a timestamp column.</p> <p>Parameters:</p> Name Type Description Default <code>timestamp_col</code> <code>str</code> <p>Timestamp column from which to extract the time component.</p> required <code>extract_str</code> <code>str</code> <p>Information to extract, e.g., \"year\", \"month\"</p> required <code>label</code> <code>str</code> <p>Column label for the extracted column.</p> required <p>Examples:</p> <pre><code>&gt;&gt;&gt; ExtractTimestampComponent(\"col1\", \"year\", \"year\")(table)\n&gt;&gt;&gt; ExtractTimestampComponent(\"col1\", \"month\", \"month\")(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class ExtractTimestampComponent(QueryOp):\n    \"\"\"Extract a component such as year or month from a timestamp column.\n\n    Parameters\n    ----------\n    timestamp_col\n        Timestamp column from which to extract the time component.\n    extract_str\n        Information to extract, e.g., \"year\", \"month\"\n    label\n        Column label for the extracted column.\n\n    Examples\n    --------\n    &gt;&gt;&gt; ExtractTimestampComponent(\"col1\", \"year\", \"year\")(table)\n    &gt;&gt;&gt; ExtractTimestampComponent(\"col1\", \"month\", \"month\")(table)\n\n    \"\"\"\n\n    def __init__(self, timestamp_col: str, extract_str: str, label: str):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.timestamp_col = timestamp_col\n        self.extract_str = extract_str\n        self.label = label\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        table = _process_checks(\n            table,\n            timestamp_cols=self.timestamp_col,\n            cols_not_in=self.label,\n        )\n        table = select(\n            table,\n            extract(self.extract_str, get_column(table, self.timestamp_col)).label(\n                self.label,\n            ),\n        )\n\n        return Cast(self.label, \"int\")(table)\n</code></pre>"},{"location":"api/#cycquery.ops.ExtractTimestampComponent.__init__","title":"__init__","text":"<pre><code>__init__(timestamp_col, extract_str, label)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(self, timestamp_col: str, extract_str: str, label: str):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.timestamp_col = timestamp_col\n    self.extract_str = extract_str\n    self.label = label\n</code></pre>"},{"location":"api/#cycquery.ops.ExtractTimestampComponent.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    table = _process_checks(\n        table,\n        timestamp_cols=self.timestamp_col,\n        cols_not_in=self.label,\n    )\n    table = select(\n        table,\n        extract(self.extract_str, get_column(table, self.timestamp_col)).label(\n            self.label,\n        ),\n    )\n\n    return Cast(self.label, \"int\")(table)\n</code></pre>"},{"location":"api/#cycquery.ops.AddNumeric","title":"AddNumeric","text":"<p>               Bases: <code>QueryOp</code></p> <p>Add a numeric value to some columns.</p> <p>Parameters:</p> Name Type Description Default <code>add_to</code> <code>Union[str, List[str]]</code> <p>Column names specifying to which columns is being added.</p> required <code>add</code> <code>Union[int, float, List[int], List[float]]</code> <p>Adds this value to the add_to columns.</p> required <code>new_col_labels</code> <code>Optional[Union[str, List[str]]]</code> <p>If specified, create new columns with these labels. Otherwise, apply the function to the existing columns.</p> <code>None</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; AddNumeric(\"col1\", 1)(table)\n&gt;&gt;&gt; AddNumeric([\"col1\", \"col2\"], 1)(table)\n&gt;&gt;&gt; AddNumeric(\"col1\", 1, \"col1_plus_1\")(table)\n&gt;&gt;&gt; AddNumeric([\"col1\", \"col2\"], 1, [\"col1_plus_1\", \"col2_plus_1\"])(table)\n&gt;&gt;&gt; AddNumeric([\"col1\", \"col2\"], [1, 2.2])(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class AddNumeric(QueryOp):\n    \"\"\"Add a numeric value to some columns.\n\n    Parameters\n    ----------\n    add_to\n        Column names specifying to which columns is being added.\n    add\n        Adds this value to the add_to columns.\n    new_col_labels\n        If specified, create new columns with these labels. Otherwise,\n        apply the function to the existing columns.\n\n    Examples\n    --------\n    &gt;&gt;&gt; AddNumeric(\"col1\", 1)(table)\n    &gt;&gt;&gt; AddNumeric([\"col1\", \"col2\"], 1)(table)\n    &gt;&gt;&gt; AddNumeric(\"col1\", 1, \"col1_plus_1\")(table)\n    &gt;&gt;&gt; AddNumeric([\"col1\", \"col2\"], 1, [\"col1_plus_1\", \"col2_plus_1\"])(table)\n    &gt;&gt;&gt; AddNumeric([\"col1\", \"col2\"], [1, 2.2])(table)\n\n    \"\"\"\n\n    def __init__(\n        self,\n        add_to: typing.Union[str, typing.List[str]],\n        add: typing.Union[int, float, typing.List[int], typing.List[float]],\n        new_col_labels: typing.Optional[typing.Union[str, typing.List[str]]] = None,\n    ):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.add_to = add_to\n        self.add = add\n        self.new_col_labels = new_col_labels\n\n    def _gen_lambda(\n        self,\n        add: typing.Union[int, float],\n    ) -&gt; typing.Callable[[sqlalchemy.sql.schema.Column], sqlalchemy.sql.schema.Column]:\n        \"\"\"Generate the lambda function.\"\"\"\n        return lambda x: x + add  # type: ignore[return-value]\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        table = _process_checks(\n            table,\n            cols=self.add_to,\n            cols_not_in=self.new_col_labels,\n        )\n        self.add_to = to_list(self.add_to)\n        if isinstance(self.add, (int, float)) and len(self.add_to) &gt; 1:\n            add = [self.add] * len(self.add_to)\n        elif isinstance(self.add, (int, float)) and len(self.add_to) == 1:\n            add = [self.add]\n        elif isinstance(self.add, list) and len(self.add_to) != len(self.add):\n            raise ValueError(\n                \"Length of add_to and add must be the same if add is a list.\",\n            )\n\n        return apply_to_columns(\n            table,\n            self.add_to,\n            [self._gen_lambda(add_num) for add_num in add],\n            new_col_labels=self.new_col_labels,\n        )\n</code></pre>"},{"location":"api/#cycquery.ops.AddNumeric.__init__","title":"__init__","text":"<pre><code>__init__(add_to, add, new_col_labels=None)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    add_to: typing.Union[str, typing.List[str]],\n    add: typing.Union[int, float, typing.List[int], typing.List[float]],\n    new_col_labels: typing.Optional[typing.Union[str, typing.List[str]]] = None,\n):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.add_to = add_to\n    self.add = add\n    self.new_col_labels = new_col_labels\n</code></pre>"},{"location":"api/#cycquery.ops.AddNumeric.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    table = _process_checks(\n        table,\n        cols=self.add_to,\n        cols_not_in=self.new_col_labels,\n    )\n    self.add_to = to_list(self.add_to)\n    if isinstance(self.add, (int, float)) and len(self.add_to) &gt; 1:\n        add = [self.add] * len(self.add_to)\n    elif isinstance(self.add, (int, float)) and len(self.add_to) == 1:\n        add = [self.add]\n    elif isinstance(self.add, list) and len(self.add_to) != len(self.add):\n        raise ValueError(\n            \"Length of add_to and add must be the same if add is a list.\",\n        )\n\n    return apply_to_columns(\n        table,\n        self.add_to,\n        [self._gen_lambda(add_num) for add_num in add],\n        new_col_labels=self.new_col_labels,\n    )\n</code></pre>"},{"location":"api/#cycquery.ops.AddDeltaConstant","title":"AddDeltaConstant","text":"<p>               Bases: <code>QueryOp</code></p> <p>Construct and add a datetime.timedelta object to some columns.</p> <p>Parameters:</p> Name Type Description Default <code>add_to</code> <code>Union[str, List[str]]</code> <p>Column names specifying to which columns is being added.</p> required <code>delta</code> <code>timedelta</code> <p>A timedelta object.</p> required <code>new_col_labels</code> <code>Optional[Union[str, List[str]]]</code> <p>If specified, create new columns with these labels. Otherwise, apply the function to the existing columns.</p> <code>None</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; AddDeltaConstant(\"col1\", datetime.timedelta(days=1))(table)\n&gt;&gt;&gt; AddDeltaConstant([\"col1\", \"col2\"], datetime.timedelta(days=1))(table)\n&gt;&gt;&gt; AddDeltaConstant(\"col1\", datetime.timedelta(days=1), \"col1_plus_1\")(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class AddDeltaConstant(QueryOp):\n    \"\"\"Construct and add a datetime.timedelta object to some columns.\n\n    Parameters\n    ----------\n    add_to\n        Column names specifying to which columns is being added.\n    delta\n        A timedelta object.\n    new_col_labels\n        If specified, create new columns with these labels. Otherwise,\n        apply the function to the existing columns.\n\n    Examples\n    --------\n    &gt;&gt;&gt; AddDeltaConstant(\"col1\", datetime.timedelta(days=1))(table)\n    &gt;&gt;&gt; AddDeltaConstant([\"col1\", \"col2\"], datetime.timedelta(days=1))(table)\n    &gt;&gt;&gt; AddDeltaConstant(\"col1\", datetime.timedelta(days=1), \"col1_plus_1\")(table)\n\n    \"\"\"\n\n    def __init__(\n        self,\n        add_to: typing.Union[str, typing.List[str]],\n        delta: timedelta,\n        new_col_labels: typing.Optional[typing.Union[str, typing.List[str]]] = None,\n    ):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.add_to = add_to\n        self.delta = delta\n        self.new_col_labels = new_col_labels\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        table = _process_checks(\n            table,\n            timestamp_cols=self.add_to,\n            cols_not_in=self.new_col_labels,\n        )\n\n        return apply_to_columns(\n            table,\n            self.add_to,\n            lambda x: x + self.delta,\n            new_col_labels=self.new_col_labels,\n        )\n</code></pre>"},{"location":"api/#cycquery.ops.AddDeltaConstant.__init__","title":"__init__","text":"<pre><code>__init__(add_to, delta, new_col_labels=None)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    add_to: typing.Union[str, typing.List[str]],\n    delta: timedelta,\n    new_col_labels: typing.Optional[typing.Union[str, typing.List[str]]] = None,\n):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.add_to = add_to\n    self.delta = delta\n    self.new_col_labels = new_col_labels\n</code></pre>"},{"location":"api/#cycquery.ops.AddDeltaConstant.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    table = _process_checks(\n        table,\n        timestamp_cols=self.add_to,\n        cols_not_in=self.new_col_labels,\n    )\n\n    return apply_to_columns(\n        table,\n        self.add_to,\n        lambda x: x + self.delta,\n        new_col_labels=self.new_col_labels,\n    )\n</code></pre>"},{"location":"api/#cycquery.ops.AddColumn","title":"AddColumn","text":"<p>               Bases: <code>QueryOp</code></p> <p>Add a column to some columns.</p> <p>Parameters:</p> Name Type Description Default <code>add_to</code> <code>Union[str, List[str]]</code> <p>Column names specifying to which columns is being added.</p> required <code>col</code> <code>str</code> <p>Column name of column to add to the add_to columns.</p> required <code>negative</code> <code>Optional[bool]</code> <p>Subtract the column rather than adding.</p> <code>False</code> <code>new_col_labels</code> <code>Optional[Union[str, List[str]]]</code> <p>If specified, create new columns with these labels. Otherwise, apply the function to the existing columns.</p> <code>None</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; AddColumn(\"col1\", \"col2\")(table)\n&gt;&gt;&gt; AddColumn([\"col1\", \"col2\"], \"col3\")(table)\n&gt;&gt;&gt; AddColumn(\"col1\", \"col2\", negative=True)(table)\n&gt;&gt;&gt; AddColumn(\"col1\", \"col2\", \"col1_plus_col2\")(table)\n&gt;&gt;&gt; AddColumn([\"col1\", \"col2\"], \"col3\", [\"col1_plus_col3\", \"col2_plus_col3\"])(table)\n</code></pre> Warning <p>Pay attention to column types. Some combinations will work, whereas others will not.</p> Source code in <code>cycquery/ops.py</code> <pre><code>class AddColumn(QueryOp):\n    \"\"\"Add a column to some columns.\n\n    Parameters\n    ----------\n    add_to\n        Column names specifying to which columns is being added.\n    col\n        Column name of column to add to the add_to columns.\n    negative\n        Subtract the column rather than adding.\n    new_col_labels\n        If specified, create new columns with these labels. Otherwise,\n        apply the function to the existing columns.\n\n    Examples\n    --------\n    &gt;&gt;&gt; AddColumn(\"col1\", \"col2\")(table)\n    &gt;&gt;&gt; AddColumn([\"col1\", \"col2\"], \"col3\")(table)\n    &gt;&gt;&gt; AddColumn(\"col1\", \"col2\", negative=True)(table)\n    &gt;&gt;&gt; AddColumn(\"col1\", \"col2\", \"col1_plus_col2\")(table)\n    &gt;&gt;&gt; AddColumn([\"col1\", \"col2\"], \"col3\", [\"col1_plus_col3\", \"col2_plus_col3\"])(table)\n\n    Warning\n    -------\n    Pay attention to column types. Some combinations will work,\n    whereas others will not.\n\n    \"\"\"\n\n    def __init__(\n        self,\n        add_to: typing.Union[str, typing.List[str]],\n        col: str,\n        negative: typing.Optional[bool] = False,\n        new_col_labels: typing.Optional[typing.Union[str, typing.List[str]]] = None,\n    ):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.add_to = add_to\n        self.col = col\n        self.negative = negative\n        self.new_col_labels = new_col_labels\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        # If the column being added is a timestamp column, ensure the others are too\n        if check_timestamp_columns(table, self.col):\n            table = _process_checks(\n                table,\n                timestamp_cols=self.add_to,\n                cols_not_in=self.new_col_labels,\n            )\n        else:\n            table = _process_checks(\n                table,\n                cols=self.add_to,\n                cols_not_in=self.new_col_labels,\n            )\n        col = get_column(table, self.col)\n        if self.negative:\n            return apply_to_columns(\n                table,\n                self.add_to,\n                lambda x: x - col,\n                new_col_labels=self.new_col_labels,\n            )\n\n        return apply_to_columns(\n            table,\n            self.add_to,\n            lambda x: x + col,\n            new_col_labels=self.new_col_labels,\n        )\n</code></pre>"},{"location":"api/#cycquery.ops.AddColumn.__init__","title":"__init__","text":"<pre><code>__init__(add_to, col, negative=False, new_col_labels=None)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    add_to: typing.Union[str, typing.List[str]],\n    col: str,\n    negative: typing.Optional[bool] = False,\n    new_col_labels: typing.Optional[typing.Union[str, typing.List[str]]] = None,\n):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.add_to = add_to\n    self.col = col\n    self.negative = negative\n    self.new_col_labels = new_col_labels\n</code></pre>"},{"location":"api/#cycquery.ops.AddColumn.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    # If the column being added is a timestamp column, ensure the others are too\n    if check_timestamp_columns(table, self.col):\n        table = _process_checks(\n            table,\n            timestamp_cols=self.add_to,\n            cols_not_in=self.new_col_labels,\n        )\n    else:\n        table = _process_checks(\n            table,\n            cols=self.add_to,\n            cols_not_in=self.new_col_labels,\n        )\n    col = get_column(table, self.col)\n    if self.negative:\n        return apply_to_columns(\n            table,\n            self.add_to,\n            lambda x: x - col,\n            new_col_labels=self.new_col_labels,\n        )\n\n    return apply_to_columns(\n        table,\n        self.add_to,\n        lambda x: x + col,\n        new_col_labels=self.new_col_labels,\n    )\n</code></pre>"},{"location":"api/#cycquery.ops.AddDeltaColumn","title":"AddDeltaColumn","text":"<p>               Bases: <code>QueryOp</code></p> <p>Construct and add an interval column to some columns.</p> <p>Parameters:</p> Name Type Description Default <code>add_to</code> <code>Union[str, List[str]]</code> <p>Column names specifying to which columns is being added.</p> required <code>negative</code> <code>Optional[bool]</code> <p>Subtract the object rather than adding.</p> <code>False</code> <code>new_col_labels</code> <code>Optional[Union[str, List[str]]]</code> <p>If specified, create new columns with these labels. Otherwise, apply the function to the existing columns.</p> <code>None</code> <code>**delta_kwargs</code> <code>Any</code> <p>The arguments used to create the Interval column.</p> <code>{}</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; AddDeltaColumn(\"col1\", \"col2\")(table)\n&gt;&gt;&gt; AddDeltaColumn([\"col1\", \"col2\"], \"col3\")(table)\n&gt;&gt;&gt; AddDeltaColumn(\"col1\", \"col2\", negative=True)(table)\n&gt;&gt;&gt; AddDeltaColumn(\"col1\", \"col2\", \"col1_plus_col2\")(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class AddDeltaColumn(QueryOp):\n    \"\"\"Construct and add an interval column to some columns.\n\n    Parameters\n    ----------\n    add_to\n        Column names specifying to which columns is being added.\n    negative\n        Subtract the object rather than adding.\n    new_col_labels\n        If specified, create new columns with these labels. Otherwise,\n        apply the function to the existing columns.\n    **delta_kwargs\n        The arguments used to create the Interval column.\n\n    Examples\n    --------\n    &gt;&gt;&gt; AddDeltaColumn(\"col1\", \"col2\")(table)\n    &gt;&gt;&gt; AddDeltaColumn([\"col1\", \"col2\"], \"col3\")(table)\n    &gt;&gt;&gt; AddDeltaColumn(\"col1\", \"col2\", negative=True)(table)\n    &gt;&gt;&gt; AddDeltaColumn(\"col1\", \"col2\", \"col1_plus_col2\")(table)\n\n    \"\"\"\n\n    def __init__(\n        self,\n        add_to: typing.Union[str, typing.List[str]],\n        negative: typing.Optional[bool] = False,\n        new_col_labels: typing.Optional[typing.Union[str, typing.List[str]]] = None,\n        **delta_kwargs: typing.Any,\n    ) -&gt; None:\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.add_to = add_to\n        self.negative = negative\n        self.new_col_labels = new_col_labels\n        self.delta_kwargs = delta_kwargs\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        table = _process_checks(\n            table,\n            timestamp_cols=self.add_to,\n            cols_not_in=self.new_col_labels,\n        )\n        delta = get_delta_column(table, **self.delta_kwargs)\n        if self.negative:\n            return apply_to_columns(\n                table,\n                self.add_to,\n                lambda x: x - delta,\n                new_col_labels=self.new_col_labels,\n            )\n\n        return apply_to_columns(\n            table,\n            self.add_to,\n            lambda x: x + delta,\n            new_col_labels=self.new_col_labels,\n        )\n</code></pre>"},{"location":"api/#cycquery.ops.AddDeltaColumn.__init__","title":"__init__","text":"<pre><code>__init__(\n    add_to,\n    negative=False,\n    new_col_labels=None,\n    **delta_kwargs\n)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    add_to: typing.Union[str, typing.List[str]],\n    negative: typing.Optional[bool] = False,\n    new_col_labels: typing.Optional[typing.Union[str, typing.List[str]]] = None,\n    **delta_kwargs: typing.Any,\n) -&gt; None:\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.add_to = add_to\n    self.negative = negative\n    self.new_col_labels = new_col_labels\n    self.delta_kwargs = delta_kwargs\n</code></pre>"},{"location":"api/#cycquery.ops.AddDeltaColumn.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    table = _process_checks(\n        table,\n        timestamp_cols=self.add_to,\n        cols_not_in=self.new_col_labels,\n    )\n    delta = get_delta_column(table, **self.delta_kwargs)\n    if self.negative:\n        return apply_to_columns(\n            table,\n            self.add_to,\n            lambda x: x - delta,\n            new_col_labels=self.new_col_labels,\n        )\n\n    return apply_to_columns(\n        table,\n        self.add_to,\n        lambda x: x + delta,\n        new_col_labels=self.new_col_labels,\n    )\n</code></pre>"},{"location":"api/#cycquery.ops.Cast","title":"Cast","text":"<p>               Bases: <code>QueryOp</code></p> <p>Cast columns to a specified type.</p> <p>Currently supporting conversions to str, int, float, date, bool and timestamp.</p> <p>Parameters:</p> Name Type Description Default <code>cols</code> <code>Union[str, List[str]]</code> <p>Columns to cast.</p> required <code>type_</code> <code>str</code> <p>Name of type to which to convert. Must be supported.</p> required <p>Examples:</p> <pre><code>&gt;&gt;&gt; Cast(\"col1\", \"str\")(table)\n&gt;&gt;&gt; Cast([\"col1\", \"col2\"], \"int\")(table)\n&gt;&gt;&gt; Cast(\"col1\", \"float\")(table)\n&gt;&gt;&gt; Cast(\"col1\", \"date\")(table)\n&gt;&gt;&gt; Cast(\"col1\", \"bool\")(table)\n&gt;&gt;&gt; Cast(\"col1\", \"timestamp\")(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class Cast(QueryOp):\n    \"\"\"Cast columns to a specified type.\n\n    Currently supporting conversions to str, int, float, date, bool and timestamp.\n\n    Parameters\n    ----------\n    cols\n        Columns to cast.\n    type_\n        Name of type to which to convert. Must be supported.\n\n    Examples\n    --------\n    &gt;&gt;&gt; Cast(\"col1\", \"str\")(table)\n    &gt;&gt;&gt; Cast([\"col1\", \"col2\"], \"int\")(table)\n    &gt;&gt;&gt; Cast(\"col1\", \"float\")(table)\n    &gt;&gt;&gt; Cast(\"col1\", \"date\")(table)\n    &gt;&gt;&gt; Cast(\"col1\", \"bool\")(table)\n    &gt;&gt;&gt; Cast(\"col1\", \"timestamp\")(table)\n\n    \"\"\"\n\n    def __init__(self, cols: typing.Union[str, typing.List[str]], type_: str):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.cols = cols\n        self.type_ = type_\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        table = _process_checks(table, cols=self.cols)\n        cast_type_map = {\n            \"str\": \"to_str\",\n            \"int\": \"to_int\",\n            \"float\": \"to_float\",\n            \"date\": \"to_date\",\n            \"bool\": \"to_bool\",\n            \"timestamp\": \"to_timestamp\",\n        }\n        # Assert that the type inputted is supported\n        if self.type_ not in cast_type_map:\n            supported_str = \", \".join(list(cast_type_map.keys()))\n            raise ValueError(\n                f\"\"\"Conversion to type {self.type_} not supported. Supporting\n                conversion to types {supported_str}\"\"\",\n            )\n        # Cast\n        kwargs = {cast_type_map[self.type_]: True}\n\n        return apply_to_columns(\n            table,\n            self.cols,\n            lambda x: process_column(x, **kwargs),\n        )\n</code></pre>"},{"location":"api/#cycquery.ops.Cast.__init__","title":"__init__","text":"<pre><code>__init__(cols, type_)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(self, cols: typing.Union[str, typing.List[str]], type_: str):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.cols = cols\n    self.type_ = type_\n</code></pre>"},{"location":"api/#cycquery.ops.Cast.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    table = _process_checks(table, cols=self.cols)\n    cast_type_map = {\n        \"str\": \"to_str\",\n        \"int\": \"to_int\",\n        \"float\": \"to_float\",\n        \"date\": \"to_date\",\n        \"bool\": \"to_bool\",\n        \"timestamp\": \"to_timestamp\",\n    }\n    # Assert that the type inputted is supported\n    if self.type_ not in cast_type_map:\n        supported_str = \", \".join(list(cast_type_map.keys()))\n        raise ValueError(\n            f\"\"\"Conversion to type {self.type_} not supported. Supporting\n            conversion to types {supported_str}\"\"\",\n        )\n    # Cast\n    kwargs = {cast_type_map[self.type_]: True}\n\n    return apply_to_columns(\n        table,\n        self.cols,\n        lambda x: process_column(x, **kwargs),\n    )\n</code></pre>"},{"location":"api/#cycquery.ops.Union","title":"Union","text":"<p>               Bases: <code>QueryOp</code></p> <p>Union two tables.</p> <p>Parameters:</p> Name Type Description Default <code>union_table</code> <code>TableTypes</code> <p>Table to union with the first table.</p> required <code>union_all</code> <code>Optional[bool]</code> <p>Whether to use the all keyword in the union.</p> <code>False</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; Union(table2)(table1)\n&gt;&gt;&gt; Union(table2, union_all=True)(table1)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class Union(QueryOp):\n    \"\"\"Union two tables.\n\n    Parameters\n    ----------\n    union_table\n        Table to union with the first table.\n    union_all\n        Whether to use the all keyword in the union.\n\n    Examples\n    --------\n    &gt;&gt;&gt; Union(table2)(table1)\n    &gt;&gt;&gt; Union(table2, union_all=True)(table1)\n\n    \"\"\"\n\n    def __init__(\n        self,\n        union_table: TableTypes,\n        union_all: typing.Optional[bool] = False,\n    ):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.union_table = union_table\n        self.union_all = union_all\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        table = _process_checks(table)\n        union_table = _process_checks(self.union_table)\n        if self.union_all:\n            return select(table).union_all(select(union_table)).subquery()\n\n        return select(table).union(select(union_table)).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.Union.__init__","title":"__init__","text":"<pre><code>__init__(union_table, union_all=False)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    union_table: TableTypes,\n    union_all: typing.Optional[bool] = False,\n):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.union_table = union_table\n    self.union_all = union_all\n</code></pre>"},{"location":"api/#cycquery.ops.Union.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    table = _process_checks(table)\n    union_table = _process_checks(self.union_table)\n    if self.union_all:\n        return select(table).union_all(select(union_table)).subquery()\n\n    return select(table).union(select(union_table)).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.Join","title":"Join","text":"<p>               Bases: <code>QueryOp</code></p> <p>Join a table with another table.</p> <p>Parameters:</p> Name Type Description Default <code>join_table</code> <code>TableTypes</code> <p>Table on which to join.</p> required <code>on</code> <code>Optional[Union[str, List[str], Tuple[str], List[Tuple[str, str]]]]</code> <p>A list of strings or tuples representing columns on which to join. Strings represent columns of same name in both tables. A tuple of style (table_col, join_table_col) is used to join on columns of different names. Suggested to specify this parameter as opposed to cond.</p> <code>None</code> <code>on_to_type</code> <code>Optional[Union[type, List[type]]]</code> <p>A list of types to which to convert the on columns before joining. Useful when two columns have the same values but in different format, e.g., strings of int.</p> <code>None</code> <code>cond</code> <code>Optional[BinaryExpression]</code> <p>Condition on which to join to tables.</p> <code>None</code> <code>table_cols</code> <code>Optional[Union[str, List[str]]]</code> <p>Filters to keep only these columns from the table.</p> <code>None</code> <code>join_table_cols</code> <code>Optional[Union[str, List[str]]]</code> <p>Filters to keep only these columns from the join_table.</p> <code>None</code> <code>isouter</code> <code>Optional[bool]</code> <p>Flag to say if the join is a left outer join.</p> <code>False</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; Join(table2, on=[\"col1\", (\"col2\", \"col3\")], on_to_type=[int, str])(table1)\n&gt;&gt;&gt; Join(table2, table_cols=[\"col1\", \"col2\"])(table1)\n&gt;&gt;&gt; Join(table2, join_table_cols=[\"col1\", \"col2\"])(table1)\n</code></pre> Warnings <p>If neither on nor cond parameters are specified, an expensive Cartesian product is performed.</p> Source code in <code>cycquery/ops.py</code> <pre><code>class Join(QueryOp):\n    \"\"\"Join a table with another table.\n\n    Parameters\n    ----------\n    join_table\n        Table on which to join.\n    on\n        A list of strings or tuples representing columns on which to join.\n        Strings represent columns of same name in both tables. A tuple of\n        style (table_col, join_table_col) is used to join on columns of\n        different names. Suggested to specify this parameter as opposed to\n        cond.\n    on_to_type\n        A list of types to which to convert the on columns before joining. Useful when\n        two columns have the same values but in different format, e.g., strings of int.\n    cond\n        Condition on which to join to tables.\n    table_cols\n        Filters to keep only these columns from the table.\n    join_table_cols\n        Filters to keep only these columns from the join_table.\n    isouter\n        Flag to say if the join is a left outer join.\n\n    Examples\n    --------\n    &gt;&gt;&gt; Join(table2, on=[\"col1\", (\"col2\", \"col3\")], on_to_type=[int, str])(table1)\n    &gt;&gt;&gt; Join(table2, table_cols=[\"col1\", \"col2\"])(table1)\n    &gt;&gt;&gt; Join(table2, join_table_cols=[\"col1\", \"col2\"])(table1)\n\n    Warnings\n    --------\n    If neither on nor cond parameters are specified, an\n    expensive Cartesian product is performed.\n\n    \"\"\"\n\n    @table_params_to_type(Subquery)  # type: ignore[arg-type]\n    def __init__(\n        self,\n        join_table: TableTypes,\n        on: typing.Optional[\n            typing.Union[\n                str,\n                typing.List[str],\n                typing.Tuple[str],\n                typing.List[typing.Tuple[str, str]],\n            ]\n        ] = None,\n        on_to_type: typing.Optional[typing.Union[type, typing.List[type]]] = None,\n        cond: typing.Optional[BinaryExpression] = None,\n        table_cols: typing.Optional[typing.Union[str, typing.List[str]]] = None,\n        join_table_cols: typing.Optional[typing.Union[str, typing.List[str]]] = None,\n        isouter: typing.Optional[bool] = False,\n    ) -&gt; None:\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        if on is not None and cond is not None:\n            raise ValueError(\"Cannot specify both the 'on' and 'cond' arguments.\")\n\n        self.join_table = join_table\n        self.cond = cond\n        self.on_ = to_list_optional(on)\n        self.on_to_type = to_list_optional(on_to_type)\n        self.table_cols = to_list_optional(table_cols)\n        self.join_table_cols = to_list_optional(join_table_cols)\n        self.isouter = isouter\n\n    @table_params_to_type(Subquery)  # type: ignore[arg-type]\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        # Join on the equality of values in columns of same name in both tables\n        if self.on_ is not None:\n            # Process on columns\n            on_table_cols = [\n                col_obj if isinstance(col_obj, str) else col_obj[0]\n                for col_obj in self.on_\n            ]\n            on_join_table_cols = [\n                col_obj if isinstance(col_obj, str) else col_obj[1]\n                for col_obj in self.on_\n            ]\n            table = _process_checks(\n                table,\n                cols=_none_add(self.table_cols, on_table_cols),\n            )\n            self.join_table = _process_checks(\n                self.join_table,\n                cols=_none_add(self.join_table_cols, on_join_table_cols),\n            )\n            # Filter columns, keeping those being joined on\n            table = _append_if_missing(table, self.table_cols, on_table_cols)\n            self.join_table = _append_if_missing(\n                self.join_table,\n                self.join_table_cols,\n                on_join_table_cols,\n            )\n            # Perform type conversions if given\n            if self.on_to_type is not None:\n                for i, type_ in enumerate(self.on_to_type):\n                    table = Cast(on_table_cols[i], type_)(table)\n                    self.join_table = Cast(on_join_table_cols[i], type_)(\n                        self.join_table,\n                    )\n            cond = and_(\n                *[\n                    get_column(table, on_table_cols[i])\n                    == get_column(self.join_table, on_join_table_cols[i])\n                    for i in range(len(on_table_cols))\n                ],\n            )\n            table = select(table.join(self.join_table, cond, isouter=self.isouter))  # type: ignore[arg-type]\n        else:\n            # Filter columns\n            if self.table_cols is not None:\n                table = Keep(self.table_cols)(table)\n            if self.join_table_cols is not None:\n                self.join_table = Keep(self.table_cols)(self.join_table)  # type: ignore\n\n            # Join on a specified condition\n            if self.cond is not None:\n                table = select(\n                    table.join(  # type: ignore\n                        self.join_table,  # type: ignore[arg-type]\n                        self.cond,\n                        isouter=self.isouter,  # type: ignore[arg-type]\n                    ),\n                )\n            # Join on no condition, i.e., a Cartesian product\n            else:\n                LOGGER.warning(\"A Cartesian product has been queried.\")\n                table = select(table, self.join_table)  # type: ignore[arg-type]\n\n        # Filter to include no duplicate columns\n        return select(\n            *[col for col in table.subquery().columns if \"%(\" not in col.name],\n        ).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.Join.__init__","title":"__init__","text":"<pre><code>__init__(\n    join_table,\n    on=None,\n    on_to_type=None,\n    cond=None,\n    table_cols=None,\n    join_table_cols=None,\n    isouter=False,\n)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>@table_params_to_type(Subquery)  # type: ignore[arg-type]\ndef __init__(\n    self,\n    join_table: TableTypes,\n    on: typing.Optional[\n        typing.Union[\n            str,\n            typing.List[str],\n            typing.Tuple[str],\n            typing.List[typing.Tuple[str, str]],\n        ]\n    ] = None,\n    on_to_type: typing.Optional[typing.Union[type, typing.List[type]]] = None,\n    cond: typing.Optional[BinaryExpression] = None,\n    table_cols: typing.Optional[typing.Union[str, typing.List[str]]] = None,\n    join_table_cols: typing.Optional[typing.Union[str, typing.List[str]]] = None,\n    isouter: typing.Optional[bool] = False,\n) -&gt; None:\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    if on is not None and cond is not None:\n        raise ValueError(\"Cannot specify both the 'on' and 'cond' arguments.\")\n\n    self.join_table = join_table\n    self.cond = cond\n    self.on_ = to_list_optional(on)\n    self.on_to_type = to_list_optional(on_to_type)\n    self.table_cols = to_list_optional(table_cols)\n    self.join_table_cols = to_list_optional(join_table_cols)\n    self.isouter = isouter\n</code></pre>"},{"location":"api/#cycquery.ops.Join.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>@table_params_to_type(Subquery)  # type: ignore[arg-type]\ndef __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    # Join on the equality of values in columns of same name in both tables\n    if self.on_ is not None:\n        # Process on columns\n        on_table_cols = [\n            col_obj if isinstance(col_obj, str) else col_obj[0]\n            for col_obj in self.on_\n        ]\n        on_join_table_cols = [\n            col_obj if isinstance(col_obj, str) else col_obj[1]\n            for col_obj in self.on_\n        ]\n        table = _process_checks(\n            table,\n            cols=_none_add(self.table_cols, on_table_cols),\n        )\n        self.join_table = _process_checks(\n            self.join_table,\n            cols=_none_add(self.join_table_cols, on_join_table_cols),\n        )\n        # Filter columns, keeping those being joined on\n        table = _append_if_missing(table, self.table_cols, on_table_cols)\n        self.join_table = _append_if_missing(\n            self.join_table,\n            self.join_table_cols,\n            on_join_table_cols,\n        )\n        # Perform type conversions if given\n        if self.on_to_type is not None:\n            for i, type_ in enumerate(self.on_to_type):\n                table = Cast(on_table_cols[i], type_)(table)\n                self.join_table = Cast(on_join_table_cols[i], type_)(\n                    self.join_table,\n                )\n        cond = and_(\n            *[\n                get_column(table, on_table_cols[i])\n                == get_column(self.join_table, on_join_table_cols[i])\n                for i in range(len(on_table_cols))\n            ],\n        )\n        table = select(table.join(self.join_table, cond, isouter=self.isouter))  # type: ignore[arg-type]\n    else:\n        # Filter columns\n        if self.table_cols is not None:\n            table = Keep(self.table_cols)(table)\n        if self.join_table_cols is not None:\n            self.join_table = Keep(self.table_cols)(self.join_table)  # type: ignore\n\n        # Join on a specified condition\n        if self.cond is not None:\n            table = select(\n                table.join(  # type: ignore\n                    self.join_table,  # type: ignore[arg-type]\n                    self.cond,\n                    isouter=self.isouter,  # type: ignore[arg-type]\n                ),\n            )\n        # Join on no condition, i.e., a Cartesian product\n        else:\n            LOGGER.warning(\"A Cartesian product has been queried.\")\n            table = select(table, self.join_table)  # type: ignore[arg-type]\n\n    # Filter to include no duplicate columns\n    return select(\n        *[col for col in table.subquery().columns if \"%(\" not in col.name],\n    ).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionEquals","title":"ConditionEquals","text":"<p>               Bases: <code>QueryOp</code></p> <p>Filter rows based on being equal, or not equal, to some value.</p> <p>Parameters:</p> Name Type Description Default <code>col</code> <code>str</code> <p>Column name on which to condition.</p> required <code>value</code> <code>Any</code> <p>Value to equal.</p> required <code>not_</code> <code>bool</code> <p>Take negation of condition.</p> <code>False</code> <code>binarize_col</code> <code>Optional[str]</code> <p>If specified, create a Boolean column of name binarize_col instead of filtering.</p> <code>None</code> <code>**cond_kwargs</code> <code>Any</code> <p>Optional keyword arguments for processing the condition.</p> <code>{}</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; ConditionEquals(\"col1\", 1)(table)\n&gt;&gt;&gt; ConditionEquals(\"col1\", 1, binarize_col=\"col1_bool\")(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class ConditionEquals(QueryOp):\n    \"\"\"Filter rows based on being equal, or not equal, to some value.\n\n    Parameters\n    ----------\n    col\n        Column name on which to condition.\n    value\n        Value to equal.\n    not_\n        Take negation of condition.\n    binarize_col\n        If specified, create a Boolean column of name binarize_col instead of filtering.\n    **cond_kwargs\n        Optional keyword arguments for processing the condition.\n\n    Examples\n    --------\n    &gt;&gt;&gt; ConditionEquals(\"col1\", 1)(table)\n    &gt;&gt;&gt; ConditionEquals(\"col1\", 1, binarize_col=\"col1_bool\")(table)\n\n    \"\"\"\n\n    def __init__(\n        self,\n        col: str,\n        value: typing.Any,\n        not_: bool = False,\n        binarize_col: typing.Optional[str] = None,\n        **cond_kwargs: typing.Any,\n    ) -&gt; None:\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.col = col\n        self.value = value\n        self.not_ = not_\n        self.binarize_col = binarize_col\n        self.cond_kwargs = cond_kwargs\n\n    def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n        return_cond\n            Return the condition instead of filtering.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        if return_cond and self.binarize_col:\n            raise ValueError(\n                \"Cannot return condition and binarize column simultaneously.\",\n            )\n        table = _process_checks(table, cols=self.col, cols_not_in=self.binarize_col)\n        cond = equals(\n            get_column(table, self.col),\n            self.value,\n            True,\n            True,\n            **self.cond_kwargs,\n        )\n        if self.not_:\n            cond = cond._negate()  # type: ignore[no-untyped-call]\n        if return_cond:\n            return cond  # type: ignore[return-value]\n        if self.binarize_col is not None:\n            return select(\n                table,\n                cast(cond, Boolean).label(self.binarize_col),\n            ).subquery()\n\n        return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionEquals.__init__","title":"__init__","text":"<pre><code>__init__(\n    col, value, not_=False, binarize_col=None, **cond_kwargs\n)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    col: str,\n    value: typing.Any,\n    not_: bool = False,\n    binarize_col: typing.Optional[str] = None,\n    **cond_kwargs: typing.Any,\n) -&gt; None:\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.col = col\n    self.value = value\n    self.not_ = not_\n    self.binarize_col = binarize_col\n    self.cond_kwargs = cond_kwargs\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionEquals.__call__","title":"__call__","text":"<pre><code>__call__(table, return_cond=False)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <code>return_cond</code> <code>bool</code> <p>Return the condition instead of filtering.</p> <code>False</code> <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n    return_cond\n        Return the condition instead of filtering.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    if return_cond and self.binarize_col:\n        raise ValueError(\n            \"Cannot return condition and binarize column simultaneously.\",\n        )\n    table = _process_checks(table, cols=self.col, cols_not_in=self.binarize_col)\n    cond = equals(\n        get_column(table, self.col),\n        self.value,\n        True,\n        True,\n        **self.cond_kwargs,\n    )\n    if self.not_:\n        cond = cond._negate()  # type: ignore[no-untyped-call]\n    if return_cond:\n        return cond  # type: ignore[return-value]\n    if self.binarize_col is not None:\n        return select(\n            table,\n            cast(cond, Boolean).label(self.binarize_col),\n        ).subquery()\n\n    return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionGreaterThan","title":"ConditionGreaterThan","text":"<p>               Bases: <code>QueryOp</code></p> <p>Filter rows based on greater than (or equal), to some value.</p> <p>Parameters:</p> Name Type Description Default <code>col</code> <code>str</code> <p>Column name on which to condition.</p> required <code>value</code> <code>Any</code> <p>Value greater than.</p> required <code>equal</code> <code>bool</code> <p>Include equality to the value.</p> <code>False</code> <code>not_</code> <code>bool</code> <p>Take negation of condition.</p> <code>False</code> <code>binarize_col</code> <code>Optional[str]</code> <p>If specified, create a Boolean column of name binarize_col instead of filtering.</p> <code>None</code> <code>**cond_kwargs</code> <code>Any</code> <p>Optional keyword arguments for processing the condition.</p> <code>{}</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; ConditionGreaterThan(\"col1\", 1)(table)\n&gt;&gt;&gt; ConditionGreaterThan(\"col1\", 1, binarize_col=\"col1_bool\")(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class ConditionGreaterThan(QueryOp):\n    \"\"\"Filter rows based on greater than (or equal), to some value.\n\n    Parameters\n    ----------\n    col\n        Column name on which to condition.\n    value\n        Value greater than.\n    equal\n        Include equality to the value.\n    not_\n        Take negation of condition.\n    binarize_col\n        If specified, create a Boolean column of name binarize_col instead of filtering.\n    **cond_kwargs\n        Optional keyword arguments for processing the condition.\n\n    Examples\n    --------\n    &gt;&gt;&gt; ConditionGreaterThan(\"col1\", 1)(table)\n    &gt;&gt;&gt; ConditionGreaterThan(\"col1\", 1, binarize_col=\"col1_bool\")(table)\n\n    \"\"\"\n\n    def __init__(\n        self,\n        col: str,\n        value: typing.Any,\n        equal: bool = False,\n        not_: bool = False,\n        binarize_col: typing.Optional[str] = None,\n        **cond_kwargs: typing.Any,\n    ) -&gt; None:\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.col = col\n        self.value = value\n        self.equal = equal\n        self.not_ = not_\n        self.binarize_col = binarize_col\n        self.cond_kwargs = cond_kwargs\n\n    def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n        return_cond\n            Return the condition instead of filtering.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        if return_cond and self.binarize_col:\n            raise ValueError(\n                \"Cannot return condition and binarize column simultaneously.\",\n            )\n        table = _process_checks(table, cols=self.col, cols_not_in=self.binarize_col)\n        cond = greater_than(\n            get_column(table, self.col),\n            self.value,\n            True,\n            True,\n            self.equal,\n            **self.cond_kwargs,\n        )\n        if self.not_:\n            cond = cond._negate()  # type: ignore[no-untyped-call]\n        if return_cond:\n            return cond  # type: ignore[return-value]\n        if self.binarize_col is not None:\n            return select(\n                table,\n                cast(cond, Boolean).label(self.binarize_col),\n            ).subquery()\n\n        return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionGreaterThan.__init__","title":"__init__","text":"<pre><code>__init__(\n    col,\n    value,\n    equal=False,\n    not_=False,\n    binarize_col=None,\n    **cond_kwargs\n)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    col: str,\n    value: typing.Any,\n    equal: bool = False,\n    not_: bool = False,\n    binarize_col: typing.Optional[str] = None,\n    **cond_kwargs: typing.Any,\n) -&gt; None:\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.col = col\n    self.value = value\n    self.equal = equal\n    self.not_ = not_\n    self.binarize_col = binarize_col\n    self.cond_kwargs = cond_kwargs\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionGreaterThan.__call__","title":"__call__","text":"<pre><code>__call__(table, return_cond=False)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <code>return_cond</code> <code>bool</code> <p>Return the condition instead of filtering.</p> <code>False</code> <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n    return_cond\n        Return the condition instead of filtering.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    if return_cond and self.binarize_col:\n        raise ValueError(\n            \"Cannot return condition and binarize column simultaneously.\",\n        )\n    table = _process_checks(table, cols=self.col, cols_not_in=self.binarize_col)\n    cond = greater_than(\n        get_column(table, self.col),\n        self.value,\n        True,\n        True,\n        self.equal,\n        **self.cond_kwargs,\n    )\n    if self.not_:\n        cond = cond._negate()  # type: ignore[no-untyped-call]\n    if return_cond:\n        return cond  # type: ignore[return-value]\n    if self.binarize_col is not None:\n        return select(\n            table,\n            cast(cond, Boolean).label(self.binarize_col),\n        ).subquery()\n\n    return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionLessThan","title":"ConditionLessThan","text":"<p>               Bases: <code>QueryOp</code></p> <p>Filter rows based on less than (or equal), to some value.</p> <p>Parameters:</p> Name Type Description Default <code>col</code> <code>str</code> <p>Column name on which to condition.</p> required <code>value</code> <code>Any</code> <p>Value greater than.</p> required <code>equal</code> <code>bool</code> <p>Include equality to the value.</p> <code>False</code> <code>not_</code> <code>bool</code> <p>Take negation of condition.</p> <code>False</code> <code>binarize_col</code> <code>Optional[str]</code> <p>If specified, create a Boolean column of name binarize_col instead of filtering.</p> <code>None</code> <code>**cond_kwargs</code> <code>Any</code> <p>Optional keyword arguments for processing the condition.</p> <code>{}</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; ConditionLessThan(\"col1\", 1)(table)\n&gt;&gt;&gt; ConditionLessThan(\"col1\", 1, binarize_col=\"col1_bool\")(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class ConditionLessThan(QueryOp):\n    \"\"\"Filter rows based on less than (or equal), to some value.\n\n    Parameters\n    ----------\n    col\n        Column name on which to condition.\n    value\n        Value greater than.\n    equal\n        Include equality to the value.\n    not_\n        Take negation of condition.\n    binarize_col\n        If specified, create a Boolean column of name binarize_col instead of filtering.\n    **cond_kwargs\n        Optional keyword arguments for processing the condition.\n\n    Examples\n    --------\n    &gt;&gt;&gt; ConditionLessThan(\"col1\", 1)(table)\n    &gt;&gt;&gt; ConditionLessThan(\"col1\", 1, binarize_col=\"col1_bool\")(table)\n\n    \"\"\"\n\n    def __init__(\n        self,\n        col: str,\n        value: typing.Any,\n        equal: bool = False,\n        not_: bool = False,\n        binarize_col: typing.Optional[str] = None,\n        **cond_kwargs: typing.Any,\n    ) -&gt; None:\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.col = col\n        self.value = value\n        self.equal = equal\n        self.not_ = not_\n        self.binarize_col = binarize_col\n        self.cond_kwargs = cond_kwargs\n\n    def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n        return_cond\n            Return the condition instead of filtering.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        if return_cond and self.binarize_col:\n            raise ValueError(\n                \"Cannot return condition and binarize column simultaneously.\",\n            )\n        table = _process_checks(table, cols=self.col, cols_not_in=self.binarize_col)\n        cond = less_than(\n            get_column(table, self.col),\n            self.value,\n            True,\n            True,\n            self.equal,\n            **self.cond_kwargs,\n        )\n        if self.not_:\n            cond = cond._negate()  # type: ignore[no-untyped-call]\n        if return_cond:\n            return cond  # type: ignore[return-value]\n        if self.binarize_col is not None:\n            return select(\n                table,\n                cast(cond, Boolean).label(self.binarize_col),\n            ).subquery()\n\n        return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionLessThan.__init__","title":"__init__","text":"<pre><code>__init__(\n    col,\n    value,\n    equal=False,\n    not_=False,\n    binarize_col=None,\n    **cond_kwargs\n)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    col: str,\n    value: typing.Any,\n    equal: bool = False,\n    not_: bool = False,\n    binarize_col: typing.Optional[str] = None,\n    **cond_kwargs: typing.Any,\n) -&gt; None:\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.col = col\n    self.value = value\n    self.equal = equal\n    self.not_ = not_\n    self.binarize_col = binarize_col\n    self.cond_kwargs = cond_kwargs\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionLessThan.__call__","title":"__call__","text":"<pre><code>__call__(table, return_cond=False)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <code>return_cond</code> <code>bool</code> <p>Return the condition instead of filtering.</p> <code>False</code> <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n    return_cond\n        Return the condition instead of filtering.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    if return_cond and self.binarize_col:\n        raise ValueError(\n            \"Cannot return condition and binarize column simultaneously.\",\n        )\n    table = _process_checks(table, cols=self.col, cols_not_in=self.binarize_col)\n    cond = less_than(\n        get_column(table, self.col),\n        self.value,\n        True,\n        True,\n        self.equal,\n        **self.cond_kwargs,\n    )\n    if self.not_:\n        cond = cond._negate()  # type: ignore[no-untyped-call]\n    if return_cond:\n        return cond  # type: ignore[return-value]\n    if self.binarize_col is not None:\n        return select(\n            table,\n            cast(cond, Boolean).label(self.binarize_col),\n        ).subquery()\n\n    return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionRegexMatch","title":"ConditionRegexMatch","text":"<p>               Bases: <code>QueryOp</code></p> <p>Filter rows based on matching a regular expression.</p> <p>Parameters:</p> Name Type Description Default <code>col</code> <code>str</code> <p>Column name on which to condition.</p> required <code>regex</code> <code>str</code> <p>Regular expression to match.</p> required <code>not_</code> <code>bool</code> <p>Take negation of condition.</p> <code>False</code> <code>binarize_col</code> <code>Optional[str]</code> <p>If specified, create a Boolean column of name binarize_col instead of filtering.</p> <code>None</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; ConditionRegexMatch(\"col1\", \".*\")(table)\n&gt;&gt;&gt; ConditionRegexMatch(\"col1\", \".*\", binarize_col=\"col1_bool\")(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class ConditionRegexMatch(QueryOp):\n    \"\"\"Filter rows based on matching a regular expression.\n\n    Parameters\n    ----------\n    col\n        Column name on which to condition.\n    regex\n        Regular expression to match.\n    not_\n        Take negation of condition.\n    binarize_col\n        If specified, create a Boolean column of name binarize_col instead of filtering.\n\n    Examples\n    --------\n    &gt;&gt;&gt; ConditionRegexMatch(\"col1\", \".*\")(table)\n    &gt;&gt;&gt; ConditionRegexMatch(\"col1\", \".*\", binarize_col=\"col1_bool\")(table)\n\n    \"\"\"\n\n    def __init__(\n        self,\n        col: str,\n        regex: str,\n        not_: bool = False,\n        binarize_col: typing.Optional[str] = None,\n    ):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.col = col\n        self.regex = regex\n        self.not_ = not_\n        self.binarize_col = binarize_col\n\n    def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n        return_cond\n            Return the condition instead of filtering.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        if return_cond and self.binarize_col:\n            raise ValueError(\n                \"Cannot return condition and binarize column simultaneously.\",\n            )\n        table = _process_checks(table, cols=self.col, cols_not_in=self.binarize_col)\n        cond = get_column(table, self.col).regexp_match(self.regex)\n        if self.not_:\n            cond = cond._negate()\n        if return_cond:\n            return cond\n        if self.binarize_col is not None:\n            return select(\n                table,\n                cast(cond, Boolean).label(self.binarize_col),\n            ).subquery()\n\n        return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionRegexMatch.__init__","title":"__init__","text":"<pre><code>__init__(col, regex, not_=False, binarize_col=None)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    col: str,\n    regex: str,\n    not_: bool = False,\n    binarize_col: typing.Optional[str] = None,\n):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.col = col\n    self.regex = regex\n    self.not_ = not_\n    self.binarize_col = binarize_col\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionRegexMatch.__call__","title":"__call__","text":"<pre><code>__call__(table, return_cond=False)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <code>return_cond</code> <code>bool</code> <p>Return the condition instead of filtering.</p> <code>False</code> <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n    return_cond\n        Return the condition instead of filtering.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    if return_cond and self.binarize_col:\n        raise ValueError(\n            \"Cannot return condition and binarize column simultaneously.\",\n        )\n    table = _process_checks(table, cols=self.col, cols_not_in=self.binarize_col)\n    cond = get_column(table, self.col).regexp_match(self.regex)\n    if self.not_:\n        cond = cond._negate()\n    if return_cond:\n        return cond\n    if self.binarize_col is not None:\n        return select(\n            table,\n            cast(cond, Boolean).label(self.binarize_col),\n        ).subquery()\n\n    return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionIn","title":"ConditionIn","text":"<p>               Bases: <code>QueryOp</code></p> <p>Filter rows based on having a value in list of values.</p> <p>Parameters:</p> Name Type Description Default <code>col</code> <code>str</code> <p>Column name on which to condition.</p> required <code>values</code> <code>Union[Any, List[Any]]</code> <p>Values in which the column value must be.</p> required <code>not_</code> <code>bool</code> <p>Take negation of condition.</p> <code>False</code> <code>binarize_col</code> <code>Optional[str]</code> <p>If specified, create a Boolean column of name binarize_col instead of filtering.</p> <code>None</code> <code>**cond_kwargs</code> <code>Any</code> <p>Optional keyword arguments for processing the condition.</p> <code>{}</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; ConditionIn(\"col1\", [1, 2])(table)\n&gt;&gt;&gt; ConditionIn(\"col1\", [1, 2], binarize_col=\"col1_bool\")(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class ConditionIn(QueryOp):\n    \"\"\"Filter rows based on having a value in list of values.\n\n    Parameters\n    ----------\n    col\n        Column name on which to condition.\n    values\n        Values in which the column value must be.\n    not_\n        Take negation of condition.\n    binarize_col\n        If specified, create a Boolean column of name binarize_col instead of filtering.\n    **cond_kwargs\n        Optional keyword arguments for processing the condition.\n\n    Examples\n    --------\n    &gt;&gt;&gt; ConditionIn(\"col1\", [1, 2])(table)\n    &gt;&gt;&gt; ConditionIn(\"col1\", [1, 2], binarize_col=\"col1_bool\")(table)\n\n    \"\"\"\n\n    def __init__(\n        self,\n        col: str,\n        values: typing.Union[typing.Any, typing.List[typing.Any]],\n        not_: bool = False,\n        binarize_col: typing.Optional[str] = None,\n        **cond_kwargs: typing.Any,\n    ) -&gt; None:\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.col = col\n        self.values = values\n        self.not_ = not_\n        self.binarize_col = binarize_col\n        self.cond_kwargs = cond_kwargs\n\n    def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n        return_cond\n            Return the condition instead of filtering.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        if return_cond and self.binarize_col:\n            raise ValueError(\n                \"Cannot return condition and binarize column simultaneously.\",\n            )\n        table = _process_checks(table, cols=self.col, cols_not_in=self.binarize_col)\n        cond = in_(\n            get_column(table, self.col),\n            to_list(self.values),\n            True,\n            True,\n            **self.cond_kwargs,\n        )\n        if self.not_:\n            cond = cond._negate()  # type: ignore[no-untyped-call]\n        if return_cond:\n            return cond  # type: ignore[return-value]\n        if self.binarize_col is not None:\n            return select(\n                table,\n                cast(cond, Boolean).label(self.binarize_col),\n            ).subquery()\n\n        return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionIn.__init__","title":"__init__","text":"<pre><code>__init__(\n    col,\n    values,\n    not_=False,\n    binarize_col=None,\n    **cond_kwargs\n)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    col: str,\n    values: typing.Union[typing.Any, typing.List[typing.Any]],\n    not_: bool = False,\n    binarize_col: typing.Optional[str] = None,\n    **cond_kwargs: typing.Any,\n) -&gt; None:\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.col = col\n    self.values = values\n    self.not_ = not_\n    self.binarize_col = binarize_col\n    self.cond_kwargs = cond_kwargs\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionIn.__call__","title":"__call__","text":"<pre><code>__call__(table, return_cond=False)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <code>return_cond</code> <code>bool</code> <p>Return the condition instead of filtering.</p> <code>False</code> <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n    return_cond\n        Return the condition instead of filtering.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    if return_cond and self.binarize_col:\n        raise ValueError(\n            \"Cannot return condition and binarize column simultaneously.\",\n        )\n    table = _process_checks(table, cols=self.col, cols_not_in=self.binarize_col)\n    cond = in_(\n        get_column(table, self.col),\n        to_list(self.values),\n        True,\n        True,\n        **self.cond_kwargs,\n    )\n    if self.not_:\n        cond = cond._negate()  # type: ignore[no-untyped-call]\n    if return_cond:\n        return cond  # type: ignore[return-value]\n    if self.binarize_col is not None:\n        return select(\n            table,\n            cast(cond, Boolean).label(self.binarize_col),\n        ).subquery()\n\n    return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionSubstring","title":"ConditionSubstring","text":"<p>               Bases: <code>QueryOp</code></p> <p>Filter rows on based on having substrings.</p> <p>Can be specified whether it must have any or all of the specified substrings. This makes no difference when only one substring is provided</p> <p>Parameters:</p> Name Type Description Default <code>col</code> <code>str</code> <p>Column name on which to condition.</p> required <code>substrings</code> <code>Union[str, List[str]]</code> <p>Substrings.</p> required <code>any_</code> <code>bool</code> <p>If true, the row must have just one of the substrings. If false, it must have all of the substrings.</p> <code>True</code> <code>not_</code> <code>bool</code> <p>Take negation of condition.</p> <code>False</code> <code>binarize_col</code> <code>Optional[str]</code> <p>If specified, create a Boolean column of name binarize_col instead of filtering.</p> <code>None</code> <code>**cond_kwargs</code> <code>Any</code> <p>Optional keyword arguments for processing the condition.</p> <code>{}</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; ConditionSubstring(\"col1\", [\"a\", \"b\"])(table)\n&gt;&gt;&gt; ConditionSubstring(\"col1\", [\"a\", \"b\"], any_=False)(table)\n&gt;&gt;&gt; ConditionSubstring(\"col1\", [\"a\", \"b\"], binarize_col=\"col1_bool\")(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class ConditionSubstring(QueryOp):\n    \"\"\"Filter rows on based on having substrings.\n\n    Can be specified whether it must have any or all of the specified substrings.\n    This makes no difference when only one substring is provided\n\n    Parameters\n    ----------\n    col\n        Column name on which to condition.\n    substrings\n        Substrings.\n    any_\n        If true, the row must have just one of the substrings. If false, it must\n        have all of the substrings.\n    not_\n        Take negation of condition.\n    binarize_col\n        If specified, create a Boolean column of name binarize_col instead of filtering.\n    **cond_kwargs\n        Optional keyword arguments for processing the condition.\n\n    Examples\n    --------\n    &gt;&gt;&gt; ConditionSubstring(\"col1\", [\"a\", \"b\"])(table)\n    &gt;&gt;&gt; ConditionSubstring(\"col1\", [\"a\", \"b\"], any_=False)(table)\n    &gt;&gt;&gt; ConditionSubstring(\"col1\", [\"a\", \"b\"], binarize_col=\"col1_bool\")(table)\n\n    \"\"\"\n\n    def __init__(\n        self,\n        col: str,\n        substrings: typing.Union[str, typing.List[str]],\n        any_: bool = True,\n        not_: bool = False,\n        binarize_col: typing.Optional[str] = None,\n        **cond_kwargs: typing.Any,\n    ) -&gt; None:\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.col = col\n        self.substrings = to_list(substrings)\n        self.any_ = any_\n        self.not_ = not_\n        self.binarize_col = binarize_col\n        self.cond_kwargs = cond_kwargs\n\n    def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n        return_cond\n            Return the condition instead of filtering.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        if return_cond and self.binarize_col:\n            raise ValueError(\n                \"Cannot return condition and binarize column simultaneously.\",\n            )\n        table = _process_checks(table, cols=self.col, cols_not_in=self.binarize_col)\n        conds = [\n            has_substring(get_column(table, self.col), sub, True, **self.cond_kwargs)\n            for sub in self.substrings\n        ]\n        cond = or_(*conds) if self.any_ else and_(*conds)\n        if self.not_:\n            cond = cond._negate()\n        if return_cond:\n            return cond  # type: ignore[return-value]\n        if self.binarize_col is not None:\n            return select(\n                table,\n                cast(cond, Boolean).label(self.binarize_col),\n            ).subquery()\n\n        return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionSubstring.__init__","title":"__init__","text":"<pre><code>__init__(\n    col,\n    substrings,\n    any_=True,\n    not_=False,\n    binarize_col=None,\n    **cond_kwargs\n)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    col: str,\n    substrings: typing.Union[str, typing.List[str]],\n    any_: bool = True,\n    not_: bool = False,\n    binarize_col: typing.Optional[str] = None,\n    **cond_kwargs: typing.Any,\n) -&gt; None:\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.col = col\n    self.substrings = to_list(substrings)\n    self.any_ = any_\n    self.not_ = not_\n    self.binarize_col = binarize_col\n    self.cond_kwargs = cond_kwargs\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionSubstring.__call__","title":"__call__","text":"<pre><code>__call__(table, return_cond=False)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <code>return_cond</code> <code>bool</code> <p>Return the condition instead of filtering.</p> <code>False</code> <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n    return_cond\n        Return the condition instead of filtering.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    if return_cond and self.binarize_col:\n        raise ValueError(\n            \"Cannot return condition and binarize column simultaneously.\",\n        )\n    table = _process_checks(table, cols=self.col, cols_not_in=self.binarize_col)\n    conds = [\n        has_substring(get_column(table, self.col), sub, True, **self.cond_kwargs)\n        for sub in self.substrings\n    ]\n    cond = or_(*conds) if self.any_ else and_(*conds)\n    if self.not_:\n        cond = cond._negate()\n    if return_cond:\n        return cond  # type: ignore[return-value]\n    if self.binarize_col is not None:\n        return select(\n            table,\n            cast(cond, Boolean).label(self.binarize_col),\n        ).subquery()\n\n    return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionStartsWith","title":"ConditionStartsWith","text":"<p>               Bases: <code>QueryOp</code></p> <p>Filter rows based on starting with some string.</p> <p>Parameters:</p> Name Type Description Default <code>col</code> <code>str</code> <p>Column name on which to condition.</p> required <code>string</code> <code>str</code> <p>String.</p> required <code>not_</code> <code>bool</code> <p>Take negation of condition.</p> <code>False</code> <code>binarize_col</code> <code>Optional[str]</code> <p>If specified, create a Boolean column of name binarize_col instead of filtering.</p> <code>None</code> <code>**cond_kwargs</code> <code>Any</code> <p>Optional keyword arguments for processing the condition.</p> <code>{}</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; ConditionStartsWith(\"col1\", \"a\")(table)\n&gt;&gt;&gt; ConditionStartsWith(\"col1\", \"a\", binarize_col=\"col1_bool\")(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class ConditionStartsWith(QueryOp):\n    \"\"\"Filter rows based on starting with some string.\n\n    Parameters\n    ----------\n    col: str\n        Column name on which to condition.\n    string: any\n        String.\n    not_: bool, default=False\n        Take negation of condition.\n    binarize_col: str, optional\n        If specified, create a Boolean column of name binarize_col instead of filtering.\n    **cond_kwargs\n        Optional keyword arguments for processing the condition.\n\n    Examples\n    --------\n    &gt;&gt;&gt; ConditionStartsWith(\"col1\", \"a\")(table)\n    &gt;&gt;&gt; ConditionStartsWith(\"col1\", \"a\", binarize_col=\"col1_bool\")(table)\n\n    \"\"\"\n\n    def __init__(\n        self,\n        col: str,\n        string: str,\n        not_: bool = False,\n        binarize_col: typing.Optional[str] = None,\n        **cond_kwargs: typing.Any,\n    ) -&gt; None:\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.col = col\n        self.string = string\n        self.not_ = not_\n        self.binarize_col = binarize_col\n        self.cond_kwargs = cond_kwargs\n\n    def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n        return_cond\n            Return the condition instead of filtering.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        if return_cond and self.binarize_col:\n            raise ValueError(\n                \"Cannot return condition and binarize column simultaneously.\",\n            )\n        table = _process_checks(table, cols=self.col, cols_not_in=self.binarize_col)\n        cond = starts_with(\n            get_column(table, self.col),\n            self.string,\n            True,\n            True,\n            **self.cond_kwargs,\n        )\n        if self.not_:\n            cond = cond._negate()  # type: ignore[no-untyped-call]\n        if return_cond:\n            return cond  # type: ignore[return-value]\n        if self.binarize_col is not None:\n            return select(\n                table,\n                cast(cond, Boolean).label(self.binarize_col),\n            ).subquery()\n\n        return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionStartsWith.__init__","title":"__init__","text":"<pre><code>__init__(\n    col,\n    string,\n    not_=False,\n    binarize_col=None,\n    **cond_kwargs\n)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    col: str,\n    string: str,\n    not_: bool = False,\n    binarize_col: typing.Optional[str] = None,\n    **cond_kwargs: typing.Any,\n) -&gt; None:\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.col = col\n    self.string = string\n    self.not_ = not_\n    self.binarize_col = binarize_col\n    self.cond_kwargs = cond_kwargs\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionStartsWith.__call__","title":"__call__","text":"<pre><code>__call__(table, return_cond=False)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <code>return_cond</code> <code>bool</code> <p>Return the condition instead of filtering.</p> <code>False</code> <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n    return_cond\n        Return the condition instead of filtering.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    if return_cond and self.binarize_col:\n        raise ValueError(\n            \"Cannot return condition and binarize column simultaneously.\",\n        )\n    table = _process_checks(table, cols=self.col, cols_not_in=self.binarize_col)\n    cond = starts_with(\n        get_column(table, self.col),\n        self.string,\n        True,\n        True,\n        **self.cond_kwargs,\n    )\n    if self.not_:\n        cond = cond._negate()  # type: ignore[no-untyped-call]\n    if return_cond:\n        return cond  # type: ignore[return-value]\n    if self.binarize_col is not None:\n        return select(\n            table,\n            cast(cond, Boolean).label(self.binarize_col),\n        ).subquery()\n\n    return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionEndsWith","title":"ConditionEndsWith","text":"<p>               Bases: <code>QueryOp</code></p> <p>Filter rows based on ending with some string.</p> <p>Parameters:</p> Name Type Description Default <code>col</code> <code>str</code> <p>Column name on which to condition.</p> required <code>string</code> <code>str</code> <p>String to end with.</p> required <code>not_</code> <code>bool</code> <p>Take negation of condition.</p> <code>False</code> <code>binarize_col</code> <code>Optional[str]</code> <p>If specified, create a Boolean column of name binarize_col instead of filtering.</p> <code>None</code> <code>**cond_kwargs</code> <code>Any</code> <p>Optional keyword arguments for processing the condition.</p> <code>{}</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; ConditionEndsWith(\"col1\", \"a\")(table)\n&gt;&gt;&gt; ConditionEndsWith(\"col1\", \"a\", binarize_col=\"col1_bool\")(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class ConditionEndsWith(QueryOp):\n    \"\"\"Filter rows based on ending with some string.\n\n    Parameters\n    ----------\n    col\n        Column name on which to condition.\n    string\n        String to end with.\n    not_\n        Take negation of condition.\n    binarize_col\n        If specified, create a Boolean column of name binarize_col instead of filtering.\n    **cond_kwargs\n        Optional keyword arguments for processing the condition.\n\n    Examples\n    --------\n    &gt;&gt;&gt; ConditionEndsWith(\"col1\", \"a\")(table)\n    &gt;&gt;&gt; ConditionEndsWith(\"col1\", \"a\", binarize_col=\"col1_bool\")(table)\n\n    \"\"\"\n\n    def __init__(\n        self,\n        col: str,\n        string: str,\n        not_: bool = False,\n        binarize_col: typing.Optional[str] = None,\n        **cond_kwargs: typing.Any,\n    ) -&gt; None:\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.col = col\n        self.string = string\n        self.not_ = not_\n        self.binarize_col = binarize_col\n        self.cond_kwargs = cond_kwargs\n\n    def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n        return_cond\n            Return the condition instead of filtering.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        if return_cond and self.binarize_col:\n            raise ValueError(\n                \"Cannot return condition and binarize column simultaneously.\",\n            )\n        table = _process_checks(table, cols=self.col, cols_not_in=self.binarize_col)\n        cond = ends_with(\n            get_column(table, self.col),\n            self.string,\n            True,\n            True,\n            **self.cond_kwargs,\n        )\n        if self.not_:\n            cond = cond._negate()  # type: ignore[no-untyped-call]\n        if return_cond:\n            return cond  # type: ignore[return-value]\n        if self.binarize_col is not None:\n            return select(\n                table,\n                cast(cond, Boolean).label(self.binarize_col),\n            ).subquery()\n\n        return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionEndsWith.__init__","title":"__init__","text":"<pre><code>__init__(\n    col,\n    string,\n    not_=False,\n    binarize_col=None,\n    **cond_kwargs\n)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    col: str,\n    string: str,\n    not_: bool = False,\n    binarize_col: typing.Optional[str] = None,\n    **cond_kwargs: typing.Any,\n) -&gt; None:\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.col = col\n    self.string = string\n    self.not_ = not_\n    self.binarize_col = binarize_col\n    self.cond_kwargs = cond_kwargs\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionEndsWith.__call__","title":"__call__","text":"<pre><code>__call__(table, return_cond=False)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <code>return_cond</code> <code>bool</code> <p>Return the condition instead of filtering.</p> <code>False</code> <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n    return_cond\n        Return the condition instead of filtering.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    if return_cond and self.binarize_col:\n        raise ValueError(\n            \"Cannot return condition and binarize column simultaneously.\",\n        )\n    table = _process_checks(table, cols=self.col, cols_not_in=self.binarize_col)\n    cond = ends_with(\n        get_column(table, self.col),\n        self.string,\n        True,\n        True,\n        **self.cond_kwargs,\n    )\n    if self.not_:\n        cond = cond._negate()  # type: ignore[no-untyped-call]\n    if return_cond:\n        return cond  # type: ignore[return-value]\n    if self.binarize_col is not None:\n        return select(\n            table,\n            cast(cond, Boolean).label(self.binarize_col),\n        ).subquery()\n\n    return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionInYears","title":"ConditionInYears","text":"<p>               Bases: <code>QueryOp</code></p> <p>Filter rows based on a timestamp column being in a list of years.</p> <p>Parameters:</p> Name Type Description Default <code>timestamp_col</code> <code>str</code> <p>Timestamp column name.</p> required <code>years</code> <code>Union[int, List[int]]</code> <p>Years in which the timestamps must be.</p> required <code>not_</code> <code>bool</code> <p>Take negation of condition.</p> <code>False</code> <code>binarize_col</code> <code>Optional[str]</code> <p>If specified, create a Boolean column of name binarize_col instead of filtering.</p> <code>None</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; ConditionInYears(\"col1\", [2019, 2020])(table)\n&gt;&gt;&gt; ConditionInYears(\"col1\", 2019)(table)\n&gt;&gt;&gt; ConditionInYears(\"col1\", 2019, binarize_col=\"col1_bool\")(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class ConditionInYears(QueryOp):\n    \"\"\"Filter rows based on a timestamp column being in a list of years.\n\n    Parameters\n    ----------\n    timestamp_col\n        Timestamp column name.\n    years\n        Years in which the timestamps must be.\n    not_\n        Take negation of condition.\n    binarize_col\n        If specified, create a Boolean column of name binarize_col instead of filtering.\n\n    Examples\n    --------\n    &gt;&gt;&gt; ConditionInYears(\"col1\", [2019, 2020])(table)\n    &gt;&gt;&gt; ConditionInYears(\"col1\", 2019)(table)\n    &gt;&gt;&gt; ConditionInYears(\"col1\", 2019, binarize_col=\"col1_bool\")(table)\n\n    \"\"\"\n\n    def __init__(\n        self,\n        timestamp_col: str,\n        years: typing.Union[int, typing.List[int]],\n        not_: bool = False,\n        binarize_col: typing.Optional[str] = None,\n    ):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.timestamp_col = timestamp_col\n        self.years = years\n        self.not_ = not_\n        self.binarize_col = binarize_col\n\n    def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n        return_cond\n            Return the condition instead of filtering.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        if return_cond and self.binarize_col:\n            raise ValueError(\n                \"Cannot return condition and binarize column simultaneously.\",\n            )\n        table = _process_checks(\n            table,\n            cols=self.timestamp_col,\n            cols_not_in=self.binarize_col,\n        )\n        cond = in_(\n            extract(\"year\", get_column(table, self.timestamp_col)),  # type: ignore[arg-type]\n            to_list(self.years),\n        )\n        if self.not_:\n            cond = cond._negate()  # type: ignore[no-untyped-call]\n        if return_cond:\n            return cond  # type: ignore[return-value]\n        if self.binarize_col is not None:\n            return select(\n                table,\n                cast(cond, Boolean).label(self.binarize_col),\n            ).subquery()\n\n        return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionInYears.__init__","title":"__init__","text":"<pre><code>__init__(\n    timestamp_col, years, not_=False, binarize_col=None\n)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    timestamp_col: str,\n    years: typing.Union[int, typing.List[int]],\n    not_: bool = False,\n    binarize_col: typing.Optional[str] = None,\n):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.timestamp_col = timestamp_col\n    self.years = years\n    self.not_ = not_\n    self.binarize_col = binarize_col\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionInYears.__call__","title":"__call__","text":"<pre><code>__call__(table, return_cond=False)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <code>return_cond</code> <code>bool</code> <p>Return the condition instead of filtering.</p> <code>False</code> <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n    return_cond\n        Return the condition instead of filtering.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    if return_cond and self.binarize_col:\n        raise ValueError(\n            \"Cannot return condition and binarize column simultaneously.\",\n        )\n    table = _process_checks(\n        table,\n        cols=self.timestamp_col,\n        cols_not_in=self.binarize_col,\n    )\n    cond = in_(\n        extract(\"year\", get_column(table, self.timestamp_col)),  # type: ignore[arg-type]\n        to_list(self.years),\n    )\n    if self.not_:\n        cond = cond._negate()  # type: ignore[no-untyped-call]\n    if return_cond:\n        return cond  # type: ignore[return-value]\n    if self.binarize_col is not None:\n        return select(\n            table,\n            cast(cond, Boolean).label(self.binarize_col),\n        ).subquery()\n\n    return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionInMonths","title":"ConditionInMonths","text":"<p>               Bases: <code>QueryOp</code></p> <p>Filter rows based on a timestamp being in a list of years.</p> <p>Parameters:</p> Name Type Description Default <code>timestamp_col</code> <code>str</code> <p>Timestamp column name.</p> required <code>months</code> <code>Union[int, List[int]]</code> <p>Months in which the timestamps must be.</p> required <code>not_</code> <code>bool</code> <p>Take negation of condition.</p> <code>False</code> <code>binarize_col</code> <code>Optional[str]</code> <p>If specified, create a Boolean column of name binarize_col instead of filtering.</p> <code>None</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; ConditionInMonths(\"col1\", [1, 2])(table)\n&gt;&gt;&gt; ConditionInMonths(\"col1\", 1)(table)\n&gt;&gt;&gt; ConditionInMonths(\"col1\", 1, binarize_col=\"col1_bool\")(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class ConditionInMonths(QueryOp):\n    \"\"\"Filter rows based on a timestamp being in a list of years.\n\n    Parameters\n    ----------\n    timestamp_col\n        Timestamp column name.\n    months\n        Months in which the timestamps must be.\n    not_\n        Take negation of condition.\n    binarize_col\n        If specified, create a Boolean column of name binarize_col instead of filtering.\n\n    Examples\n    --------\n    &gt;&gt;&gt; ConditionInMonths(\"col1\", [1, 2])(table)\n    &gt;&gt;&gt; ConditionInMonths(\"col1\", 1)(table)\n    &gt;&gt;&gt; ConditionInMonths(\"col1\", 1, binarize_col=\"col1_bool\")(table)\n\n    \"\"\"\n\n    def __init__(\n        self,\n        timestamp_col: str,\n        months: typing.Union[int, typing.List[int]],\n        not_: bool = False,\n        binarize_col: typing.Optional[str] = None,\n    ):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.timestamp_col = timestamp_col\n        self.months = months\n        self.not_ = not_\n        self.binarize_col = binarize_col\n\n    def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n        return_cond\n            Return the condition instead of filtering.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        if return_cond and self.binarize_col:\n            raise ValueError(\n                \"Cannot return condition and binarize column simultaneously.\",\n            )\n        table = _process_checks(\n            table,\n            cols=self.timestamp_col,\n            cols_not_in=self.binarize_col,\n        )\n        cond = in_(\n            extract(\"month\", get_column(table, self.timestamp_col)),  # type: ignore[arg-type]\n            to_list(self.months),\n        )\n        if self.not_:\n            cond = cond._negate()  # type: ignore[no-untyped-call]\n        if return_cond:\n            return cond  # type: ignore[return-value]\n        if self.binarize_col is not None:\n            return select(\n                table,\n                cast(cond, Boolean).label(self.binarize_col),\n            ).subquery()\n\n        return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionInMonths.__init__","title":"__init__","text":"<pre><code>__init__(\n    timestamp_col, months, not_=False, binarize_col=None\n)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    timestamp_col: str,\n    months: typing.Union[int, typing.List[int]],\n    not_: bool = False,\n    binarize_col: typing.Optional[str] = None,\n):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.timestamp_col = timestamp_col\n    self.months = months\n    self.not_ = not_\n    self.binarize_col = binarize_col\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionInMonths.__call__","title":"__call__","text":"<pre><code>__call__(table, return_cond=False)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <code>return_cond</code> <code>bool</code> <p>Return the condition instead of filtering.</p> <code>False</code> <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n    return_cond\n        Return the condition instead of filtering.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    if return_cond and self.binarize_col:\n        raise ValueError(\n            \"Cannot return condition and binarize column simultaneously.\",\n        )\n    table = _process_checks(\n        table,\n        cols=self.timestamp_col,\n        cols_not_in=self.binarize_col,\n    )\n    cond = in_(\n        extract(\"month\", get_column(table, self.timestamp_col)),  # type: ignore[arg-type]\n        to_list(self.months),\n    )\n    if self.not_:\n        cond = cond._negate()  # type: ignore[no-untyped-call]\n    if return_cond:\n        return cond  # type: ignore[return-value]\n    if self.binarize_col is not None:\n        return select(\n            table,\n            cast(cond, Boolean).label(self.binarize_col),\n        ).subquery()\n\n    return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionBeforeDate","title":"ConditionBeforeDate","text":"<p>               Bases: <code>QueryOp</code></p> <p>Filter rows based on a timestamp being before some date.</p> <p>Parameters:</p> Name Type Description Default <code>timestamp_col</code> <code>str</code> <p>Timestamp column name.</p> required <code>timestamp</code> <code>Union[str, datetime]</code> <p>A datetime object or str in YYYY-MM-DD format.</p> required <code>not_</code> <code>bool</code> <p>Take negation of condition.</p> <code>False</code> <code>binarize_col</code> <code>Optional[str]</code> <p>If specified, create a Boolean column of name binarize_col instead of filtering.</p> <code>None</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; ConditionBeforeDate(\"col1\", \"2020-01-01\")(table)\n&gt;&gt;&gt; ConditionBeforeDate(\"col1\", datetime.datetime(2020, 1, 1))(table)\n&gt;&gt;&gt; ConditionBeforeDate(\"col1\", \"2020-01-01\", binarize_col=\"col1_bool\")(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class ConditionBeforeDate(QueryOp):\n    \"\"\"Filter rows based on a timestamp being before some date.\n\n    Parameters\n    ----------\n    timestamp_col\n        Timestamp column name.\n    timestamp\n        A datetime object or str in YYYY-MM-DD format.\n    not_\n        Take negation of condition.\n    binarize_col\n        If specified, create a Boolean column of name binarize_col instead of filtering.\n\n    Examples\n    --------\n    &gt;&gt;&gt; ConditionBeforeDate(\"col1\", \"2020-01-01\")(table)\n    &gt;&gt;&gt; ConditionBeforeDate(\"col1\", datetime.datetime(2020, 1, 1))(table)\n    &gt;&gt;&gt; ConditionBeforeDate(\"col1\", \"2020-01-01\", binarize_col=\"col1_bool\")(table)\n\n    \"\"\"\n\n    def __init__(\n        self,\n        timestamp_col: str,\n        timestamp: typing.Union[str, datetime],\n        not_: bool = False,\n        binarize_col: typing.Optional[str] = None,\n    ):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.timestamp_col = timestamp_col\n        self.timestamp = timestamp\n        self.not_ = not_\n        self.binarize_col = binarize_col\n\n    def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n        return_cond\n            Return the condition instead of filtering.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        if return_cond and self.binarize_col:\n            raise ValueError(\n                \"Cannot return condition and binarize column simultaneously.\",\n            )\n        table = _process_checks(table, timestamp_cols=self.timestamp_col)\n        if isinstance(self.timestamp, str):\n            timestamp = to_datetime_format(self.timestamp)\n        else:\n            timestamp = self.timestamp\n        cond = get_column(table, self.timestamp_col) &lt;= timestamp\n        if self.not_:\n            cond = cond._negate()\n        if return_cond:\n            return cond\n        if self.binarize_col is not None:\n            return select(\n                table,\n                cast(cond, Boolean).label(self.binarize_col),\n            ).subquery()\n\n        return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionBeforeDate.__init__","title":"__init__","text":"<pre><code>__init__(\n    timestamp_col, timestamp, not_=False, binarize_col=None\n)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    timestamp_col: str,\n    timestamp: typing.Union[str, datetime],\n    not_: bool = False,\n    binarize_col: typing.Optional[str] = None,\n):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.timestamp_col = timestamp_col\n    self.timestamp = timestamp\n    self.not_ = not_\n    self.binarize_col = binarize_col\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionBeforeDate.__call__","title":"__call__","text":"<pre><code>__call__(table, return_cond=False)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <code>return_cond</code> <code>bool</code> <p>Return the condition instead of filtering.</p> <code>False</code> <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n    return_cond\n        Return the condition instead of filtering.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    if return_cond and self.binarize_col:\n        raise ValueError(\n            \"Cannot return condition and binarize column simultaneously.\",\n        )\n    table = _process_checks(table, timestamp_cols=self.timestamp_col)\n    if isinstance(self.timestamp, str):\n        timestamp = to_datetime_format(self.timestamp)\n    else:\n        timestamp = self.timestamp\n    cond = get_column(table, self.timestamp_col) &lt;= timestamp\n    if self.not_:\n        cond = cond._negate()\n    if return_cond:\n        return cond\n    if self.binarize_col is not None:\n        return select(\n            table,\n            cast(cond, Boolean).label(self.binarize_col),\n        ).subquery()\n\n    return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionAfterDate","title":"ConditionAfterDate","text":"<p>               Bases: <code>QueryOp</code></p> <p>Filter rows based on a timestamp being after some date.</p> <p>Parameters:</p> Name Type Description Default <code>timestamp_col</code> <code>str</code> <p>Timestamp column name.</p> required <code>timestamp</code> <code>Union[str, datetime]</code> <p>A datetime object or str in YYYY-MM-DD format.</p> required <code>not_</code> <code>bool</code> <p>Take negation of condition.</p> <code>False</code> <code>binarize_col</code> <code>Optional[str]</code> <p>If specified, create a Boolean column of name binarize_col instead of filtering.</p> <code>None</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; ConditionAfterDate(\"col1\", \"2020-01-01\")(table)\n&gt;&gt;&gt; ConditionAfterDate(\"col1\", datetime.datetime(2020, 1, 1))(table)\n&gt;&gt;&gt; ConditionAfterDate(\"col1\", \"2020-01-01\", binarize_col=\"col1_bool\")(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class ConditionAfterDate(QueryOp):\n    \"\"\"Filter rows based on a timestamp being after some date.\n\n    Parameters\n    ----------\n    timestamp_col\n        Timestamp column name.\n    timestamp\n        A datetime object or str in YYYY-MM-DD format.\n    not_\n        Take negation of condition.\n    binarize_col\n        If specified, create a Boolean column of name binarize_col instead of filtering.\n\n    Examples\n    --------\n    &gt;&gt;&gt; ConditionAfterDate(\"col1\", \"2020-01-01\")(table)\n    &gt;&gt;&gt; ConditionAfterDate(\"col1\", datetime.datetime(2020, 1, 1))(table)\n    &gt;&gt;&gt; ConditionAfterDate(\"col1\", \"2020-01-01\", binarize_col=\"col1_bool\")(table)\n\n    \"\"\"\n\n    def __init__(\n        self,\n        timestamp_col: str,\n        timestamp: typing.Union[str, datetime],\n        not_: bool = False,\n        binarize_col: typing.Optional[str] = None,\n    ):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.timestamp_col = timestamp_col\n        self.timestamp = timestamp\n        self.not_ = not_\n        self.binarize_col = binarize_col\n\n    def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n        return_cond\n            Return the condition instead of filtering.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        if return_cond and self.binarize_col:\n            raise ValueError(\n                \"Cannot return condition and binarize column simultaneously.\",\n            )\n        table = _process_checks(table, timestamp_cols=self.timestamp_col)\n        if isinstance(self.timestamp, str):\n            timestamp = to_datetime_format(self.timestamp)\n        else:\n            timestamp = self.timestamp\n        cond = get_column(table, self.timestamp_col) &gt;= timestamp\n        if self.not_:\n            cond = cond._negate()\n        if return_cond:\n            return cond\n        if self.binarize_col is not None:\n            return select(\n                table,\n                cast(cond, Boolean).label(self.binarize_col),\n            ).subquery()\n\n        return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionAfterDate.__init__","title":"__init__","text":"<pre><code>__init__(\n    timestamp_col, timestamp, not_=False, binarize_col=None\n)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    timestamp_col: str,\n    timestamp: typing.Union[str, datetime],\n    not_: bool = False,\n    binarize_col: typing.Optional[str] = None,\n):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.timestamp_col = timestamp_col\n    self.timestamp = timestamp\n    self.not_ = not_\n    self.binarize_col = binarize_col\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionAfterDate.__call__","title":"__call__","text":"<pre><code>__call__(table, return_cond=False)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <code>return_cond</code> <code>bool</code> <p>Return the condition instead of filtering.</p> <code>False</code> <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n    return_cond\n        Return the condition instead of filtering.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    if return_cond and self.binarize_col:\n        raise ValueError(\n            \"Cannot return condition and binarize column simultaneously.\",\n        )\n    table = _process_checks(table, timestamp_cols=self.timestamp_col)\n    if isinstance(self.timestamp, str):\n        timestamp = to_datetime_format(self.timestamp)\n    else:\n        timestamp = self.timestamp\n    cond = get_column(table, self.timestamp_col) &gt;= timestamp\n    if self.not_:\n        cond = cond._negate()\n    if return_cond:\n        return cond\n    if self.binarize_col is not None:\n        return select(\n            table,\n            cast(cond, Boolean).label(self.binarize_col),\n        ).subquery()\n\n    return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionLike","title":"ConditionLike","text":"<p>               Bases: <code>QueryOp</code></p> <p>Filter rows by a LIKE condition.</p> <p>Parameters:</p> Name Type Description Default <code>col</code> <code>str</code> <p>Column to filter on.</p> required <code>pattern</code> <code>str</code> <p>Pattern to filter on.</p> required <code>not_</code> <code>bool</code> <p>Take negation of condition.</p> <code>False</code> <code>binarize_col</code> <code>Optional[str]</code> <p>If specified, create a Boolean column of name binarize_col instead of filtering.</p> <code>None</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; ConditionLike(\"lab_name\", \"HbA1c\")(table)\n&gt;&gt;&gt; ConditionLike(\"lab_name\", \"HbA1c\", not_=True)(table)\n&gt;&gt;&gt; ConditionLike(\"lab_name\", \"HbA1c\", binarize_col=\"lab_name_bool\")(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class ConditionLike(QueryOp):\n    \"\"\"Filter rows by a LIKE condition.\n\n    Parameters\n    ----------\n    col\n        Column to filter on.\n    pattern\n        Pattern to filter on.\n    not_\n        Take negation of condition.\n    binarize_col\n        If specified, create a Boolean column of name binarize_col instead of filtering.\n\n    Examples\n    --------\n    &gt;&gt;&gt; ConditionLike(\"lab_name\", \"HbA1c\")(table)\n    &gt;&gt;&gt; ConditionLike(\"lab_name\", \"HbA1c\", not_=True)(table)\n    &gt;&gt;&gt; ConditionLike(\"lab_name\", \"HbA1c\", binarize_col=\"lab_name_bool\")(table)\n\n    \"\"\"\n\n    def __init__(\n        self,\n        col: str,\n        pattern: str,\n        not_: bool = False,\n        binarize_col: typing.Optional[str] = None,\n    ):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.col = col\n        self.pattern = pattern\n        self.not_ = not_\n        self.binarize_col = binarize_col\n\n    def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n        return_cond\n            Return the condition instead of filtering.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        if return_cond and self.binarize_col:\n            raise ValueError(\n                \"Cannot return condition and binarize column simultaneously.\",\n            )\n        table = _process_checks(table, cols=self.col)\n        cond = get_column(table, self.col).like(self.pattern)\n        if self.not_:\n            cond = cond._negate()\n        if return_cond:\n            return cond\n        if self.binarize_col is not None:\n            return select(\n                table,\n                cast(cond, Boolean).label(self.binarize_col),\n            ).subquery()\n\n        return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionLike.__init__","title":"__init__","text":"<pre><code>__init__(col, pattern, not_=False, binarize_col=None)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    col: str,\n    pattern: str,\n    not_: bool = False,\n    binarize_col: typing.Optional[str] = None,\n):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.col = col\n    self.pattern = pattern\n    self.not_ = not_\n    self.binarize_col = binarize_col\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionLike.__call__","title":"__call__","text":"<pre><code>__call__(table, return_cond=False)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <code>return_cond</code> <code>bool</code> <p>Return the condition instead of filtering.</p> <code>False</code> <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n    return_cond\n        Return the condition instead of filtering.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    if return_cond and self.binarize_col:\n        raise ValueError(\n            \"Cannot return condition and binarize column simultaneously.\",\n        )\n    table = _process_checks(table, cols=self.col)\n    cond = get_column(table, self.col).like(self.pattern)\n    if self.not_:\n        cond = cond._negate()\n    if return_cond:\n        return cond\n    if self.binarize_col is not None:\n        return select(\n            table,\n            cast(cond, Boolean).label(self.binarize_col),\n        ).subquery()\n\n    return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.Or","title":"Or","text":"<p>               Bases: <code>QueryOp</code></p> <p>Combine multiple condition query ops using an OR.</p> <p>Parameters:</p> Name Type Description Default <code>cond_ops</code> <code>Union[QueryOp, List[QueryOp]]</code> <p>Condition Query ops to combine.</p> <code>()</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; Or(ConditionLike(\"lab_name\", \"HbA1c\"), ConditionIn(\"name\", [\"John\", \"Jane\"]))\n&gt;&gt;&gt; Or([ConditionLike(\"lab_name\", \"HbA1c\"), ConditionIn(\"name\", [\"John\", \"Jane\"])])\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class Or(QueryOp):\n    \"\"\"Combine multiple condition query ops using an OR.\n\n    Parameters\n    ----------\n    cond_ops\n        Condition Query ops to combine.\n\n    Examples\n    --------\n    &gt;&gt;&gt; Or(ConditionLike(\"lab_name\", \"HbA1c\"), ConditionIn(\"name\", [\"John\", \"Jane\"]))\n    &gt;&gt;&gt; Or([ConditionLike(\"lab_name\", \"HbA1c\"), ConditionIn(\"name\", [\"John\", \"Jane\"])])\n\n    \"\"\"\n\n    def __init__(self, *cond_ops: typing.Union[QueryOp, typing.List[QueryOp]]):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.cond_ops = cond_ops\n\n    def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n        return_cond\n            Return the condition instead of filtering.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        ops = []\n        for cond_op in self.cond_ops:\n            if isinstance(cond_op, list):\n                if len(self.cond_ops) != 1:\n                    raise ValueError(\"Cannot combine multiple lists of conditions.\")\n                ops = [op(table, return_cond=True) for op in cond_op]\n            if isinstance(cond_op, QueryOp):\n                if len(self.cond_ops) == 1:\n                    return cond_op(table, return_cond=return_cond)\n                ops.append(cond_op(table, return_cond=True))\n        cond = or_(*ops)  # type: ignore[arg-type]\n        if return_cond:\n            return cond  # type: ignore[return-value]\n\n        return select(table).where(cond).subquery()  # type: ignore[arg-type]\n</code></pre>"},{"location":"api/#cycquery.ops.Or.__init__","title":"__init__","text":"<pre><code>__init__(*cond_ops)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(self, *cond_ops: typing.Union[QueryOp, typing.List[QueryOp]]):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.cond_ops = cond_ops\n</code></pre>"},{"location":"api/#cycquery.ops.Or.__call__","title":"__call__","text":"<pre><code>__call__(table, return_cond=False)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <code>return_cond</code> <code>bool</code> <p>Return the condition instead of filtering.</p> <code>False</code> <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n    return_cond\n        Return the condition instead of filtering.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    ops = []\n    for cond_op in self.cond_ops:\n        if isinstance(cond_op, list):\n            if len(self.cond_ops) != 1:\n                raise ValueError(\"Cannot combine multiple lists of conditions.\")\n            ops = [op(table, return_cond=True) for op in cond_op]\n        if isinstance(cond_op, QueryOp):\n            if len(self.cond_ops) == 1:\n                return cond_op(table, return_cond=return_cond)\n            ops.append(cond_op(table, return_cond=True))\n    cond = or_(*ops)  # type: ignore[arg-type]\n    if return_cond:\n        return cond  # type: ignore[return-value]\n\n    return select(table).where(cond).subquery()  # type: ignore[arg-type]\n</code></pre>"},{"location":"api/#cycquery.ops.And","title":"And","text":"<p>               Bases: <code>QueryOp</code></p> <p>Combine multiple condition query ops using an And.</p> <p>Parameters:</p> Name Type Description Default <code>*cond_ops</code> <code>Union[QueryOp, List[QueryOp]]</code> <p>Query ops to combine.</p> <code>()</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; And([ConditionLike(\"lab_name\", \"HbA1c\"), ConditionIn(\"name\", [\"John\", \"Jane\"])])\n&gt;&gt;&gt; And(ConditionLike(\"lab_name\", \"HbA1c\"), ConditionIn(\"name\", [\"John\", \"Jane\"]))\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class And(QueryOp):\n    \"\"\"Combine multiple condition query ops using an And.\n\n    Parameters\n    ----------\n    *cond_ops\n        Query ops to combine.\n\n    Examples\n    --------\n    &gt;&gt;&gt; And([ConditionLike(\"lab_name\", \"HbA1c\"), ConditionIn(\"name\", [\"John\", \"Jane\"])])\n    &gt;&gt;&gt; And(ConditionLike(\"lab_name\", \"HbA1c\"), ConditionIn(\"name\", [\"John\", \"Jane\"]))\n\n    \"\"\"\n\n    def __init__(self, *cond_ops: typing.Union[QueryOp, typing.List[QueryOp]]):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.cond_ops = cond_ops\n\n    def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n        return_cond\n            Return the condition instead of filtering.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        ops = []\n        for cond_op in self.cond_ops:\n            if isinstance(cond_op, list):\n                if len(self.cond_ops) != 1:\n                    raise ValueError(\"Cannot combine multiple lists of conditions.\")\n                ops = [op(table, return_cond=True) for op in cond_op]\n            if isinstance(cond_op, QueryOp):\n                if len(self.cond_ops) == 1:\n                    return cond_op(table, return_cond=return_cond)\n                ops.append(cond_op(table, return_cond=True))\n        cond = and_(*ops)  # type: ignore[arg-type]\n        if return_cond:\n            return cond  # type: ignore[return-value]\n\n        return select(table).where(cond).subquery()  # type: ignore[arg-type]\n</code></pre>"},{"location":"api/#cycquery.ops.And.__init__","title":"__init__","text":"<pre><code>__init__(*cond_ops)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(self, *cond_ops: typing.Union[QueryOp, typing.List[QueryOp]]):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.cond_ops = cond_ops\n</code></pre>"},{"location":"api/#cycquery.ops.And.__call__","title":"__call__","text":"<pre><code>__call__(table, return_cond=False)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <code>return_cond</code> <code>bool</code> <p>Return the condition instead of filtering.</p> <code>False</code> <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n    return_cond\n        Return the condition instead of filtering.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    ops = []\n    for cond_op in self.cond_ops:\n        if isinstance(cond_op, list):\n            if len(self.cond_ops) != 1:\n                raise ValueError(\"Cannot combine multiple lists of conditions.\")\n            ops = [op(table, return_cond=True) for op in cond_op]\n        if isinstance(cond_op, QueryOp):\n            if len(self.cond_ops) == 1:\n                return cond_op(table, return_cond=return_cond)\n            ops.append(cond_op(table, return_cond=True))\n    cond = and_(*ops)  # type: ignore[arg-type]\n    if return_cond:\n        return cond  # type: ignore[return-value]\n\n    return select(table).where(cond).subquery()  # type: ignore[arg-type]\n</code></pre>"},{"location":"api/#cycquery.ops.Limit","title":"Limit","text":"<p>               Bases: <code>QueryOp</code></p> <p>Limit the number of rows returned in a query.</p> <p>Parameters:</p> Name Type Description Default <code>number</code> <code>int</code> <p>Number of rows to return in the limit.</p> required <p>Examples:</p> <pre><code>&gt;&gt;&gt; Limit(10)(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class Limit(QueryOp):\n    \"\"\"Limit the number of rows returned in a query.\n\n    Parameters\n    ----------\n    number\n        Number of rows to return in the limit.\n\n    Examples\n    --------\n    &gt;&gt;&gt; Limit(10)(table)\n\n    \"\"\"\n\n    def __init__(self, number: int):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.number = number\n\n    @table_params_to_type(Select)  # type: ignore[arg-type]\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        return table.limit(self.number).subquery()  # type: ignore\n</code></pre>"},{"location":"api/#cycquery.ops.Limit.__init__","title":"__init__","text":"<pre><code>__init__(number)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(self, number: int):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.number = number\n</code></pre>"},{"location":"api/#cycquery.ops.Limit.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>@table_params_to_type(Select)  # type: ignore[arg-type]\ndef __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    return table.limit(self.number).subquery()  # type: ignore\n</code></pre>"},{"location":"api/#cycquery.ops.RandomizeOrder","title":"RandomizeOrder","text":"<p>               Bases: <code>QueryOp</code></p> <p>Randomize order of table rows.</p> <p>Useful when the data is ordered, so certain rows cannot be seen or analyzed when limited.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; RandomizeOrder()(table)\n</code></pre> Warnings <p>Becomes quite slow on large tables.</p> Source code in <code>cycquery/ops.py</code> <pre><code>class RandomizeOrder(QueryOp):\n    \"\"\"Randomize order of table rows.\n\n    Useful when the data is ordered, so certain rows cannot\n    be seen or analyzed when limited.\n\n    Examples\n    --------\n    &gt;&gt;&gt; RandomizeOrder()(table)\n\n    Warnings\n    --------\n    Becomes quite slow on large tables.\n\n    \"\"\"\n\n    @table_params_to_type(Subquery)  # type: ignore[arg-type]\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        return select(table).order_by(func.random()).subquery()  # type: ignore[arg-type]\n</code></pre>"},{"location":"api/#cycquery.ops.RandomizeOrder.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>@table_params_to_type(Subquery)  # type: ignore[arg-type]\ndef __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    return select(table).order_by(func.random()).subquery()  # type: ignore[arg-type]\n</code></pre>"},{"location":"api/#cycquery.ops.DropNulls","title":"DropNulls","text":"<p>               Bases: <code>QueryOp</code></p> <p>Remove rows with null values in some specified columns.</p> <p>Parameters:</p> Name Type Description Default <code>cols</code> <code>Union[str, List[str]]</code> <p>Columns in which, if a value is null, the corresponding row is removed.</p> required <p>Examples:</p> <pre><code>&gt;&gt;&gt; DropNulls(\"col1\")(table)\n&gt;&gt;&gt; DropNulls([\"col1\", \"col2\"])(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class DropNulls(QueryOp):\n    \"\"\"Remove rows with null values in some specified columns.\n\n    Parameters\n    ----------\n    cols\n        Columns in which, if a value is null, the corresponding row is removed.\n\n    Examples\n    --------\n    &gt;&gt;&gt; DropNulls(\"col1\")(table)\n    &gt;&gt;&gt; DropNulls([\"col1\", \"col2\"])(table)\n\n    \"\"\"\n\n    def __init__(self, cols: typing.Union[str, typing.List[str]]):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.cols = cols\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        self.cols = to_list(self.cols)\n        table = _process_checks(table, cols=self.cols)\n\n        cond = and_(*[not_equals(get_column(table, col), None) for col in self.cols])\n        return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.DropNulls.__init__","title":"__init__","text":"<pre><code>__init__(cols)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(self, cols: typing.Union[str, typing.List[str]]):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.cols = cols\n</code></pre>"},{"location":"api/#cycquery.ops.DropNulls.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    self.cols = to_list(self.cols)\n    table = _process_checks(table, cols=self.cols)\n\n    cond = and_(*[not_equals(get_column(table, col), None) for col in self.cols])\n    return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.DropEmpty","title":"DropEmpty","text":"<p>               Bases: <code>QueryOp</code></p> <p>Remove rows with empty values in some specified columns.</p> <p>Parameters:</p> Name Type Description Default <code>cols</code> <code>Union[str, List[str]]</code> <p>Columns in which, if a value is empty, the corresponding row is removed.</p> required <p>Examples:</p> <pre><code>&gt;&gt;&gt; DropEmpty(\"col1\")(table)\n&gt;&gt;&gt; DropEmpty([\"col1\", \"col2\"])(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class DropEmpty(QueryOp):\n    \"\"\"Remove rows with empty values in some specified columns.\n\n    Parameters\n    ----------\n    cols\n        Columns in which, if a value is empty, the corresponding row is removed.\n\n    Examples\n    --------\n    &gt;&gt;&gt; DropEmpty(\"col1\")(table)\n    &gt;&gt;&gt; DropEmpty([\"col1\", \"col2\"])(table)\n\n    \"\"\"\n\n    def __init__(self, cols: typing.Union[str, typing.List[str]]):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.cols = cols\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        self.cols = to_list(self.cols)\n        table = _process_checks(table, cols=self.cols)\n\n        cond = and_(*[not_equals(get_column(table, col), \"\") for col in self.cols])\n        return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.DropEmpty.__init__","title":"__init__","text":"<pre><code>__init__(cols)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(self, cols: typing.Union[str, typing.List[str]]):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.cols = cols\n</code></pre>"},{"location":"api/#cycquery.ops.DropEmpty.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    self.cols = to_list(self.cols)\n    table = _process_checks(table, cols=self.cols)\n\n    cond = and_(*[not_equals(get_column(table, col), \"\") for col in self.cols])\n    return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.Apply","title":"Apply","text":"<p>               Bases: <code>QueryOp</code></p> <p>Apply function(s) to column(s).</p> <p>The function can take a sqlalchemy column object and also return a column object. It can also take multiple columns and return a single column or multiple columns. If multiple functions are provided, it is assumed that each function is applied to each input column.</p> <p>Parameters:</p> Name Type Description Default <code>cols</code> <code>Union[str, List[str]]</code> <p>Column(s) to apply the function to.</p> required <code>funcs</code> <code>Union[Callable[[Column], Column], List[Callable[[Column], Column]]]</code> <p>Function(s) that takes in sqlalchemy column(s) object and returns column(s) after applying the function or list of functions to apply to each column.</p> required <code>new_cols</code> <code>Optional[Union[str, List[str]]]</code> <p>New column name(s) after function is applied to the specified column(s).</p> <code>None</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; Apply(\"col1\", lambda x: x + 1)(table)\n&gt;&gt;&gt; Apply([\"col1\", \"col2\"], [lambda x: x + 1, lambda x: x + 2])(table)\n&gt;&gt;&gt; Apply(\"col1\", lambda x: x + 1, new_cols=\"col1_new\")(table)\n&gt;&gt;&gt; Apply([\"col1\", \"col2\"], lambda x, y: x + y, new_cols=\"col1_new\")(table)\n&gt;&gt;&gt; Apply(\n...     [\"col1\", \"col2\"],\n...     lambda x, y: (x + y, x - y),\n...     new_cols=[\"col1_new\", \"col2_new\"],\n... )(table)  # noqa: E501, pylint: disable=line-too-long\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class Apply(QueryOp):\n    \"\"\"Apply function(s) to column(s).\n\n    The function can take a sqlalchemy column object and also return a column object.\n    It can also take multiple columns and return a single column or multiple columns.\n    If multiple functions are provided, it is assumed that each function is applied to\n    each input column.\n\n    Parameters\n    ----------\n    cols\n        Column(s) to apply the function to.\n    funcs\n        Function(s) that takes in sqlalchemy column(s) object and returns column(s)\n        after applying the function or list of functions to apply to each column.\n    new_cols\n        New column name(s) after function is applied to the specified column(s).\n\n    Examples\n    --------\n    &gt;&gt;&gt; Apply(\"col1\", lambda x: x + 1)(table)\n    &gt;&gt;&gt; Apply([\"col1\", \"col2\"], [lambda x: x + 1, lambda x: x + 2])(table)\n    &gt;&gt;&gt; Apply(\"col1\", lambda x: x + 1, new_cols=\"col1_new\")(table)\n    &gt;&gt;&gt; Apply([\"col1\", \"col2\"], lambda x, y: x + y, new_cols=\"col1_new\")(table)\n    &gt;&gt;&gt; Apply(\n    ...     [\"col1\", \"col2\"],\n    ...     lambda x, y: (x + y, x - y),\n    ...     new_cols=[\"col1_new\", \"col2_new\"],\n    ... )(table)  # noqa: E501, pylint: disable=line-too-long\n\n    \"\"\"\n\n    def __init__(\n        self,\n        cols: typing.Union[str, typing.List[str]],\n        funcs: typing.Union[\n            typing.Callable[\n                [sqlalchemy.sql.schema.Column],\n                sqlalchemy.sql.schema.Column,\n            ],\n            typing.List[\n                typing.Callable[\n                    [sqlalchemy.sql.schema.Column],\n                    sqlalchemy.sql.schema.Column,\n                ]\n            ],\n        ],\n        new_cols: typing.Optional[typing.Union[str, typing.List[str]]] = None,\n    ):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.cols = cols\n        self.funcs = funcs\n        self.new_cols = new_cols\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        self.new_cols = to_list(self.new_cols)\n        if isinstance(self.funcs, list):\n            if len(self.funcs) != len(self.cols):\n                raise ValueError(\n                    \"Number of functions must be equal to number of columns.\",\n                )\n            if len(self.new_cols) != len(self.cols):\n                raise ValueError(\n                    \"Number of new columns must be equal to number of columns.\",\n                )\n        if callable(self.funcs):\n            cols = get_columns(table, self.cols)\n            result_cols = [\n                self.funcs(*cols).label(new_col) for new_col in self.new_cols\n            ]  # noqa: E501\n\n            return select(table).add_columns(*result_cols).subquery()  # type: ignore[arg-type]\n\n        return apply_to_columns(table, self.cols, self.funcs, self.new_cols)\n</code></pre>"},{"location":"api/#cycquery.ops.Apply.__init__","title":"__init__","text":"<pre><code>__init__(cols, funcs, new_cols=None)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    cols: typing.Union[str, typing.List[str]],\n    funcs: typing.Union[\n        typing.Callable[\n            [sqlalchemy.sql.schema.Column],\n            sqlalchemy.sql.schema.Column,\n        ],\n        typing.List[\n            typing.Callable[\n                [sqlalchemy.sql.schema.Column],\n                sqlalchemy.sql.schema.Column,\n            ]\n        ],\n    ],\n    new_cols: typing.Optional[typing.Union[str, typing.List[str]]] = None,\n):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.cols = cols\n    self.funcs = funcs\n    self.new_cols = new_cols\n</code></pre>"},{"location":"api/#cycquery.ops.Apply.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    self.new_cols = to_list(self.new_cols)\n    if isinstance(self.funcs, list):\n        if len(self.funcs) != len(self.cols):\n            raise ValueError(\n                \"Number of functions must be equal to number of columns.\",\n            )\n        if len(self.new_cols) != len(self.cols):\n            raise ValueError(\n                \"Number of new columns must be equal to number of columns.\",\n            )\n    if callable(self.funcs):\n        cols = get_columns(table, self.cols)\n        result_cols = [\n            self.funcs(*cols).label(new_col) for new_col in self.new_cols\n        ]  # noqa: E501\n\n        return select(table).add_columns(*result_cols).subquery()  # type: ignore[arg-type]\n\n    return apply_to_columns(table, self.cols, self.funcs, self.new_cols)\n</code></pre>"},{"location":"api/#cycquery.ops.OrderBy","title":"OrderBy","text":"<p>               Bases: <code>QueryOp</code></p> <p>Order, or sort, the rows of a table by some columns.</p> <p>Parameters:</p> Name Type Description Default <code>cols</code> <code>Union[str, List[str]]</code> <p>Columns by which to order.</p> required <code>ascending</code> <code>Optional[Union[bool, List[bool]]]</code> <p>Whether to order each columns by ascending (True) or descending (False). If not provided, orders all by ascending.</p> <code>None</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; OrderBy(\"col1\")(table)\n&gt;&gt;&gt; OrderBy([\"col1\", \"col2\"])(table)\n&gt;&gt;&gt; OrderBy([\"col1\", \"col2\"], [True, False])(table)\n&gt;&gt;&gt; OrderBy([\"col1\", \"col2\"], True)(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class OrderBy(QueryOp):\n    \"\"\"Order, or sort, the rows of a table by some columns.\n\n    Parameters\n    ----------\n    cols\n        Columns by which to order.\n    ascending\n        Whether to order each columns by ascending (True) or descending (False).\n        If not provided, orders all by ascending.\n\n    Examples\n    --------\n    &gt;&gt;&gt; OrderBy(\"col1\")(table)\n    &gt;&gt;&gt; OrderBy([\"col1\", \"col2\"])(table)\n    &gt;&gt;&gt; OrderBy([\"col1\", \"col2\"], [True, False])(table)\n    &gt;&gt;&gt; OrderBy([\"col1\", \"col2\"], True)(table)\n\n    \"\"\"\n\n    def __init__(\n        self,\n        cols: typing.Union[str, typing.List[str]],\n        ascending: typing.Optional[typing.Union[bool, typing.List[bool]]] = None,\n    ):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.cols = cols\n        self.ascending = ascending\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        self.cols = to_list(self.cols)\n        ascending = to_list_optional(self.ascending)\n        table = _process_checks(table, cols=self.cols)\n        if ascending is None:\n            ascending = [True] * len(self.cols)\n        elif len(ascending) != len(self.cols):\n            raise ValueError(\n                \"If ascending is specified. Must specify for all columns.\",\n            )\n        order_cols = [\n            col if ascending[i] else col.desc()\n            for i, col in enumerate(get_columns(table, self.cols))\n        ]\n\n        return select(table).order_by(*order_cols).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.OrderBy.__init__","title":"__init__","text":"<pre><code>__init__(cols, ascending=None)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    cols: typing.Union[str, typing.List[str]],\n    ascending: typing.Optional[typing.Union[bool, typing.List[bool]]] = None,\n):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.cols = cols\n    self.ascending = ascending\n</code></pre>"},{"location":"api/#cycquery.ops.OrderBy.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    self.cols = to_list(self.cols)\n    ascending = to_list_optional(self.ascending)\n    table = _process_checks(table, cols=self.cols)\n    if ascending is None:\n        ascending = [True] * len(self.cols)\n    elif len(ascending) != len(self.cols):\n        raise ValueError(\n            \"If ascending is specified. Must specify for all columns.\",\n        )\n    order_cols = [\n        col if ascending[i] else col.desc()\n        for i, col in enumerate(get_columns(table, self.cols))\n    ]\n\n    return select(table).order_by(*order_cols).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.GroupByAggregate","title":"GroupByAggregate","text":"<p>               Bases: <code>QueryOp</code></p> <p>Aggregate over a group by object.</p> <p>Parameters:</p> Name Type Description Default <code>groupby_cols</code> <code>Union[str, List[str]]</code> <p>Columns by which to group.</p> required <code>aggfuncs</code> <code>Union[Dict[str, Sequence[str]], Dict[str, str]]</code> <p>Specify a dictionary of key-value pairs: column name: aggfunc string or column name: (aggfunc string, new column label) This labelling prevents the aggregation of the same column using multiple aggregation functions.</p> required <code>aggseps</code> <code>Optional[Dict[str, str]]</code> <p>Specify a dictionary of key-value pairs: column name: string_aggfunc separator If string_agg used as aggfunc for a column, then a separator must be provided for the same column.</p> <code>None</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; GroupByAggregate(\"person_id\", {\"person_id\": \"count\"})(table)\n&gt;&gt;&gt; GroupByAggregate(\"person_id\", {\"person_id\": (\"count\", \"visit_count\")})(table)\n&gt;&gt;&gt; GroupByAggregate(\"person_id\", {\"lab_name\": \"string_agg\"}, {\"lab_name\": \", \"})(\n...     table\n... )\n&gt;&gt;&gt; GroupByAggregate(\"person_id\", {\"lab_name\": (\"string_agg\", \"lab_name_agg\"}, {\"lab_name\": \", \"})(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class GroupByAggregate(QueryOp):\n    \"\"\"Aggregate over a group by object.\n\n    Parameters\n    ----------\n    groupby_cols\n        Columns by which to group.\n    aggfuncs\n        Specify a dictionary of key-value pairs:\n        column name: aggfunc string or\n        column name: (aggfunc string, new column label)\n        This labelling prevents the aggregation of the same column using multiple\n        aggregation functions.\n    aggseps\n        Specify a dictionary of key-value pairs:\n        column name: string_aggfunc separator\n        If string_agg used as aggfunc for a column, then a separator must be provided\n        for the same column.\n\n    Examples\n    --------\n    &gt;&gt;&gt; GroupByAggregate(\"person_id\", {\"person_id\": \"count\"})(table)\n    &gt;&gt;&gt; GroupByAggregate(\"person_id\", {\"person_id\": (\"count\", \"visit_count\")})(table)\n    &gt;&gt;&gt; GroupByAggregate(\"person_id\", {\"lab_name\": \"string_agg\"}, {\"lab_name\": \", \"})(\n    ...     table\n    ... )\n    &gt;&gt;&gt; GroupByAggregate(\"person_id\", {\"lab_name\": (\"string_agg\", \"lab_name_agg\"}, {\"lab_name\": \", \"})(table)\n\n    \"\"\"\n\n    def __init__(\n        self,\n        groupby_cols: typing.Union[str, typing.List[str]],\n        aggfuncs: typing.Union[\n            typing.Dict[str, typing.Sequence[str]],\n            typing.Dict[str, str],\n        ],\n        aggseps: typing.Optional[typing.Dict[str, str]] = None,\n    ):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.groupby_cols = groupby_cols\n        self.aggfuncs = aggfuncs\n        if aggseps is None:\n            aggseps = {}\n        self.aggseps = aggseps\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        str_to_aggfunc = {\n            \"sum\": func.sum,\n            \"average\": func.avg,\n            \"min\": func.min,\n            \"max\": func.max,\n            \"count\": func.count,\n            \"median\": func.percentile_cont(0.5).within_group,\n            \"string_agg\": func.string_agg,\n        }\n\n        aggfunc_tuples = list(self.aggfuncs.items())\n        aggfunc_cols = [item[0] for item in aggfunc_tuples]\n        aggfunc_strs = [\n            item[1] if isinstance(item[1], str) else item[1][0]\n            for item in aggfunc_tuples\n        ]\n\n        # If not specified, aggregate column names default to that of\n        # the column being aggregated over\n        aggfunc_names = [\n            aggfunc_cols[i] if isinstance(item[1], str) else item[1][1]\n            for i, item in enumerate(aggfunc_tuples)\n        ]\n\n        groupby_names = to_list(self.groupby_cols)\n        table = _process_checks(table, cols=groupby_names + aggfunc_cols)\n\n        # Error checking\n        for i, aggfunc_str in enumerate(aggfunc_strs):\n            if aggfunc_str not in str_to_aggfunc:\n                allowed_strs = \", \".join(list(str_to_aggfunc.keys()))\n                raise ValueError(\n                    f\"Invalid aggfuncs specified. Allowed values are {allowed_strs}.\",\n                )\n            if aggfunc_str == \"string_agg\" and (\n                not bool(self.aggseps) or aggfunc_cols[i] not in self.aggseps\n            ):\n                raise ValueError(\n                    f\"\"\"Column {aggfunc_cols[i]} needs to be aggregated as string, must specify a separator!\"\"\",  # noqa: E501\n                )\n\n        all_names = groupby_names + aggfunc_names\n        if len(all_names) != len(set(all_names)):\n            raise ValueError(\n                \"\"\"Duplicate column names were found. Try naming aggregated columns\n                to avoid this issue.\"\"\",\n            )\n\n        # Perform group by\n        groupby_cols = get_columns(table, groupby_names)\n        to_agg_cols = get_columns(table, aggfunc_cols)\n        agg_cols = []\n        for i, to_agg_col in enumerate(to_agg_cols):\n            if aggfunc_strs[i] == \"string_agg\":\n                agg_col = str_to_aggfunc[aggfunc_strs[i]](  # type: ignore[operator]\n                    to_agg_col,\n                    literal_column(f\"'{self.aggseps[aggfunc_cols[i]]}'\"),\n                )\n            else:\n                agg_col = str_to_aggfunc[aggfunc_strs[i]](to_agg_col)  # type: ignore[operator]\n            agg_cols.append(agg_col.label(aggfunc_names[i]))\n\n        return select(*groupby_cols, *agg_cols).group_by(*groupby_cols).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.GroupByAggregate.__init__","title":"__init__","text":"<pre><code>__init__(groupby_cols, aggfuncs, aggseps=None)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    groupby_cols: typing.Union[str, typing.List[str]],\n    aggfuncs: typing.Union[\n        typing.Dict[str, typing.Sequence[str]],\n        typing.Dict[str, str],\n    ],\n    aggseps: typing.Optional[typing.Dict[str, str]] = None,\n):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.groupby_cols = groupby_cols\n    self.aggfuncs = aggfuncs\n    if aggseps is None:\n        aggseps = {}\n    self.aggseps = aggseps\n</code></pre>"},{"location":"api/#cycquery.ops.GroupByAggregate.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    str_to_aggfunc = {\n        \"sum\": func.sum,\n        \"average\": func.avg,\n        \"min\": func.min,\n        \"max\": func.max,\n        \"count\": func.count,\n        \"median\": func.percentile_cont(0.5).within_group,\n        \"string_agg\": func.string_agg,\n    }\n\n    aggfunc_tuples = list(self.aggfuncs.items())\n    aggfunc_cols = [item[0] for item in aggfunc_tuples]\n    aggfunc_strs = [\n        item[1] if isinstance(item[1], str) else item[1][0]\n        for item in aggfunc_tuples\n    ]\n\n    # If not specified, aggregate column names default to that of\n    # the column being aggregated over\n    aggfunc_names = [\n        aggfunc_cols[i] if isinstance(item[1], str) else item[1][1]\n        for i, item in enumerate(aggfunc_tuples)\n    ]\n\n    groupby_names = to_list(self.groupby_cols)\n    table = _process_checks(table, cols=groupby_names + aggfunc_cols)\n\n    # Error checking\n    for i, aggfunc_str in enumerate(aggfunc_strs):\n        if aggfunc_str not in str_to_aggfunc:\n            allowed_strs = \", \".join(list(str_to_aggfunc.keys()))\n            raise ValueError(\n                f\"Invalid aggfuncs specified. Allowed values are {allowed_strs}.\",\n            )\n        if aggfunc_str == \"string_agg\" and (\n            not bool(self.aggseps) or aggfunc_cols[i] not in self.aggseps\n        ):\n            raise ValueError(\n                f\"\"\"Column {aggfunc_cols[i]} needs to be aggregated as string, must specify a separator!\"\"\",  # noqa: E501\n            )\n\n    all_names = groupby_names + aggfunc_names\n    if len(all_names) != len(set(all_names)):\n        raise ValueError(\n            \"\"\"Duplicate column names were found. Try naming aggregated columns\n            to avoid this issue.\"\"\",\n        )\n\n    # Perform group by\n    groupby_cols = get_columns(table, groupby_names)\n    to_agg_cols = get_columns(table, aggfunc_cols)\n    agg_cols = []\n    for i, to_agg_col in enumerate(to_agg_cols):\n        if aggfunc_strs[i] == \"string_agg\":\n            agg_col = str_to_aggfunc[aggfunc_strs[i]](  # type: ignore[operator]\n                to_agg_col,\n                literal_column(f\"'{self.aggseps[aggfunc_cols[i]]}'\"),\n            )\n        else:\n            agg_col = str_to_aggfunc[aggfunc_strs[i]](to_agg_col)  # type: ignore[operator]\n        agg_cols.append(agg_col.label(aggfunc_names[i]))\n\n    return select(*groupby_cols, *agg_cols).group_by(*groupby_cols).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.Distinct","title":"Distinct","text":"<p>               Bases: <code>QueryOp</code></p> <p>Get distinct rows.</p> <p>Parameters:</p> Name Type Description Default <code>cols</code> <code>Union[str, List[str]]</code> <p>Columns to use for distinct.</p> required <p>Examples:</p> <pre><code>&gt;&gt;&gt; Distinct(\"person_id\")(table)\n&gt;&gt;&gt; Distinct([\"person_id\", \"visit_id\"])(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class Distinct(QueryOp):\n    \"\"\"Get distinct rows.\n\n    Parameters\n    ----------\n    cols\n        Columns to use for distinct.\n\n    Examples\n    --------\n    &gt;&gt;&gt; Distinct(\"person_id\")(table)\n    &gt;&gt;&gt; Distinct([\"person_id\", \"visit_id\"])(table)\n\n    \"\"\"\n\n    def __init__(self, cols: typing.Union[str, typing.List[str]]):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.cols = cols\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        cols = to_list(self.cols)\n        table = _process_checks(table, cols=cols)\n\n        return select(table).distinct(*get_columns(table, cols)).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.Distinct.__init__","title":"__init__","text":"<pre><code>__init__(cols)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(self, cols: typing.Union[str, typing.List[str]]):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.cols = cols\n</code></pre>"},{"location":"api/#cycquery.ops.Distinct.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    cols = to_list(self.cols)\n    table = _process_checks(table, cols=cols)\n\n    return select(table).distinct(*get_columns(table, cols)).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.Count","title":"Count","text":"<p>               Bases: <code>QueryOp</code></p> <p>Count the number of rows.</p> <p>Parameters:</p> Name Type Description Default <code>col</code> <code>str</code> <p>Column to count.</p> required <p>Examples:</p> <pre><code>&gt;&gt;&gt; Count(\"person_id\")(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class Count(QueryOp):\n    \"\"\"Count the number of rows.\n\n    Parameters\n    ----------\n    col\n        Column to count.\n\n    Examples\n    --------\n    &gt;&gt;&gt; Count(\"person_id\")(table)\n\n    \"\"\"\n\n    def __init__(self, col: str):\n        \"\"\"Initialize the count operation.\"\"\"\n        super().__init__()\n        self.col = col\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        table = _process_checks(table, cols=self.col)\n        count = func.count(get_column(table, self.col))\n\n        return select(count).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.Count.__init__","title":"__init__","text":"<pre><code>__init__(col)\n</code></pre> <p>Initialize the count operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(self, col: str):\n    \"\"\"Initialize the count operation.\"\"\"\n    super().__init__()\n    self.col = col\n</code></pre>"},{"location":"api/#cycquery.ops.Count.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    table = _process_checks(table, cols=self.col)\n    count = func.count(get_column(table, self.col))\n\n    return select(count).subquery()\n</code></pre>"},{"location":"api/#cycquery.orm","title":"orm","text":"<p>Object Relational Mapper (ORM) using sqlalchemy.</p>"},{"location":"api/#cycquery.orm.DatasetQuerierConfig","title":"DatasetQuerierConfig  <code>dataclass</code>","text":"<p>Configuration for the dataset querier.</p> <p>Attributes:</p> Name Type Description <code>dbms</code> <code>str</code> <p>The database management system type (e.g., 'postgresql', 'mysql', 'sqlite').</p> <code>user</code> <code>(str, optional)</code> <p>The username for the database, by default empty. Not used for SQLite.</p> <code>pwd</code> <code>(str, optional)</code> <p>The password for the database, by default empty. Not used for SQLite.</p> <code>host</code> <code>(str, optional)</code> <p>The host address of the database, by default empty. Not used for SQLite.</p> <code>port</code> <code>(int, optional)</code> <p>The port number for the database, by default None. Not used for SQLite.</p> <code>database</code> <code>(str, optional)</code> <p>The name of the database or the path to the database file (for SQLite), by default empty.</p> <code>schemas</code> <code>(Union[str, List[str]], optional)</code> <p>The schema(s) to be used for querying, by default None.</p> Source code in <code>cycquery/orm.py</code> <pre><code>@dataclass\nclass DatasetQuerierConfig:\n    \"\"\"Configuration for the dataset querier.\n\n    Attributes\n    ----------\n    dbms : str\n        The database management system type (e.g., 'postgresql', 'mysql', 'sqlite').\n    user : str, optional\n        The username for the database, by default empty. Not used for SQLite.\n    pwd : str, optional\n        The password for the database, by default empty. Not used for SQLite.\n    host : str, optional\n        The host address of the database, by default empty. Not used for SQLite.\n    port : int, optional\n        The port number for the database, by default None. Not used for SQLite.\n    database : str, optional\n        The name of the database or the path to the database file (for SQLite),\n        by default empty.\n    schemas : Union[str, List[str]], optional\n        The schema(s) to be used for querying, by default None.\n\n    \"\"\"\n\n    dbms: str\n    user: str = \"\"\n    password: str = \"\"\n    host: str = \"\"\n    port: int | None = None\n    database: str = \"\"\n    schemas: str | list[str] | None = None\n</code></pre>"},{"location":"api/#cycquery.orm.Database","title":"Database","text":"<p>Database class.</p> <p>Attributes:</p> Name Type Description <code>config</code> <p>Configuration stored in a dataclass.</p> <code>engine</code> <p>SQL extraction engine.</p> <code>inspector</code> <p>Module for schema inspection.</p> <code>session</code> <p>Session for ORM.</p> <code>is_connected</code> <p>Whether the database is setup, connected and ready to run queries.</p> Source code in <code>cycquery/orm.py</code> <pre><code>class Database:\n    \"\"\"Database class.\n\n    Attributes\n    ----------\n    config\n        Configuration stored in a dataclass.\n    engine\n        SQL extraction engine.\n    inspector\n        Module for schema inspection.\n    session\n        Session for ORM.\n    is_connected\n        Whether the database is setup, connected and ready to run queries.\n\n    \"\"\"\n\n    def __init__(self, config: DatasetQuerierConfig) -&gt; None:\n        \"\"\"Instantiate.\n\n        Parameters\n        ----------\n        config\n            Path to directory with config file, for overrides.\n\n        \"\"\"\n        self.config = config\n        self.is_connected = False\n\n        # Check if server is up or database file exists.\n        if self.config.dbms.lower() == \"sqlite\":\n            if not os.path.exists(self.config.database):\n                LOGGER.error(\n                    f\"\"\"Database file '{self.config.database}' does not exist!\"\"\",\n                )\n                return\n        else:\n            if not self.config.host:\n                LOGGER.error(\"\"\"No server host provided!\"\"\")\n                return\n            if not self.config.port:\n                LOGGER.error(\"\"\"No server port provided!\"\"\")\n                return\n            sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)\n            sock.settimeout(SOCKET_CONNECTION_TIMEOUT)\n            try:\n                is_port_open = sock.connect_ex((self.config.host, self.config.port))\n            except socket.gaierror:\n                LOGGER.error(\"\"\"Server name not known, cannot establish connection!\"\"\")\n                return\n            if is_port_open:\n                LOGGER.error(\n                    \"\"\"Valid server host but port seems open, check if server is up!\"\"\",\n                )\n                return\n\n        self.engine = self._create_engine()\n        self.session = self._create_session()\n        self._tables: list[str] = []\n        self._setup()\n        self.is_connected = True\n        LOGGER.info(\"Database setup, ready to run queries!\")\n\n    def _create_engine(self) -&gt; Engine:\n        \"\"\"Create an engine.\"\"\"\n        db_url = _get_db_url(\n            self.config.dbms,\n            self.config.user,\n            self.config.password,\n            self.config.host,\n            self.config.port,\n            self.config.database,\n        )\n        self.conn = db_url\n        return create_engine(db_url)\n\n    def _create_session(self) -&gt; Session:\n        \"\"\"Create session.\"\"\"\n        self.inspector = inspect(self.engine)\n\n        # Create a session for using ORM.\n        session_factory = sessionmaker(bind=self.engine)\n\n        return session_factory()\n\n    def list_tables(self) -&gt; list[str]:\n        \"\"\"List tables in a schema.\n\n        Returns\n        -------\n        list[str]\n            List of table names.\n\n        \"\"\"\n        return self._tables\n\n    def _setup(self) -&gt; None:\n        \"\"\"Prepare ORM DB.\"\"\"\n        meta: dict[str, MetaData] = {}\n        schemas = self.inspector.get_schema_names()\n        if self.config.schemas is None:\n            schemas_to_use = schemas\n        elif isinstance(self.config.schemas, str):\n            schemas_to_use = [self.config.schemas]\n        else:\n            schemas_to_use = self.config.schemas\n        for schema_name in schemas:\n            if schemas_to_use is None or schema_name in schemas_to_use:\n                metadata = MetaData(schema=schema_name)\n                with self.engine.connect() as conn:\n                    metadata.reflect(bind=conn)\n                meta[schema_name] = metadata\n                schema = DBSchema(schema_name, meta[schema_name])\n                for table_name in meta[schema_name].tables:\n                    table = DBTable(table_name, meta[schema_name].tables[table_name])  # type: ignore[arg-type]\n                    for column in meta[schema_name].tables[table_name].columns:\n                        setattr(table, column.name, column)\n                    if not isinstance(table.name_, str):\n                        table.name_ = str(table.name_)\n                    self._tables.append(table.name_)\n                    setattr(schema, get_attr_name(table.name_), table)\n                setattr(self, schema_name, schema)\n\n    @time_function\n    @table_params_to_type(Select)  # type: ignore[arg-type]\n    def run_query(\n        self,\n        query: TableTypes | str,\n        dtype_backend: str = \"pyarrow\",\n        limit: int | None = None,\n        index_col: str | None = None,\n    ) -&gt; pd.DataFrame:\n        \"\"\"Run query.\n\n        Parameters\n        ----------\n        query\n            Query to run.\n        dtype_backend\n            Backend for dtype conversion.\n        limit\n            Limit query result to limit.\n        index_col\n            Column which becomes the index, and defines the partitioning.\n            Should be a indexed column in the SQL server, and any orderable type.\n\n        Returns\n        -------\n        pandas.DataFrame\n            Extracted data from query.\n\n        \"\"\"\n        if isinstance(query, str) and limit is not None:\n            raise ValueError(\n                \"Cannot use limit argument when running raw SQL string query!\",\n            )\n        # Limit the results returned.\n        if limit is not None:\n            query = query.limit(limit)  # type: ignore\n\n        # Wrap raw SQL strings for SA 2.0 compatibility.\n        sql = text(query) if isinstance(query, str) else query\n\n        with self.engine.connect() as conn:\n            data = pd.read_sql_query(\n                sql,\n                conn,\n                index_col=index_col,\n                dtype_backend=dtype_backend,\n            )\n        LOGGER.info(\"Query returned successfully!\")\n\n        return data\n\n    @time_function\n    @table_params_to_type(Select)  # type: ignore[arg-type]\n    def save_query_to_csv(self, query: TableTypes, path: str) -&gt; str:\n        \"\"\"Save query in a .csv format.\n\n        Parameters\n        ----------\n        query\n            Query to save.\n        path\n            Save path.\n\n        Returns\n        -------\n        str\n            Processed save path for upstream use.\n\n        \"\"\"\n        path = process_file_save_path(path, \"csv\")\n\n        with self.engine.connect() as conn:\n            result = conn.execute(query)  # type: ignore\n            with open(path, \"w\", encoding=\"utf-8\") as file_descriptor:\n                outcsv = csv.writer(file_descriptor)\n                outcsv.writerow(result.keys())\n                outcsv.writerows(result)\n\n        return path\n\n    @time_function\n    @table_params_to_type(Select)  # type: ignore[arg-type]\n    def save_query_to_parquet(self, query: TableTypes, path: str) -&gt; str:\n        \"\"\"Save query in a .parquet format.\n\n        Parameters\n        ----------\n        query\n            Query to save.\n        path\n            Save path.\n\n        Returns\n        -------\n        str\n            Processed save path for upstream use.\n\n        \"\"\"\n        path = process_file_save_path(path, \"parquet\")\n\n        # Save to CSV, load with pyarrow, save to Parquet\n        csv_path = exchange_extension(path, \"csv\")\n        self.save_query_to_csv(query, csv_path)\n        table = pv.read_csv(csv_path)\n        os.remove(csv_path)\n        pq.write_table(table, path)\n\n        return path\n\n    def _query_batch_conditions(\n        self,\n        query: TableTypes,\n        index_col: str,\n        batch_size: int,\n    ) -&gt; list[BinaryExpression | BooleanClauseList]:\n        \"\"\"Return a list of WHERE conditions to segment a query into batches.\n\n        Batches are created via SQL windowing, based on segmenting the values in a\n        given column, such as an ID column, into intervals.\n\n        Requires a database that supports window functions.\n\n        Parameters\n        ----------\n        query\n            Query to run.\n        index_col\n            Name of the sample ID column by which to batch.\n        batch_size\n            Batch size for the query.\n\n        Returns\n        -------\n        list of sqlalchemy.sql.elements.BinaryExpression or\n        sqlalchemy.sql.elements.BooleanClauseList\n            The window conditions on which to filter.\n\n        \"\"\"\n\n        def _compute_query_dividers(\n            query: Subquery,\n            index_col: str,\n            maximum: int,\n        ) -&gt; list[int]:\n            # Compute the row count for each unique value\n            col = get_column(query, index_col)\n            table = select(col, func.count(col).label(\"count\")).group_by(col)\n            count_data = self.run_query(table)\n\n            # Check that all values can actually fit into the maximum batch size\n            max_count = count_data[\"count\"].max()\n            if maximum &lt; max_count:\n                raise ValueError(f\"Maximum must be at least {max_count}.\")\n\n            # Sort and create a cumulative sum of row counts\n            count_data = count_data.sort_values(index_col)\n            count_data[\"cumsum\"] = count_data[\"count\"].cumsum()\n\n            # Create query dividers\n            last_sum = 0\n\n            if len(count_data) == 0:\n                raise ValueError(\"Query is empty. Cannot return batched results.\")\n\n            dividers = [int(count_data[index_col].iloc[0])]\n            for i, cumsum in enumerate(count_data[\"cumsum\"].values[1:]):\n                # If adding the next value will put the sum over the max,\n                # then add another divider on the previous value\n                if cumsum - last_sum &gt; maximum:\n                    dividers.append(int(count_data[index_col].iloc[i]))\n                    last_sum = count_data[\"cumsum\"].iloc[i]\n\n            return dividers\n\n        def _range_condition(\n            start_id: int,\n            end_id: int | None = None,\n        ) -&gt; BinaryExpression | BooleanClauseList:\n            if end_id:\n                return and_(column &gt;= start_id, column &lt; end_id)  # type: ignore[return-value]\n\n            return column &gt;= start_id\n\n        # Create interval dividers\n        dividers = _compute_query_dividers(query, index_col, batch_size)  # type: ignore[arg-type]\n\n        # Create filtering conditions\n        column = get_column(query, index_col)\n        conditions = []\n        while dividers:\n            # Create interval ranges\n            start = dividers.pop(0)\n            end = dividers[0] if dividers else None\n\n            # Create condition\n            conditions.append(_range_condition(start, end))\n\n        return conditions\n\n    @table_params_to_type(Subquery)  # type: ignore[arg-type]\n    def run_query_batch(\n        self,\n        query: TableTypes,\n        index_col: str,\n        batch_size: int,\n        dtype_backend: str = \"pyarrow\",\n    ) -&gt; Generator[pd.DataFrame, None, None]:\n        \"\"\"Generate query batches with complete sets of IDs in a batch.\n\n        Queries are sorted and grouped such that the rows for a given sample ID are kept\n        together in a single batch.\n\n        Parameters\n        ----------\n        query\n            Query to run.\n        index_col\n            Name of the sample ID column by which to batch.\n        batch_size\n            Batch size for the query. Since the partitioning happens on the index\n            column, the batch size is the approximate number of rows that will\n            be returned in a batch.\n        dtype_backend\n            Backend for dtype conversion.\n\n        Yields\n        ------\n        pandas.DataFrame\n            A query batch with complete sets of sample IDs.\n\n        \"\"\"\n        if \"limit\" in str(query).lower():\n            raise NotImplementedError(\n                \"Currently not supporting batching for queries with a LIMIT.\",\n            )\n\n        conditions = self._query_batch_conditions(query, index_col, batch_size)\n\n        # Opportunity for easy multi-processing/parallelization here!\n        with self.engine.connect() as conn:\n            for condition in conditions:\n                run = select(query).where(condition)  # type: ignore[arg-type]\n                yield pd.read_sql_query(run, conn, dtype_backend=dtype_backend)\n</code></pre>"},{"location":"api/#cycquery.orm.Database.__init__","title":"__init__","text":"<pre><code>__init__(config)\n</code></pre> <p>Instantiate.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>DatasetQuerierConfig</code> <p>Path to directory with config file, for overrides.</p> required Source code in <code>cycquery/orm.py</code> <pre><code>def __init__(self, config: DatasetQuerierConfig) -&gt; None:\n    \"\"\"Instantiate.\n\n    Parameters\n    ----------\n    config\n        Path to directory with config file, for overrides.\n\n    \"\"\"\n    self.config = config\n    self.is_connected = False\n\n    # Check if server is up or database file exists.\n    if self.config.dbms.lower() == \"sqlite\":\n        if not os.path.exists(self.config.database):\n            LOGGER.error(\n                f\"\"\"Database file '{self.config.database}' does not exist!\"\"\",\n            )\n            return\n    else:\n        if not self.config.host:\n            LOGGER.error(\"\"\"No server host provided!\"\"\")\n            return\n        if not self.config.port:\n            LOGGER.error(\"\"\"No server port provided!\"\"\")\n            return\n        sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)\n        sock.settimeout(SOCKET_CONNECTION_TIMEOUT)\n        try:\n            is_port_open = sock.connect_ex((self.config.host, self.config.port))\n        except socket.gaierror:\n            LOGGER.error(\"\"\"Server name not known, cannot establish connection!\"\"\")\n            return\n        if is_port_open:\n            LOGGER.error(\n                \"\"\"Valid server host but port seems open, check if server is up!\"\"\",\n            )\n            return\n\n    self.engine = self._create_engine()\n    self.session = self._create_session()\n    self._tables: list[str] = []\n    self._setup()\n    self.is_connected = True\n    LOGGER.info(\"Database setup, ready to run queries!\")\n</code></pre>"},{"location":"api/#cycquery.orm.Database.list_tables","title":"list_tables","text":"<pre><code>list_tables()\n</code></pre> <p>List tables in a schema.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>List of table names.</p> Source code in <code>cycquery/orm.py</code> <pre><code>def list_tables(self) -&gt; list[str]:\n    \"\"\"List tables in a schema.\n\n    Returns\n    -------\n    list[str]\n        List of table names.\n\n    \"\"\"\n    return self._tables\n</code></pre>"},{"location":"api/#cycquery.orm.Database.run_query","title":"run_query","text":"<pre><code>run_query(\n    query,\n    dtype_backend=\"pyarrow\",\n    limit=None,\n    index_col=None,\n)\n</code></pre> <p>Run query.</p> <p>Parameters:</p> Name Type Description Default <code>query</code> <code>TableTypes | str</code> <p>Query to run.</p> required <code>dtype_backend</code> <code>str</code> <p>Backend for dtype conversion.</p> <code>'pyarrow'</code> <code>limit</code> <code>int | None</code> <p>Limit query result to limit.</p> <code>None</code> <code>index_col</code> <code>str | None</code> <p>Column which becomes the index, and defines the partitioning. Should be a indexed column in the SQL server, and any orderable type.</p> <code>None</code> <p>Returns:</p> Type Description <code>DataFrame</code> <p>Extracted data from query.</p> Source code in <code>cycquery/orm.py</code> <pre><code>@time_function\n@table_params_to_type(Select)  # type: ignore[arg-type]\ndef run_query(\n    self,\n    query: TableTypes | str,\n    dtype_backend: str = \"pyarrow\",\n    limit: int | None = None,\n    index_col: str | None = None,\n) -&gt; pd.DataFrame:\n    \"\"\"Run query.\n\n    Parameters\n    ----------\n    query\n        Query to run.\n    dtype_backend\n        Backend for dtype conversion.\n    limit\n        Limit query result to limit.\n    index_col\n        Column which becomes the index, and defines the partitioning.\n        Should be a indexed column in the SQL server, and any orderable type.\n\n    Returns\n    -------\n    pandas.DataFrame\n        Extracted data from query.\n\n    \"\"\"\n    if isinstance(query, str) and limit is not None:\n        raise ValueError(\n            \"Cannot use limit argument when running raw SQL string query!\",\n        )\n    # Limit the results returned.\n    if limit is not None:\n        query = query.limit(limit)  # type: ignore\n\n    # Wrap raw SQL strings for SA 2.0 compatibility.\n    sql = text(query) if isinstance(query, str) else query\n\n    with self.engine.connect() as conn:\n        data = pd.read_sql_query(\n            sql,\n            conn,\n            index_col=index_col,\n            dtype_backend=dtype_backend,\n        )\n    LOGGER.info(\"Query returned successfully!\")\n\n    return data\n</code></pre>"},{"location":"api/#cycquery.orm.Database.save_query_to_csv","title":"save_query_to_csv","text":"<pre><code>save_query_to_csv(query, path)\n</code></pre> <p>Save query in a .csv format.</p> <p>Parameters:</p> Name Type Description Default <code>query</code> <code>TableTypes</code> <p>Query to save.</p> required <code>path</code> <code>str</code> <p>Save path.</p> required <p>Returns:</p> Type Description <code>str</code> <p>Processed save path for upstream use.</p> Source code in <code>cycquery/orm.py</code> <pre><code>@time_function\n@table_params_to_type(Select)  # type: ignore[arg-type]\ndef save_query_to_csv(self, query: TableTypes, path: str) -&gt; str:\n    \"\"\"Save query in a .csv format.\n\n    Parameters\n    ----------\n    query\n        Query to save.\n    path\n        Save path.\n\n    Returns\n    -------\n    str\n        Processed save path for upstream use.\n\n    \"\"\"\n    path = process_file_save_path(path, \"csv\")\n\n    with self.engine.connect() as conn:\n        result = conn.execute(query)  # type: ignore\n        with open(path, \"w\", encoding=\"utf-8\") as file_descriptor:\n            outcsv = csv.writer(file_descriptor)\n            outcsv.writerow(result.keys())\n            outcsv.writerows(result)\n\n    return path\n</code></pre>"},{"location":"api/#cycquery.orm.Database.save_query_to_parquet","title":"save_query_to_parquet","text":"<pre><code>save_query_to_parquet(query, path)\n</code></pre> <p>Save query in a .parquet format.</p> <p>Parameters:</p> Name Type Description Default <code>query</code> <code>TableTypes</code> <p>Query to save.</p> required <code>path</code> <code>str</code> <p>Save path.</p> required <p>Returns:</p> Type Description <code>str</code> <p>Processed save path for upstream use.</p> Source code in <code>cycquery/orm.py</code> <pre><code>@time_function\n@table_params_to_type(Select)  # type: ignore[arg-type]\ndef save_query_to_parquet(self, query: TableTypes, path: str) -&gt; str:\n    \"\"\"Save query in a .parquet format.\n\n    Parameters\n    ----------\n    query\n        Query to save.\n    path\n        Save path.\n\n    Returns\n    -------\n    str\n        Processed save path for upstream use.\n\n    \"\"\"\n    path = process_file_save_path(path, \"parquet\")\n\n    # Save to CSV, load with pyarrow, save to Parquet\n    csv_path = exchange_extension(path, \"csv\")\n    self.save_query_to_csv(query, csv_path)\n    table = pv.read_csv(csv_path)\n    os.remove(csv_path)\n    pq.write_table(table, path)\n\n    return path\n</code></pre>"},{"location":"api/#cycquery.orm.Database.run_query_batch","title":"run_query_batch","text":"<pre><code>run_query_batch(\n    query, index_col, batch_size, dtype_backend=\"pyarrow\"\n)\n</code></pre> <p>Generate query batches with complete sets of IDs in a batch.</p> <p>Queries are sorted and grouped such that the rows for a given sample ID are kept together in a single batch.</p> <p>Parameters:</p> Name Type Description Default <code>query</code> <code>TableTypes</code> <p>Query to run.</p> required <code>index_col</code> <code>str</code> <p>Name of the sample ID column by which to batch.</p> required <code>batch_size</code> <code>int</code> <p>Batch size for the query. Since the partitioning happens on the index column, the batch size is the approximate number of rows that will be returned in a batch.</p> required <code>dtype_backend</code> <code>str</code> <p>Backend for dtype conversion.</p> <code>'pyarrow'</code> <p>Yields:</p> Type Description <code>DataFrame</code> <p>A query batch with complete sets of sample IDs.</p> Source code in <code>cycquery/orm.py</code> <pre><code>@table_params_to_type(Subquery)  # type: ignore[arg-type]\ndef run_query_batch(\n    self,\n    query: TableTypes,\n    index_col: str,\n    batch_size: int,\n    dtype_backend: str = \"pyarrow\",\n) -&gt; Generator[pd.DataFrame, None, None]:\n    \"\"\"Generate query batches with complete sets of IDs in a batch.\n\n    Queries are sorted and grouped such that the rows for a given sample ID are kept\n    together in a single batch.\n\n    Parameters\n    ----------\n    query\n        Query to run.\n    index_col\n        Name of the sample ID column by which to batch.\n    batch_size\n        Batch size for the query. Since the partitioning happens on the index\n        column, the batch size is the approximate number of rows that will\n        be returned in a batch.\n    dtype_backend\n        Backend for dtype conversion.\n\n    Yields\n    ------\n    pandas.DataFrame\n        A query batch with complete sets of sample IDs.\n\n    \"\"\"\n    if \"limit\" in str(query).lower():\n        raise NotImplementedError(\n            \"Currently not supporting batching for queries with a LIMIT.\",\n        )\n\n    conditions = self._query_batch_conditions(query, index_col, batch_size)\n\n    # Opportunity for easy multi-processing/parallelization here!\n    with self.engine.connect() as conn:\n        for condition in conditions:\n            run = select(query).where(condition)  # type: ignore[arg-type]\n            yield pd.read_sql_query(run, conn, dtype_backend=dtype_backend)\n</code></pre>"},{"location":"api/#cycquery.post_process","title":"post_process","text":"<p>Query post-process functions for datasets.</p>"},{"location":"api/#cycquery.post_process.gemini","title":"gemini","text":"<p>Post-processing functions applied to queried GEMINI data (Pandas DataFrames).</p>"},{"location":"api/#cycquery.post_process.gemini.process_gemini_care_unit_changepoints","title":"process_gemini_care_unit_changepoints","text":"<pre><code>process_gemini_care_unit_changepoints(data)\n</code></pre> <p>Process GEMINI changepoint care unit information in a hierarchical fashion.</p> <p>Using the admit, discharge, and care unit information, create a changepoint DataFrame usable for aggregation labelling purposes. If a patient is in multiple care units at a changepoint, the care unit highest in the hierarchy is selected.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>DataFrame</code> <p>The admit, discharge, and care unit information for a single encounter. Expects columns \"admit\", \"discharge\", and CARE_UNIT.</p> required <p>Returns:</p> Type Description <code>DataFrame</code> <p>Changepoint information with associated care unit.</p> Source code in <code>cycquery/post_process/gemini.py</code> <pre><code>def process_gemini_care_unit_changepoints(data: pd.DataFrame) -&gt; pd.DataFrame:\n    \"\"\"Process GEMINI changepoint care unit information in a hierarchical fashion.\n\n    Using the admit, discharge, and care unit information, create a\n    changepoint DataFrame usable for aggregation labelling purposes.\n    If a patient is in multiple care units at a changepoint, the care\n    unit highest in the hierarchy is selected.\n\n    Parameters\n    ----------\n    data: pandas.DataFrame\n        The admit, discharge, and care unit information for a single encounter.\n        Expects columns \"admit\", \"discharge\", and CARE_UNIT.\n\n    Returns\n    -------\n    pandas.DataFrame\n        Changepoint information with associated care unit.\n\n    \"\"\"\n    return process_care_unit_changepoints(data, CARE_UNIT_HIERARCHY)\n</code></pre>"},{"location":"api/#cycquery.post_process.mimiciv","title":"mimiciv","text":"<p>Post-processing functions applied to queried MIMIC data (Pandas DataFrames).</p>"},{"location":"api/#cycquery.post_process.mimiciv.process_mimic_care_unit_changepoints","title":"process_mimic_care_unit_changepoints","text":"<pre><code>process_mimic_care_unit_changepoints(data)\n</code></pre> <p>Process MIMIC changepoint care unit information in a hierarchical fashion.</p> <p>Using the admit, discharge, and care unit information, create a changepoint DataFrame usable for aggregation labelling purposes. If a patient is in multiple care units at a changepoint, the care unit highest in the hierarchy is selected.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>DataFrame</code> <p>The admit, discharge, and care unit information for a single encounter. Expects columns \"admit\", \"discharge\", and CARE_UNIT.</p> required <p>Returns:</p> Type Description <code>DataFrame</code> <p>Changepoint information with associated care unit.</p> Source code in <code>cycquery/post_process/mimiciv.py</code> <pre><code>def process_mimic_care_unit_changepoints(data: pd.DataFrame) -&gt; pd.DataFrame:\n    \"\"\"Process MIMIC changepoint care unit information in a hierarchical fashion.\n\n    Using the admit, discharge, and care unit information, create a\n    changepoint DataFrame usable for aggregation labelling purposes.\n    If a patient is in multiple care units at a changepoint, the care\n    unit highest in the hierarchy is selected.\n\n    Parameters\n    ----------\n    data: pandas.DataFrame\n        The admit, discharge, and care unit information for a single encounter.\n        Expects columns \"admit\", \"discharge\", and CARE_UNIT.\n\n    Returns\n    -------\n    pandas.DataFrame\n        Changepoint information with associated care unit.\n\n    \"\"\"\n    return process_care_unit_changepoints(data, CARE_UNIT_HIERARCHY)\n</code></pre>"},{"location":"api/#cycquery.post_process.mimiciv.process_mimic_care_units","title":"process_mimic_care_units","text":"<pre><code>process_mimic_care_units(transfers, specific=False)\n</code></pre> <p>Process care unit data.</p> <p>Processes the MIMIC Transfers table into a cleaned and simplified care units DataFrame.</p> <p>Parameters:</p> Name Type Description Default <code>transfers</code> <code>DataFrame</code> <p>MIMIC transfers table as a DataFrame.</p> required <code>specific</code> <code>bool</code> <p>Whether care_unit_name column has specific or non-specific care units.</p> <code>False</code> <p>Returns:</p> Type Description <code>DataFrame</code> <p>Processed care units for MIMIC encounters.</p> Source code in <code>cycquery/post_process/mimiciv.py</code> <pre><code>@time_function\ndef process_mimic_care_units(\n    transfers: pd.DataFrame,\n    specific: bool = False,\n) -&gt; pd.DataFrame:\n    \"\"\"Process care unit data.\n\n    Processes the MIMIC Transfers table into a cleaned and simplified care\n    units DataFrame.\n\n    Parameters\n    ----------\n    transfers : pandas.DataFrame\n        MIMIC transfers table as a DataFrame.\n    specific : bool, optional\n        Whether care_unit_name column has specific or non-specific care units.\n\n    Returns\n    -------\n    pandas.DataFrame\n        Processed care units for MIMIC encounters.\n\n    \"\"\"\n    transfers.rename(\n        columns={\n            \"intime\": \"admit\",\n            \"outtime\": \"discharge\",\n            \"careunit\": CARE_UNIT,\n        },\n        inplace=True,\n    )\n\n    # Drop rows with eventtype discharge.\n    # Its admit timestamp is the discharge timestamp of eventtype admit.\n    transfers = transfers[transfers[\"eventtype\"] != \"discharge\"]\n    transfers = transfers.drop(\"eventtype\", axis=1)\n    transfers = transfers[transfers[CARE_UNIT] != \"Unknown\"]\n\n    # Create replacement dictionary for care unit categories depending on specificity.\n    replace_dict = {}\n    for unit, unit_dict in CARE_UNIT_MAP.items():\n        for specific_unit, unit_list in unit_dict.items():\n            value = specific_unit if specific else unit\n            replace_dict.update(dict.fromkeys(unit_list, value))\n    transfers[CARE_UNIT].replace(replace_dict, inplace=True)\n\n    transfers.dropna(inplace=True)\n\n    return transfers\n</code></pre>"},{"location":"api/#cycquery.post_process.util","title":"util","text":"<p>Post-processing functions applied to queried data (Pandas DataFrames).</p>"},{"location":"api/#cycquery.post_process.util.event_time_between","title":"event_time_between","text":"<pre><code>event_time_between(\n    event,\n    admit,\n    discharge,\n    admit_inclusive=True,\n    discharge_inclusive=False,\n)\n</code></pre> <p>Return whether an event time is between some start and end time.</p> <p>May also specify whether the comparison operators are inclusive or not..</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <code>Timestamp</code> <p>Event time.</p> required <code>admit</code> <code>Series</code> <p>A series of timestamps.</p> required <code>discharge</code> <code>Series</code> <p>A series of timestamps.</p> required <code>admit_inclusive</code> <code>bool</code> <p>Whether to have an inclusive inequality for the admit condition.</p> <code>True</code> <code>discharge_inclusive</code> <code>bool</code> <p>Whether to have an inclusive inequality for the discharge condition.</p> <code>False</code> <p>Returns:</p> Type Description <code>Series</code> <p>A boolean Series representing whether the event is between the start and end timestamps.</p> Source code in <code>cycquery/post_process/util.py</code> <pre><code>def event_time_between(\n    event: Timestamp,\n    admit: pd.Series,\n    discharge: pd.Series,\n    admit_inclusive: bool = True,\n    discharge_inclusive: bool = False,\n) -&gt; pd.Series:\n    \"\"\"Return whether an event time is between some start and end time.\n\n    May also specify whether the comparison operators are inclusive or not..\n\n    Parameters\n    ----------\n    event: pandas.Timestamp\n        Event time.\n    admit: pandas.Series\n        A series of timestamps.\n    discharge: pandas.Series\n        A series of timestamps.\n    admit_inclusive: bool\n        Whether to have an inclusive inequality for the admit condition.\n    discharge_inclusive: bool\n        Whether to have an inclusive inequality for the discharge condition.\n\n    Returns\n    -------\n    pandas.Series\n        A boolean Series representing whether the event is between\n        the start and end timestamps.\n\n    \"\"\"\n    admit_cond = event &gt;= admit if admit_inclusive else event &gt; admit\n\n    discharge_cond = event &lt;= discharge if discharge_inclusive else event &lt; discharge\n\n    return admit_cond &amp; discharge_cond\n</code></pre>"},{"location":"api/#cycquery.post_process.util.process_care_unit_changepoints","title":"process_care_unit_changepoints","text":"<pre><code>process_care_unit_changepoints(data, care_unit_hierarchy)\n</code></pre> <p>Process changepoint care unit information in a hierarchical fashion.</p> <p>Using the admit, discharge, and care unit information, create a changepoint DataFrame usable for aggregation labelling purposes. If a patient is in multiple care units at a changepoint, the care unit highest in the hierarchy is selected.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>DataFrame</code> <p>The admit, discharge, and care unit information for a single encounter. Expects columns \"admit\", \"discharge\", and CARE_UNIT.</p> required <code>care_unit_hierarchy</code> <code>List[str]</code> <p>Ordered list of care units from most relevant to to least.</p> required <p>Returns:</p> Type Description <code>DataFrame</code> <p>Changepoint information with associated care unit. The care unit information is relevant up until the next change point</p> Source code in <code>cycquery/post_process/util.py</code> <pre><code>def process_care_unit_changepoints(\n    data: pd.DataFrame,\n    care_unit_hierarchy: List[str],\n) -&gt; pd.DataFrame:\n    \"\"\"Process changepoint care unit information in a hierarchical fashion.\n\n    Using the admit, discharge, and care unit information, create a\n    changepoint DataFrame usable for aggregation labelling purposes.\n    If a patient is in multiple care units at a changepoint, the care\n    unit highest in the hierarchy is selected.\n\n    Parameters\n    ----------\n    data: pandas.DataFrame\n        The admit, discharge, and care unit information for a single encounter.\n        Expects columns \"admit\", \"discharge\", and CARE_UNIT.\n    care_unit_hierarchy: list\n        Ordered list of care units from most relevant to to least.\n\n    Returns\n    -------\n    pandas.DataFrame\n        Changepoint information with associated care unit. The care unit\n        information is relevant up until the next change point\n\n    \"\"\"\n    # Define mapping dictionaries\n    hierarchy = {care_unit_hierarchy[i]: i for i in range(len(care_unit_hierarchy))}\n    hierarchy_inv = {i: care_unit_hierarchy[i] for i in range(len(care_unit_hierarchy))}\n\n    # Create changepoints\n    changepoints = pd.concat([data[\"admit\"], data[\"discharge\"]])\n    changepoints.sort_values(inplace=True)\n    changepoints = changepoints.unique()\n\n    # Remove the final changepoint, which is the final discharge (has no careunit)\n    changepoints = changepoints[:-1]\n\n    # Select the most relevant care unit for each changepoint\n    changepoint_data = []\n    for changepoint in changepoints:\n        is_between = event_time_between(\n            changepoint,\n            data[\"admit\"],\n            data[\"discharge\"],\n            admit_inclusive=True,\n            discharge_inclusive=False,\n        )\n        care_units = data[is_between][CARE_UNIT].unique()\n        if len(care_units) &gt; 0:\n            care_unit_inds = [hierarchy[x] for x in care_units]\n            care_unit_selected = hierarchy_inv[min(care_unit_inds)]\n        else:\n            care_unit_selected = \"unknown\"\n        changepoint_data.append([changepoint, care_unit_selected])\n\n    checkpoint_df = pd.DataFrame(changepoint_data, columns={\"changepoint\", \"care_unit\"})\n\n    # Remove consequtive duplicates, i.e., remove a changepoint if the\n    # previous changepoint has the same care unit\n    change_mask = checkpoint_df[\"care_unit\"] != checkpoint_df[\"care_unit\"].shift(-1)\n\n    return checkpoint_df[change_mask]\n</code></pre>"},{"location":"api/#cycquery.util","title":"util","text":"<p>Utility functions for querying.</p>"},{"location":"api/#cycquery.util.DBSchema","title":"DBSchema  <code>dataclass</code>","text":"<p>Database schema wrapper.</p> <p>Parameters:</p> Name Type Description Default <code>name_</code> <code>str</code> <p>Name of schema.</p> required <code>data_</code> <code>MetaData</code> <p>Metadata for schema.</p> required Source code in <code>cycquery/util.py</code> <pre><code>@dataclass\nclass DBSchema:\n    \"\"\"Database schema wrapper.\n\n    Parameters\n    ----------\n    name_: str\n        Name of schema.\n    data_: sqlalchemy.sql.schema.MetaData\n        Metadata for schema.\n\n    \"\"\"\n\n    name_: str\n    data_: sqlalchemy.MetaData\n</code></pre>"},{"location":"api/#cycquery.util.DBTable","title":"DBTable  <code>dataclass</code>","text":"<p>Database table wrapper.</p> <p>Parameters:</p> Name Type Description Default <code>name_</code> <code>str</code> <p>Name of table.</p> required <code>data_</code> <code>MetaData</code> <p>Metadata for schema.</p> required Source code in <code>cycquery/util.py</code> <pre><code>@dataclass\nclass DBTable:\n    \"\"\"Database table wrapper.\n\n    Parameters\n    ----------\n    name_: str\n        Name of table.\n    data_: sqlalchemy.sql.schema.Table\n        Metadata for schema.\n\n    \"\"\"\n\n    name_: str\n    data_: sqlalchemy.MetaData\n</code></pre>"},{"location":"api/#cycquery.util.get_attr_name","title":"get_attr_name","text":"<pre><code>get_attr_name(name)\n</code></pre> <p>Get attribute name (second part of first.second).</p> Source code in <code>cycquery/util.py</code> <pre><code>def get_attr_name(name: str) -&gt; str:\n    \"\"\"Get attribute name (second part of first.second).\"\"\"\n    return name.rsplit(\".\", maxsplit=1)[-1]\n</code></pre>"},{"location":"api/#cycquery.util.param_types_to_type","title":"param_types_to_type","text":"<pre><code>param_types_to_type(relevant_types, to_type_fn)\n</code></pre> <p>Convert TableTypes parameters to a specified type.</p> <p>A decorator which processes a function's arguments by taking all parameters with type in relevant_types and converting them using some to_type_fn function. Non-relevant types are left alone.</p> <p>Parameters:</p> Name Type Description Default <code>relevant_types</code> <code>list</code> <p>Types to process.</p> required <code>to_type_fn</code> <code>Callable</code> <p>Function to process the relevant types</p> required <p>Returns:</p> Type Description <code>Callable</code> <p>The processed function.</p> Source code in <code>cycquery/util.py</code> <pre><code>def param_types_to_type(\n    relevant_types: list[Any],\n    to_type_fn: Callable[..., Any],\n) -&gt; Callable[..., Any]:\n    \"\"\"Convert TableTypes parameters to a specified type.\n\n    A decorator which processes a function's arguments by taking all\n    parameters with type in relevant_types and converting them using\n    some to_type_fn function. Non-relevant types are left alone.\n\n    Parameters\n    ----------\n    relevant_types : list\n        Types to process.\n    to_type_fn : Callable\n        Function to process the relevant types\n\n    Returns\n    -------\n    Callable\n        The processed function.\n\n    \"\"\"\n\n    def decorator(func_: Callable[..., Any]) -&gt; Callable[..., Any]:\n        \"\"\"Decorate function to convert TableTypes parameters to a specified type.\"\"\"\n\n        @wraps(func_)\n        def wrapper_func(*args: Any, **kwargs: Any) -&gt; Any:\n            # Convert relevant arguments.\n            args_list = list(args)\n            for i, arg in enumerate(args_list):\n                if type(arg) in relevant_types:\n                    args_list[i] = to_type_fn(arg)\n\n            # Convert relevant keyword arguments.\n            kwargs = dict(kwargs)\n            for key, kwarg in kwargs.items():\n                if type(kwarg) in relevant_types:\n                    kwargs[key] = to_type_fn(kwarg)\n\n            return func_(*tuple(args_list), **kwargs)\n\n        return wrapper_func\n\n    return decorator\n</code></pre>"},{"location":"api/#cycquery.util.table_params_to_type","title":"table_params_to_type","text":"<pre><code>table_params_to_type(to_type)\n</code></pre> <p>Decorate to convert TableTypes params to a specified type.</p> <p>Parameters:</p> Name Type Description Default <code>to_type</code> <code>TableTypes</code> <p>The type to which to convert.</p> required <p>Returns:</p> Type Description <code>Callable</code> <p>The processed function.</p> Source code in <code>cycquery/util.py</code> <pre><code>def table_params_to_type(to_type: TableTypes) -&gt; Callable[..., Any]:\n    \"\"\"Decorate to convert TableTypes params to a specified type.\n\n    Parameters\n    ----------\n    to_type: cycquery.util.TableTypes\n        The type to which to convert.\n\n    Returns\n    -------\n    Callable\n        The processed function.\n\n    \"\"\"\n    # Dictionary mapping query type -&gt; query type conversion function.\n    table_to_type_fn_map = {\n        Subquery: _to_subquery,\n        Select: _to_select,\n        Table: lambda x: x,\n        DBTable: lambda x: x,\n    }\n    if to_type not in TABLE_OBJECTS:  # type: ignore[comparison-overlap]\n        raise ValueError(f\"to_type must be in {TABLE_OBJECTS}\")\n\n    to_type_fn = table_to_type_fn_map[to_type]  # type: ignore[index]\n\n    return param_types_to_type(TABLE_OBJECTS, to_type_fn)\n</code></pre>"},{"location":"api/#cycquery.util.get_column","title":"get_column","text":"<pre><code>get_column(table, col)\n</code></pre> <p>Extract a column object from a table by name.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>The table with the column.</p> required <code>col</code> <code>str</code> <p>Name of column to extract.</p> required <p>Returns:</p> Type Description <code>Column</code> <p>The corresponding column in the table.</p> Source code in <code>cycquery/util.py</code> <pre><code>@table_params_to_type(Subquery)  # type: ignore[arg-type]\ndef get_column(\n    table: TableTypes,\n    col: str,\n) -&gt; Column:\n    \"\"\"Extract a column object from a table by name.\n\n    Parameters\n    ----------\n    table: cycquery.util.TableTypes\n        The table with the column.\n    col: str\n        Name of column to extract.\n\n    Returns\n    -------\n    sqlalchemy.sql.schema.Column\n        The corresponding column in the table.\n\n    \"\"\"\n    col_names = get_column_names(table)\n    if col not in col_names:\n        raise ValueError(f\"Table does not contain column {col}\")\n\n    return table.c[col_names.index(col)]  # type: ignore\n</code></pre>"},{"location":"api/#cycquery.util.filter_columns","title":"filter_columns","text":"<pre><code>filter_columns(table, cols)\n</code></pre> <p>Filter a table, keeping only the specified columns.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>The table with the column.</p> required <code>cols</code> <code>str | list[str]</code> <p>Name of columns to keep.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Table with only the specified columns.</p> Source code in <code>cycquery/util.py</code> <pre><code>@table_params_to_type(Subquery)  # type: ignore[arg-type]\ndef filter_columns(\n    table: TableTypes,\n    cols: str | list[str],\n) -&gt; Subquery:\n    \"\"\"Filter a table, keeping only the specified columns.\n\n    Parameters\n    ----------\n    table: cycquery.util.TableTypes\n        The table with the column.\n    cols: str or list of str\n        Name of columns to keep.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Table with only the specified columns.\n\n    \"\"\"\n    cols = to_list(cols)\n    col_names = get_column_names(table)\n    filtered = []\n    for col in cols:\n        if col not in col_names:\n            continue\n        filtered.append(table.c[col_names.index(col)])  # type: ignore\n\n    return select(*filtered).subquery()\n</code></pre>"},{"location":"api/#cycquery.util.get_columns","title":"get_columns","text":"<pre><code>get_columns(table, cols)\n</code></pre> <p>Extract a number of columns from the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>The table.</p> required <code>cols</code> <code>str | list[str]</code> <p>Names of columns to extract.</p> required <p>Returns:</p> Type Description <code>list of sqlalchemy.sql.schema.Column</code> <p>The corresponding columns in the table.</p> Source code in <code>cycquery/util.py</code> <pre><code>@table_params_to_type(Subquery)  # type: ignore[arg-type]\ndef get_columns(\n    table: TableTypes,\n    cols: str | list[str],\n) -&gt; list[Column]:\n    \"\"\"Extract a number of columns from the table.\n\n    Parameters\n    ----------\n    table: cycquery.util.TableTypes\n        The table.\n    cols: str or list of str\n        Names of columns to extract.\n\n    Returns\n    -------\n    list of sqlalchemy.sql.schema.Column\n        The corresponding columns in the table.\n\n    \"\"\"\n    return [get_column(table, col) for col in to_list(cols)]\n</code></pre>"},{"location":"api/#cycquery.util.get_column_names","title":"get_column_names","text":"<pre><code>get_column_names(table)\n</code></pre> <p>Extract column names from a table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>The table.</p> required <p>Returns:</p> Type Description <code>list of str</code> <p>The table column names.</p> Source code in <code>cycquery/util.py</code> <pre><code>@table_params_to_type(Subquery)  # type: ignore[arg-type]\ndef get_column_names(table: TableTypes) -&gt; list[str]:\n    \"\"\"Extract column names from a table.\n\n    Parameters\n    ----------\n    table: cycquery.util.TableTypes\n        The table.\n\n    Returns\n    -------\n    list of str\n        The table column names.\n\n    \"\"\"\n    return [c.name for c in table.columns]  # type: ignore\n</code></pre>"},{"location":"api/#cycquery.util.has_columns","title":"has_columns","text":"<pre><code>has_columns(table, cols, raise_error=False)\n</code></pre> <p>Check whether a table has all of the specified columns.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table to check.</p> required <code>cols</code> <code>str | list[str]</code> <p>Required columns.</p> required <code>raise_error</code> <code>bool</code> <p>Whether to raise an error if the required columns are not found.</p> <code>False</code> <p>Returns:</p> Type Description <code>bool</code> <p>True if all required columns are present, otherwise False.</p> Source code in <code>cycquery/util.py</code> <pre><code>@table_params_to_type(Subquery)  # type: ignore[arg-type]\ndef has_columns(\n    table: TableTypes,\n    cols: str | list[str],\n    raise_error: bool = False,\n) -&gt; bool:\n    \"\"\"Check whether a table has all of the specified columns.\n\n    Parameters\n    ----------\n    table : cycquery.util.TableTypes\n        Table to check.\n    cols: str or list of str\n        Required columns.\n    raise_error: bool\n        Whether to raise an error if the required columns are not found.\n\n    Returns\n    -------\n    bool\n        True if all required columns are present, otherwise False.\n\n    \"\"\"\n    cols = to_list(cols)\n    required_set = set(cols)\n    columns = set(get_column_names(table))\n    present = required_set.issubset(columns)\n\n    if raise_error and not present:\n        missing = required_set - columns\n        raise ValueError(f\"Missing required columns {', '.join(missing)}.\")\n\n    return present\n</code></pre>"},{"location":"api/#cycquery.util.assert_table_has_columns","title":"assert_table_has_columns","text":"<pre><code>assert_table_has_columns(*args, **kwargs)\n</code></pre> <p>Assert that TableTypes params have the necessary columns.</p> <p>assert_table_has_columns([\"A\", \"B\"], None) is equivalent to assert_table_has_columns([\"A\", \"B\"]) but may be necessary when wanting to check, assert_table_has_columns([\"A\"], None, [\"C\"])</p> <p>Can also check keyword arguments, e.g., optional queries, assert_table_has_columns([\"A\"], kwarg_table=[\"D\"])</p> <p>Parameters:</p> Name Type Description Default <code>*args</code> <code>Any</code> <p>Ordered arguments corresponding to the function's table-type args.</p> <code>()</code> <code>**kwargs</code> <code>Any</code> <p>Keyword arguments corresponding to the function's table-type kwargs.</p> <code>{}</code> <p>Returns:</p> Type Description <code>Callable</code> <p>Decorator function.</p> Source code in <code>cycquery/util.py</code> <pre><code>@table_params_to_type(Subquery)  # type: ignore[arg-type]\ndef assert_table_has_columns(\n    *args: Any,\n    **kwargs: Any,\n) -&gt; Callable[[TableTypes], TableTypes]:\n    \"\"\"Assert that TableTypes params have the necessary columns.\n\n    assert_table_has_columns([\"A\", \"B\"], None) is equivalent to\n    assert_table_has_columns([\"A\", \"B\"]) but may be necessary when\n    wanting to check, assert_table_has_columns([\"A\"], None, [\"C\"])\n\n    Can also check keyword arguments, e.g., optional queries,\n    assert_table_has_columns([\"A\"], kwarg_table=[\"D\"])\n\n    Parameters\n    ----------\n    *args\n        Ordered arguments corresponding to the function's table-type args.\n    **kwargs\n        Keyword arguments corresponding to the function's table-type kwargs.\n\n    Returns\n    -------\n    Callable\n        Decorator function.\n\n    \"\"\"\n\n    def decorator(\n        func_: Callable[..., Any],\n    ) -&gt; Callable[..., Any]:\n        @wraps(func_)\n        def wrapper_func(*fn_args: Any, **fn_kwargs: Any) -&gt; Any:\n            # Check only the table arguments\n            table_args = [i for i in fn_args if isinstance(i, Subquery)]\n\n            assert len(args) &lt;= len(table_args)\n\n            for i, arg in enumerate(args):\n                if arg is None:  # Can specify None to skip over checking a query\n                    continue\n                has_columns(table_args[i], arg, raise_error=True)\n\n            for key, required_cols in kwargs.items():\n                # If an optional table is not provided, or is None,\n                # it is skipped\n                if key not in fn_kwargs:\n                    continue\n\n                if fn_kwargs[key] is None:\n                    continue\n\n                assert isinstance(fn_kwargs[key], Subquery)\n                has_columns(fn_kwargs[key], required_cols, raise_error=True)\n\n            return func_(*fn_args, **fn_kwargs)\n\n        return wrapper_func\n\n    return decorator  # type: ignore\n</code></pre>"},{"location":"api/#cycquery.util.drop_columns","title":"drop_columns","text":"<pre><code>drop_columns(table, drop_cols)\n</code></pre> <p>Drop, or remove, some columns from a table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>The table.</p> required <code>drop_cols</code> <code>str | list[str]</code> <p>Names of columns to drop.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>The corresponding table with columns dropped.</p> Source code in <code>cycquery/util.py</code> <pre><code>@table_params_to_type(Subquery)  # type: ignore[arg-type]\ndef drop_columns(\n    table: TableTypes,\n    drop_cols: str | list[str],\n) -&gt; Subquery:\n    \"\"\"Drop, or remove, some columns from a table.\n\n    Parameters\n    ----------\n    table: cycquery.util.TableTypes\n        The table.\n    drop_cols: str or list of str\n        Names of columns to drop.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        The corresponding table with columns dropped.\n\n    \"\"\"\n    drop_cols = get_columns(table, drop_cols)\n\n    return select(*[c for c in table.c if c not in drop_cols]).subquery()  # type: ignore\n</code></pre>"},{"location":"api/#cycquery.util.rename_columns","title":"rename_columns","text":"<pre><code>rename_columns(table, rename_map)\n</code></pre> <p>Rename a table's columns.</p> <p>Rename the table's columns according to a dictionary of strings, where the key is the current name, and the value is the replacement.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>The table.</p> required <code>rename_map</code> <code>dict</code> <p>Dictionary mapping current column names (key) to new ones (value).</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>The corresponding table with columns renamed.</p> Source code in <code>cycquery/util.py</code> <pre><code>@table_params_to_type(Subquery)  # type: ignore[arg-type]\ndef rename_columns(table: TableTypes, rename_map: dict[str, str]) -&gt; Subquery:\n    \"\"\"Rename a table's columns.\n\n    Rename the table's columns according to a dictionary of strings,\n    where the key is the current name, and the value is the replacement.\n\n    Parameters\n    ----------\n    table: cycquery.util.TableTypes\n        The table.\n    rename_map : dict\n        Dictionary mapping current column names (key) to new ones (value).\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        The corresponding table with columns renamed.\n\n    \"\"\"\n    return select(\n        *[\n            c.label(rename_map[c.name]) if c.name in rename_map else c\n            for c in table.columns  # type: ignore\n        ],\n    ).subquery()\n</code></pre>"},{"location":"api/#cycquery.util.reorder_columns","title":"reorder_columns","text":"<pre><code>reorder_columns(table, cols)\n</code></pre> <p>Reorder a table's columns.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>The table to reorder.</p> required <code>cols</code> <code>list of str</code> <p>New order of columns, which must include all existing columns.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>The reordered table.</p> Source code in <code>cycquery/util.py</code> <pre><code>@table_params_to_type(Subquery)  # type: ignore[arg-type]\ndef reorder_columns(table: TableTypes, cols: list[str]) -&gt; Subquery:\n    \"\"\"Reorder a table's columns.\n\n    Parameters\n    ----------\n    table: cycquery.util.TableTypes\n        The table to reorder.\n    cols : list of str\n        New order of columns, which must include all existing columns.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        The reordered table.\n\n    \"\"\"\n    # Get the old/new column names.\n    old_order = get_column_names(table)\n    new_order = [c.name for c in get_columns(table, cols)]\n\n    # Make sure we have exactly the same set of old/new column names.\n    if set(old_order) != set(new_order):\n        old_order_print = \", \".join(old_order)\n        new_order_print = \", \".join(new_order)\n        raise ValueError(\n            f\"\"\"Must specify all columns {old_order_print}\n            to re-order, not {new_order_print}.\"\"\",\n        )\n\n    # Reorder the columns.\n    new_cols = []\n    for col in new_order:\n        new_cols.append(table.c[old_order.index(col)])  # type: ignore\n\n    return select(*new_cols).subquery()\n</code></pre>"},{"location":"api/#cycquery.util.apply_to_columns","title":"apply_to_columns","text":"<pre><code>apply_to_columns(\n    table, col_names, funcs, new_col_labels=None\n)\n</code></pre> <p>Apply a function to some columns.</p> <p>This function can change existing columns or create new columns depending on whether new_col_labels is specified.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>The table.</p> required <code>col_names</code> <code>str | list[str]</code> <p>Columns to which to apply the function.</p> required <code>funcs</code> <code>Callable[[Column], Column] | list[Callable[[Column], Column]]</code> <p>Function(s) to apply to the columns, where the function takes an column as its only parameter and returns another column object.</p> required <code>new_col_labels</code> <code>str | list[str] | None</code> <p>If specified, create new columns with these labels. Otherwise, apply the function to the existing columns.</p> <code>None</code> <p>Returns:</p> Type Description <code>Subquery</code> <p>The table with function applied.</p> Source code in <code>cycquery/util.py</code> <pre><code>@table_params_to_type(Subquery)  # type: ignore[arg-type]\ndef apply_to_columns(\n    table: TableTypes,\n    col_names: str | list[str],\n    funcs: Callable[[sqlalchemy.sql.schema.Column], sqlalchemy.sql.schema.Column]\n    | list[Callable[[sqlalchemy.sql.schema.Column], sqlalchemy.sql.schema.Column]],\n    new_col_labels: str | list[str] | None = None,\n) -&gt; Subquery:\n    \"\"\"Apply a function to some columns.\n\n    This function can change existing columns or create new\n    columns depending on whether new_col_labels is specified.\n\n    Parameters\n    ----------\n    table: cycquery.util.TableTypes\n        The table.\n    col_names: str or list of str\n        Columns to which to apply the function.\n    funcs: callable or list of callable\n        Function(s) to apply to the columns, where the function takes an column\n        as its only parameter and returns another column object.\n    new_col_labels: str or list of str, optional\n        If specified, create new columns with these labels. Otherwise,\n        apply the function to the existing columns.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        The table with function applied.\n\n    \"\"\"\n    col_names = to_list(col_names)\n    new_col_labels = to_list_optional(new_col_labels)\n    cols = get_columns(table, col_names)\n    if isinstance(funcs, list):\n        if len(funcs) != len(cols):\n            raise ValueError(\n                f\"Must specify a function for each column, not {len(funcs)} functions.\",\n            )\n    else:\n        funcs = [funcs] * len(cols)\n    if new_col_labels is None:\n        # Apply to existing columns\n        prev_order = get_column_names(table)\n        table = select(table).add_columns(  # type: ignore[arg-type]\n            *[\n                funcs[i](col).label(\"__\" + col_names[i] + \"__\")\n                for i, col in enumerate(cols)\n            ],\n        )\n        rename = {\"__\" + name + \"__\": name for name in col_names}\n        table = drop_columns(table, col_names)\n        table = rename_columns(table, rename)\n        table = reorder_columns(table, prev_order)\n    else:\n        # Apply to new columns\n        new_cols = [\n            funcs[i](col).label(new_col_labels[i]) for i, col in enumerate(cols)\n        ]\n        table = select(table).add_columns(*new_cols)  # type: ignore[arg-type]\n\n    return _to_subquery(table)\n</code></pre>"},{"location":"api/#cycquery.util.trim_columns","title":"trim_columns","text":"<pre><code>trim_columns(table, cols, new_col_labels=None)\n</code></pre> <p>Trim, or strip, specified columns.</p> <p>Trimming refers to the removal of leading/trailing whitespace.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>The table.</p> required <code>cols</code> <code>str | list[str]</code> <p>Names of columns to trim.</p> required <code>new_col_labels</code> <code>str | list[str] | None</code> <p>If specified, create new columns with these labels. Otherwise, apply the function to the existing columns.</p> <code>None</code> <p>Returns:</p> Type Description <code>Subquery</code> <p>The table with the specified columns trimmed.</p> Source code in <code>cycquery/util.py</code> <pre><code>def trim_columns(\n    table: TableTypes,\n    cols: str | list[str],\n    new_col_labels: str | list[str] | None = None,\n) -&gt; Subquery:\n    \"\"\"Trim, or strip, specified columns.\n\n    Trimming refers to the removal of leading/trailing whitespace.\n\n    Parameters\n    ----------\n    table: cycquery.util.TableTypes\n        The table.\n    cols: str or list of str\n        Names of columns to trim.\n    new_col_labels: str or list of str, optional\n        If specified, create new columns with these labels. Otherwise,\n        apply the function to the existing columns.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        The table with the specified columns trimmed.\n\n    \"\"\"\n    return apply_to_columns(\n        table,\n        cols,\n        lambda x: process_column(x, to_str=True, trim=True),\n        new_col_labels=new_col_labels,\n    )\n</code></pre>"},{"location":"api/#cycquery.util.process_elem","title":"process_elem","text":"<pre><code>process_elem(elem, **kwargs)\n</code></pre> <p>Preprocess some basic object such as an integer, float, or string.</p> <p>Parameters:</p> Name Type Description Default <code>elem</code> <code>Any</code> <p>An element such as an integer, float, or string.</p> required <code>**kwargs</code> <code>dict</code> <p>Preprocessing keyword arguments.</p> <code>{}</code> <p>Returns:</p> Type Description <code>Any</code> <p>The preprocessed element.</p> Source code in <code>cycquery/util.py</code> <pre><code>def process_elem(elem: Any, **kwargs: bool) -&gt; Any:\n    \"\"\"Preprocess some basic object such as an integer, float, or string.\n\n    Parameters\n    ----------\n    elem: any\n        An element such as an integer, float, or string.\n    **kwargs : dict, optional\n        Preprocessing keyword arguments.\n\n    Returns\n    -------\n    Any\n        The preprocessed element.\n\n    \"\"\"\n    # Extract kwargs.\n    lower = kwargs.get(\"lower\", False)\n    trim = kwargs.get(\"trim\", False)\n    to_str = kwargs.get(\"to_str\", False)\n    to_int = kwargs.get(\"to_int\", False)\n    to_float = kwargs.get(\"to_float\", False)\n    to_bool = kwargs.get(\"to_bool\", False)\n\n    # Convert to string.\n    if to_str:\n        elem = str(elem)\n\n    # If a string.\n    if isinstance(elem, str):\n        if lower:\n            elem = elem.lower()\n\n        if trim:\n            elem = elem.strip()\n\n    if to_int:\n        elem = int(elem)\n\n    if to_float:\n        elem = float(elem)\n\n    if to_bool:\n        elem = bool(elem)\n\n    return elem\n</code></pre>"},{"location":"api/#cycquery.util.process_list","title":"process_list","text":"<pre><code>process_list(lst, **kwargs)\n</code></pre> <p>Preprocess a list of elements.</p> <p>Parameters:</p> Name Type Description Default <code>lst</code> <code>any or list of any</code> <p>A list of elements such as integers, floats, or strings.</p> required <code>**kwargs</code> <code>dict</code> <p>Preprocessing keyword arguments.</p> <code>{}</code> <p>Returns:</p> Type Description <code>Any</code> <p>The preprocessed element.</p> Source code in <code>cycquery/util.py</code> <pre><code>def process_list(lst: Any | list[Any], **kwargs: bool) -&gt; list[Any]:\n    \"\"\"Preprocess a list of elements.\n\n    Parameters\n    ----------\n    lst : any or list of any\n        A list of elements such as integers, floats, or strings.\n    **kwargs : dict, optional\n        Preprocessing keyword arguments.\n\n    Returns\n    -------\n    Any\n        The preprocessed element.\n\n    \"\"\"\n    # Convert potentially non-list variable to list.\n    lst = to_list(lst)\n\n    # Process elements.\n    return [process_elem(i, **kwargs) for i in lst]\n</code></pre>"},{"location":"api/#cycquery.util.process_column","title":"process_column","text":"<pre><code>process_column(col, **kwargs)\n</code></pre> <p>Preprocess a Column object.</p> <p>Parameters:</p> Name Type Description Default <code>col</code> <code>Column</code> <p>A column to preprocess.</p> required <code>**kwargs</code> <code>dict</code> <p>Preprocessing keyword arguments.</p> <code>{}</code> <p>Returns:</p> Type Description <code>Column</code> <p>The processed column.</p> Source code in <code>cycquery/util.py</code> <pre><code>def process_column(col: Column, **kwargs: bool) -&gt; Column:\n    \"\"\"Preprocess a Column object.\n\n    Parameters\n    ----------\n    col : sqlalchemy.sql.schema.Column\n        A column to preprocess.\n    **kwargs : dict, optional\n        Preprocessing keyword arguments.\n\n    Returns\n    -------\n    sqlalchemy.sql.schema.Column\n        The processed column.\n\n    \"\"\"\n    # Extract kwargs.\n    lower = kwargs.get(\"lower\", False)\n    trim = kwargs.get(\"trim\", False)\n    to_str = kwargs.get(\"to_str\", False)\n    to_int = kwargs.get(\"to_int\", False)\n    to_float = kwargs.get(\"to_float\", False)\n    to_bool = kwargs.get(\"to_bool\", False)\n    to_date = kwargs.get(\"to_date\", False)\n    to_timestamp = kwargs.get(\"to_timestamp\", False)\n\n    # Convert to string.\n    if to_str:\n        col = cast(col, String)  # type: ignore[assignment]\n\n    # If a string column (covers VARCHAR, TEXT, and other String subtypes).\n    if isinstance(col.type, String) or \"VARCHAR\" in str(col.type).upper():\n        # Lower column.\n        if lower:\n            col = func.lower(col)  # type: ignore[assignment]\n\n        # Trim whitespace.\n        if trim:\n            col = func.trim(col)  # type: ignore[assignment]\n\n    if to_int:\n        col = cast(col, Integer)  # type: ignore[assignment]\n\n    if to_float:\n        col = cast(col, Float)  # type: ignore[assignment]\n\n    if to_bool:\n        col = cast(col, Boolean)  # type: ignore[assignment]\n\n    if to_date:\n        col = cast(col, Date)  # type: ignore[assignment]\n\n    if to_timestamp:\n        col = cast(col, DateTime)  # type: ignore[assignment]\n\n    return col\n</code></pre>"},{"location":"api/#cycquery.util.equals","title":"equals","text":"<pre><code>equals(col, value, lower=True, trim=True, **kwargs)\n</code></pre> <p>Condition that a column has some value.</p> <p>Assumes that if searching for a string, both the value and column values should be converted to lowercase and trimmed of leading/trailing whitespace.</p> <p>Parameters:</p> Name Type Description Default <code>col</code> <code>Column</code> <p>The column to condition.</p> required <code>value</code> <code>Any</code> <p>The value to match in the column.</p> required <code>lower</code> <code>bool</code> <p>Whether to convert the value and column to lowercase. This is only relevant when the column/value are strings.</p> <code>True</code> <code>trim</code> <code>bool</code> <p>Whether to trim (strip) whitespace on the value and column. This is only relevant when the column/value are strings.</p> <code>True</code> <code>**kwargs</code> <code>dict</code> <p>Remaining preprocessing keyword arguments.</p> <code>{}</code> <p>Returns:</p> Type Description <code>BinaryExpression</code> <p>An expression representing where the condition was satisfied.</p> Source code in <code>cycquery/util.py</code> <pre><code>def equals(\n    col: Column,\n    value: Any,\n    lower: bool = True,\n    trim: bool = True,\n    **kwargs: bool,\n) -&gt; BinaryExpression:\n    \"\"\"Condition that a column has some value.\n\n    Assumes that if searching for a string, both the value and column values\n    should be converted to lowercase and trimmed of leading/trailing whitespace.\n\n    Parameters\n    ----------\n    col : sqlalchemy.sql.schema.Column\n        The column to condition.\n    value : Any\n        The value to match in the column.\n    lower : bool, default=True\n        Whether to convert the value and column to lowercase.\n        This is only relevant when the column/value are strings.\n    trim : bool, default=True\n        Whether to trim (strip) whitespace on the value and column.\n        This is only relevant when the column/value are strings.\n    **kwargs : dict, optional\n        Remaining preprocessing keyword arguments.\n\n    Returns\n    -------\n    sqlalchemy.sql.elements.BinaryExpression\n        An expression representing where the condition was satisfied.\n\n    \"\"\"\n    return process_column(col, lower=lower, trim=trim, **kwargs) == process_elem(\n        value,\n        lower=lower,\n        trim=trim,\n        **kwargs,\n    )\n</code></pre>"},{"location":"api/#cycquery.util.greater_than","title":"greater_than","text":"<pre><code>greater_than(\n    col, value, lower=True, trim=True, equal=False, **kwargs\n)\n</code></pre> <p>Condition that a column is greater than some value.</p> <p>Assumes that if searching for a string, both the value and column values should be converted to lowercase and trimmed of leading/trailing whitespace.</p> <p>Parameters:</p> Name Type Description Default <code>col</code> <code>Column</code> <p>The column to condition.</p> required <code>value</code> <code>Any</code> <p>The value to match in the column.</p> required <code>lower</code> <code>bool</code> <p>Whether to convert the value and column to lowercase. This is only relevant when the column/value are strings.</p> <code>True</code> <code>trim</code> <code>bool</code> <p>Whether to trim (strip) whitespace on the value and column. This is only relevant when the column/value are strings.</p> <code>True</code> <code>equal</code> <code>bool</code> <p>Whether to also include equal to the value.</p> <code>False</code> <code>**kwargs</code> <code>dict</code> <p>Remaining preprocessing keyword arguments.</p> <code>{}</code> <p>Returns:</p> Type Description <code>BinaryExpression</code> <p>An expression representing where the condition was satisfied.</p> Source code in <code>cycquery/util.py</code> <pre><code>def greater_than(\n    col: Column,\n    value: Any,\n    lower: bool = True,\n    trim: bool = True,\n    equal: bool = False,\n    **kwargs: bool,\n) -&gt; BinaryExpression:\n    \"\"\"Condition that a column is greater than some value.\n\n    Assumes that if searching for a string, both the value and column values\n    should be converted to lowercase and trimmed of leading/trailing whitespace.\n\n    Parameters\n    ----------\n    col : sqlalchemy.sql.schema.Column\n        The column to condition.\n    value : Any\n        The value to match in the column.\n    lower : bool, default=True\n        Whether to convert the value and column to lowercase.\n        This is only relevant when the column/value are strings.\n    trim : bool, default=True\n        Whether to trim (strip) whitespace on the value and column.\n        This is only relevant when the column/value are strings.\n    equal: bool, default=False\n        Whether to also include equal to the value.\n    **kwargs : dict, optional\n        Remaining preprocessing keyword arguments.\n\n    Returns\n    -------\n    sqlalchemy.sql.elements.BinaryExpression\n        An expression representing where the condition was satisfied.\n\n    \"\"\"\n    if equal:\n        return process_column(col, lower=lower, trim=trim, **kwargs) &gt;= process_elem(\n            value,\n            lower=lower,\n            trim=trim,\n            **kwargs,\n        )\n    return process_column(col, lower=lower, trim=trim, **kwargs) &gt; process_elem(\n        value,\n        lower=lower,\n        trim=trim,\n        **kwargs,\n    )\n</code></pre>"},{"location":"api/#cycquery.util.less_than","title":"less_than","text":"<pre><code>less_than(\n    col, value, lower=True, trim=True, equal=False, **kwargs\n)\n</code></pre> <p>Condition that a column is less than some value.</p> <p>Assumes that if searching for a string, both the value and column values should be converted to lowercase and trimmed of leading/trailing whitespace.</p> <p>Parameters:</p> Name Type Description Default <code>col</code> <code>Column</code> <p>The column to condition.</p> required <code>value</code> <code>Any</code> <p>The value to match in the column.</p> required <code>lower</code> <code>bool</code> <p>Whether to convert the value and column to lowercase. This is only relevant when the column/value are strings.</p> <code>True</code> <code>trim</code> <code>bool</code> <p>Whether to trim (strip) whitespace on the value and column. This is only relevant when the column/value are strings.</p> <code>True</code> <code>equal</code> <code>bool</code> <p>Whether to also include equal to the value.</p> <code>False</code> <code>**kwargs</code> <code>dict</code> <p>Remaining preprocessing keyword arguments.</p> <code>{}</code> <p>Returns:</p> Type Description <code>BinaryExpression</code> <p>An expression representing where the condition was satisfied.</p> Source code in <code>cycquery/util.py</code> <pre><code>def less_than(\n    col: Column,\n    value: Any,\n    lower: bool = True,\n    trim: bool = True,\n    equal: bool = False,\n    **kwargs: bool,\n) -&gt; BinaryExpression:\n    \"\"\"Condition that a column is less than some value.\n\n    Assumes that if searching for a string, both the value and column values\n    should be converted to lowercase and trimmed of leading/trailing whitespace.\n\n    Parameters\n    ----------\n    col : sqlalchemy.sql.schema.Column\n        The column to condition.\n    value : Any\n        The value to match in the column.\n    lower : bool, default=True\n        Whether to convert the value and column to lowercase.\n        This is only relevant when the column/value are strings.\n    trim : bool, default=True\n        Whether to trim (strip) whitespace on the value and column.\n        This is only relevant when the column/value are strings.\n    equal: bool, default=False\n        Whether to also include equal to the value.\n    **kwargs : dict, optional\n        Remaining preprocessing keyword arguments.\n\n    Returns\n    -------\n    sqlalchemy.sql.elements.BinaryExpression\n        An expression representing where the condition was satisfied.\n\n    \"\"\"\n    if equal:\n        return process_column(col, lower=lower, trim=trim, **kwargs) &lt;= process_elem(\n            value,\n            lower=lower,\n            trim=trim,\n            **kwargs,\n        )\n    return process_column(col, lower=lower, trim=trim, **kwargs) &lt; process_elem(\n        value,\n        lower=lower,\n        trim=trim,\n        **kwargs,\n    )\n</code></pre>"},{"location":"api/#cycquery.util.not_equals","title":"not_equals","text":"<pre><code>not_equals(col, value, lower=True, trim=True, **kwargs)\n</code></pre> <p>Condition that a column is not equal to some value.</p> <p>Assumes that if searching for a string, both the value and column values should be converted to lowercase and trimmed of leading/trailing whitespace.</p> <p>Parameters:</p> Name Type Description Default <code>col</code> <code>Column</code> <p>The column to condition.</p> required <code>value</code> <code>Any</code> <p>The value to match in the column.</p> required <code>lower</code> <code>bool</code> <p>Whether to convert the value and column to lowercase. This is only relevant when the column/value are strings.</p> <code>True</code> <code>trim</code> <code>bool</code> <p>Whether to trim (strip) whitespace on the value and column. This is only relevant when the column/value are strings.</p> <code>True</code> <code>**kwargs</code> <code>dict</code> <p>Remaining preprocessing keyword arguments.</p> <code>{}</code> <p>Returns:</p> Type Description <code>BinaryExpression</code> <p>An expression representing where the condition was satisfied.</p> Source code in <code>cycquery/util.py</code> <pre><code>def not_equals(\n    col: Column,\n    value: Any,\n    lower: bool = True,\n    trim: bool = True,\n    **kwargs: bool,\n) -&gt; BinaryExpression:\n    \"\"\"Condition that a column is not equal to some value.\n\n    Assumes that if searching for a string, both the value and column values\n    should be converted to lowercase and trimmed of leading/trailing whitespace.\n\n    Parameters\n    ----------\n    col : sqlalchemy.sql.schema.Column\n        The column to condition.\n    value : Any\n        The value to match in the column.\n    lower : bool, default=True\n        Whether to convert the value and column to lowercase.\n        This is only relevant when the column/value are strings.\n    trim : bool, default=True\n        Whether to trim (strip) whitespace on the value and column.\n        This is only relevant when the column/value are strings.\n    **kwargs : dict, optional\n        Remaining preprocessing keyword arguments.\n\n    Returns\n    -------\n    sqlalchemy.sql.elements.BinaryExpression\n        An expression representing where the condition was satisfied.\n\n    \"\"\"\n    return process_column(col, lower=lower, trim=trim, **kwargs) != process_elem(\n        value,\n        lower=lower,\n        trim=trim,\n        **kwargs,\n    )\n</code></pre>"},{"location":"api/#cycquery.util.has_string_format","title":"has_string_format","text":"<pre><code>has_string_format(col, value, fmt, to_str=True, **kwargs)\n</code></pre> <p>Condition that a column has some string formatting.</p> <p>Assumes that we're searching for a string, performing the relevant conversion.</p> <p>Parameters:</p> Name Type Description Default <code>col</code> <code>Column</code> <p>The column to condition.</p> required <code>value</code> <code>Any</code> <p>A value to be implanted in the string formatting.</p> required <code>fmt</code> <code>str</code> <p>The string format to match in the column.</p> required <code>to_str</code> <code>bool</code> <p>Whether to convert the value/column to string type.</p> <code>True</code> <code>**kwargs</code> <code>dict</code> <p>Remaining preprocessing keyword arguments.</p> <code>{}</code> <p>Returns:</p> Type Description <code>BinaryExpression</code> <p>An expression representing where the condition was satisfied.</p> Source code in <code>cycquery/util.py</code> <pre><code>def has_string_format(\n    col: Column,\n    value: Any,\n    fmt: str,\n    to_str: bool = True,\n    **kwargs: bool,\n) -&gt; BinaryExpression:\n    \"\"\"Condition that a column has some string formatting.\n\n    Assumes that we're searching for a string, performing\n    the relevant conversion.\n\n    Parameters\n    ----------\n    col : sqlalchemy.sql.schema.Column\n        The column to condition.\n    value: Any\n        A value to be implanted in the string formatting.\n    fmt : str\n        The string format to match in the column.\n    to_str : bool, default=True\n        Whether to convert the value/column to string type.\n    **kwargs : dict, optional\n        Remaining preprocessing keyword arguments.\n\n    Returns\n    -------\n    sqlalchemy.sql.elements.BinaryExpression\n        An expression representing where the condition was satisfied.\n\n    \"\"\"\n    return process_column(col, to_str=to_str, **kwargs).like(\n        fmt.format(process_elem(value, to_str=to_str, **kwargs)),\n    )\n</code></pre>"},{"location":"api/#cycquery.util.has_substring","title":"has_substring","text":"<pre><code>has_substring(col, substring, lower=True, **kwargs)\n</code></pre> <p>Condition that a column has some substring.</p> <p>Assumes that we're searching for a string, where both the value and column values should be converted to strings and made lowercase.</p> <p>Parameters:</p> Name Type Description Default <code>col</code> <code>Column</code> <p>The column to condition.</p> required <code>substring</code> <code>Any</code> <p>The substring to match in the column.</p> required <code>lower</code> <code>bool</code> <p>Whether to convert the value and column to lowercase. This is only relevant when the column/value are strings.</p> <code>True</code> <code>**kwargs</code> <code>dict</code> <p>Remaining preprocessing keyword arguments.</p> <code>{}</code> <p>Returns:</p> Type Description <code>BinaryExpression</code> <p>An expression representing where the condition was satisfied.</p> Source code in <code>cycquery/util.py</code> <pre><code>def has_substring(\n    col: Column,\n    substring: Any,\n    lower: bool = True,\n    **kwargs: bool,\n) -&gt; BinaryExpression:\n    \"\"\"Condition that a column has some substring.\n\n    Assumes that we're searching for a string, where both the value and\n    column values should be converted to strings and made lowercase.\n\n    Parameters\n    ----------\n    col : sqlalchemy.sql.schema.Column\n        The column to condition.\n    substring : Any\n        The substring to match in the column.\n    lower : bool, default=True\n        Whether to convert the value and column to lowercase.\n        This is only relevant when the column/value are strings.\n    **kwargs : dict, optional\n        Remaining preprocessing keyword arguments.\n\n    Returns\n    -------\n    sqlalchemy.sql.elements.BinaryExpression\n        An expression representing where the condition was satisfied.\n\n    \"\"\"\n    return has_string_format(col, substring, \"%%{}%%\", lower=lower, **kwargs)\n</code></pre>"},{"location":"api/#cycquery.util.starts_with","title":"starts_with","text":"<pre><code>starts_with(col, value, lower=True, trim=True, **kwargs)\n</code></pre> <p>Condition that a column starts with some value/string.</p> <p>Assumes that we're searching for a string, where both the value and column values should be converted to strings, made lowercase, and trimmed of leading/trailing whitespace.</p> <p>Parameters:</p> Name Type Description Default <code>col</code> <code>Column</code> <p>The column to condition.</p> required <code>value</code> <code>Any</code> <p>The value to match at the start.</p> required <code>lower</code> <code>bool</code> <p>Whether to convert the value and column to lowercase. This is only relevant when the column/value are strings.</p> <code>True</code> <code>trim</code> <code>bool</code> <p>Whether to trim (strip) whitespace on the value and column. This is only relevant when the column/value are strings.</p> <code>True</code> <code>**kwargs</code> <code>dict</code> <p>Remaining preprocessing keyword arguments.</p> <code>{}</code> <p>Returns:</p> Type Description <code>BinaryExpression</code> <p>An expression representing where the condition was satisfied.</p> Source code in <code>cycquery/util.py</code> <pre><code>def starts_with(\n    col: Column,\n    value: Any,\n    lower: bool = True,\n    trim: bool = True,\n    **kwargs: bool,\n) -&gt; BinaryExpression:\n    \"\"\"Condition that a column starts with some value/string.\n\n    Assumes that we're searching for a string, where both the value and\n    column values should be converted to strings, made lowercase, and\n    trimmed of leading/trailing whitespace.\n\n    Parameters\n    ----------\n    col : sqlalchemy.sql.schema.Column\n        The column to condition.\n    value : Any\n        The value to match at the start.\n    lower : bool, default=True\n        Whether to convert the value and column to lowercase.\n        This is only relevant when the column/value are strings.\n    trim : bool, default=True\n        Whether to trim (strip) whitespace on the value and column.\n        This is only relevant when the column/value are strings.\n    **kwargs : dict, optional\n        Remaining preprocessing keyword arguments.\n\n    Returns\n    -------\n    sqlalchemy.sql.elements.BinaryExpression\n        An expression representing where the condition was satisfied.\n\n    \"\"\"\n    return has_string_format(col, value, \"{}%%\", lower=lower, trim=trim, **kwargs)\n</code></pre>"},{"location":"api/#cycquery.util.ends_with","title":"ends_with","text":"<pre><code>ends_with(col, value, lower=True, trim=True, **kwargs)\n</code></pre> <p>Condition that a column ends with some value/string.</p> <p>Assumes that we're searching for a string, where both the value and column values should be converted to strings, made lowercase, and trimmed of leading/trailing whitespace.</p> <p>Parameters:</p> Name Type Description Default <code>col</code> <code>Column</code> <p>The column to condition.</p> required <code>value</code> <code>Any</code> <p>The value to match at the end.</p> required <code>lower</code> <code>bool</code> <p>Whether to convert the value and column to lowercase. This is only relevant when the column/value are strings.</p> <code>True</code> <code>trim</code> <code>bool</code> <p>Whether to trim (strip) whitespace on the value and column. This is only relevant when the column/value are strings.</p> <code>True</code> <code>**kwargs</code> <code>dict</code> <p>Remaining preprocessing keyword arguments.</p> <code>{}</code> <p>Returns:</p> Type Description <code>BinaryExpression</code> <p>An expression representing where the condition was satisfied.</p> Source code in <code>cycquery/util.py</code> <pre><code>def ends_with(\n    col: Column,\n    value: Any,\n    lower: bool = True,\n    trim: bool = True,\n    **kwargs: bool,\n) -&gt; BinaryExpression:\n    \"\"\"Condition that a column ends with some value/string.\n\n    Assumes that we're searching for a string, where both the value and\n    column values should be converted to strings, made lowercase, and\n    trimmed of leading/trailing whitespace.\n\n    Parameters\n    ----------\n    col : sqlalchemy.sql.schema.Column\n        The column to condition.\n    value : Any\n        The value to match at the end.\n    lower : bool, default=True\n        Whether to convert the value and column to lowercase.\n        This is only relevant when the column/value are strings.\n    trim : bool, default=True\n        Whether to trim (strip) whitespace on the value and column.\n        This is only relevant when the column/value are strings.\n    **kwargs : dict, optional\n        Remaining preprocessing keyword arguments.\n\n    Returns\n    -------\n    sqlalchemy.sql.elements.BinaryExpression\n        An expression representing where the condition was satisfied.\n\n    \"\"\"\n    return has_string_format(col, value, \"%%{}\", lower=lower, trim=trim, **kwargs)\n</code></pre>"},{"location":"api/#cycquery.util.in_","title":"in_","text":"<pre><code>in_(col, lst, lower=True, trim=True, **kwargs)\n</code></pre> <p>Condition that a column value is in a list of values.</p> <p>Assumes that if searching for a string, both the value and column values should be converted to lowercase and trimmed of leading/trailing whitespace.</p> <p>Parameters:</p> Name Type Description Default <code>col</code> <code>Column</code> <p>The column to condition.</p> required <code>lst</code> <code>list of any</code> <p>The value to match at the start.</p> required <code>lower</code> <code>bool</code> <p>Whether to convert the value and column to lowercase. This is only relevant when the column/value are strings.</p> <code>True</code> <code>trim</code> <code>bool</code> <p>Whether to trim (strip) whitespace on the value and column. This is only relevant when the column/value are strings.</p> <code>True</code> <code>**kwargs</code> <code>dict</code> <p>Remaining preprocessing keyword arguments.</p> <code>{}</code> <p>Returns:</p> Type Description <code>BinaryExpression</code> <p>An expression representing where the condition was satisfied.</p> Source code in <code>cycquery/util.py</code> <pre><code>def in_(\n    col: Column,\n    lst: list[Any],\n    lower: bool = True,\n    trim: bool = True,\n    **kwargs: bool,\n) -&gt; BinaryExpression:\n    \"\"\"Condition that a column value is in a list of values.\n\n    Assumes that if searching for a string, both the value and column values\n    should be converted to lowercase and trimmed of leading/trailing whitespace.\n\n    Parameters\n    ----------\n    col : sqlalchemy.sql.schema.Column\n        The column to condition.\n    lst : list of any\n        The value to match at the start.\n    lower : bool, default=True\n        Whether to convert the value and column to lowercase.\n        This is only relevant when the column/value are strings.\n    trim : bool, default=True\n        Whether to trim (strip) whitespace on the value and column.\n        This is only relevant when the column/value are strings.\n    **kwargs : dict, optional\n        Remaining preprocessing keyword arguments.\n\n    Returns\n    -------\n    sqlalchemy.sql.elements.BinaryExpression\n        An expression representing where the condition was satisfied.\n\n    \"\"\"\n    return process_column(col, lower=lower, trim=trim, **kwargs).in_(\n        process_list(lst, lower=lower, trim=trim, **kwargs),\n    )\n</code></pre>"},{"location":"api/#cycquery.util.check_timestamp_columns","title":"check_timestamp_columns","text":"<pre><code>check_timestamp_columns(table, cols, raise_error=False)\n</code></pre> <p>Check whether some columns are Date or DateTime columns.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>The table.</p> required <code>cols</code> <code>str | list[str]</code> <p>Column names to check.</p> required <code>raise_error</code> <code>bool</code> <p>Whether to raise an error if one of the columns are none of the types.</p> <code>False</code> <p>Returns:</p> Type Description <code>bool</code> <p>Whether all of the columns are one of the types.</p> Source code in <code>cycquery/util.py</code> <pre><code>def check_timestamp_columns(\n    table: TableTypes,\n    cols: str | list[str],\n    raise_error: bool = False,\n) -&gt; bool:\n    \"\"\"Check whether some columns are Date or DateTime columns.\n\n    Parameters\n    ----------\n    table: cycquery.util.TableTypes\n        The table.\n    cols: str or list of str\n        Column names to check.\n    raise_error: bool\n        Whether to raise an error if one of the columns are none of the types.\n\n    Returns\n    -------\n    bool\n        Whether all of the columns are one of the types.\n\n    \"\"\"\n    return _check_column_type(table, cols, [Date, DateTime], raise_error=raise_error)\n</code></pre>"},{"location":"api/#cycquery.util.get_delta_column","title":"get_delta_column","text":"<pre><code>get_delta_column(\n    table, years=None, months=None, days=None, hours=None\n)\n</code></pre> <p>Create a time delta column.</p> <p>Create a time delta (interval) column from a number of numeric timestamp columns.</p> <p>Warning: Null values in each specified numeric time column are coalesced to 0.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>The table.</p> required <code>years</code> <code>str | None</code> <p>Years column.</p> <code>None</code> <code>months</code> <code>str | None</code> <p>Months column.</p> <code>None</code> <code>days</code> <code>str | None</code> <p>Days column.</p> <code>None</code> <code>hours</code> <code>str | None</code> <p>Hours column.</p> <code>None</code> <p>Returns:</p> Type Description <code>Column</code> <p>Combined delta/interval column.</p> Source code in <code>cycquery/util.py</code> <pre><code>@table_params_to_type(Subquery)  # type: ignore[arg-type]\ndef get_delta_column(\n    table: TableTypes,\n    years: str | None = None,\n    months: str | None = None,\n    days: str | None = None,\n    hours: str | None = None,\n) -&gt; Column:\n    \"\"\"Create a time delta column.\n\n    Create a time delta (interval) column from a number of\n    numeric timestamp columns.\n\n    Warning: Null values in each specified numeric time column are coalesced to 0.\n\n    Parameters\n    ----------\n    table: cycquery.util.TableTypes\n        The table.\n    years: None or str\n        Years column.\n    months: None or str\n        Months column.\n    days: None or str\n        Days column.\n    hours: None or str\n        Hours column.\n\n    Returns\n    -------\n    sqlalchemy.sql.schema.Column\n        Combined delta/interval column.\n\n    \"\"\"\n\n    def get_col_or_none(col: str | None = None) -&gt; Column | None:\n        \"\"\"If col is not None, get interval column from names.\"\"\"\n        return None if col is None else get_column(table, col)\n\n    years = get_col_or_none(years)  # type: ignore[assignment]\n    months = get_col_or_none(months)  # type: ignore[assignment]\n    days = get_col_or_none(days)  # type: ignore[assignment]\n    hours = get_col_or_none(hours)  # type: ignore[assignment]\n\n    time_cols = [years, months, days, hours]\n    names = [\"YEARS\", \"MONTHS\", \"DAYS\", \"HOURS\"]\n\n    # Consider only the non-null columns.\n    names = [names[i] for i in range(len(names)) if time_cols[i] is not None]\n    time_cols = [col for col in time_cols if col is not None]\n\n    if len(time_cols) == 0:\n        raise ValueError(\"One or more time interval columns must be specified.\")\n\n    # Create interval columns.\n    interval_cols = []\n    for i, col in enumerate(time_cols):\n        interval_cols.append(\n            func.cast(func.concat(func.coalesce(col, 0), \" \" + names[i]), Interval),\n        )\n\n    # Create combined interval column.\n    combined_interval_col = interval_cols[0]\n    for i in range(1, len(interval_cols)):\n        combined_interval_col = combined_interval_col + interval_cols[i]  # type: ignore[assignment]\n\n    return combined_interval_col  # type: ignore[return-value]\n</code></pre>"},{"location":"api/#cycquery.utils","title":"utils","text":"<p>Utility package.</p>"},{"location":"api/#cycquery.utils.common","title":"common","text":"<p>Common utility functions that can be used across multiple cycquery packages.</p>"},{"location":"api/#cycquery.utils.common.to_timestamp","title":"to_timestamp","text":"<pre><code>to_timestamp(data)\n</code></pre> <p>Convert a Pandas series or NumPy array to a datetime/timestamp type.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>Series | NDArray[Any]</code> <p>Data to be converted.</p> required <p>Returns:</p> Type Description <code>Series</code> <p>The converted data.</p> Source code in <code>cycquery/utils/common.py</code> <pre><code>def to_timestamp(data: pd.Series | npt.NDArray[Any]) -&gt; pd.Series:\n    \"\"\"Convert a Pandas series or NumPy array to a datetime/timestamp type.\n\n    Parameters\n    ----------\n    data: pandas.Series or numpy.ndarray\n        Data to be converted.\n\n    Returns\n    -------\n    pandas.Series\n        The converted data.\n\n    \"\"\"\n    if isinstance(data, pd.Series):\n        return pd.to_datetime(data)\n\n    if isinstance(data, np.ndarray):\n        return pd.to_datetime(pd.Series(data))\n\n    raise ValueError(f\"Type of data argument ({type(data)}) not supported.\")\n</code></pre>"},{"location":"api/#cycquery.utils.common.add_years_approximate","title":"add_years_approximate","text":"<pre><code>add_years_approximate(timestamp_series, years_series)\n</code></pre> <p>Approximately add together a timestamp series with a years series row-by-row.</p> <p>Approximates are typically exact or incorrect by one day, e.g., on leap days.</p> <p>Parameters:</p> Name Type Description Default <code>timestamp_series</code> <code>Series</code> <p>The series of timestamps to which to add.</p> required <code>years_series</code> <code>Series</code> <p>The series of years to add.</p> required <p>Returns:</p> Type Description <code>Series</code> <p>The timestamp series with the approximately added years.</p> Source code in <code>cycquery/utils/common.py</code> <pre><code>def add_years_approximate(\n    timestamp_series: pd.Series,\n    years_series: pd.Series,\n) -&gt; pd.Series:\n    \"\"\"Approximately add together a timestamp series with a years series row-by-row.\n\n    Approximates are typically exact or incorrect by one day, e.g., on leap days.\n\n    Parameters\n    ----------\n    timestamp_series: pandas.Series\n        The series of timestamps to which to add.\n    years_series: panadas.Series\n        The series of years to add.\n\n    Returns\n    -------\n    pandas.Series\n        The timestamp series with the approximately added years.\n\n    \"\"\"\n    # Add to the years column\n    year = timestamp_series.dt.year + years_series\n\n    # Handle the other columns\n    month = timestamp_series.dt.month\n    day = timestamp_series.dt.day\n    hour = timestamp_series.dt.hour\n    minute = timestamp_series.dt.minute\n\n    # Create new timestamp column\n    data = pd.DataFrame(\n        {\"year\": year, \"month\": month, \"day\": day, \"hour\": hour, \"minute\": minute},\n    )\n\n    # Subtract 1 from potentially invalid leap days to avoid issues\n    leap_days = (month == 2) &amp; (day == 29)\n    data.loc[leap_days, \"day\"] -= 1\n\n    return pd.to_datetime(data)\n</code></pre>"},{"location":"api/#cycquery.utils.common.add_years_exact","title":"add_years_exact","text":"<pre><code>add_years_exact(timestamp_series, years_series)\n</code></pre> <p>Add together a timestamp series with a years series row-by-row.</p> <p>Warning: Very slow. It is worth using the add_years_approximate function even moderately large data.</p> <p>Parameters:</p> Name Type Description Default <code>timestamp_series</code> <code>Series</code> <p>The series of timestamps to which to add.</p> required <code>years_series</code> <code>Series</code> <p>The series of years to add.</p> required <p>Returns:</p> Type Description <code>Series</code> <p>The timestamp series with the approximately added years.</p> Source code in <code>cycquery/utils/common.py</code> <pre><code>def add_years_exact(timestamp_series: pd.Series, years_series: pd.Series) -&gt; pd.Series:\n    \"\"\"Add together a timestamp series with a years series row-by-row.\n\n    Warning: Very slow. It is worth using the add_years_approximate function even\n    moderately large data.\n\n    Parameters\n    ----------\n    timestamp_series: pandas.Series\n        The series of timestamps to which to add.\n    years_series: panadas.Series\n        The series of years to add.\n\n    Returns\n    -------\n    pandas.Series\n        The timestamp series with the approximately added years.\n\n    \"\"\"\n    warnings.warn(\n        (\n            \"Computing the exact addition cannot be vectorized and is very slow. \"\n            \"Consider using the quick, approximate calculation.\"\n        ),\n        PerformanceWarning,\n        stacklevel=1,\n    )\n    return timestamp_series + years_series.apply(lambda x: pd.DateOffset(years=x))\n</code></pre>"},{"location":"api/#cycquery.utils.common.to_list","title":"to_list","text":"<pre><code>to_list(obj)\n</code></pre> <p>Convert some object to a list of object(s) unless already one.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>any</code> <p>The object to convert to a list.</p> required <p>Returns:</p> Type Description <code>list</code> <p>The processed object.</p> Source code in <code>cycquery/utils/common.py</code> <pre><code>def to_list(obj: Any) -&gt; list[Any]:\n    \"\"\"Convert some object to a list of object(s) unless already one.\n\n    Parameters\n    ----------\n    obj : any\n        The object to convert to a list.\n\n    Returns\n    -------\n    list\n        The processed object.\n\n    \"\"\"\n    if isinstance(obj, list):\n        return obj\n\n    if isinstance(obj, (np.ndarray, set, dict)):\n        return list(obj)\n\n    return [obj]\n</code></pre>"},{"location":"api/#cycquery.utils.common.to_list_optional","title":"to_list_optional","text":"<pre><code>to_list_optional(obj, none_to_empty=False)\n</code></pre> <p>Convert some object to a list of object(s) unless already None or a list.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>any</code> <p>The object to convert to a list.</p> required <code>none_to_empty</code> <code>bool</code> <p>If true, return a None obj as an empty list. Otherwise, return as None.</p> <code>False</code> <p>Returns:</p> Type Description <code>list or None</code> <p>The processed object.</p> Source code in <code>cycquery/utils/common.py</code> <pre><code>def to_list_optional(\n    obj: Any | None,\n    none_to_empty: bool = False,\n) -&gt; list[Any] | None:\n    \"\"\"Convert some object to a list of object(s) unless already None or a list.\n\n    Parameters\n    ----------\n    obj : any\n        The object to convert to a list.\n    none_to_empty: bool, default = False\n        If true, return a None obj as an empty list. Otherwise, return as None.\n\n    Returns\n    -------\n    list or None\n        The processed object.\n\n    \"\"\"\n    if obj is None:\n        if none_to_empty:\n            return []\n        return None\n\n    return to_list(obj)\n</code></pre>"},{"location":"api/#cycquery.utils.common.to_datetime_format","title":"to_datetime_format","text":"<pre><code>to_datetime_format(date, fmt='%Y-%m-%d')\n</code></pre> <p>Convert string date to datetime.</p> <p>Parameters:</p> Name Type Description Default <code>date</code> <code>str</code> <p>Input date in string format.</p> required <code>fmt</code> <code>str</code> <p>Date formatting string.</p> <code>'%Y-%m-%d'</code> <p>Returns:</p> Type Description <code>datetime</code> <p>Date in datetime format.</p> Source code in <code>cycquery/utils/common.py</code> <pre><code>def to_datetime_format(date: str, fmt: str = \"%Y-%m-%d\") -&gt; datetime:\n    \"\"\"Convert string date to datetime.\n\n    Parameters\n    ----------\n    date: str\n        Input date in string format.\n    fmt: str, optional\n        Date formatting string.\n\n    Returns\n    -------\n    datetime\n        Date in datetime format.\n\n    \"\"\"\n    return datetime.strptime(date, fmt)\n</code></pre>"},{"location":"api/#cycquery.utils.common.list_swap","title":"list_swap","text":"<pre><code>list_swap(lst, index1, index2)\n</code></pre> <p>Swap items in a list given the item index and new item index.</p> <p>Parameters:</p> Name Type Description Default <code>lst</code> <code>list[Any]</code> <p>List in which elements will be swapped.</p> required <code>index1</code> <code>int</code> <p>Index of first item to swap.</p> required <code>index2</code> <code>int</code> <p>Index of second item to swap.</p> required <p>Returns:</p> Type Description <code>list</code> <p>List with elements swapped.</p> Source code in <code>cycquery/utils/common.py</code> <pre><code>def list_swap(lst: list[Any], index1: int, index2: int) -&gt; list[Any]:\n    \"\"\"Swap items in a list given the item index and new item index.\n\n    Parameters\n    ----------\n    lst: list\n        List in which elements will be swapped.\n    index1: int\n        Index of first item to swap.\n    index2: int\n        Index of second item to swap.\n\n    Returns\n    -------\n    list\n        List with elements swapped.\n\n    \"\"\"\n    if not 0 &lt;= index1 &lt; len(lst):\n        raise ValueError(\"index 1 is out of range.\")\n\n    if not 0 &lt;= index2 &lt; len(lst):\n        raise ValueError(\"index 2 is out of range.\")\n\n    lst[index1], lst[index2] = lst[index2], lst[index1]\n\n    return lst\n</code></pre>"},{"location":"api/#cycquery.utils.file","title":"file","text":"<p>Utility functions for saving/loading files.</p>"},{"location":"api/#cycquery.utils.file.join","title":"join","text":"<pre><code>join(*paths)\n</code></pre> <p>Robustly join paths.</p> <p>os.path.join only may cause problems with some filepaths (especially on Windows).</p> <p>Parameters:</p> Name Type Description Default <code>paths</code> <code>str</code> <p>file paths</p> <code>()</code> <p>Returns:</p> Type Description <code>str</code> <p>The joined path of all input paths.</p> Source code in <code>cycquery/utils/file.py</code> <pre><code>def join(*paths: str) -&gt; str:\n    \"\"\"Robustly join paths.\n\n    os.path.join only may cause problems with some filepaths (especially on Windows).\n\n    Parameters\n    ----------\n    paths: str\n        file paths\n\n    Returns\n    -------\n    str\n        The joined path of all input paths.\n\n    \"\"\"\n    return os.path.join(*paths).replace(\"\\\\\", \"/\")\n</code></pre>"},{"location":"api/#cycquery.utils.file.exchange_extension","title":"exchange_extension","text":"<pre><code>exchange_extension(file_path, new_ext)\n</code></pre> <p>Exchange one file extension for another.</p> <p>Parameters:</p> Name Type Description Default <code>file_path</code> <code>str</code> <p>File path in which to exchange the extension.</p> required <code>new_ext</code> <code>str</code> <p>New extension to replace the existing extension.</p> required <p>Returns:</p> Type Description <code>str</code> <p>File path with the new extension.</p> Source code in <code>cycquery/utils/file.py</code> <pre><code>def exchange_extension(file_path: str, new_ext: str) -&gt; str:\n    \"\"\"Exchange one file extension for another.\n\n    Parameters\n    ----------\n    file_path: str\n        File path in which to exchange the extension.\n    new_ext: str\n        New extension to replace the existing extension.\n\n    Returns\n    -------\n    str\n        File path with the new extension.\n\n    \"\"\"\n    # Remove a leading dot\n    new_ext = new_ext.strip(\".\")\n    _, old_ext = os.path.splitext(file_path)\n    return file_path[: -len(old_ext)] + \".\" + new_ext\n</code></pre>"},{"location":"api/#cycquery.utils.file.process_file_save_path","title":"process_file_save_path","text":"<pre><code>process_file_save_path(\n    save_path, file_format, create_dir=True\n)\n</code></pre> <p>Process file save path, perform checks, and possibly create a parent directory.</p> <p>Parameters:</p> Name Type Description Default <code>save_path</code> <code>str</code> <p>Path where the file will be saved.</p> required <code>file_format</code> <code>str</code> <p>File format of the file to save.</p> required <code>create_dir</code> <code>bool</code> <p>If True, create the parent directory path if needed.</p> <code>True</code> <p>Returns:</p> Type Description <code>str</code> <p>The processed save path.</p> Source code in <code>cycquery/utils/file.py</code> <pre><code>def process_file_save_path(\n    save_path: str,\n    file_format: str,\n    create_dir: bool = True,\n) -&gt; str:\n    \"\"\"Process file save path, perform checks, and possibly create a parent directory.\n\n    Parameters\n    ----------\n    save_path: str\n        Path where the file will be saved.\n    file_format: str\n        File format of the file to save.\n    create_dir: bool\n        If True, create the parent directory path if needed.\n\n    Returns\n    -------\n    str\n        The processed save path.\n\n    \"\"\"\n    # Create the directory if it doesn't already exist.\n    directory, _ = os.path.split(save_path)\n\n    if create_dir and directory != \"\":\n        os.makedirs(directory, exist_ok=True)\n\n    # Add the .parquet extension if it isn't there.\n    _, ext = os.path.splitext(save_path)\n\n    if ext == \"\":\n        save_path = save_path + \".\" + file_format\n    elif ext != \".\" + file_format:\n        raise ValueError(\n            f\"\"\"The file extension on the save path must be {file_format}.\n            Alternatively, consider changing the file format.\"\"\",\n        )\n\n    return save_path\n</code></pre>"},{"location":"api/#cycquery.utils.file.process_dir_save_path","title":"process_dir_save_path","text":"<pre><code>process_dir_save_path(save_path, create_dir=True)\n</code></pre> <p>Process directory save path, perform checks, and possibly create the directory.</p> <p>Parameters:</p> Name Type Description Default <code>save_path</code> <code>str</code> <p>Path where the file will be saved.</p> required <code>create_dir</code> <code>bool</code> <p>If True, create the directory if needed.</p> <code>True</code> <p>Returns:</p> Type Description <code>str</code> <p>The processed save path.</p> Source code in <code>cycquery/utils/file.py</code> <pre><code>def process_dir_save_path(save_path: str, create_dir: bool = True) -&gt; str:\n    \"\"\"Process directory save path, perform checks, and possibly create the directory.\n\n    Parameters\n    ----------\n    save_path: str\n        Path where the file will be saved.\n    create_dir: bool\n        If True, create the directory if needed.\n\n    Returns\n    -------\n    str\n        The processed save path.\n\n    \"\"\"\n    if os.path.exists(save_path):\n        if os.path.isdir(save_path):\n            return save_path\n        raise ValueError(\"If save path exists, it must be a directory.\")\n\n    if create_dir:\n        os.makedirs(save_path)\n        return save_path\n\n    raise ValueError(\"Directory does not exist.\")\n</code></pre>"},{"location":"api/#cycquery.utils.file.save_dataframe","title":"save_dataframe","text":"<pre><code>save_dataframe(\n    data, save_path, file_format=\"parquet\", log=True\n)\n</code></pre> <p>Save a pandas.DataFrame or dask.DataFrame object to file.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>DataFrame</code> <p>Dataframe to save.</p> required <code>save_path</code> <code>str</code> <p>Path where the file will be saved.</p> required <code>file_format</code> <code>str</code> <p>File format of the file to save.</p> <code>'parquet'</code> <code>log</code> <code>bool</code> <p>Whether to log the occurence.</p> <code>True</code> <p>Returns:</p> Type Description <code>str</code> <p>Processed save path for upstream use.</p> Source code in <code>cycquery/utils/file.py</code> <pre><code>def save_dataframe(\n    data: pd.DataFrame,\n    save_path: str,\n    file_format: str = \"parquet\",\n    log: bool = True,\n) -&gt; str:\n    \"\"\"Save a pandas.DataFrame or dask.DataFrame object to file.\n\n    Parameters\n    ----------\n    data: pandas.DataFrame\n        Dataframe to save.\n    save_path: str\n        Path where the file will be saved.\n    file_format: str\n        File format of the file to save.\n    log: bool\n        Whether to log the occurence.\n\n    Returns\n    -------\n    str\n        Processed save path for upstream use.\n\n    \"\"\"\n    if not isinstance(data, pd.DataFrame):\n        raise ValueError(\"Input data is not a DataFrame.\")\n    save_path = process_file_save_path(save_path, file_format)\n    if log:\n        LOGGER.info(\"Saving dataframe to %s\", save_path)\n    if file_format == \"parquet\":\n        if isinstance(data, pd.DataFrame):\n            data.to_parquet(save_path, schema=None)\n    elif file_format == \"csv\":\n        data.to_csv(save_path)\n    else:\n        raise ValueError(\n            \"Invalid file formated provided. Currently supporting 'parquet' and 'csv'.\",\n        )\n\n    return save_path\n</code></pre>"},{"location":"api/#cycquery.utils.file.load_dataframe","title":"load_dataframe","text":"<pre><code>load_dataframe(load_path, file_format='parquet', log=True)\n</code></pre> <p>Load file to a pandas.DataFrame object.</p> <p>Parameters:</p> Name Type Description Default <code>load_path</code> <code>str</code> <p>Path where the file to load.</p> required <code>file_format</code> <code>str</code> <p>File format of the file to load.</p> <code>'parquet'</code> <code>log</code> <code>bool</code> <p>Whether to log the occurence.</p> <code>True</code> <p>Returns:</p> Type Description <code>DataFrame or DataFrame</code> <p>Loaded data.</p> Source code in <code>cycquery/utils/file.py</code> <pre><code>def load_dataframe(\n    load_path: str,\n    file_format: str = \"parquet\",\n    log: bool = True,\n) -&gt; pd.DataFrame:\n    \"\"\"Load file to a pandas.DataFrame object.\n\n    Parameters\n    ----------\n    load_path: str\n        Path where the file to load.\n    file_format: str\n        File format of the file to load.\n    log: bool\n        Whether to log the occurence.\n\n    Returns\n    -------\n    pandas.DataFrame or dask.DataFrame\n        Loaded data.\n\n    \"\"\"\n    load_path = process_file_save_path(load_path, file_format)\n    if log:\n        LOGGER.info(\"Loading DataFrame from %s\", load_path)\n    if file_format == \"parquet\":\n        data = pd.read_parquet(load_path)\n    elif file_format == \"csv\":\n        data = pd.read_csv(load_path, index_col=[0])\n    else:\n        raise ValueError(\n            \"Invalid file formated provided. Currently supporting 'parquet' and 'csv'.\",\n        )\n\n    return data\n</code></pre>"},{"location":"api/#cycquery.utils.file.save_array","title":"save_array","text":"<pre><code>save_array(data, save_path, file_format='npy', log=True)\n</code></pre> <p>Save a numpy.ndarray object to file.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>ArrayLike</code> <p>Array to save.</p> required <code>save_path</code> <code>str</code> <p>Path where the file will be saved.</p> required <code>file_format</code> <code>str</code> <p>File format of the file to save.</p> <code>'npy'</code> <code>log</code> <code>bool</code> <p>Whether to log the occurence.</p> <code>True</code> <p>Returns:</p> Type Description <code>str</code> <p>Processed save path for upstream use.</p> Source code in <code>cycquery/utils/file.py</code> <pre><code>def save_array(\n    data: np.typing.ArrayLike,\n    save_path: str,\n    file_format: str = \"npy\",\n    log: bool = True,\n) -&gt; str:\n    \"\"\"Save a numpy.ndarray object to file.\n\n    Parameters\n    ----------\n    data: numpy.ndarray\n        Array to save.\n    save_path: str\n        Path where the file will be saved.\n    file_format: str\n        File format of the file to save.\n    log: bool\n        Whether to log the occurence.\n\n    Returns\n    -------\n    str\n        Processed save path for upstream use.\n\n    \"\"\"\n    save_path = process_file_save_path(save_path, file_format)\n\n    if not isinstance(data, np.ndarray):\n        raise ValueError(\"Input data is not an array.\")\n\n    if log:\n        LOGGER.info(\"Saving array to %s\", save_path)\n\n    if file_format == \"npy\":\n        np.save(save_path, data)\n    else:\n        raise ValueError(\"Invalid file formated provided. Currently supporting 'npy'.\")\n\n    return save_path\n</code></pre>"},{"location":"api/#cycquery.utils.file.load_array","title":"load_array","text":"<pre><code>load_array(load_path, file_format='npy', log=True)\n</code></pre> <p>Load file to a numpy.ndarray object.</p> <p>Parameters:</p> Name Type Description Default <code>load_path</code> <code>str</code> <p>Path where the file to load.</p> required <code>file_format</code> <code>str</code> <p>File format of the file to load.</p> <code>'npy'</code> <code>log</code> <code>bool</code> <p>Whether to log the occurence.</p> <code>True</code> <p>Returns:</p> Type Description <code>ndarray</code> <p>Loaded data.</p> Source code in <code>cycquery/utils/file.py</code> <pre><code>def load_array(\n    load_path: str,\n    file_format: str = \"npy\",\n    log: bool = True,\n) -&gt; Any:\n    \"\"\"Load file to a numpy.ndarray object.\n\n    Parameters\n    ----------\n    load_path: str\n        Path where the file to load.\n    file_format: str\n        File format of the file to load.\n    log: bool\n        Whether to log the occurence.\n\n    Returns\n    -------\n    numpy.ndarray\n        Loaded data.\n\n    \"\"\"\n    load_path = process_file_save_path(load_path, file_format)\n\n    if log:\n        LOGGER.info(\"Loading array from %s\", load_path)\n\n    if file_format == \"npy\":\n        data = np.load(load_path)\n    else:\n        raise ValueError(\"Invalid file formated provided. Currently supporting 'npy'.\")\n\n    if not isinstance(data, np.ndarray):\n        raise ValueError(\"Loaded data is not an array.\")\n\n    return data\n</code></pre>"},{"location":"api/#cycquery.utils.file.save_pickle","title":"save_pickle","text":"<pre><code>save_pickle(data, save_path, log=True)\n</code></pre> <p>Save a object to pickle file.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>Any</code> <p>Data to save.</p> required <code>save_path</code> <code>str</code> <p>Path where the file will be saved.</p> required <code>log</code> <code>bool</code> <p>Whether to log the occurence.</p> <code>True</code> <p>Returns:</p> Type Description <code>str</code> <p>Processed save path for upstream use.</p> Source code in <code>cycquery/utils/file.py</code> <pre><code>def save_pickle(\n    data: Any,\n    save_path: str,\n    log: bool = True,\n) -&gt; str:\n    \"\"\"Save a object to pickle file.\n\n    Parameters\n    ----------\n    data: any\n        Data to save.\n    save_path: str\n        Path where the file will be saved.\n    log: bool\n        Whether to log the occurence.\n\n    Returns\n    -------\n    str\n        Processed save path for upstream use.\n\n    \"\"\"\n    save_path = process_file_save_path(save_path, \"pkl\")\n\n    if log:\n        LOGGER.info(\"Pickling data to %s\", save_path)\n\n    with open(save_path, \"wb\") as handle:\n        pickle.dump(data, handle)\n\n    return save_path\n</code></pre>"},{"location":"api/#cycquery.utils.file.load_pickle","title":"load_pickle","text":"<pre><code>load_pickle(load_path, log=True)\n</code></pre> <p>Load an object from a pickle file.</p> <p>Parameters:</p> Name Type Description Default <code>load_path</code> <code>str</code> <p>Path where the file to load.</p> required <code>log</code> <code>bool</code> <p>Whether to log the occurence.</p> <code>True</code> <p>Returns:</p> Type Description <code>any</code> <p>Loaded data.</p> Source code in <code>cycquery/utils/file.py</code> <pre><code>def load_pickle(\n    load_path: str,\n    log: bool = True,\n) -&gt; Any:\n    \"\"\"Load an object from a pickle file.\n\n    Parameters\n    ----------\n    load_path: str\n        Path where the file to load.\n    log: bool\n        Whether to log the occurence.\n\n    Returns\n    -------\n    any\n        Loaded data.\n\n    \"\"\"\n    load_path = process_file_save_path(load_path, \"pkl\")\n\n    if log:\n        LOGGER.info(\"Loading pickled data from %s\", load_path)\n\n    with open(load_path, \"rb\") as handle:\n        return pickle.load(handle)\n</code></pre>"},{"location":"api/#cycquery.utils.file.listdir_nonhidden","title":"listdir_nonhidden","text":"<pre><code>listdir_nonhidden(path)\n</code></pre> <p>List the non-hidden files of a directory.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Directory path.</p> required <p>Returns:</p> Type Description <code>list</code> <p>List of non-hidden files.</p> Source code in <code>cycquery/utils/file.py</code> <pre><code>def listdir_nonhidden(path: str) -&gt; list[str]:\n    \"\"\"List the non-hidden files of a directory.\n\n    Parameters\n    ----------\n    path: str\n        Directory path.\n\n    Returns\n    -------\n    list\n        List of non-hidden files.\n\n    \"\"\"\n    return [f for f in os.listdir(path) if not f.startswith(\".\")]\n</code></pre>"},{"location":"api/#cycquery.utils.file.yield_dataframes","title":"yield_dataframes","text":"<pre><code>yield_dataframes(\n    dir_path, sort=True, skip_n=None, log=True\n)\n</code></pre> <p>Yield DataFrames loaded from a directory.</p> <p>Any non-hidden files in the directory must be loadable as a DataFrame.</p> <p>Parameters:</p> Name Type Description Default <code>dir_path</code> <code>str</code> <p>Directory path of files.</p> required <code>sort</code> <code>bool</code> <p>Whether to sort the files and yield them in an ordered manner.</p> <code>True</code> <code>skip_n</code> <code>int | None</code> <p>If specified, skip the first n files when yielding the files. This is especially useful in lieu of the execution being interrupted.</p> <code>None</code> <code>log</code> <code>bool</code> <p>Whether to log the occurence.</p> <code>True</code> <p>Yields:</p> Type Description <code>DataFrame</code> <p>A DataFrame.</p> Source code in <code>cycquery/utils/file.py</code> <pre><code>def yield_dataframes(\n    dir_path: str,\n    sort: bool = True,\n    skip_n: int | None = None,\n    log: bool = True,\n) -&gt; Generator[pd.DataFrame, None, None]:\n    \"\"\"Yield DataFrames loaded from a directory.\n\n    Any non-hidden files in the directory must be loadable as a DataFrame.\n\n    Parameters\n    ----------\n    dir_path: str\n        Directory path of files.\n    sort: bool, default = True\n        Whether to sort the files and yield them in an ordered manner.\n    skip_n: int, optional\n        If specified, skip the first n files when yielding the files.\n        This is especially useful in lieu of the execution being interrupted.\n    log: bool\n        Whether to log the occurence.\n\n    Yields\n    ------\n    pandas.DataFrame\n        A DataFrame.\n\n    \"\"\"\n    files = list(listdir_nonhidden(dir_path))\n\n    if sort:\n        files.sort()\n\n    if skip_n:\n        files = files[skip_n:]\n\n    for file in files:\n        yield load_dataframe(join(dir_path, file), log=log)\n</code></pre>"},{"location":"api/#cycquery.utils.file.yield_pickled_files","title":"yield_pickled_files","text":"<pre><code>yield_pickled_files(\n    dir_path, sort=True, skip_n=None, log=True\n)\n</code></pre> <p>Yield pickled files loaded from a directory.</p> <p>Any non-hidden files in the directory must be loadable with pickle.</p> <p>Parameters:</p> Name Type Description Default <code>dir_path</code> <code>str</code> <p>Directory path of files.</p> required <code>sort</code> <code>bool</code> <p>Whether to sort the files and yield them in an ordered manner.</p> <code>True</code> <code>skip_n</code> <code>int | None</code> <p>If specified, skip the first n files when yielding the files. This is especially useful in lieu of the execution being interrupted.</p> <code>None</code> <code>log</code> <code>bool</code> <p>Whether to log the occurence.</p> <code>True</code> <p>Yields:</p> Type Description <code>any</code> <p>Previously pickled data.</p> Source code in <code>cycquery/utils/file.py</code> <pre><code>def yield_pickled_files(\n    dir_path: str,\n    sort: bool = True,\n    skip_n: int | None = None,\n    log: bool = True,\n) -&gt; Generator[pd.DataFrame, None, None]:\n    \"\"\"Yield pickled files loaded from a directory.\n\n    Any non-hidden files in the directory must be loadable with pickle.\n\n    Parameters\n    ----------\n    dir_path: str\n        Directory path of files.\n    sort: bool, default = True\n        Whether to sort the files and yield them in an ordered manner.\n    skip_n: int, optional\n        If specified, skip the first n files when yielding the files.\n        This is especially useful in lieu of the execution being interrupted.\n    log: bool\n        Whether to log the occurence.\n\n    Yields\n    ------\n    any\n        Previously pickled data.\n\n    \"\"\"\n    files = list(listdir_nonhidden(dir_path))\n\n    if sort:\n        files.sort()\n\n    if skip_n:\n        files = files[skip_n:]\n\n    for file in files:\n        yield load_pickle(join(dir_path, file), log=log)\n</code></pre>"},{"location":"api/#cycquery.utils.log","title":"log","text":"<p>Python logging function.</p>"},{"location":"api/#cycquery.utils.log.Formatter","title":"Formatter","text":"<p>               Bases: <code>Formatter</code></p> <p>Formatter.</p> <p>Attributes:</p> Name Type Description <code>use_color</code> <code>bool</code> <p>Flag to toggle use of color.</p> Source code in <code>cycquery/utils/log.py</code> <pre><code>class Formatter(logging.Formatter):\n    \"\"\"Formatter.\n\n    Attributes\n    ----------\n    use_color: bool\n        Flag to toggle use of color.\n\n    \"\"\"\n\n    def __init__(\n        self,\n        msg: Optional[str] = None,\n        use_color: bool = True,\n    ) -&gt; None:\n        \"\"\"Instantiate.\n\n        Parameters\n        ----------\n        msg: str\n            Message format.\n        use_color: bool\n            Flag to set using colored formatting.\n\n        \"\"\"\n        if msg is None:\n            msg = formatter_message(LOG_FORMAT, True)\n        logging.Formatter.__init__(self, msg)\n        self.use_color = use_color\n\n    def format(self, record: logging.LogRecord) -&gt; str:  # noqa: A003\n        \"\"\"Apply formatting.\n\n        Parameters\n        ----------\n        record: logging.LogRecord\n            Record object for logging.\n\n        Returns\n        -------\n        str\n            Formatted string for log.\n\n        \"\"\"\n        levelname = record.levelname\n        if self.use_color and levelname in COLORS:\n            levelname_color = (\n                COLOR_SEQ % (30 + COLORS[levelname]) + levelname + RESET_SEQ\n            )\n            record.levelname = levelname_color\n        return logging.Formatter.format(self, record)\n</code></pre>"},{"location":"api/#cycquery.utils.log.Formatter.__init__","title":"__init__","text":"<pre><code>__init__(msg=None, use_color=True)\n</code></pre> <p>Instantiate.</p> <p>Parameters:</p> Name Type Description Default <code>msg</code> <code>Optional[str]</code> <p>Message format.</p> <code>None</code> <code>use_color</code> <code>bool</code> <p>Flag to set using colored formatting.</p> <code>True</code> Source code in <code>cycquery/utils/log.py</code> <pre><code>def __init__(\n    self,\n    msg: Optional[str] = None,\n    use_color: bool = True,\n) -&gt; None:\n    \"\"\"Instantiate.\n\n    Parameters\n    ----------\n    msg: str\n        Message format.\n    use_color: bool\n        Flag to set using colored formatting.\n\n    \"\"\"\n    if msg is None:\n        msg = formatter_message(LOG_FORMAT, True)\n    logging.Formatter.__init__(self, msg)\n    self.use_color = use_color\n</code></pre>"},{"location":"api/#cycquery.utils.log.Formatter.format","title":"format","text":"<pre><code>format(record)\n</code></pre> <p>Apply formatting.</p> <p>Parameters:</p> Name Type Description Default <code>record</code> <code>LogRecord</code> <p>Record object for logging.</p> required <p>Returns:</p> Type Description <code>str</code> <p>Formatted string for log.</p> Source code in <code>cycquery/utils/log.py</code> <pre><code>def format(self, record: logging.LogRecord) -&gt; str:  # noqa: A003\n    \"\"\"Apply formatting.\n\n    Parameters\n    ----------\n    record: logging.LogRecord\n        Record object for logging.\n\n    Returns\n    -------\n    str\n        Formatted string for log.\n\n    \"\"\"\n    levelname = record.levelname\n    if self.use_color and levelname in COLORS:\n        levelname_color = (\n            COLOR_SEQ % (30 + COLORS[levelname]) + levelname + RESET_SEQ\n        )\n        record.levelname = levelname_color\n    return logging.Formatter.format(self, record)\n</code></pre>"},{"location":"api/#cycquery.utils.log.formatter_message","title":"formatter_message","text":"<pre><code>formatter_message(message, use_color=True)\n</code></pre> <p>Format message.</p> <p>Parameters:</p> Name Type Description Default <code>message</code> <code>str</code> <p>Message format.</p> required <code>use_color</code> <code>Optional[bool]</code> <p>Use colored logging.</p> <code>True</code> <p>Returns:</p> Type Description <code>str</code> <p>Formatted message.</p> Source code in <code>cycquery/utils/log.py</code> <pre><code>def formatter_message(message: str, use_color: Optional[bool] = True) -&gt; str:\n    \"\"\"Format message.\n\n    Parameters\n    ----------\n    message: str\n        Message format.\n    use_color: bool, optional\n        Use colored logging.\n\n    Returns\n    -------\n    str\n        Formatted message.\n\n    \"\"\"\n    if use_color:\n        message = message.replace(\"$RESET\", RESET_SEQ).replace(\"$BOLD\", BOLD_SEQ)\n    else:\n        message = message.replace(\"$RESET\", \"\").replace(\"$BOLD\", \"\")\n    return message\n</code></pre>"},{"location":"api/#cycquery.utils.log.setup_logging","title":"setup_logging","text":"<pre><code>setup_logging(\n    log_path=None,\n    log_level=\"DEBUG\",\n    print_level=\"INFO\",\n    logger=None,\n    use_color=True,\n)\n</code></pre> <p>Create logger, and set it up.</p> <p>Parameters:</p> Name Type Description Default <code>log_path</code> <code>str</code> <p>Path to output log file.</p> <code>None</code> <code>log_level</code> <code>str</code> <p>Log level for logging, defaults to DEBUG.</p> <code>'DEBUG'</code> <code>print_level</code> <code>str</code> <p>Print level for logging, defaults to INFO.</p> <code>'INFO'</code> <code>logger</code> <code>Logger</code> <p>Pass logger if already exists, else a new logger object is created.</p> <code>None</code> <code>use_color</code> <code>bool</code> <p>Use colored logging.</p> <code>True</code> Source code in <code>cycquery/utils/log.py</code> <pre><code>def setup_logging(\n    log_path: Optional[str] = None,\n    log_level: Union[int, str] = \"DEBUG\",\n    print_level: Union[int, str] = \"INFO\",\n    logger: Optional[logging.Logger] = None,\n    use_color: bool = True,\n) -&gt; None:\n    \"\"\"Create logger, and set it up.\n\n    Parameters\n    ----------\n    log_path : str, optional\n        Path to output log file.\n    log_level : str, optional\n        Log level for logging, defaults to DEBUG.\n    print_level : str, optional\n        Print level for logging, defaults to INFO.\n    logger : logging.Logger, optional\n        Pass logger if already exists, else a new logger object is created.\n    use_color: bool, optional\n        Use colored logging.\n\n    \"\"\"\n    fmt = formatter_message(LOG_FORMAT, use_color)\n    logger = logger if logger else logging.getLogger()\n    logger.setLevel(log_level)\n    logger.handlers = []\n\n    stream_handler = logging.StreamHandler()\n    stream_handler.setFormatter(Formatter(fmt, use_color=use_color))\n    stream_handler.setLevel(print_level)\n    logger.addHandler(stream_handler)\n\n    if log_path:\n        file_handler = logging.FileHandler(log_path)\n        file_handler.setFormatter(Formatter(fmt, use_color=use_color))\n        logger.addHandler(file_handler)\n</code></pre>"},{"location":"api/#cycquery.utils.profile","title":"profile","text":"<p>Useful functions for timing, profiling.</p>"},{"location":"api/#cycquery.utils.profile.time_function","title":"time_function","text":"<pre><code>time_function(func)\n</code></pre> <p>Time decorator function.</p> <p>Parameters:</p> Name Type Description Default <code>func</code> <code>Callable[..., Any]</code> <p>Function to apply decorator.</p> required <p>Returns:</p> Type Description <code>Callable</code> <p>Wrapper function to apply as decorator.</p> Source code in <code>cycquery/utils/profile.py</code> <pre><code>def time_function(func: Callable[..., Any]) -&gt; Callable[..., Any]:\n    \"\"\"Time decorator function.\n\n    Parameters\n    ----------\n    func: function\n        Function to apply decorator.\n\n    Returns\n    -------\n    Callable\n        Wrapper function to apply as decorator.\n\n    \"\"\"\n\n    def wrapper_func(*args: List[Any], **kwargs: Dict[str, Any]) -&gt; Any:\n        start_time = time.time()\n        result = func(*args, **kwargs)\n        time_taken = time.time() - start_time\n        LOGGER.info(\"Finished executing function %s in %f s\", func.__name__, time_taken)\n        return result\n\n    return wrapper_func\n</code></pre>"},{"location":"api/#interface-module","title":"Interface Module","text":""},{"location":"api/#cycquery.interface","title":"cycquery.interface","text":"<p>A query interface to wrap database objects and queries.</p>"},{"location":"api/#cycquery.interface.QueryInterface","title":"QueryInterface","text":"<p>An interface dataclass to wrap queries, and run them.</p> <p>Parameters:</p> Name Type Description Default <code>database</code> <code>Database</code> <p>Database object to create ORM, and query data.</p> required <code>query</code> <code>TableTypes | 'QueryInterface'</code> <p>The query.</p> required Source code in <code>cycquery/interface.py</code> <pre><code>class QueryInterface:\n    \"\"\"An interface dataclass to wrap queries, and run them.\n\n    Parameters\n    ----------\n    database\n        Database object to create ORM, and query data.\n    query\n        The query.\n\n    \"\"\"\n\n    def __init__(\n        self,\n        database: Database,\n        query: TableTypes | \"QueryInterface\",\n    ) -&gt; None:\n        \"\"\"Initialize the QueryInterface object, join and chain operations.\"\"\"\n        self.database = database\n        if isinstance(query, QueryInterface):\n            self.query = query.query  # type: ignore\n        else:\n            self.query = query\n        self._data = None\n\n    @property\n    def data(self) -&gt; pd.DataFrame | None:\n        \"\"\"Get data.\"\"\"\n        return self._data\n\n    def join(\n        self,\n        join_table: TableTypes | \"QueryInterface\",\n        on: str | list[str] | Tuple[str] | list[Tuple[str, str]] | None = None,\n        on_to_type: type | list[type] | None = None,\n        cond: BinaryExpression | None = None,\n        table_cols: str | list[str] | None = None,\n        join_table_cols: str | list[str] | None = None,\n        isouter: bool | None = False,\n    ) -&gt; \"QueryInterface\":\n        \"\"\"Join the query with another table.\n\n        Parameters\n        ----------\n        join_table\n            Table to join with.\n        on\n            Column(s) to join on.\n        on_to_type\n            Type(s) to cast the column(s) to join on.\n        cond\n            Condition to join on.\n        table_cols\n            Columns to select from the original table.\n        join_table_cols\n            Columns to select from the joined table.\n        isouter\n            Whether to perform an outer join.\n\n        Returns\n        -------\n        QueryInterface\n            QueryInterface object with the join operation added.\n\n        \"\"\"\n        on = to_list_optional(on)\n        on_to_type = to_list_optional(on_to_type)\n        table_cols = to_list_optional(table_cols)\n        join_table_cols = to_list_optional(join_table_cols)\n        if isinstance(join_table, QueryInterface):\n            join_table = join_table.query\n        query = qo.Join(\n            join_table=join_table,\n            on=on,\n            on_to_type=on_to_type,\n            cond=cond,\n            table_cols=table_cols,\n            join_table_cols=join_table_cols,\n            isouter=isouter,\n        )(self.query)\n\n        return QueryInterface(self.database, query)\n\n    def ops(\n        self,\n        ops: qo.QueryOp | qo.Sequential,\n    ) -&gt; \"QueryInterface\":\n        \"\"\"Chain operations with the query.\n\n        Parameters\n        ----------\n        ops\n            Operations to perform on the query.\n\n        Returns\n        -------\n        QueryInterface\n            QueryInterface object with the operations added.\n\n        \"\"\"\n        query = ops(self.query)\n\n        return QueryInterface(self.database, query)\n\n    def union(\n        self,\n        other: \"QueryInterface\",\n    ) -&gt; \"QueryInterface\":\n        \"\"\"Union the query with another query.\n\n        Parameters\n        ----------\n        other\n            The other query to union with.\n\n        Returns\n        -------\n        QueryInterface\n            QueryInterface object with the union operation added.\n\n        \"\"\"\n        query = qo.Union(other.query)(self.query)\n\n        return QueryInterface(self.database, query)\n\n    def union_all(\n        self,\n        other: \"QueryInterface\",\n    ) -&gt; \"QueryInterface\":\n        \"\"\"Union all the query with another query.\n\n        Parameters\n        ----------\n        other\n            The other query to union all with.\n\n        Returns\n        -------\n        QueryInterface\n            QueryInterface object with the union all operation added.\n\n        \"\"\"\n        query = qo.Union(other.query, union_all=True)(self.query)\n\n        return QueryInterface(self.database, query)\n\n    def run(\n        self,\n        limit: int | None = None,\n        index_col: str | None = None,\n        batch_mode: bool = False,\n        batch_size: int = 1000000,\n        dtype_backend: str = \"pyarrow\",\n    ) -&gt; pd.DataFrame | Generator[pd.DataFrame, None, None]:\n        \"\"\"Run the query, and fetch data.\n\n        Parameters\n        ----------\n        limit\n            No. of rows to limit the query return.\n        index_col\n            Column which becomes the index, and defines the partitioning.\n            Should be a indexed column in the SQL server, and any orderable type.\n        batch_mode\n            Whether to run the query in batch mode. A generator is returned if True.\n        batch_size\n            Batch size for the query, default 1 million rows.\n        dtype_backend\n            Data type to use for the backend, default pyarrow.\n\n        Returns\n        -------\n        pandas.DataFrame or Generator[pandas.DataFrame, None, None]\n            Query result.\n\n        \"\"\"\n        if not batch_mode:\n            self._data = self.database.run_query(\n                self.query,\n                limit=limit,\n                index_col=index_col,\n                dtype_backend=dtype_backend,\n            )\n        else:\n            self._data = self.database.run_query_batch(\n                self.query,\n                index_col=index_col,\n                batch_size=batch_size,\n                dtype_backend=dtype_backend,\n            )\n\n        return self._data\n\n    def save(\n        self,\n        path: str,\n        file_format: Literal[\"parquet\", \"csv\"] = \"parquet\",\n    ) -&gt; str:\n        \"\"\"Save the query.\n\n        Parameters\n        ----------\n        path\n            Path where the file will be saved.\n        file_format\n            File format of the file to save.\n\n        Returns\n        -------\n        str\n            Processed save path for upstream use.\n\n        \"\"\"\n        # If the query was already run.\n        if self._data is not None:\n            if isinstance(self._data, pd.DataFrame):\n                return save_dataframe(self._data, path, file_format=file_format)\n            if isinstance(self._data, Generator):\n                for i, df in enumerate(self._data):\n                    save_dataframe(df, f\"{path}/batch-{i:03d}\", file_format=file_format)\n        # Save without running.\n        if file_format == \"csv\":\n            path = self.database.save_query_to_csv(self.query, path)\n        elif file_format == \"parquet\":\n            path = self.database.save_query_to_parquet(self.query, path)\n        else:\n            raise ValueError(\n                f\"Invalid file format '{file_format}'. Use 'parquet' or 'csv'.\"\n            )\n\n        return path\n\n    def clear_data(self) -&gt; None:\n        \"\"\"Clear data container.\n\n        Sets the data attribute to None, thus clearing the dataframe contained.\n\n        \"\"\"\n        self._data = None\n</code></pre>"},{"location":"api/#cycquery.interface.QueryInterface.data","title":"data  <code>property</code>","text":"<pre><code>data\n</code></pre> <p>Get data.</p>"},{"location":"api/#cycquery.interface.QueryInterface.__init__","title":"__init__","text":"<pre><code>__init__(database, query)\n</code></pre> <p>Initialize the QueryInterface object, join and chain operations.</p> Source code in <code>cycquery/interface.py</code> <pre><code>def __init__(\n    self,\n    database: Database,\n    query: TableTypes | \"QueryInterface\",\n) -&gt; None:\n    \"\"\"Initialize the QueryInterface object, join and chain operations.\"\"\"\n    self.database = database\n    if isinstance(query, QueryInterface):\n        self.query = query.query  # type: ignore\n    else:\n        self.query = query\n    self._data = None\n</code></pre>"},{"location":"api/#cycquery.interface.QueryInterface.join","title":"join","text":"<pre><code>join(\n    join_table,\n    on=None,\n    on_to_type=None,\n    cond=None,\n    table_cols=None,\n    join_table_cols=None,\n    isouter=False,\n)\n</code></pre> <p>Join the query with another table.</p> <p>Parameters:</p> Name Type Description Default <code>join_table</code> <code>TableTypes | 'QueryInterface'</code> <p>Table to join with.</p> required <code>on</code> <code>str | list[str] | Tuple[str] | list[Tuple[str, str]] | None</code> <p>Column(s) to join on.</p> <code>None</code> <code>on_to_type</code> <code>type | list[type] | None</code> <p>Type(s) to cast the column(s) to join on.</p> <code>None</code> <code>cond</code> <code>BinaryExpression | None</code> <p>Condition to join on.</p> <code>None</code> <code>table_cols</code> <code>str | list[str] | None</code> <p>Columns to select from the original table.</p> <code>None</code> <code>join_table_cols</code> <code>str | list[str] | None</code> <p>Columns to select from the joined table.</p> <code>None</code> <code>isouter</code> <code>bool | None</code> <p>Whether to perform an outer join.</p> <code>False</code> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>QueryInterface object with the join operation added.</p> Source code in <code>cycquery/interface.py</code> <pre><code>def join(\n    self,\n    join_table: TableTypes | \"QueryInterface\",\n    on: str | list[str] | Tuple[str] | list[Tuple[str, str]] | None = None,\n    on_to_type: type | list[type] | None = None,\n    cond: BinaryExpression | None = None,\n    table_cols: str | list[str] | None = None,\n    join_table_cols: str | list[str] | None = None,\n    isouter: bool | None = False,\n) -&gt; \"QueryInterface\":\n    \"\"\"Join the query with another table.\n\n    Parameters\n    ----------\n    join_table\n        Table to join with.\n    on\n        Column(s) to join on.\n    on_to_type\n        Type(s) to cast the column(s) to join on.\n    cond\n        Condition to join on.\n    table_cols\n        Columns to select from the original table.\n    join_table_cols\n        Columns to select from the joined table.\n    isouter\n        Whether to perform an outer join.\n\n    Returns\n    -------\n    QueryInterface\n        QueryInterface object with the join operation added.\n\n    \"\"\"\n    on = to_list_optional(on)\n    on_to_type = to_list_optional(on_to_type)\n    table_cols = to_list_optional(table_cols)\n    join_table_cols = to_list_optional(join_table_cols)\n    if isinstance(join_table, QueryInterface):\n        join_table = join_table.query\n    query = qo.Join(\n        join_table=join_table,\n        on=on,\n        on_to_type=on_to_type,\n        cond=cond,\n        table_cols=table_cols,\n        join_table_cols=join_table_cols,\n        isouter=isouter,\n    )(self.query)\n\n    return QueryInterface(self.database, query)\n</code></pre>"},{"location":"api/#cycquery.interface.QueryInterface.ops","title":"ops","text":"<pre><code>ops(ops)\n</code></pre> <p>Chain operations with the query.</p> <p>Parameters:</p> Name Type Description Default <code>ops</code> <code>QueryOp | Sequential</code> <p>Operations to perform on the query.</p> required <p>Returns:</p> Type Description <code>QueryInterface</code> <p>QueryInterface object with the operations added.</p> Source code in <code>cycquery/interface.py</code> <pre><code>def ops(\n    self,\n    ops: qo.QueryOp | qo.Sequential,\n) -&gt; \"QueryInterface\":\n    \"\"\"Chain operations with the query.\n\n    Parameters\n    ----------\n    ops\n        Operations to perform on the query.\n\n    Returns\n    -------\n    QueryInterface\n        QueryInterface object with the operations added.\n\n    \"\"\"\n    query = ops(self.query)\n\n    return QueryInterface(self.database, query)\n</code></pre>"},{"location":"api/#cycquery.interface.QueryInterface.union","title":"union","text":"<pre><code>union(other)\n</code></pre> <p>Union the query with another query.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>'QueryInterface'</code> <p>The other query to union with.</p> required <p>Returns:</p> Type Description <code>QueryInterface</code> <p>QueryInterface object with the union operation added.</p> Source code in <code>cycquery/interface.py</code> <pre><code>def union(\n    self,\n    other: \"QueryInterface\",\n) -&gt; \"QueryInterface\":\n    \"\"\"Union the query with another query.\n\n    Parameters\n    ----------\n    other\n        The other query to union with.\n\n    Returns\n    -------\n    QueryInterface\n        QueryInterface object with the union operation added.\n\n    \"\"\"\n    query = qo.Union(other.query)(self.query)\n\n    return QueryInterface(self.database, query)\n</code></pre>"},{"location":"api/#cycquery.interface.QueryInterface.union_all","title":"union_all","text":"<pre><code>union_all(other)\n</code></pre> <p>Union all the query with another query.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>'QueryInterface'</code> <p>The other query to union all with.</p> required <p>Returns:</p> Type Description <code>QueryInterface</code> <p>QueryInterface object with the union all operation added.</p> Source code in <code>cycquery/interface.py</code> <pre><code>def union_all(\n    self,\n    other: \"QueryInterface\",\n) -&gt; \"QueryInterface\":\n    \"\"\"Union all the query with another query.\n\n    Parameters\n    ----------\n    other\n        The other query to union all with.\n\n    Returns\n    -------\n    QueryInterface\n        QueryInterface object with the union all operation added.\n\n    \"\"\"\n    query = qo.Union(other.query, union_all=True)(self.query)\n\n    return QueryInterface(self.database, query)\n</code></pre>"},{"location":"api/#cycquery.interface.QueryInterface.run","title":"run","text":"<pre><code>run(\n    limit=None,\n    index_col=None,\n    batch_mode=False,\n    batch_size=1000000,\n    dtype_backend=\"pyarrow\",\n)\n</code></pre> <p>Run the query, and fetch data.</p> <p>Parameters:</p> Name Type Description Default <code>limit</code> <code>int | None</code> <p>No. of rows to limit the query return.</p> <code>None</code> <code>index_col</code> <code>str | None</code> <p>Column which becomes the index, and defines the partitioning. Should be a indexed column in the SQL server, and any orderable type.</p> <code>None</code> <code>batch_mode</code> <code>bool</code> <p>Whether to run the query in batch mode. A generator is returned if True.</p> <code>False</code> <code>batch_size</code> <code>int</code> <p>Batch size for the query, default 1 million rows.</p> <code>1000000</code> <code>dtype_backend</code> <code>str</code> <p>Data type to use for the backend, default pyarrow.</p> <code>'pyarrow'</code> <p>Returns:</p> Type Description <code>DataFrame or Generator[DataFrame, None, None]</code> <p>Query result.</p> Source code in <code>cycquery/interface.py</code> <pre><code>def run(\n    self,\n    limit: int | None = None,\n    index_col: str | None = None,\n    batch_mode: bool = False,\n    batch_size: int = 1000000,\n    dtype_backend: str = \"pyarrow\",\n) -&gt; pd.DataFrame | Generator[pd.DataFrame, None, None]:\n    \"\"\"Run the query, and fetch data.\n\n    Parameters\n    ----------\n    limit\n        No. of rows to limit the query return.\n    index_col\n        Column which becomes the index, and defines the partitioning.\n        Should be a indexed column in the SQL server, and any orderable type.\n    batch_mode\n        Whether to run the query in batch mode. A generator is returned if True.\n    batch_size\n        Batch size for the query, default 1 million rows.\n    dtype_backend\n        Data type to use for the backend, default pyarrow.\n\n    Returns\n    -------\n    pandas.DataFrame or Generator[pandas.DataFrame, None, None]\n        Query result.\n\n    \"\"\"\n    if not batch_mode:\n        self._data = self.database.run_query(\n            self.query,\n            limit=limit,\n            index_col=index_col,\n            dtype_backend=dtype_backend,\n        )\n    else:\n        self._data = self.database.run_query_batch(\n            self.query,\n            index_col=index_col,\n            batch_size=batch_size,\n            dtype_backend=dtype_backend,\n        )\n\n    return self._data\n</code></pre>"},{"location":"api/#cycquery.interface.QueryInterface.save","title":"save","text":"<pre><code>save(path, file_format='parquet')\n</code></pre> <p>Save the query.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Path where the file will be saved.</p> required <code>file_format</code> <code>Literal['parquet', 'csv']</code> <p>File format of the file to save.</p> <code>'parquet'</code> <p>Returns:</p> Type Description <code>str</code> <p>Processed save path for upstream use.</p> Source code in <code>cycquery/interface.py</code> <pre><code>def save(\n    self,\n    path: str,\n    file_format: Literal[\"parquet\", \"csv\"] = \"parquet\",\n) -&gt; str:\n    \"\"\"Save the query.\n\n    Parameters\n    ----------\n    path\n        Path where the file will be saved.\n    file_format\n        File format of the file to save.\n\n    Returns\n    -------\n    str\n        Processed save path for upstream use.\n\n    \"\"\"\n    # If the query was already run.\n    if self._data is not None:\n        if isinstance(self._data, pd.DataFrame):\n            return save_dataframe(self._data, path, file_format=file_format)\n        if isinstance(self._data, Generator):\n            for i, df in enumerate(self._data):\n                save_dataframe(df, f\"{path}/batch-{i:03d}\", file_format=file_format)\n    # Save without running.\n    if file_format == \"csv\":\n        path = self.database.save_query_to_csv(self.query, path)\n    elif file_format == \"parquet\":\n        path = self.database.save_query_to_parquet(self.query, path)\n    else:\n        raise ValueError(\n            f\"Invalid file format '{file_format}'. Use 'parquet' or 'csv'.\"\n        )\n\n    return path\n</code></pre>"},{"location":"api/#cycquery.interface.QueryInterface.clear_data","title":"clear_data","text":"<pre><code>clear_data()\n</code></pre> <p>Clear data container.</p> <p>Sets the data attribute to None, thus clearing the dataframe contained.</p> Source code in <code>cycquery/interface.py</code> <pre><code>def clear_data(self) -&gt; None:\n    \"\"\"Clear data container.\n\n    Sets the data attribute to None, thus clearing the dataframe contained.\n\n    \"\"\"\n    self._data = None\n</code></pre>"},{"location":"api/#operations-module","title":"Operations Module","text":""},{"location":"api/#cycquery.ops","title":"cycquery.ops","text":"<p>Query operations.</p>"},{"location":"api/#cycquery.ops.QueryOp","title":"QueryOp","text":"<p>Base class for query operations.</p> Source code in <code>cycquery/ops.py</code> <pre><code>class QueryOp:\n    \"\"\"Base class for query operations.\"\"\"\n\n    _ops: typing.Dict[str, \"QueryOp\"]\n\n    def __init__(self, *args: typing.Any, **kwargs: typing.Any) -&gt; None:\n        \"\"\"Initialize the class.\"\"\"\n        super().__setattr__(\"_ops\", OrderedDict())\n\n    @abstractmethod\n    def __call__(self, *args: typing.Any, **kwargs: typing.Any) -&gt; Subquery:\n        \"\"\"Implement a calling function.\"\"\"\n        pass\n\n    def _add_op(self, name: str, op_: \"QueryOp\") -&gt; None:\n        \"\"\"Add a child operation to the current query operation.\n\n        The query op can be accessed as an attribute using the given name.\n\n        Parameters\n        ----------\n        name\n            Name of the child op. The child op can be accessed from this op using\n            the given name\n        op_\n            Child op to be added to the parent query op.\n\n        \"\"\"\n        if not isinstance(op_, QueryOp) and op_ is not None:\n            raise TypeError(\"{} is not a QueryOp subclass\".format(str(op_)))\n        if not isinstance(name, str):\n            raise TypeError(\"Query op name should be a string\")\n        if hasattr(self, name) and name not in self._ops:\n            raise KeyError(\"Attribute '{}' already exists\".format(name))\n        if \".\" in name:\n            raise KeyError('Query op name can\\'t contain \".\", got: {}'.format(name))\n        if name == \"\":\n            raise KeyError('Query op name can\\'t be empty string \"\"')\n        self._ops[name] = op_\n\n    def _get_ops(self) -&gt; typing.Iterator[\"QueryOp\"]:\n        \"\"\"Return an iterator over the child operations.\n\n        Returns\n        -------\n        typing.Iterator[QueryOp]\n            Iterator over the child operations.\n\n        \"\"\"\n        for _, op_ in self._ops.items():\n            yield op_\n\n    def _get_name(self) -&gt; str:\n        \"\"\"Get the name of the query op.\n\n        Returns\n        -------\n        str\n            Name of the query op.\n\n        \"\"\"\n        return self.__class__.__name__\n\n    def __setattr__(self, name: str, value: \"QueryOp\") -&gt; None:\n        \"\"\"Set an attribute.\n\n        Parameters\n        ----------\n        name\n            Name of the attribute.\n        value\n            Value of the attribute.\n\n        \"\"\"\n        ops = self.__dict__.get(\"_ops\")\n        if isinstance(value, QueryOp):\n            if ops is None:\n                raise AttributeError(\"Can't assign op before QueryOp.__init__() call\")\n            ops[name] = value\n        elif ops is not None and name in ops:\n            if value is not None:\n                raise TypeError(\n                    \"Cannot assign '{}' as child op '{}' (QueryOp or None expected)\",\n                )\n            ops[name] = value\n        else:\n            super().__setattr__(name, value)\n\n    def _extra_repr(self) -&gt; str:\n        \"\"\"Set the extra representation of the query op.\n\n        To print customized extra information, you should re-implement\n        this method in your own query ops. Both single-line and multi-line\n        strings are acceptable.\n\n        Returns\n        -------\n        str\n            Extra representation of the query op.\n\n        \"\"\"\n        return \"\"\n\n    def __repr__(self) -&gt; str:\n        \"\"\"Return the string representation of the query op.\n\n        Returns\n        -------\n        str\n            String representation of the query op.\n\n        \"\"\"\n        extra_lines = []\n        extra_repr = self._extra_repr()\n        if extra_repr:\n            extra_lines = extra_repr.split(\"\\n\")\n        child_lines = []\n        for key, op_ in self._ops.items():\n            mod_str = repr(op_)\n            mod_str = _addindent(mod_str, 2)\n            child_lines.append(\"(\" + key + \"): \" + mod_str)\n        lines = extra_lines + child_lines\n        main_str = self._get_name() + \"(\"\n        if lines:\n            if len(extra_lines) == 1 and not child_lines:\n                main_str += extra_lines[0]\n            else:\n                main_str += \"\\n  \" + \"\\n  \".join(lines) + \"\\n\"\n        main_str += \")\"\n\n        return main_str\n\n    def __getattr__(self, name: str) -&gt; \"QueryOp\":\n        \"\"\"Get an attribute.\n\n        Parameters\n        ----------\n        name\n            Name of the attribute.\n\n        Returns\n        -------\n        QueryOp\n            The child operation with the given name.\n\n        \"\"\"\n        if name in self._ops:\n            return self._ops[name]\n        raise AttributeError(\n            f\"'{self.__class__.__name__}' object has no attribute '{name}'\",\n        )\n</code></pre>"},{"location":"api/#cycquery.ops.QueryOp.__init__","title":"__init__","text":"<pre><code>__init__(*args, **kwargs)\n</code></pre> <p>Initialize the class.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(self, *args: typing.Any, **kwargs: typing.Any) -&gt; None:\n    \"\"\"Initialize the class.\"\"\"\n    super().__setattr__(\"_ops\", OrderedDict())\n</code></pre>"},{"location":"api/#cycquery.ops.QueryOp.__call__","title":"__call__  <code>abstractmethod</code>","text":"<pre><code>__call__(*args, **kwargs)\n</code></pre> <p>Implement a calling function.</p> Source code in <code>cycquery/ops.py</code> <pre><code>@abstractmethod\ndef __call__(self, *args: typing.Any, **kwargs: typing.Any) -&gt; Subquery:\n    \"\"\"Implement a calling function.\"\"\"\n    pass\n</code></pre>"},{"location":"api/#cycquery.ops.QueryOp.__setattr__","title":"__setattr__","text":"<pre><code>__setattr__(name, value)\n</code></pre> <p>Set an attribute.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of the attribute.</p> required <code>value</code> <code>'QueryOp'</code> <p>Value of the attribute.</p> required Source code in <code>cycquery/ops.py</code> <pre><code>def __setattr__(self, name: str, value: \"QueryOp\") -&gt; None:\n    \"\"\"Set an attribute.\n\n    Parameters\n    ----------\n    name\n        Name of the attribute.\n    value\n        Value of the attribute.\n\n    \"\"\"\n    ops = self.__dict__.get(\"_ops\")\n    if isinstance(value, QueryOp):\n        if ops is None:\n            raise AttributeError(\"Can't assign op before QueryOp.__init__() call\")\n        ops[name] = value\n    elif ops is not None and name in ops:\n        if value is not None:\n            raise TypeError(\n                \"Cannot assign '{}' as child op '{}' (QueryOp or None expected)\",\n            )\n        ops[name] = value\n    else:\n        super().__setattr__(name, value)\n</code></pre>"},{"location":"api/#cycquery.ops.QueryOp.__repr__","title":"__repr__","text":"<pre><code>__repr__()\n</code></pre> <p>Return the string representation of the query op.</p> <p>Returns:</p> Type Description <code>str</code> <p>String representation of the query op.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __repr__(self) -&gt; str:\n    \"\"\"Return the string representation of the query op.\n\n    Returns\n    -------\n    str\n        String representation of the query op.\n\n    \"\"\"\n    extra_lines = []\n    extra_repr = self._extra_repr()\n    if extra_repr:\n        extra_lines = extra_repr.split(\"\\n\")\n    child_lines = []\n    for key, op_ in self._ops.items():\n        mod_str = repr(op_)\n        mod_str = _addindent(mod_str, 2)\n        child_lines.append(\"(\" + key + \"): \" + mod_str)\n    lines = extra_lines + child_lines\n    main_str = self._get_name() + \"(\"\n    if lines:\n        if len(extra_lines) == 1 and not child_lines:\n            main_str += extra_lines[0]\n        else:\n            main_str += \"\\n  \" + \"\\n  \".join(lines) + \"\\n\"\n    main_str += \")\"\n\n    return main_str\n</code></pre>"},{"location":"api/#cycquery.ops.QueryOp.__getattr__","title":"__getattr__","text":"<pre><code>__getattr__(name)\n</code></pre> <p>Get an attribute.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of the attribute.</p> required <p>Returns:</p> Type Description <code>QueryOp</code> <p>The child operation with the given name.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __getattr__(self, name: str) -&gt; \"QueryOp\":\n    \"\"\"Get an attribute.\n\n    Parameters\n    ----------\n    name\n        Name of the attribute.\n\n    Returns\n    -------\n    QueryOp\n        The child operation with the given name.\n\n    \"\"\"\n    if name in self._ops:\n        return self._ops[name]\n    raise AttributeError(\n        f\"'{self.__class__.__name__}' object has no attribute '{name}'\",\n    )\n</code></pre>"},{"location":"api/#cycquery.ops.Sequential","title":"Sequential","text":"<p>               Bases: <code>QueryOp</code></p> <p>Sequential query operations class.</p> <p>Chains a sequence of query operations and executes the final query on a table.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; Sequential(Drop([\"col1\", \"col2\"]), ...)\n&gt;&gt;&gt; Sequential([Drop([\"col1\", \"col2\"]), ...])\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class Sequential(QueryOp):\n    \"\"\"Sequential query operations class.\n\n    Chains a sequence of query operations and executes the final query on a table.\n\n    Examples\n    --------\n    &gt;&gt;&gt; Sequential(Drop([\"col1\", \"col2\"]), ...)\n    &gt;&gt;&gt; Sequential([Drop([\"col1\", \"col2\"]), ...])\n\n    \"\"\"\n\n    @typing.overload\n    def __init__(self, *ops: QueryOp) -&gt; None: ...\n\n    @typing.overload\n    def __init__(self, ops: typing.List[QueryOp]) -&gt; None: ...\n\n    @typing.overload\n    def __init__(self, op: OrderedDict[str, QueryOp]) -&gt; None: ...\n\n    def __init__(self, *args: QueryOp) -&gt; None:  # type: ignore\n        \"\"\"Initialize the class.\n\n        Parameters\n        ----------\n        args\n            Query operations to be chained sequentially.\n\n        \"\"\"\n        super().__init__()\n        if len(args) == 1 and isinstance(args[0], OrderedDict):\n            for key, op_ in args[0].items():\n                self._add_op(key, op_)\n        elif len(args) == 1 and isinstance(args[0], list):\n            for idx, op_ in enumerate(args[0]):\n                self._add_op(str(idx), op_)\n        else:\n            for idx, op_ in enumerate(args):\n                self._add_op(str(idx), op_)\n\n    def __len__(self) -&gt; int:\n        \"\"\"Return the number of query ops in the Sequential.\n\n        Returns\n        -------\n        int\n            Number of query ops in the Sequential.\n\n        \"\"\"\n        return len(self._ops)\n\n    def __iter__(self) -&gt; typing.Iterator[QueryOp]:\n        \"\"\"Return an iterator over the query ops.\n\n        Returns\n        -------\n        typing.Iterator[QueryOp]\n            Iterator over the query ops.\n\n        \"\"\"\n        return iter(self._ops.values())\n\n    def __add__(self, other: \"Sequential\") -&gt; \"Sequential\":\n        \"\"\"Add two Sequential objects.\n\n        Parameters\n        ----------\n        other\n            Sequential object to be added.\n\n        Returns\n        -------\n        Sequential\n            Sequential object with the two Sequential objects chained.\n\n        \"\"\"\n        if isinstance(other, Sequential):\n            ret = Sequential()\n            for op_ in self:\n                ret.append(op_)\n            for op_ in other:\n                ret.append(op_)\n            return ret\n        raise ValueError(\n            \"Add operator supports only objects \"\n            \"of Sequential class, but {} is given.\".format(str(type(other))),\n        )\n\n    def __iadd__(self, other: \"Sequential\") -&gt; \"Sequential\":\n        \"\"\"Add two Sequential objects inplace.\n\n        Parameters\n        ----------\n        other\n            Sequential object to be added.\n\n        Returns\n        -------\n        Sequential\n            Sequential object with the two Sequential objects chained.\n\n        \"\"\"\n        if isinstance(other, Sequential):\n            offset = len(self)\n            for i, op_ in enumerate(other):\n                self._add_op(str(i + offset), op_)\n            return self\n        raise ValueError(\n            \"Add operator supports only objects \"\n            \"of Sequential class, but {} is given.\".format(str(type(other))),\n        )\n\n    def _get_item_by_idx(\n        self,\n        iterator: typing.Iterator[typing.Any],\n        idx: int,\n    ) -&gt; typing.Any:\n        \"\"\"Get the idx-th item of the iterator.\n\n        Parameters\n        ----------\n        iterator\n            Iterator to get the item from.\n        idx\n            Index of the item to get.\n\n        Returns\n        -------\n        QueryOp\n            The idx-th item of the iterator.\n\n        \"\"\"\n        size = len(self)\n        idx = operator.index(idx)\n        if not -size &lt;= idx &lt; size:\n            raise IndexError(\"index {} is out of range\".format(idx))\n        idx %= size\n\n        return next(islice(iterator, idx, None))\n\n    def __getitem__(\n        self,\n        idx: typing.Union[slice, int],\n    ) -&gt; typing.Any:\n        \"\"\"Get the idx-th item of the sequential query op.\n\n        Parameters\n        ----------\n        idx\n            Index of the item to get.\n\n        Returns\n        -------\n        Sequential or QueryOp\n            The idx-th item of the sequential query op.\n\n        \"\"\"\n        if isinstance(idx, slice):\n            return self.__class__(OrderedDict(list(self._ops.items())[idx]))\n\n        return self._get_item_by_idx(self._ops.values(), idx)  # type: ignore\n\n    def __setitem__(self, idx: int, op_: QueryOp) -&gt; None:\n        \"\"\"Set the idx-th item of the sequential query op.\n\n        Parameters\n        ----------\n        idx\n            Index of the item to set.\n        op_\n            Query op to set.\n\n        \"\"\"\n        key: str = self._get_item_by_idx(self._ops.keys(), idx)  # type: ignore\n        return setattr(self, key, op_)\n\n    def __delitem__(self, idx: typing.Union[slice, int]) -&gt; None:\n        \"\"\"Delete the idx-th item of the sequential query op.\n\n        Parameters\n        ----------\n        idx\n            Index of the item to delete.\n\n        \"\"\"\n        if isinstance(idx, slice):\n            for key in list(self._ops.keys())[idx]:\n                delattr(self, key)\n        else:\n            key = self._get_item_by_idx(self._ops.keys(), idx)  # type: ignore\n            delattr(self, key)\n        str_indices = [str(i) for i in range(len(self._ops))]\n        self._ops = OrderedDict(list(zip(str_indices, self._ops.values())))\n\n    def append(self, op_: QueryOp) -&gt; \"Sequential\":\n        \"\"\"Append a given query op to the end.\n\n        Parameters\n        ----------\n        op_\n            Query op to append.\n\n        Returns\n        -------\n        Sequential\n            Sequential object with the query op appended.\n\n        \"\"\"\n        self._add_op(str(len(self)), op_)\n        return self\n\n    def pop(self, key: typing.Union[int, slice]) -&gt; QueryOp:\n        \"\"\"Pop the query op at the given index.\n\n        Parameters\n        ----------\n        key\n            Index of the query op to pop.\n\n        Returns\n        -------\n        QueryOp\n            Popped query op.\n\n        \"\"\"\n        v = self[key]\n        del self[key]\n\n        return v\n\n    def insert(self, index: int, op_: QueryOp) -&gt; \"Sequential\":\n        \"\"\"Insert a given query op at the given index.\n\n        Parameters\n        ----------\n        index\n            Index to insert the query op at.\n        op_\n            Query op to insert.\n\n        Returns\n        -------\n        Sequential\n            Sequential object with the query op inserted.\n\n        \"\"\"\n        if not isinstance(op_, QueryOp):\n            raise AssertionError(\"Module should be of type: {}\".format(QueryOp))\n        n = len(self._ops)\n        if not (-n &lt;= index &lt;= n):\n            raise IndexError(\"Index out of range: {}\".format(index))\n        if index &lt; 0:\n            index += n\n        for i in range(n, index, -1):\n            self._ops[str(i)] = self._ops[str(i - 1)]\n        self._ops[str(index)] = op_\n\n        return self\n\n    def extend(self, sequential: \"Sequential\") -&gt; \"Sequential\":\n        \"\"\"Extend the sequential query op with another sequential query op.\n\n        Parameters\n        ----------\n        sequential\n            Sequential object to extend with.\n\n        Returns\n        -------\n        Sequential\n            Sequential object with the other sequential query op extended.\n\n        \"\"\"\n        for op_ in sequential:\n            self.append(op_)\n\n        return self\n\n    @table_params_to_type(Subquery)  # type: ignore[arg-type]\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Execute the query operations on the table.\n\n        Parameters\n        ----------\n        table\n            Table to be queried.\n\n        Returns\n        -------\n        Subquery\n            Query result after chaining the query operations.\n\n        \"\"\"\n        return _chain_ops(table, self._get_ops())  # type: ignore[arg-type]\n</code></pre>"},{"location":"api/#cycquery.ops.Sequential.__init__","title":"__init__","text":"<pre><code>__init__(*ops: QueryOp) -&gt; None\n</code></pre><pre><code>__init__(ops: List[QueryOp]) -&gt; None\n</code></pre><pre><code>__init__(op: OrderedDict[str, QueryOp]) -&gt; None\n</code></pre> <pre><code>__init__(*args)\n</code></pre> <p>Initialize the class.</p> <p>Parameters:</p> Name Type Description Default <code>args</code> <code>QueryOp</code> <p>Query operations to be chained sequentially.</p> <code>()</code> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(self, *args: QueryOp) -&gt; None:  # type: ignore\n    \"\"\"Initialize the class.\n\n    Parameters\n    ----------\n    args\n        Query operations to be chained sequentially.\n\n    \"\"\"\n    super().__init__()\n    if len(args) == 1 and isinstance(args[0], OrderedDict):\n        for key, op_ in args[0].items():\n            self._add_op(key, op_)\n    elif len(args) == 1 and isinstance(args[0], list):\n        for idx, op_ in enumerate(args[0]):\n            self._add_op(str(idx), op_)\n    else:\n        for idx, op_ in enumerate(args):\n            self._add_op(str(idx), op_)\n</code></pre>"},{"location":"api/#cycquery.ops.Sequential.__len__","title":"__len__","text":"<pre><code>__len__()\n</code></pre> <p>Return the number of query ops in the Sequential.</p> <p>Returns:</p> Type Description <code>int</code> <p>Number of query ops in the Sequential.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __len__(self) -&gt; int:\n    \"\"\"Return the number of query ops in the Sequential.\n\n    Returns\n    -------\n    int\n        Number of query ops in the Sequential.\n\n    \"\"\"\n    return len(self._ops)\n</code></pre>"},{"location":"api/#cycquery.ops.Sequential.__iter__","title":"__iter__","text":"<pre><code>__iter__()\n</code></pre> <p>Return an iterator over the query ops.</p> <p>Returns:</p> Type Description <code>Iterator[QueryOp]</code> <p>Iterator over the query ops.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __iter__(self) -&gt; typing.Iterator[QueryOp]:\n    \"\"\"Return an iterator over the query ops.\n\n    Returns\n    -------\n    typing.Iterator[QueryOp]\n        Iterator over the query ops.\n\n    \"\"\"\n    return iter(self._ops.values())\n</code></pre>"},{"location":"api/#cycquery.ops.Sequential.__add__","title":"__add__","text":"<pre><code>__add__(other)\n</code></pre> <p>Add two Sequential objects.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>'Sequential'</code> <p>Sequential object to be added.</p> required <p>Returns:</p> Type Description <code>Sequential</code> <p>Sequential object with the two Sequential objects chained.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __add__(self, other: \"Sequential\") -&gt; \"Sequential\":\n    \"\"\"Add two Sequential objects.\n\n    Parameters\n    ----------\n    other\n        Sequential object to be added.\n\n    Returns\n    -------\n    Sequential\n        Sequential object with the two Sequential objects chained.\n\n    \"\"\"\n    if isinstance(other, Sequential):\n        ret = Sequential()\n        for op_ in self:\n            ret.append(op_)\n        for op_ in other:\n            ret.append(op_)\n        return ret\n    raise ValueError(\n        \"Add operator supports only objects \"\n        \"of Sequential class, but {} is given.\".format(str(type(other))),\n    )\n</code></pre>"},{"location":"api/#cycquery.ops.Sequential.__iadd__","title":"__iadd__","text":"<pre><code>__iadd__(other)\n</code></pre> <p>Add two Sequential objects inplace.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>'Sequential'</code> <p>Sequential object to be added.</p> required <p>Returns:</p> Type Description <code>Sequential</code> <p>Sequential object with the two Sequential objects chained.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __iadd__(self, other: \"Sequential\") -&gt; \"Sequential\":\n    \"\"\"Add two Sequential objects inplace.\n\n    Parameters\n    ----------\n    other\n        Sequential object to be added.\n\n    Returns\n    -------\n    Sequential\n        Sequential object with the two Sequential objects chained.\n\n    \"\"\"\n    if isinstance(other, Sequential):\n        offset = len(self)\n        for i, op_ in enumerate(other):\n            self._add_op(str(i + offset), op_)\n        return self\n    raise ValueError(\n        \"Add operator supports only objects \"\n        \"of Sequential class, but {} is given.\".format(str(type(other))),\n    )\n</code></pre>"},{"location":"api/#cycquery.ops.Sequential.__getitem__","title":"__getitem__","text":"<pre><code>__getitem__(idx)\n</code></pre> <p>Get the idx-th item of the sequential query op.</p> <p>Parameters:</p> Name Type Description Default <code>idx</code> <code>Union[slice, int]</code> <p>Index of the item to get.</p> required <p>Returns:</p> Type Description <code>Sequential or QueryOp</code> <p>The idx-th item of the sequential query op.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __getitem__(\n    self,\n    idx: typing.Union[slice, int],\n) -&gt; typing.Any:\n    \"\"\"Get the idx-th item of the sequential query op.\n\n    Parameters\n    ----------\n    idx\n        Index of the item to get.\n\n    Returns\n    -------\n    Sequential or QueryOp\n        The idx-th item of the sequential query op.\n\n    \"\"\"\n    if isinstance(idx, slice):\n        return self.__class__(OrderedDict(list(self._ops.items())[idx]))\n\n    return self._get_item_by_idx(self._ops.values(), idx)  # type: ignore\n</code></pre>"},{"location":"api/#cycquery.ops.Sequential.__setitem__","title":"__setitem__","text":"<pre><code>__setitem__(idx, op_)\n</code></pre> <p>Set the idx-th item of the sequential query op.</p> <p>Parameters:</p> Name Type Description Default <code>idx</code> <code>int</code> <p>Index of the item to set.</p> required <code>op_</code> <code>QueryOp</code> <p>Query op to set.</p> required Source code in <code>cycquery/ops.py</code> <pre><code>def __setitem__(self, idx: int, op_: QueryOp) -&gt; None:\n    \"\"\"Set the idx-th item of the sequential query op.\n\n    Parameters\n    ----------\n    idx\n        Index of the item to set.\n    op_\n        Query op to set.\n\n    \"\"\"\n    key: str = self._get_item_by_idx(self._ops.keys(), idx)  # type: ignore\n    return setattr(self, key, op_)\n</code></pre>"},{"location":"api/#cycquery.ops.Sequential.__delitem__","title":"__delitem__","text":"<pre><code>__delitem__(idx)\n</code></pre> <p>Delete the idx-th item of the sequential query op.</p> <p>Parameters:</p> Name Type Description Default <code>idx</code> <code>Union[slice, int]</code> <p>Index of the item to delete.</p> required Source code in <code>cycquery/ops.py</code> <pre><code>def __delitem__(self, idx: typing.Union[slice, int]) -&gt; None:\n    \"\"\"Delete the idx-th item of the sequential query op.\n\n    Parameters\n    ----------\n    idx\n        Index of the item to delete.\n\n    \"\"\"\n    if isinstance(idx, slice):\n        for key in list(self._ops.keys())[idx]:\n            delattr(self, key)\n    else:\n        key = self._get_item_by_idx(self._ops.keys(), idx)  # type: ignore\n        delattr(self, key)\n    str_indices = [str(i) for i in range(len(self._ops))]\n    self._ops = OrderedDict(list(zip(str_indices, self._ops.values())))\n</code></pre>"},{"location":"api/#cycquery.ops.Sequential.append","title":"append","text":"<pre><code>append(op_)\n</code></pre> <p>Append a given query op to the end.</p> <p>Parameters:</p> Name Type Description Default <code>op_</code> <code>QueryOp</code> <p>Query op to append.</p> required <p>Returns:</p> Type Description <code>Sequential</code> <p>Sequential object with the query op appended.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def append(self, op_: QueryOp) -&gt; \"Sequential\":\n    \"\"\"Append a given query op to the end.\n\n    Parameters\n    ----------\n    op_\n        Query op to append.\n\n    Returns\n    -------\n    Sequential\n        Sequential object with the query op appended.\n\n    \"\"\"\n    self._add_op(str(len(self)), op_)\n    return self\n</code></pre>"},{"location":"api/#cycquery.ops.Sequential.pop","title":"pop","text":"<pre><code>pop(key)\n</code></pre> <p>Pop the query op at the given index.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>Union[int, slice]</code> <p>Index of the query op to pop.</p> required <p>Returns:</p> Type Description <code>QueryOp</code> <p>Popped query op.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def pop(self, key: typing.Union[int, slice]) -&gt; QueryOp:\n    \"\"\"Pop the query op at the given index.\n\n    Parameters\n    ----------\n    key\n        Index of the query op to pop.\n\n    Returns\n    -------\n    QueryOp\n        Popped query op.\n\n    \"\"\"\n    v = self[key]\n    del self[key]\n\n    return v\n</code></pre>"},{"location":"api/#cycquery.ops.Sequential.insert","title":"insert","text":"<pre><code>insert(index, op_)\n</code></pre> <p>Insert a given query op at the given index.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>int</code> <p>Index to insert the query op at.</p> required <code>op_</code> <code>QueryOp</code> <p>Query op to insert.</p> required <p>Returns:</p> Type Description <code>Sequential</code> <p>Sequential object with the query op inserted.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def insert(self, index: int, op_: QueryOp) -&gt; \"Sequential\":\n    \"\"\"Insert a given query op at the given index.\n\n    Parameters\n    ----------\n    index\n        Index to insert the query op at.\n    op_\n        Query op to insert.\n\n    Returns\n    -------\n    Sequential\n        Sequential object with the query op inserted.\n\n    \"\"\"\n    if not isinstance(op_, QueryOp):\n        raise AssertionError(\"Module should be of type: {}\".format(QueryOp))\n    n = len(self._ops)\n    if not (-n &lt;= index &lt;= n):\n        raise IndexError(\"Index out of range: {}\".format(index))\n    if index &lt; 0:\n        index += n\n    for i in range(n, index, -1):\n        self._ops[str(i)] = self._ops[str(i - 1)]\n    self._ops[str(index)] = op_\n\n    return self\n</code></pre>"},{"location":"api/#cycquery.ops.Sequential.extend","title":"extend","text":"<pre><code>extend(sequential)\n</code></pre> <p>Extend the sequential query op with another sequential query op.</p> <p>Parameters:</p> Name Type Description Default <code>sequential</code> <code>'Sequential'</code> <p>Sequential object to extend with.</p> required <p>Returns:</p> Type Description <code>Sequential</code> <p>Sequential object with the other sequential query op extended.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def extend(self, sequential: \"Sequential\") -&gt; \"Sequential\":\n    \"\"\"Extend the sequential query op with another sequential query op.\n\n    Parameters\n    ----------\n    sequential\n        Sequential object to extend with.\n\n    Returns\n    -------\n    Sequential\n        Sequential object with the other sequential query op extended.\n\n    \"\"\"\n    for op_ in sequential:\n        self.append(op_)\n\n    return self\n</code></pre>"},{"location":"api/#cycquery.ops.Sequential.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Execute the query operations on the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table to be queried.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Query result after chaining the query operations.</p> Source code in <code>cycquery/ops.py</code> <pre><code>@table_params_to_type(Subquery)  # type: ignore[arg-type]\ndef __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Execute the query operations on the table.\n\n    Parameters\n    ----------\n    table\n        Table to be queried.\n\n    Returns\n    -------\n    Subquery\n        Query result after chaining the query operations.\n\n    \"\"\"\n    return _chain_ops(table, self._get_ops())  # type: ignore[arg-type]\n</code></pre>"},{"location":"api/#cycquery.ops.FillNull","title":"FillNull","text":"<p>               Bases: <code>QueryOp</code></p> <p>Fill NULL values with a given value.</p> <p>Parameters:</p> Name Type Description Default <code>cols</code> <code>Union[str, List[str]]</code> <p>Columns to fill.</p> required <code>fill_values</code> <code>Union[Any, List[Any]]</code> <p>Value(s) to fill with.</p> required <code>new_col_names</code> <code>Optional[Union[str, List[str]]]</code> <p>New column name(s) for the filled columns. If not provided,</p> <code>None</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; FillNull(\"col1\", 0)(table)\n&gt;&gt;&gt; FillNull([\"col1\", \"col2\"], [0, 1])(table)\n&gt;&gt;&gt; FillNull([\"col1\", \"col2\"], [0, 1], [\"col1_new\", \"col2_new\"])(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class FillNull(QueryOp):\n    \"\"\"Fill NULL values with a given value.\n\n    Parameters\n    ----------\n    cols\n        Columns to fill.\n    fill_values\n        Value(s) to fill with.\n    new_col_names\n        New column name(s) for the filled columns. If not provided,\n\n    Examples\n    --------\n    &gt;&gt;&gt; FillNull(\"col1\", 0)(table)\n    &gt;&gt;&gt; FillNull([\"col1\", \"col2\"], [0, 1])(table)\n    &gt;&gt;&gt; FillNull([\"col1\", \"col2\"], [0, 1], [\"col1_new\", \"col2_new\"])(table)\n\n    \"\"\"\n\n    def __init__(\n        self,\n        cols: typing.Union[str, typing.List[str]],\n        fill_values: typing.Union[typing.Any, typing.List[typing.Any]],\n        new_col_names: typing.Optional[typing.Union[str, typing.List[str]]] = None,\n    ) -&gt; None:\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.cols = cols\n        self.fill_values = fill_values\n        self.new_col_names = new_col_names\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Fill NULL values with a given value.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        Subquery\n            Table with NULL values filled.\n\n        \"\"\"\n        cols = to_list(self.cols)\n        fill_values = to_list(self.fill_values)\n        new_col_names = to_list_optional(self.new_col_names)\n        if new_col_names and len(cols) != len(new_col_names):\n            raise ValueError(\n                \"\"\"Number of columns to fill and number of new column names\n                    must match.\"\"\",\n            )\n        table = _process_checks(table, cols=self.cols)\n        if len(fill_values) == 1:\n            fill_values = fill_values * len(cols)\n\n        # Build coalesced expressions map for all columns at once (avoids N nested subqueries)\n        coalesce_map = {\n            col: func.coalesce(table.c[col], fill)\n            for col, fill in zip(cols, fill_values)\n        }\n\n        if new_col_names:\n            # Keep all original columns and add new coalesced columns\n            new_col_map = dict(zip(cols, new_col_names))\n            result_cols = list(table.columns) + [\n                coalesce_map[col].label(new_col_map[col]) for col in cols\n            ]\n        else:\n            # Replace original columns with coalesced versions (same names)\n            result_cols = [\n                coalesce_map[c.name].label(c.name) if c.name in coalesce_map else c\n                for c in table.columns\n            ]\n\n        return select(*result_cols).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.FillNull.__init__","title":"__init__","text":"<pre><code>__init__(cols, fill_values, new_col_names=None)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    cols: typing.Union[str, typing.List[str]],\n    fill_values: typing.Union[typing.Any, typing.List[typing.Any]],\n    new_col_names: typing.Optional[typing.Union[str, typing.List[str]]] = None,\n) -&gt; None:\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.cols = cols\n    self.fill_values = fill_values\n    self.new_col_names = new_col_names\n</code></pre>"},{"location":"api/#cycquery.ops.FillNull.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Fill NULL values with a given value.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Table with NULL values filled.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Fill NULL values with a given value.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    Subquery\n        Table with NULL values filled.\n\n    \"\"\"\n    cols = to_list(self.cols)\n    fill_values = to_list(self.fill_values)\n    new_col_names = to_list_optional(self.new_col_names)\n    if new_col_names and len(cols) != len(new_col_names):\n        raise ValueError(\n            \"\"\"Number of columns to fill and number of new column names\n                must match.\"\"\",\n        )\n    table = _process_checks(table, cols=self.cols)\n    if len(fill_values) == 1:\n        fill_values = fill_values * len(cols)\n\n    # Build coalesced expressions map for all columns at once (avoids N nested subqueries)\n    coalesce_map = {\n        col: func.coalesce(table.c[col], fill)\n        for col, fill in zip(cols, fill_values)\n    }\n\n    if new_col_names:\n        # Keep all original columns and add new coalesced columns\n        new_col_map = dict(zip(cols, new_col_names))\n        result_cols = list(table.columns) + [\n            coalesce_map[col].label(new_col_map[col]) for col in cols\n        ]\n    else:\n        # Replace original columns with coalesced versions (same names)\n        result_cols = [\n            coalesce_map[c.name].label(c.name) if c.name in coalesce_map else c\n            for c in table.columns\n        ]\n\n    return select(*result_cols).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.Drop","title":"Drop","text":"<p>               Bases: <code>QueryOp</code></p> <p>Drop some columns.</p> <p>Parameters:</p> Name Type Description Default <code>cols</code> <code>Union[str, List[str]]</code> <p>Columns to drop.</p> required <p>Examples:</p> <pre><code>&gt;&gt;&gt; Drop(\"col1\")(table)\n&gt;&gt;&gt; Drop([\"col1\", \"col2\"])(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class Drop(QueryOp):\n    \"\"\"Drop some columns.\n\n    Parameters\n    ----------\n    cols\n        Columns to drop.\n\n    Examples\n    --------\n    &gt;&gt;&gt; Drop(\"col1\")(table)\n    &gt;&gt;&gt; Drop([\"col1\", \"col2\"])(table)\n\n    \"\"\"\n\n    def __init__(self, cols: typing.Union[str, typing.List[str]]) -&gt; None:\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.cols = cols\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        table = _process_checks(table, cols=self.cols)\n\n        return drop_columns(table, self.cols)\n</code></pre>"},{"location":"api/#cycquery.ops.Drop.__init__","title":"__init__","text":"<pre><code>__init__(cols)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(self, cols: typing.Union[str, typing.List[str]]) -&gt; None:\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.cols = cols\n</code></pre>"},{"location":"api/#cycquery.ops.Drop.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    table = _process_checks(table, cols=self.cols)\n\n    return drop_columns(table, self.cols)\n</code></pre>"},{"location":"api/#cycquery.ops.Rename","title":"Rename","text":"<p>               Bases: <code>QueryOp</code></p> <p>Rename some columns.</p> <p>Parameters:</p> Name Type Description Default <code>rename_map</code> <code>Dict[str, str]</code> <p>Map from an existing column name to another name.</p> required <code>check_exists</code> <code>bool</code> <p>Whether to check if all of the keys in the map exist as columns.</p> <code>True</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; Rename({\"col1\": \"col1_new\"})(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class Rename(QueryOp):\n    \"\"\"Rename some columns.\n\n    Parameters\n    ----------\n    rename_map\n        Map from an existing column name to another name.\n    check_exists\n        Whether to check if all of the keys in the map exist as columns.\n\n    Examples\n    --------\n    &gt;&gt;&gt; Rename({\"col1\": \"col1_new\"})(table)\n\n    \"\"\"\n\n    def __init__(self, rename_map: typing.Dict[str, str], check_exists: bool = True):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.rename_map = rename_map\n        self.check_exists = check_exists\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        if self.check_exists:\n            table = _process_checks(table, cols=list(self.rename_map.keys()))\n\n        return rename_columns(table, self.rename_map)\n</code></pre>"},{"location":"api/#cycquery.ops.Rename.__init__","title":"__init__","text":"<pre><code>__init__(rename_map, check_exists=True)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(self, rename_map: typing.Dict[str, str], check_exists: bool = True):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.rename_map = rename_map\n    self.check_exists = check_exists\n</code></pre>"},{"location":"api/#cycquery.ops.Rename.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    if self.check_exists:\n        table = _process_checks(table, cols=list(self.rename_map.keys()))\n\n    return rename_columns(table, self.rename_map)\n</code></pre>"},{"location":"api/#cycquery.ops.Substring","title":"Substring","text":"<p>               Bases: <code>QueryOp</code></p> <p>Get substring of a string column.</p> <p>Parameters:</p> Name Type Description Default <code>col</code> <code>str</code> <p>Name of column which has string, where substring needs to be extracted.</p> required <code>start_index</code> <code>int</code> <p>Start index of substring.</p> required <code>stop_index</code> <code>int</code> <p>Stop index of substring.</p> required <code>new_col_label</code> <code>Optional[str]</code> <p>Name of the new column with extracted substring.</p> <code>None</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; Substring(\"col1\", 0, 2, \"col1_substring\")(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class Substring(QueryOp):\n    \"\"\"Get substring of a string column.\n\n    Parameters\n    ----------\n    col\n        Name of column which has string, where substring needs\n        to be extracted.\n    start_index\n        Start index of substring.\n    stop_index\n        Stop index of substring.\n    new_col_label\n        Name of the new column with extracted substring.\n\n    Examples\n    --------\n    &gt;&gt;&gt; Substring(\"col1\", 0, 2, \"col1_substring\")(table)\n\n    \"\"\"\n\n    def __init__(\n        self,\n        col: str,\n        start_index: int,\n        stop_index: int,\n        new_col_label: typing.Optional[str] = None,\n    ):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.col = col\n        self.start_index = start_index\n        self.stop_index = stop_index\n        self.new_col_label = new_col_label\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        table = _process_checks(table, cols=self.col, cols_not_in=self.new_col_label)\n\n        return apply_to_columns(\n            table,\n            self.col,\n            lambda x: func.substr(\n                process_column(x, to_str=True),\n                self.start_index,\n                self.stop_index,\n            ),\n            new_col_labels=self.new_col_label,\n        )\n</code></pre>"},{"location":"api/#cycquery.ops.Substring.__init__","title":"__init__","text":"<pre><code>__init__(col, start_index, stop_index, new_col_label=None)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    col: str,\n    start_index: int,\n    stop_index: int,\n    new_col_label: typing.Optional[str] = None,\n):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.col = col\n    self.start_index = start_index\n    self.stop_index = stop_index\n    self.new_col_label = new_col_label\n</code></pre>"},{"location":"api/#cycquery.ops.Substring.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    table = _process_checks(table, cols=self.col, cols_not_in=self.new_col_label)\n\n    return apply_to_columns(\n        table,\n        self.col,\n        lambda x: func.substr(\n            process_column(x, to_str=True),\n            self.start_index,\n            self.stop_index,\n        ),\n        new_col_labels=self.new_col_label,\n    )\n</code></pre>"},{"location":"api/#cycquery.ops.Reorder","title":"Reorder","text":"<p>               Bases: <code>QueryOp</code></p> <p>Reorder the columns in a table.</p> <p>Parameters:</p> Name Type Description Default <code>cols</code> <code>List[str]</code> <p>Complete list of table column names in the new order.</p> required <p>Examples:</p> <pre><code>&gt;&gt;&gt; Reorder([\"col2\", \"col1\"])(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class Reorder(QueryOp):\n    \"\"\"Reorder the columns in a table.\n\n    Parameters\n    ----------\n    cols\n        Complete list of table column names in the new order.\n\n    Examples\n    --------\n    &gt;&gt;&gt; Reorder([\"col2\", \"col1\"])(table)\n\n    \"\"\"\n\n    def __init__(self, cols: typing.List[str]):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.cols = cols\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        table = _process_checks(table, cols=self.cols)\n\n        return reorder_columns(table, self.cols)\n</code></pre>"},{"location":"api/#cycquery.ops.Reorder.__init__","title":"__init__","text":"<pre><code>__init__(cols)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(self, cols: typing.List[str]):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.cols = cols\n</code></pre>"},{"location":"api/#cycquery.ops.Reorder.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    table = _process_checks(table, cols=self.cols)\n\n    return reorder_columns(table, self.cols)\n</code></pre>"},{"location":"api/#cycquery.ops.ReorderAfter","title":"ReorderAfter","text":"<p>               Bases: <code>QueryOp</code></p> <p>Reorder a number of columns to come after a specified column.</p> <p>Parameters:</p> Name Type Description Default <code>cols</code> <code>Union[str, List[str]]</code> <p>Ordered list of column names which will come after a specified column.</p> required <code>after</code> <code>str</code> <p>Column name for the column after which the other columns will follow.</p> required <p>Examples:</p> <pre><code>&gt;&gt;&gt; ReorderAfter([\"col2\", \"col1\"], \"col3\")(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class ReorderAfter(QueryOp):\n    \"\"\"Reorder a number of columns to come after a specified column.\n\n    Parameters\n    ----------\n    cols\n        Ordered list of column names which will come after a specified column.\n    after\n        Column name for the column after which the other columns will follow.\n\n    Examples\n    --------\n    &gt;&gt;&gt; ReorderAfter([\"col2\", \"col1\"], \"col3\")(table)\n\n    \"\"\"\n\n    def __init__(self, cols: typing.Union[str, typing.List[str]], after: str):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.cols = cols\n        self.after = after\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        self.cols = to_list(self.cols)\n        table = _process_checks(table, cols=self.cols + [self.after])\n        names = get_column_names(table)\n        names = [name for name in names if name not in self.cols]\n        name_after_ind = names.index(self.after) + 1\n        new_order = names[:name_after_ind] + self.cols + names[name_after_ind:]\n\n        return Reorder(new_order)(table)\n</code></pre>"},{"location":"api/#cycquery.ops.ReorderAfter.__init__","title":"__init__","text":"<pre><code>__init__(cols, after)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(self, cols: typing.Union[str, typing.List[str]], after: str):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.cols = cols\n    self.after = after\n</code></pre>"},{"location":"api/#cycquery.ops.ReorderAfter.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    self.cols = to_list(self.cols)\n    table = _process_checks(table, cols=self.cols + [self.after])\n    names = get_column_names(table)\n    names = [name for name in names if name not in self.cols]\n    name_after_ind = names.index(self.after) + 1\n    new_order = names[:name_after_ind] + self.cols + names[name_after_ind:]\n\n    return Reorder(new_order)(table)\n</code></pre>"},{"location":"api/#cycquery.ops.Keep","title":"Keep","text":"<p>               Bases: <code>QueryOp</code></p> <p>Keep only the specified columns in a table.</p> <p>Parameters:</p> Name Type Description Default <code>cols</code> <code>Union[str, List[str]]</code> <p>The columns to keep.</p> required <p>Examples:</p> <pre><code>&gt;&gt;&gt; Keep(\"col1\")(table)\n&gt;&gt;&gt; Keep([\"col1\", \"col2\"])(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class Keep(QueryOp):\n    \"\"\"Keep only the specified columns in a table.\n\n    Parameters\n    ----------\n    cols\n        The columns to keep.\n\n    Examples\n    --------\n    &gt;&gt;&gt; Keep(\"col1\")(table)\n    &gt;&gt;&gt; Keep([\"col1\", \"col2\"])(table)\n\n    \"\"\"\n\n    def __init__(self, cols: typing.Union[str, typing.List[str]]):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.cols = cols\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        table = _process_checks(table, cols=self.cols)\n\n        return filter_columns(table, self.cols)\n</code></pre>"},{"location":"api/#cycquery.ops.Keep.__init__","title":"__init__","text":"<pre><code>__init__(cols)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(self, cols: typing.Union[str, typing.List[str]]):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.cols = cols\n</code></pre>"},{"location":"api/#cycquery.ops.Keep.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    table = _process_checks(table, cols=self.cols)\n\n    return filter_columns(table, self.cols)\n</code></pre>"},{"location":"api/#cycquery.ops.Trim","title":"Trim","text":"<p>               Bases: <code>QueryOp</code></p> <p>Trim the whitespace from some string columns.</p> <p>Parameters:</p> Name Type Description Default <code>cols</code> <code>Union[str, List[str]]</code> <p>Columns to trim.</p> required <code>new_col_labels</code> <code>Optional[Union[str, List[str]]]</code> <p>If specified, create new columns with these labels. Otherwise, apply the function to the existing columns.</p> <code>None</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; Trim(\"col1\")(table)\n&gt;&gt;&gt; Trim([\"col1\", \"col2\"])(table)\n&gt;&gt;&gt; Trim(\"col1\", \"col1_trimmed\")(table)\n&gt;&gt;&gt; Trim([\"col1\", \"col2\"], [\"col1_trimmed\", \"col2_trimmed\"])(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class Trim(QueryOp):\n    \"\"\"Trim the whitespace from some string columns.\n\n    Parameters\n    ----------\n    cols\n        Columns to trim.\n    new_col_labels\n        If specified, create new columns with these labels. Otherwise,\n        apply the function to the existing columns.\n\n    Examples\n    --------\n    &gt;&gt;&gt; Trim(\"col1\")(table)\n    &gt;&gt;&gt; Trim([\"col1\", \"col2\"])(table)\n    &gt;&gt;&gt; Trim(\"col1\", \"col1_trimmed\")(table)\n    &gt;&gt;&gt; Trim([\"col1\", \"col2\"], [\"col1_trimmed\", \"col2_trimmed\"])(table)\n\n    \"\"\"\n\n    def __init__(\n        self,\n        cols: typing.Union[str, typing.List[str]],\n        new_col_labels: typing.Optional[typing.Union[str, typing.List[str]]] = None,\n    ):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.cols = cols\n        self.new_col_labels = new_col_labels\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        table = _process_checks(table, cols=self.cols)\n\n        return trim_columns(table, self.cols, new_col_labels=self.new_col_labels)\n</code></pre>"},{"location":"api/#cycquery.ops.Trim.__init__","title":"__init__","text":"<pre><code>__init__(cols, new_col_labels=None)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    cols: typing.Union[str, typing.List[str]],\n    new_col_labels: typing.Optional[typing.Union[str, typing.List[str]]] = None,\n):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.cols = cols\n    self.new_col_labels = new_col_labels\n</code></pre>"},{"location":"api/#cycquery.ops.Trim.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    table = _process_checks(table, cols=self.cols)\n\n    return trim_columns(table, self.cols, new_col_labels=self.new_col_labels)\n</code></pre>"},{"location":"api/#cycquery.ops.Literal","title":"Literal","text":"<p>               Bases: <code>QueryOp</code></p> <p>Add a literal column to a table.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>Any</code> <p>Value of the literal, e.g., a string or integer.</p> required <code>col</code> <code>str</code> <p>Label of the new literal column.</p> required <p>Examples:</p> <pre><code>&gt;&gt;&gt; Literal(1, \"col1\")(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class Literal(QueryOp):\n    \"\"\"Add a literal column to a table.\n\n    Parameters\n    ----------\n    value\n        Value of the literal, e.g., a string or integer.\n    col\n        Label of the new literal column.\n\n    Examples\n    --------\n    &gt;&gt;&gt; Literal(1, \"col1\")(table)\n\n    \"\"\"\n\n    def __init__(self, value: typing.Any, col: str):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.value = value\n        self.col = col\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        table = _process_checks(table, cols_not_in=self.col)\n\n        return select(table, literal(self.value).label(self.col)).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.Literal.__init__","title":"__init__","text":"<pre><code>__init__(value, col)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(self, value: typing.Any, col: str):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.value = value\n    self.col = col\n</code></pre>"},{"location":"api/#cycquery.ops.Literal.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    table = _process_checks(table, cols_not_in=self.col)\n\n    return select(table, literal(self.value).label(self.col)).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ExtractTimestampComponent","title":"ExtractTimestampComponent","text":"<p>               Bases: <code>QueryOp</code></p> <p>Extract a component such as year or month from a timestamp column.</p> <p>Parameters:</p> Name Type Description Default <code>timestamp_col</code> <code>str</code> <p>Timestamp column from which to extract the time component.</p> required <code>extract_str</code> <code>str</code> <p>Information to extract, e.g., \"year\", \"month\"</p> required <code>label</code> <code>str</code> <p>Column label for the extracted column.</p> required <p>Examples:</p> <pre><code>&gt;&gt;&gt; ExtractTimestampComponent(\"col1\", \"year\", \"year\")(table)\n&gt;&gt;&gt; ExtractTimestampComponent(\"col1\", \"month\", \"month\")(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class ExtractTimestampComponent(QueryOp):\n    \"\"\"Extract a component such as year or month from a timestamp column.\n\n    Parameters\n    ----------\n    timestamp_col\n        Timestamp column from which to extract the time component.\n    extract_str\n        Information to extract, e.g., \"year\", \"month\"\n    label\n        Column label for the extracted column.\n\n    Examples\n    --------\n    &gt;&gt;&gt; ExtractTimestampComponent(\"col1\", \"year\", \"year\")(table)\n    &gt;&gt;&gt; ExtractTimestampComponent(\"col1\", \"month\", \"month\")(table)\n\n    \"\"\"\n\n    def __init__(self, timestamp_col: str, extract_str: str, label: str):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.timestamp_col = timestamp_col\n        self.extract_str = extract_str\n        self.label = label\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        table = _process_checks(\n            table,\n            timestamp_cols=self.timestamp_col,\n            cols_not_in=self.label,\n        )\n        table = select(\n            table,\n            extract(self.extract_str, get_column(table, self.timestamp_col)).label(\n                self.label,\n            ),\n        )\n\n        return Cast(self.label, \"int\")(table)\n</code></pre>"},{"location":"api/#cycquery.ops.ExtractTimestampComponent.__init__","title":"__init__","text":"<pre><code>__init__(timestamp_col, extract_str, label)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(self, timestamp_col: str, extract_str: str, label: str):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.timestamp_col = timestamp_col\n    self.extract_str = extract_str\n    self.label = label\n</code></pre>"},{"location":"api/#cycquery.ops.ExtractTimestampComponent.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    table = _process_checks(\n        table,\n        timestamp_cols=self.timestamp_col,\n        cols_not_in=self.label,\n    )\n    table = select(\n        table,\n        extract(self.extract_str, get_column(table, self.timestamp_col)).label(\n            self.label,\n        ),\n    )\n\n    return Cast(self.label, \"int\")(table)\n</code></pre>"},{"location":"api/#cycquery.ops.AddNumeric","title":"AddNumeric","text":"<p>               Bases: <code>QueryOp</code></p> <p>Add a numeric value to some columns.</p> <p>Parameters:</p> Name Type Description Default <code>add_to</code> <code>Union[str, List[str]]</code> <p>Column names specifying to which columns is being added.</p> required <code>add</code> <code>Union[int, float, List[int], List[float]]</code> <p>Adds this value to the add_to columns.</p> required <code>new_col_labels</code> <code>Optional[Union[str, List[str]]]</code> <p>If specified, create new columns with these labels. Otherwise, apply the function to the existing columns.</p> <code>None</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; AddNumeric(\"col1\", 1)(table)\n&gt;&gt;&gt; AddNumeric([\"col1\", \"col2\"], 1)(table)\n&gt;&gt;&gt; AddNumeric(\"col1\", 1, \"col1_plus_1\")(table)\n&gt;&gt;&gt; AddNumeric([\"col1\", \"col2\"], 1, [\"col1_plus_1\", \"col2_plus_1\"])(table)\n&gt;&gt;&gt; AddNumeric([\"col1\", \"col2\"], [1, 2.2])(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class AddNumeric(QueryOp):\n    \"\"\"Add a numeric value to some columns.\n\n    Parameters\n    ----------\n    add_to\n        Column names specifying to which columns is being added.\n    add\n        Adds this value to the add_to columns.\n    new_col_labels\n        If specified, create new columns with these labels. Otherwise,\n        apply the function to the existing columns.\n\n    Examples\n    --------\n    &gt;&gt;&gt; AddNumeric(\"col1\", 1)(table)\n    &gt;&gt;&gt; AddNumeric([\"col1\", \"col2\"], 1)(table)\n    &gt;&gt;&gt; AddNumeric(\"col1\", 1, \"col1_plus_1\")(table)\n    &gt;&gt;&gt; AddNumeric([\"col1\", \"col2\"], 1, [\"col1_plus_1\", \"col2_plus_1\"])(table)\n    &gt;&gt;&gt; AddNumeric([\"col1\", \"col2\"], [1, 2.2])(table)\n\n    \"\"\"\n\n    def __init__(\n        self,\n        add_to: typing.Union[str, typing.List[str]],\n        add: typing.Union[int, float, typing.List[int], typing.List[float]],\n        new_col_labels: typing.Optional[typing.Union[str, typing.List[str]]] = None,\n    ):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.add_to = add_to\n        self.add = add\n        self.new_col_labels = new_col_labels\n\n    def _gen_lambda(\n        self,\n        add: typing.Union[int, float],\n    ) -&gt; typing.Callable[[sqlalchemy.sql.schema.Column], sqlalchemy.sql.schema.Column]:\n        \"\"\"Generate the lambda function.\"\"\"\n        return lambda x: x + add  # type: ignore[return-value]\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        table = _process_checks(\n            table,\n            cols=self.add_to,\n            cols_not_in=self.new_col_labels,\n        )\n        self.add_to = to_list(self.add_to)\n        if isinstance(self.add, (int, float)) and len(self.add_to) &gt; 1:\n            add = [self.add] * len(self.add_to)\n        elif isinstance(self.add, (int, float)) and len(self.add_to) == 1:\n            add = [self.add]\n        elif isinstance(self.add, list) and len(self.add_to) != len(self.add):\n            raise ValueError(\n                \"Length of add_to and add must be the same if add is a list.\",\n            )\n\n        return apply_to_columns(\n            table,\n            self.add_to,\n            [self._gen_lambda(add_num) for add_num in add],\n            new_col_labels=self.new_col_labels,\n        )\n</code></pre>"},{"location":"api/#cycquery.ops.AddNumeric.__init__","title":"__init__","text":"<pre><code>__init__(add_to, add, new_col_labels=None)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    add_to: typing.Union[str, typing.List[str]],\n    add: typing.Union[int, float, typing.List[int], typing.List[float]],\n    new_col_labels: typing.Optional[typing.Union[str, typing.List[str]]] = None,\n):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.add_to = add_to\n    self.add = add\n    self.new_col_labels = new_col_labels\n</code></pre>"},{"location":"api/#cycquery.ops.AddNumeric.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    table = _process_checks(\n        table,\n        cols=self.add_to,\n        cols_not_in=self.new_col_labels,\n    )\n    self.add_to = to_list(self.add_to)\n    if isinstance(self.add, (int, float)) and len(self.add_to) &gt; 1:\n        add = [self.add] * len(self.add_to)\n    elif isinstance(self.add, (int, float)) and len(self.add_to) == 1:\n        add = [self.add]\n    elif isinstance(self.add, list) and len(self.add_to) != len(self.add):\n        raise ValueError(\n            \"Length of add_to and add must be the same if add is a list.\",\n        )\n\n    return apply_to_columns(\n        table,\n        self.add_to,\n        [self._gen_lambda(add_num) for add_num in add],\n        new_col_labels=self.new_col_labels,\n    )\n</code></pre>"},{"location":"api/#cycquery.ops.AddDeltaConstant","title":"AddDeltaConstant","text":"<p>               Bases: <code>QueryOp</code></p> <p>Construct and add a datetime.timedelta object to some columns.</p> <p>Parameters:</p> Name Type Description Default <code>add_to</code> <code>Union[str, List[str]]</code> <p>Column names specifying to which columns is being added.</p> required <code>delta</code> <code>timedelta</code> <p>A timedelta object.</p> required <code>new_col_labels</code> <code>Optional[Union[str, List[str]]]</code> <p>If specified, create new columns with these labels. Otherwise, apply the function to the existing columns.</p> <code>None</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; AddDeltaConstant(\"col1\", datetime.timedelta(days=1))(table)\n&gt;&gt;&gt; AddDeltaConstant([\"col1\", \"col2\"], datetime.timedelta(days=1))(table)\n&gt;&gt;&gt; AddDeltaConstant(\"col1\", datetime.timedelta(days=1), \"col1_plus_1\")(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class AddDeltaConstant(QueryOp):\n    \"\"\"Construct and add a datetime.timedelta object to some columns.\n\n    Parameters\n    ----------\n    add_to\n        Column names specifying to which columns is being added.\n    delta\n        A timedelta object.\n    new_col_labels\n        If specified, create new columns with these labels. Otherwise,\n        apply the function to the existing columns.\n\n    Examples\n    --------\n    &gt;&gt;&gt; AddDeltaConstant(\"col1\", datetime.timedelta(days=1))(table)\n    &gt;&gt;&gt; AddDeltaConstant([\"col1\", \"col2\"], datetime.timedelta(days=1))(table)\n    &gt;&gt;&gt; AddDeltaConstant(\"col1\", datetime.timedelta(days=1), \"col1_plus_1\")(table)\n\n    \"\"\"\n\n    def __init__(\n        self,\n        add_to: typing.Union[str, typing.List[str]],\n        delta: timedelta,\n        new_col_labels: typing.Optional[typing.Union[str, typing.List[str]]] = None,\n    ):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.add_to = add_to\n        self.delta = delta\n        self.new_col_labels = new_col_labels\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        table = _process_checks(\n            table,\n            timestamp_cols=self.add_to,\n            cols_not_in=self.new_col_labels,\n        )\n\n        return apply_to_columns(\n            table,\n            self.add_to,\n            lambda x: x + self.delta,\n            new_col_labels=self.new_col_labels,\n        )\n</code></pre>"},{"location":"api/#cycquery.ops.AddDeltaConstant.__init__","title":"__init__","text":"<pre><code>__init__(add_to, delta, new_col_labels=None)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    add_to: typing.Union[str, typing.List[str]],\n    delta: timedelta,\n    new_col_labels: typing.Optional[typing.Union[str, typing.List[str]]] = None,\n):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.add_to = add_to\n    self.delta = delta\n    self.new_col_labels = new_col_labels\n</code></pre>"},{"location":"api/#cycquery.ops.AddDeltaConstant.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    table = _process_checks(\n        table,\n        timestamp_cols=self.add_to,\n        cols_not_in=self.new_col_labels,\n    )\n\n    return apply_to_columns(\n        table,\n        self.add_to,\n        lambda x: x + self.delta,\n        new_col_labels=self.new_col_labels,\n    )\n</code></pre>"},{"location":"api/#cycquery.ops.AddColumn","title":"AddColumn","text":"<p>               Bases: <code>QueryOp</code></p> <p>Add a column to some columns.</p> <p>Parameters:</p> Name Type Description Default <code>add_to</code> <code>Union[str, List[str]]</code> <p>Column names specifying to which columns is being added.</p> required <code>col</code> <code>str</code> <p>Column name of column to add to the add_to columns.</p> required <code>negative</code> <code>Optional[bool]</code> <p>Subtract the column rather than adding.</p> <code>False</code> <code>new_col_labels</code> <code>Optional[Union[str, List[str]]]</code> <p>If specified, create new columns with these labels. Otherwise, apply the function to the existing columns.</p> <code>None</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; AddColumn(\"col1\", \"col2\")(table)\n&gt;&gt;&gt; AddColumn([\"col1\", \"col2\"], \"col3\")(table)\n&gt;&gt;&gt; AddColumn(\"col1\", \"col2\", negative=True)(table)\n&gt;&gt;&gt; AddColumn(\"col1\", \"col2\", \"col1_plus_col2\")(table)\n&gt;&gt;&gt; AddColumn([\"col1\", \"col2\"], \"col3\", [\"col1_plus_col3\", \"col2_plus_col3\"])(table)\n</code></pre> Warning <p>Pay attention to column types. Some combinations will work, whereas others will not.</p> Source code in <code>cycquery/ops.py</code> <pre><code>class AddColumn(QueryOp):\n    \"\"\"Add a column to some columns.\n\n    Parameters\n    ----------\n    add_to\n        Column names specifying to which columns is being added.\n    col\n        Column name of column to add to the add_to columns.\n    negative\n        Subtract the column rather than adding.\n    new_col_labels\n        If specified, create new columns with these labels. Otherwise,\n        apply the function to the existing columns.\n\n    Examples\n    --------\n    &gt;&gt;&gt; AddColumn(\"col1\", \"col2\")(table)\n    &gt;&gt;&gt; AddColumn([\"col1\", \"col2\"], \"col3\")(table)\n    &gt;&gt;&gt; AddColumn(\"col1\", \"col2\", negative=True)(table)\n    &gt;&gt;&gt; AddColumn(\"col1\", \"col2\", \"col1_plus_col2\")(table)\n    &gt;&gt;&gt; AddColumn([\"col1\", \"col2\"], \"col3\", [\"col1_plus_col3\", \"col2_plus_col3\"])(table)\n\n    Warning\n    -------\n    Pay attention to column types. Some combinations will work,\n    whereas others will not.\n\n    \"\"\"\n\n    def __init__(\n        self,\n        add_to: typing.Union[str, typing.List[str]],\n        col: str,\n        negative: typing.Optional[bool] = False,\n        new_col_labels: typing.Optional[typing.Union[str, typing.List[str]]] = None,\n    ):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.add_to = add_to\n        self.col = col\n        self.negative = negative\n        self.new_col_labels = new_col_labels\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        # If the column being added is a timestamp column, ensure the others are too\n        if check_timestamp_columns(table, self.col):\n            table = _process_checks(\n                table,\n                timestamp_cols=self.add_to,\n                cols_not_in=self.new_col_labels,\n            )\n        else:\n            table = _process_checks(\n                table,\n                cols=self.add_to,\n                cols_not_in=self.new_col_labels,\n            )\n        col = get_column(table, self.col)\n        if self.negative:\n            return apply_to_columns(\n                table,\n                self.add_to,\n                lambda x: x - col,\n                new_col_labels=self.new_col_labels,\n            )\n\n        return apply_to_columns(\n            table,\n            self.add_to,\n            lambda x: x + col,\n            new_col_labels=self.new_col_labels,\n        )\n</code></pre>"},{"location":"api/#cycquery.ops.AddColumn.__init__","title":"__init__","text":"<pre><code>__init__(add_to, col, negative=False, new_col_labels=None)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    add_to: typing.Union[str, typing.List[str]],\n    col: str,\n    negative: typing.Optional[bool] = False,\n    new_col_labels: typing.Optional[typing.Union[str, typing.List[str]]] = None,\n):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.add_to = add_to\n    self.col = col\n    self.negative = negative\n    self.new_col_labels = new_col_labels\n</code></pre>"},{"location":"api/#cycquery.ops.AddColumn.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    # If the column being added is a timestamp column, ensure the others are too\n    if check_timestamp_columns(table, self.col):\n        table = _process_checks(\n            table,\n            timestamp_cols=self.add_to,\n            cols_not_in=self.new_col_labels,\n        )\n    else:\n        table = _process_checks(\n            table,\n            cols=self.add_to,\n            cols_not_in=self.new_col_labels,\n        )\n    col = get_column(table, self.col)\n    if self.negative:\n        return apply_to_columns(\n            table,\n            self.add_to,\n            lambda x: x - col,\n            new_col_labels=self.new_col_labels,\n        )\n\n    return apply_to_columns(\n        table,\n        self.add_to,\n        lambda x: x + col,\n        new_col_labels=self.new_col_labels,\n    )\n</code></pre>"},{"location":"api/#cycquery.ops.AddDeltaColumn","title":"AddDeltaColumn","text":"<p>               Bases: <code>QueryOp</code></p> <p>Construct and add an interval column to some columns.</p> <p>Parameters:</p> Name Type Description Default <code>add_to</code> <code>Union[str, List[str]]</code> <p>Column names specifying to which columns is being added.</p> required <code>negative</code> <code>Optional[bool]</code> <p>Subtract the object rather than adding.</p> <code>False</code> <code>new_col_labels</code> <code>Optional[Union[str, List[str]]]</code> <p>If specified, create new columns with these labels. Otherwise, apply the function to the existing columns.</p> <code>None</code> <code>**delta_kwargs</code> <code>Any</code> <p>The arguments used to create the Interval column.</p> <code>{}</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; AddDeltaColumn(\"col1\", \"col2\")(table)\n&gt;&gt;&gt; AddDeltaColumn([\"col1\", \"col2\"], \"col3\")(table)\n&gt;&gt;&gt; AddDeltaColumn(\"col1\", \"col2\", negative=True)(table)\n&gt;&gt;&gt; AddDeltaColumn(\"col1\", \"col2\", \"col1_plus_col2\")(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class AddDeltaColumn(QueryOp):\n    \"\"\"Construct and add an interval column to some columns.\n\n    Parameters\n    ----------\n    add_to\n        Column names specifying to which columns is being added.\n    negative\n        Subtract the object rather than adding.\n    new_col_labels\n        If specified, create new columns with these labels. Otherwise,\n        apply the function to the existing columns.\n    **delta_kwargs\n        The arguments used to create the Interval column.\n\n    Examples\n    --------\n    &gt;&gt;&gt; AddDeltaColumn(\"col1\", \"col2\")(table)\n    &gt;&gt;&gt; AddDeltaColumn([\"col1\", \"col2\"], \"col3\")(table)\n    &gt;&gt;&gt; AddDeltaColumn(\"col1\", \"col2\", negative=True)(table)\n    &gt;&gt;&gt; AddDeltaColumn(\"col1\", \"col2\", \"col1_plus_col2\")(table)\n\n    \"\"\"\n\n    def __init__(\n        self,\n        add_to: typing.Union[str, typing.List[str]],\n        negative: typing.Optional[bool] = False,\n        new_col_labels: typing.Optional[typing.Union[str, typing.List[str]]] = None,\n        **delta_kwargs: typing.Any,\n    ) -&gt; None:\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.add_to = add_to\n        self.negative = negative\n        self.new_col_labels = new_col_labels\n        self.delta_kwargs = delta_kwargs\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        table = _process_checks(\n            table,\n            timestamp_cols=self.add_to,\n            cols_not_in=self.new_col_labels,\n        )\n        delta = get_delta_column(table, **self.delta_kwargs)\n        if self.negative:\n            return apply_to_columns(\n                table,\n                self.add_to,\n                lambda x: x - delta,\n                new_col_labels=self.new_col_labels,\n            )\n\n        return apply_to_columns(\n            table,\n            self.add_to,\n            lambda x: x + delta,\n            new_col_labels=self.new_col_labels,\n        )\n</code></pre>"},{"location":"api/#cycquery.ops.AddDeltaColumn.__init__","title":"__init__","text":"<pre><code>__init__(\n    add_to,\n    negative=False,\n    new_col_labels=None,\n    **delta_kwargs\n)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    add_to: typing.Union[str, typing.List[str]],\n    negative: typing.Optional[bool] = False,\n    new_col_labels: typing.Optional[typing.Union[str, typing.List[str]]] = None,\n    **delta_kwargs: typing.Any,\n) -&gt; None:\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.add_to = add_to\n    self.negative = negative\n    self.new_col_labels = new_col_labels\n    self.delta_kwargs = delta_kwargs\n</code></pre>"},{"location":"api/#cycquery.ops.AddDeltaColumn.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    table = _process_checks(\n        table,\n        timestamp_cols=self.add_to,\n        cols_not_in=self.new_col_labels,\n    )\n    delta = get_delta_column(table, **self.delta_kwargs)\n    if self.negative:\n        return apply_to_columns(\n            table,\n            self.add_to,\n            lambda x: x - delta,\n            new_col_labels=self.new_col_labels,\n        )\n\n    return apply_to_columns(\n        table,\n        self.add_to,\n        lambda x: x + delta,\n        new_col_labels=self.new_col_labels,\n    )\n</code></pre>"},{"location":"api/#cycquery.ops.Cast","title":"Cast","text":"<p>               Bases: <code>QueryOp</code></p> <p>Cast columns to a specified type.</p> <p>Currently supporting conversions to str, int, float, date, bool and timestamp.</p> <p>Parameters:</p> Name Type Description Default <code>cols</code> <code>Union[str, List[str]]</code> <p>Columns to cast.</p> required <code>type_</code> <code>str</code> <p>Name of type to which to convert. Must be supported.</p> required <p>Examples:</p> <pre><code>&gt;&gt;&gt; Cast(\"col1\", \"str\")(table)\n&gt;&gt;&gt; Cast([\"col1\", \"col2\"], \"int\")(table)\n&gt;&gt;&gt; Cast(\"col1\", \"float\")(table)\n&gt;&gt;&gt; Cast(\"col1\", \"date\")(table)\n&gt;&gt;&gt; Cast(\"col1\", \"bool\")(table)\n&gt;&gt;&gt; Cast(\"col1\", \"timestamp\")(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class Cast(QueryOp):\n    \"\"\"Cast columns to a specified type.\n\n    Currently supporting conversions to str, int, float, date, bool and timestamp.\n\n    Parameters\n    ----------\n    cols\n        Columns to cast.\n    type_\n        Name of type to which to convert. Must be supported.\n\n    Examples\n    --------\n    &gt;&gt;&gt; Cast(\"col1\", \"str\")(table)\n    &gt;&gt;&gt; Cast([\"col1\", \"col2\"], \"int\")(table)\n    &gt;&gt;&gt; Cast(\"col1\", \"float\")(table)\n    &gt;&gt;&gt; Cast(\"col1\", \"date\")(table)\n    &gt;&gt;&gt; Cast(\"col1\", \"bool\")(table)\n    &gt;&gt;&gt; Cast(\"col1\", \"timestamp\")(table)\n\n    \"\"\"\n\n    def __init__(self, cols: typing.Union[str, typing.List[str]], type_: str):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.cols = cols\n        self.type_ = type_\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        table = _process_checks(table, cols=self.cols)\n        cast_type_map = {\n            \"str\": \"to_str\",\n            \"int\": \"to_int\",\n            \"float\": \"to_float\",\n            \"date\": \"to_date\",\n            \"bool\": \"to_bool\",\n            \"timestamp\": \"to_timestamp\",\n        }\n        # Assert that the type inputted is supported\n        if self.type_ not in cast_type_map:\n            supported_str = \", \".join(list(cast_type_map.keys()))\n            raise ValueError(\n                f\"\"\"Conversion to type {self.type_} not supported. Supporting\n                conversion to types {supported_str}\"\"\",\n            )\n        # Cast\n        kwargs = {cast_type_map[self.type_]: True}\n\n        return apply_to_columns(\n            table,\n            self.cols,\n            lambda x: process_column(x, **kwargs),\n        )\n</code></pre>"},{"location":"api/#cycquery.ops.Cast.__init__","title":"__init__","text":"<pre><code>__init__(cols, type_)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(self, cols: typing.Union[str, typing.List[str]], type_: str):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.cols = cols\n    self.type_ = type_\n</code></pre>"},{"location":"api/#cycquery.ops.Cast.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    table = _process_checks(table, cols=self.cols)\n    cast_type_map = {\n        \"str\": \"to_str\",\n        \"int\": \"to_int\",\n        \"float\": \"to_float\",\n        \"date\": \"to_date\",\n        \"bool\": \"to_bool\",\n        \"timestamp\": \"to_timestamp\",\n    }\n    # Assert that the type inputted is supported\n    if self.type_ not in cast_type_map:\n        supported_str = \", \".join(list(cast_type_map.keys()))\n        raise ValueError(\n            f\"\"\"Conversion to type {self.type_} not supported. Supporting\n            conversion to types {supported_str}\"\"\",\n        )\n    # Cast\n    kwargs = {cast_type_map[self.type_]: True}\n\n    return apply_to_columns(\n        table,\n        self.cols,\n        lambda x: process_column(x, **kwargs),\n    )\n</code></pre>"},{"location":"api/#cycquery.ops.Union","title":"Union","text":"<p>               Bases: <code>QueryOp</code></p> <p>Union two tables.</p> <p>Parameters:</p> Name Type Description Default <code>union_table</code> <code>TableTypes</code> <p>Table to union with the first table.</p> required <code>union_all</code> <code>Optional[bool]</code> <p>Whether to use the all keyword in the union.</p> <code>False</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; Union(table2)(table1)\n&gt;&gt;&gt; Union(table2, union_all=True)(table1)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class Union(QueryOp):\n    \"\"\"Union two tables.\n\n    Parameters\n    ----------\n    union_table\n        Table to union with the first table.\n    union_all\n        Whether to use the all keyword in the union.\n\n    Examples\n    --------\n    &gt;&gt;&gt; Union(table2)(table1)\n    &gt;&gt;&gt; Union(table2, union_all=True)(table1)\n\n    \"\"\"\n\n    def __init__(\n        self,\n        union_table: TableTypes,\n        union_all: typing.Optional[bool] = False,\n    ):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.union_table = union_table\n        self.union_all = union_all\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        table = _process_checks(table)\n        union_table = _process_checks(self.union_table)\n        if self.union_all:\n            return select(table).union_all(select(union_table)).subquery()\n\n        return select(table).union(select(union_table)).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.Union.__init__","title":"__init__","text":"<pre><code>__init__(union_table, union_all=False)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    union_table: TableTypes,\n    union_all: typing.Optional[bool] = False,\n):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.union_table = union_table\n    self.union_all = union_all\n</code></pre>"},{"location":"api/#cycquery.ops.Union.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    table = _process_checks(table)\n    union_table = _process_checks(self.union_table)\n    if self.union_all:\n        return select(table).union_all(select(union_table)).subquery()\n\n    return select(table).union(select(union_table)).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.Join","title":"Join","text":"<p>               Bases: <code>QueryOp</code></p> <p>Join a table with another table.</p> <p>Parameters:</p> Name Type Description Default <code>join_table</code> <code>TableTypes</code> <p>Table on which to join.</p> required <code>on</code> <code>Optional[Union[str, List[str], Tuple[str], List[Tuple[str, str]]]]</code> <p>A list of strings or tuples representing columns on which to join. Strings represent columns of same name in both tables. A tuple of style (table_col, join_table_col) is used to join on columns of different names. Suggested to specify this parameter as opposed to cond.</p> <code>None</code> <code>on_to_type</code> <code>Optional[Union[type, List[type]]]</code> <p>A list of types to which to convert the on columns before joining. Useful when two columns have the same values but in different format, e.g., strings of int.</p> <code>None</code> <code>cond</code> <code>Optional[BinaryExpression]</code> <p>Condition on which to join to tables.</p> <code>None</code> <code>table_cols</code> <code>Optional[Union[str, List[str]]]</code> <p>Filters to keep only these columns from the table.</p> <code>None</code> <code>join_table_cols</code> <code>Optional[Union[str, List[str]]]</code> <p>Filters to keep only these columns from the join_table.</p> <code>None</code> <code>isouter</code> <code>Optional[bool]</code> <p>Flag to say if the join is a left outer join.</p> <code>False</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; Join(table2, on=[\"col1\", (\"col2\", \"col3\")], on_to_type=[int, str])(table1)\n&gt;&gt;&gt; Join(table2, table_cols=[\"col1\", \"col2\"])(table1)\n&gt;&gt;&gt; Join(table2, join_table_cols=[\"col1\", \"col2\"])(table1)\n</code></pre> Warnings <p>If neither on nor cond parameters are specified, an expensive Cartesian product is performed.</p> Source code in <code>cycquery/ops.py</code> <pre><code>class Join(QueryOp):\n    \"\"\"Join a table with another table.\n\n    Parameters\n    ----------\n    join_table\n        Table on which to join.\n    on\n        A list of strings or tuples representing columns on which to join.\n        Strings represent columns of same name in both tables. A tuple of\n        style (table_col, join_table_col) is used to join on columns of\n        different names. Suggested to specify this parameter as opposed to\n        cond.\n    on_to_type\n        A list of types to which to convert the on columns before joining. Useful when\n        two columns have the same values but in different format, e.g., strings of int.\n    cond\n        Condition on which to join to tables.\n    table_cols\n        Filters to keep only these columns from the table.\n    join_table_cols\n        Filters to keep only these columns from the join_table.\n    isouter\n        Flag to say if the join is a left outer join.\n\n    Examples\n    --------\n    &gt;&gt;&gt; Join(table2, on=[\"col1\", (\"col2\", \"col3\")], on_to_type=[int, str])(table1)\n    &gt;&gt;&gt; Join(table2, table_cols=[\"col1\", \"col2\"])(table1)\n    &gt;&gt;&gt; Join(table2, join_table_cols=[\"col1\", \"col2\"])(table1)\n\n    Warnings\n    --------\n    If neither on nor cond parameters are specified, an\n    expensive Cartesian product is performed.\n\n    \"\"\"\n\n    @table_params_to_type(Subquery)  # type: ignore[arg-type]\n    def __init__(\n        self,\n        join_table: TableTypes,\n        on: typing.Optional[\n            typing.Union[\n                str,\n                typing.List[str],\n                typing.Tuple[str],\n                typing.List[typing.Tuple[str, str]],\n            ]\n        ] = None,\n        on_to_type: typing.Optional[typing.Union[type, typing.List[type]]] = None,\n        cond: typing.Optional[BinaryExpression] = None,\n        table_cols: typing.Optional[typing.Union[str, typing.List[str]]] = None,\n        join_table_cols: typing.Optional[typing.Union[str, typing.List[str]]] = None,\n        isouter: typing.Optional[bool] = False,\n    ) -&gt; None:\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        if on is not None and cond is not None:\n            raise ValueError(\"Cannot specify both the 'on' and 'cond' arguments.\")\n\n        self.join_table = join_table\n        self.cond = cond\n        self.on_ = to_list_optional(on)\n        self.on_to_type = to_list_optional(on_to_type)\n        self.table_cols = to_list_optional(table_cols)\n        self.join_table_cols = to_list_optional(join_table_cols)\n        self.isouter = isouter\n\n    @table_params_to_type(Subquery)  # type: ignore[arg-type]\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        # Join on the equality of values in columns of same name in both tables\n        if self.on_ is not None:\n            # Process on columns\n            on_table_cols = [\n                col_obj if isinstance(col_obj, str) else col_obj[0]\n                for col_obj in self.on_\n            ]\n            on_join_table_cols = [\n                col_obj if isinstance(col_obj, str) else col_obj[1]\n                for col_obj in self.on_\n            ]\n            table = _process_checks(\n                table,\n                cols=_none_add(self.table_cols, on_table_cols),\n            )\n            self.join_table = _process_checks(\n                self.join_table,\n                cols=_none_add(self.join_table_cols, on_join_table_cols),\n            )\n            # Filter columns, keeping those being joined on\n            table = _append_if_missing(table, self.table_cols, on_table_cols)\n            self.join_table = _append_if_missing(\n                self.join_table,\n                self.join_table_cols,\n                on_join_table_cols,\n            )\n            # Perform type conversions if given\n            if self.on_to_type is not None:\n                for i, type_ in enumerate(self.on_to_type):\n                    table = Cast(on_table_cols[i], type_)(table)\n                    self.join_table = Cast(on_join_table_cols[i], type_)(\n                        self.join_table,\n                    )\n            cond = and_(\n                *[\n                    get_column(table, on_table_cols[i])\n                    == get_column(self.join_table, on_join_table_cols[i])\n                    for i in range(len(on_table_cols))\n                ],\n            )\n            table = select(table.join(self.join_table, cond, isouter=self.isouter))  # type: ignore[arg-type]\n        else:\n            # Filter columns\n            if self.table_cols is not None:\n                table = Keep(self.table_cols)(table)\n            if self.join_table_cols is not None:\n                self.join_table = Keep(self.table_cols)(self.join_table)  # type: ignore\n\n            # Join on a specified condition\n            if self.cond is not None:\n                table = select(\n                    table.join(  # type: ignore\n                        self.join_table,  # type: ignore[arg-type]\n                        self.cond,\n                        isouter=self.isouter,  # type: ignore[arg-type]\n                    ),\n                )\n            # Join on no condition, i.e., a Cartesian product\n            else:\n                LOGGER.warning(\"A Cartesian product has been queried.\")\n                table = select(table, self.join_table)  # type: ignore[arg-type]\n\n        # Filter to include no duplicate columns\n        return select(\n            *[col for col in table.subquery().columns if \"%(\" not in col.name],\n        ).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.Join.__init__","title":"__init__","text":"<pre><code>__init__(\n    join_table,\n    on=None,\n    on_to_type=None,\n    cond=None,\n    table_cols=None,\n    join_table_cols=None,\n    isouter=False,\n)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>@table_params_to_type(Subquery)  # type: ignore[arg-type]\ndef __init__(\n    self,\n    join_table: TableTypes,\n    on: typing.Optional[\n        typing.Union[\n            str,\n            typing.List[str],\n            typing.Tuple[str],\n            typing.List[typing.Tuple[str, str]],\n        ]\n    ] = None,\n    on_to_type: typing.Optional[typing.Union[type, typing.List[type]]] = None,\n    cond: typing.Optional[BinaryExpression] = None,\n    table_cols: typing.Optional[typing.Union[str, typing.List[str]]] = None,\n    join_table_cols: typing.Optional[typing.Union[str, typing.List[str]]] = None,\n    isouter: typing.Optional[bool] = False,\n) -&gt; None:\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    if on is not None and cond is not None:\n        raise ValueError(\"Cannot specify both the 'on' and 'cond' arguments.\")\n\n    self.join_table = join_table\n    self.cond = cond\n    self.on_ = to_list_optional(on)\n    self.on_to_type = to_list_optional(on_to_type)\n    self.table_cols = to_list_optional(table_cols)\n    self.join_table_cols = to_list_optional(join_table_cols)\n    self.isouter = isouter\n</code></pre>"},{"location":"api/#cycquery.ops.Join.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>@table_params_to_type(Subquery)  # type: ignore[arg-type]\ndef __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    # Join on the equality of values in columns of same name in both tables\n    if self.on_ is not None:\n        # Process on columns\n        on_table_cols = [\n            col_obj if isinstance(col_obj, str) else col_obj[0]\n            for col_obj in self.on_\n        ]\n        on_join_table_cols = [\n            col_obj if isinstance(col_obj, str) else col_obj[1]\n            for col_obj in self.on_\n        ]\n        table = _process_checks(\n            table,\n            cols=_none_add(self.table_cols, on_table_cols),\n        )\n        self.join_table = _process_checks(\n            self.join_table,\n            cols=_none_add(self.join_table_cols, on_join_table_cols),\n        )\n        # Filter columns, keeping those being joined on\n        table = _append_if_missing(table, self.table_cols, on_table_cols)\n        self.join_table = _append_if_missing(\n            self.join_table,\n            self.join_table_cols,\n            on_join_table_cols,\n        )\n        # Perform type conversions if given\n        if self.on_to_type is not None:\n            for i, type_ in enumerate(self.on_to_type):\n                table = Cast(on_table_cols[i], type_)(table)\n                self.join_table = Cast(on_join_table_cols[i], type_)(\n                    self.join_table,\n                )\n        cond = and_(\n            *[\n                get_column(table, on_table_cols[i])\n                == get_column(self.join_table, on_join_table_cols[i])\n                for i in range(len(on_table_cols))\n            ],\n        )\n        table = select(table.join(self.join_table, cond, isouter=self.isouter))  # type: ignore[arg-type]\n    else:\n        # Filter columns\n        if self.table_cols is not None:\n            table = Keep(self.table_cols)(table)\n        if self.join_table_cols is not None:\n            self.join_table = Keep(self.table_cols)(self.join_table)  # type: ignore\n\n        # Join on a specified condition\n        if self.cond is not None:\n            table = select(\n                table.join(  # type: ignore\n                    self.join_table,  # type: ignore[arg-type]\n                    self.cond,\n                    isouter=self.isouter,  # type: ignore[arg-type]\n                ),\n            )\n        # Join on no condition, i.e., a Cartesian product\n        else:\n            LOGGER.warning(\"A Cartesian product has been queried.\")\n            table = select(table, self.join_table)  # type: ignore[arg-type]\n\n    # Filter to include no duplicate columns\n    return select(\n        *[col for col in table.subquery().columns if \"%(\" not in col.name],\n    ).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionEquals","title":"ConditionEquals","text":"<p>               Bases: <code>QueryOp</code></p> <p>Filter rows based on being equal, or not equal, to some value.</p> <p>Parameters:</p> Name Type Description Default <code>col</code> <code>str</code> <p>Column name on which to condition.</p> required <code>value</code> <code>Any</code> <p>Value to equal.</p> required <code>not_</code> <code>bool</code> <p>Take negation of condition.</p> <code>False</code> <code>binarize_col</code> <code>Optional[str]</code> <p>If specified, create a Boolean column of name binarize_col instead of filtering.</p> <code>None</code> <code>**cond_kwargs</code> <code>Any</code> <p>Optional keyword arguments for processing the condition.</p> <code>{}</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; ConditionEquals(\"col1\", 1)(table)\n&gt;&gt;&gt; ConditionEquals(\"col1\", 1, binarize_col=\"col1_bool\")(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class ConditionEquals(QueryOp):\n    \"\"\"Filter rows based on being equal, or not equal, to some value.\n\n    Parameters\n    ----------\n    col\n        Column name on which to condition.\n    value\n        Value to equal.\n    not_\n        Take negation of condition.\n    binarize_col\n        If specified, create a Boolean column of name binarize_col instead of filtering.\n    **cond_kwargs\n        Optional keyword arguments for processing the condition.\n\n    Examples\n    --------\n    &gt;&gt;&gt; ConditionEquals(\"col1\", 1)(table)\n    &gt;&gt;&gt; ConditionEquals(\"col1\", 1, binarize_col=\"col1_bool\")(table)\n\n    \"\"\"\n\n    def __init__(\n        self,\n        col: str,\n        value: typing.Any,\n        not_: bool = False,\n        binarize_col: typing.Optional[str] = None,\n        **cond_kwargs: typing.Any,\n    ) -&gt; None:\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.col = col\n        self.value = value\n        self.not_ = not_\n        self.binarize_col = binarize_col\n        self.cond_kwargs = cond_kwargs\n\n    def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n        return_cond\n            Return the condition instead of filtering.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        if return_cond and self.binarize_col:\n            raise ValueError(\n                \"Cannot return condition and binarize column simultaneously.\",\n            )\n        table = _process_checks(table, cols=self.col, cols_not_in=self.binarize_col)\n        cond = equals(\n            get_column(table, self.col),\n            self.value,\n            True,\n            True,\n            **self.cond_kwargs,\n        )\n        if self.not_:\n            cond = cond._negate()  # type: ignore[no-untyped-call]\n        if return_cond:\n            return cond  # type: ignore[return-value]\n        if self.binarize_col is not None:\n            return select(\n                table,\n                cast(cond, Boolean).label(self.binarize_col),\n            ).subquery()\n\n        return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionEquals.__init__","title":"__init__","text":"<pre><code>__init__(\n    col, value, not_=False, binarize_col=None, **cond_kwargs\n)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    col: str,\n    value: typing.Any,\n    not_: bool = False,\n    binarize_col: typing.Optional[str] = None,\n    **cond_kwargs: typing.Any,\n) -&gt; None:\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.col = col\n    self.value = value\n    self.not_ = not_\n    self.binarize_col = binarize_col\n    self.cond_kwargs = cond_kwargs\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionEquals.__call__","title":"__call__","text":"<pre><code>__call__(table, return_cond=False)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <code>return_cond</code> <code>bool</code> <p>Return the condition instead of filtering.</p> <code>False</code> <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n    return_cond\n        Return the condition instead of filtering.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    if return_cond and self.binarize_col:\n        raise ValueError(\n            \"Cannot return condition and binarize column simultaneously.\",\n        )\n    table = _process_checks(table, cols=self.col, cols_not_in=self.binarize_col)\n    cond = equals(\n        get_column(table, self.col),\n        self.value,\n        True,\n        True,\n        **self.cond_kwargs,\n    )\n    if self.not_:\n        cond = cond._negate()  # type: ignore[no-untyped-call]\n    if return_cond:\n        return cond  # type: ignore[return-value]\n    if self.binarize_col is not None:\n        return select(\n            table,\n            cast(cond, Boolean).label(self.binarize_col),\n        ).subquery()\n\n    return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionGreaterThan","title":"ConditionGreaterThan","text":"<p>               Bases: <code>QueryOp</code></p> <p>Filter rows based on greater than (or equal), to some value.</p> <p>Parameters:</p> Name Type Description Default <code>col</code> <code>str</code> <p>Column name on which to condition.</p> required <code>value</code> <code>Any</code> <p>Value greater than.</p> required <code>equal</code> <code>bool</code> <p>Include equality to the value.</p> <code>False</code> <code>not_</code> <code>bool</code> <p>Take negation of condition.</p> <code>False</code> <code>binarize_col</code> <code>Optional[str]</code> <p>If specified, create a Boolean column of name binarize_col instead of filtering.</p> <code>None</code> <code>**cond_kwargs</code> <code>Any</code> <p>Optional keyword arguments for processing the condition.</p> <code>{}</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; ConditionGreaterThan(\"col1\", 1)(table)\n&gt;&gt;&gt; ConditionGreaterThan(\"col1\", 1, binarize_col=\"col1_bool\")(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class ConditionGreaterThan(QueryOp):\n    \"\"\"Filter rows based on greater than (or equal), to some value.\n\n    Parameters\n    ----------\n    col\n        Column name on which to condition.\n    value\n        Value greater than.\n    equal\n        Include equality to the value.\n    not_\n        Take negation of condition.\n    binarize_col\n        If specified, create a Boolean column of name binarize_col instead of filtering.\n    **cond_kwargs\n        Optional keyword arguments for processing the condition.\n\n    Examples\n    --------\n    &gt;&gt;&gt; ConditionGreaterThan(\"col1\", 1)(table)\n    &gt;&gt;&gt; ConditionGreaterThan(\"col1\", 1, binarize_col=\"col1_bool\")(table)\n\n    \"\"\"\n\n    def __init__(\n        self,\n        col: str,\n        value: typing.Any,\n        equal: bool = False,\n        not_: bool = False,\n        binarize_col: typing.Optional[str] = None,\n        **cond_kwargs: typing.Any,\n    ) -&gt; None:\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.col = col\n        self.value = value\n        self.equal = equal\n        self.not_ = not_\n        self.binarize_col = binarize_col\n        self.cond_kwargs = cond_kwargs\n\n    def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n        return_cond\n            Return the condition instead of filtering.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        if return_cond and self.binarize_col:\n            raise ValueError(\n                \"Cannot return condition and binarize column simultaneously.\",\n            )\n        table = _process_checks(table, cols=self.col, cols_not_in=self.binarize_col)\n        cond = greater_than(\n            get_column(table, self.col),\n            self.value,\n            True,\n            True,\n            self.equal,\n            **self.cond_kwargs,\n        )\n        if self.not_:\n            cond = cond._negate()  # type: ignore[no-untyped-call]\n        if return_cond:\n            return cond  # type: ignore[return-value]\n        if self.binarize_col is not None:\n            return select(\n                table,\n                cast(cond, Boolean).label(self.binarize_col),\n            ).subquery()\n\n        return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionGreaterThan.__init__","title":"__init__","text":"<pre><code>__init__(\n    col,\n    value,\n    equal=False,\n    not_=False,\n    binarize_col=None,\n    **cond_kwargs\n)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    col: str,\n    value: typing.Any,\n    equal: bool = False,\n    not_: bool = False,\n    binarize_col: typing.Optional[str] = None,\n    **cond_kwargs: typing.Any,\n) -&gt; None:\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.col = col\n    self.value = value\n    self.equal = equal\n    self.not_ = not_\n    self.binarize_col = binarize_col\n    self.cond_kwargs = cond_kwargs\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionGreaterThan.__call__","title":"__call__","text":"<pre><code>__call__(table, return_cond=False)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <code>return_cond</code> <code>bool</code> <p>Return the condition instead of filtering.</p> <code>False</code> <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n    return_cond\n        Return the condition instead of filtering.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    if return_cond and self.binarize_col:\n        raise ValueError(\n            \"Cannot return condition and binarize column simultaneously.\",\n        )\n    table = _process_checks(table, cols=self.col, cols_not_in=self.binarize_col)\n    cond = greater_than(\n        get_column(table, self.col),\n        self.value,\n        True,\n        True,\n        self.equal,\n        **self.cond_kwargs,\n    )\n    if self.not_:\n        cond = cond._negate()  # type: ignore[no-untyped-call]\n    if return_cond:\n        return cond  # type: ignore[return-value]\n    if self.binarize_col is not None:\n        return select(\n            table,\n            cast(cond, Boolean).label(self.binarize_col),\n        ).subquery()\n\n    return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionLessThan","title":"ConditionLessThan","text":"<p>               Bases: <code>QueryOp</code></p> <p>Filter rows based on less than (or equal), to some value.</p> <p>Parameters:</p> Name Type Description Default <code>col</code> <code>str</code> <p>Column name on which to condition.</p> required <code>value</code> <code>Any</code> <p>Value greater than.</p> required <code>equal</code> <code>bool</code> <p>Include equality to the value.</p> <code>False</code> <code>not_</code> <code>bool</code> <p>Take negation of condition.</p> <code>False</code> <code>binarize_col</code> <code>Optional[str]</code> <p>If specified, create a Boolean column of name binarize_col instead of filtering.</p> <code>None</code> <code>**cond_kwargs</code> <code>Any</code> <p>Optional keyword arguments for processing the condition.</p> <code>{}</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; ConditionLessThan(\"col1\", 1)(table)\n&gt;&gt;&gt; ConditionLessThan(\"col1\", 1, binarize_col=\"col1_bool\")(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class ConditionLessThan(QueryOp):\n    \"\"\"Filter rows based on less than (or equal), to some value.\n\n    Parameters\n    ----------\n    col\n        Column name on which to condition.\n    value\n        Value greater than.\n    equal\n        Include equality to the value.\n    not_\n        Take negation of condition.\n    binarize_col\n        If specified, create a Boolean column of name binarize_col instead of filtering.\n    **cond_kwargs\n        Optional keyword arguments for processing the condition.\n\n    Examples\n    --------\n    &gt;&gt;&gt; ConditionLessThan(\"col1\", 1)(table)\n    &gt;&gt;&gt; ConditionLessThan(\"col1\", 1, binarize_col=\"col1_bool\")(table)\n\n    \"\"\"\n\n    def __init__(\n        self,\n        col: str,\n        value: typing.Any,\n        equal: bool = False,\n        not_: bool = False,\n        binarize_col: typing.Optional[str] = None,\n        **cond_kwargs: typing.Any,\n    ) -&gt; None:\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.col = col\n        self.value = value\n        self.equal = equal\n        self.not_ = not_\n        self.binarize_col = binarize_col\n        self.cond_kwargs = cond_kwargs\n\n    def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n        return_cond\n            Return the condition instead of filtering.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        if return_cond and self.binarize_col:\n            raise ValueError(\n                \"Cannot return condition and binarize column simultaneously.\",\n            )\n        table = _process_checks(table, cols=self.col, cols_not_in=self.binarize_col)\n        cond = less_than(\n            get_column(table, self.col),\n            self.value,\n            True,\n            True,\n            self.equal,\n            **self.cond_kwargs,\n        )\n        if self.not_:\n            cond = cond._negate()  # type: ignore[no-untyped-call]\n        if return_cond:\n            return cond  # type: ignore[return-value]\n        if self.binarize_col is not None:\n            return select(\n                table,\n                cast(cond, Boolean).label(self.binarize_col),\n            ).subquery()\n\n        return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionLessThan.__init__","title":"__init__","text":"<pre><code>__init__(\n    col,\n    value,\n    equal=False,\n    not_=False,\n    binarize_col=None,\n    **cond_kwargs\n)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    col: str,\n    value: typing.Any,\n    equal: bool = False,\n    not_: bool = False,\n    binarize_col: typing.Optional[str] = None,\n    **cond_kwargs: typing.Any,\n) -&gt; None:\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.col = col\n    self.value = value\n    self.equal = equal\n    self.not_ = not_\n    self.binarize_col = binarize_col\n    self.cond_kwargs = cond_kwargs\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionLessThan.__call__","title":"__call__","text":"<pre><code>__call__(table, return_cond=False)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <code>return_cond</code> <code>bool</code> <p>Return the condition instead of filtering.</p> <code>False</code> <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n    return_cond\n        Return the condition instead of filtering.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    if return_cond and self.binarize_col:\n        raise ValueError(\n            \"Cannot return condition and binarize column simultaneously.\",\n        )\n    table = _process_checks(table, cols=self.col, cols_not_in=self.binarize_col)\n    cond = less_than(\n        get_column(table, self.col),\n        self.value,\n        True,\n        True,\n        self.equal,\n        **self.cond_kwargs,\n    )\n    if self.not_:\n        cond = cond._negate()  # type: ignore[no-untyped-call]\n    if return_cond:\n        return cond  # type: ignore[return-value]\n    if self.binarize_col is not None:\n        return select(\n            table,\n            cast(cond, Boolean).label(self.binarize_col),\n        ).subquery()\n\n    return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionRegexMatch","title":"ConditionRegexMatch","text":"<p>               Bases: <code>QueryOp</code></p> <p>Filter rows based on matching a regular expression.</p> <p>Parameters:</p> Name Type Description Default <code>col</code> <code>str</code> <p>Column name on which to condition.</p> required <code>regex</code> <code>str</code> <p>Regular expression to match.</p> required <code>not_</code> <code>bool</code> <p>Take negation of condition.</p> <code>False</code> <code>binarize_col</code> <code>Optional[str]</code> <p>If specified, create a Boolean column of name binarize_col instead of filtering.</p> <code>None</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; ConditionRegexMatch(\"col1\", \".*\")(table)\n&gt;&gt;&gt; ConditionRegexMatch(\"col1\", \".*\", binarize_col=\"col1_bool\")(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class ConditionRegexMatch(QueryOp):\n    \"\"\"Filter rows based on matching a regular expression.\n\n    Parameters\n    ----------\n    col\n        Column name on which to condition.\n    regex\n        Regular expression to match.\n    not_\n        Take negation of condition.\n    binarize_col\n        If specified, create a Boolean column of name binarize_col instead of filtering.\n\n    Examples\n    --------\n    &gt;&gt;&gt; ConditionRegexMatch(\"col1\", \".*\")(table)\n    &gt;&gt;&gt; ConditionRegexMatch(\"col1\", \".*\", binarize_col=\"col1_bool\")(table)\n\n    \"\"\"\n\n    def __init__(\n        self,\n        col: str,\n        regex: str,\n        not_: bool = False,\n        binarize_col: typing.Optional[str] = None,\n    ):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.col = col\n        self.regex = regex\n        self.not_ = not_\n        self.binarize_col = binarize_col\n\n    def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n        return_cond\n            Return the condition instead of filtering.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        if return_cond and self.binarize_col:\n            raise ValueError(\n                \"Cannot return condition and binarize column simultaneously.\",\n            )\n        table = _process_checks(table, cols=self.col, cols_not_in=self.binarize_col)\n        cond = get_column(table, self.col).regexp_match(self.regex)\n        if self.not_:\n            cond = cond._negate()\n        if return_cond:\n            return cond\n        if self.binarize_col is not None:\n            return select(\n                table,\n                cast(cond, Boolean).label(self.binarize_col),\n            ).subquery()\n\n        return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionRegexMatch.__init__","title":"__init__","text":"<pre><code>__init__(col, regex, not_=False, binarize_col=None)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    col: str,\n    regex: str,\n    not_: bool = False,\n    binarize_col: typing.Optional[str] = None,\n):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.col = col\n    self.regex = regex\n    self.not_ = not_\n    self.binarize_col = binarize_col\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionRegexMatch.__call__","title":"__call__","text":"<pre><code>__call__(table, return_cond=False)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <code>return_cond</code> <code>bool</code> <p>Return the condition instead of filtering.</p> <code>False</code> <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n    return_cond\n        Return the condition instead of filtering.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    if return_cond and self.binarize_col:\n        raise ValueError(\n            \"Cannot return condition and binarize column simultaneously.\",\n        )\n    table = _process_checks(table, cols=self.col, cols_not_in=self.binarize_col)\n    cond = get_column(table, self.col).regexp_match(self.regex)\n    if self.not_:\n        cond = cond._negate()\n    if return_cond:\n        return cond\n    if self.binarize_col is not None:\n        return select(\n            table,\n            cast(cond, Boolean).label(self.binarize_col),\n        ).subquery()\n\n    return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionIn","title":"ConditionIn","text":"<p>               Bases: <code>QueryOp</code></p> <p>Filter rows based on having a value in list of values.</p> <p>Parameters:</p> Name Type Description Default <code>col</code> <code>str</code> <p>Column name on which to condition.</p> required <code>values</code> <code>Union[Any, List[Any]]</code> <p>Values in which the column value must be.</p> required <code>not_</code> <code>bool</code> <p>Take negation of condition.</p> <code>False</code> <code>binarize_col</code> <code>Optional[str]</code> <p>If specified, create a Boolean column of name binarize_col instead of filtering.</p> <code>None</code> <code>**cond_kwargs</code> <code>Any</code> <p>Optional keyword arguments for processing the condition.</p> <code>{}</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; ConditionIn(\"col1\", [1, 2])(table)\n&gt;&gt;&gt; ConditionIn(\"col1\", [1, 2], binarize_col=\"col1_bool\")(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class ConditionIn(QueryOp):\n    \"\"\"Filter rows based on having a value in list of values.\n\n    Parameters\n    ----------\n    col\n        Column name on which to condition.\n    values\n        Values in which the column value must be.\n    not_\n        Take negation of condition.\n    binarize_col\n        If specified, create a Boolean column of name binarize_col instead of filtering.\n    **cond_kwargs\n        Optional keyword arguments for processing the condition.\n\n    Examples\n    --------\n    &gt;&gt;&gt; ConditionIn(\"col1\", [1, 2])(table)\n    &gt;&gt;&gt; ConditionIn(\"col1\", [1, 2], binarize_col=\"col1_bool\")(table)\n\n    \"\"\"\n\n    def __init__(\n        self,\n        col: str,\n        values: typing.Union[typing.Any, typing.List[typing.Any]],\n        not_: bool = False,\n        binarize_col: typing.Optional[str] = None,\n        **cond_kwargs: typing.Any,\n    ) -&gt; None:\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.col = col\n        self.values = values\n        self.not_ = not_\n        self.binarize_col = binarize_col\n        self.cond_kwargs = cond_kwargs\n\n    def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n        return_cond\n            Return the condition instead of filtering.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        if return_cond and self.binarize_col:\n            raise ValueError(\n                \"Cannot return condition and binarize column simultaneously.\",\n            )\n        table = _process_checks(table, cols=self.col, cols_not_in=self.binarize_col)\n        cond = in_(\n            get_column(table, self.col),\n            to_list(self.values),\n            True,\n            True,\n            **self.cond_kwargs,\n        )\n        if self.not_:\n            cond = cond._negate()  # type: ignore[no-untyped-call]\n        if return_cond:\n            return cond  # type: ignore[return-value]\n        if self.binarize_col is not None:\n            return select(\n                table,\n                cast(cond, Boolean).label(self.binarize_col),\n            ).subquery()\n\n        return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionIn.__init__","title":"__init__","text":"<pre><code>__init__(\n    col,\n    values,\n    not_=False,\n    binarize_col=None,\n    **cond_kwargs\n)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    col: str,\n    values: typing.Union[typing.Any, typing.List[typing.Any]],\n    not_: bool = False,\n    binarize_col: typing.Optional[str] = None,\n    **cond_kwargs: typing.Any,\n) -&gt; None:\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.col = col\n    self.values = values\n    self.not_ = not_\n    self.binarize_col = binarize_col\n    self.cond_kwargs = cond_kwargs\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionIn.__call__","title":"__call__","text":"<pre><code>__call__(table, return_cond=False)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <code>return_cond</code> <code>bool</code> <p>Return the condition instead of filtering.</p> <code>False</code> <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n    return_cond\n        Return the condition instead of filtering.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    if return_cond and self.binarize_col:\n        raise ValueError(\n            \"Cannot return condition and binarize column simultaneously.\",\n        )\n    table = _process_checks(table, cols=self.col, cols_not_in=self.binarize_col)\n    cond = in_(\n        get_column(table, self.col),\n        to_list(self.values),\n        True,\n        True,\n        **self.cond_kwargs,\n    )\n    if self.not_:\n        cond = cond._negate()  # type: ignore[no-untyped-call]\n    if return_cond:\n        return cond  # type: ignore[return-value]\n    if self.binarize_col is not None:\n        return select(\n            table,\n            cast(cond, Boolean).label(self.binarize_col),\n        ).subquery()\n\n    return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionSubstring","title":"ConditionSubstring","text":"<p>               Bases: <code>QueryOp</code></p> <p>Filter rows on based on having substrings.</p> <p>Can be specified whether it must have any or all of the specified substrings. This makes no difference when only one substring is provided</p> <p>Parameters:</p> Name Type Description Default <code>col</code> <code>str</code> <p>Column name on which to condition.</p> required <code>substrings</code> <code>Union[str, List[str]]</code> <p>Substrings.</p> required <code>any_</code> <code>bool</code> <p>If true, the row must have just one of the substrings. If false, it must have all of the substrings.</p> <code>True</code> <code>not_</code> <code>bool</code> <p>Take negation of condition.</p> <code>False</code> <code>binarize_col</code> <code>Optional[str]</code> <p>If specified, create a Boolean column of name binarize_col instead of filtering.</p> <code>None</code> <code>**cond_kwargs</code> <code>Any</code> <p>Optional keyword arguments for processing the condition.</p> <code>{}</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; ConditionSubstring(\"col1\", [\"a\", \"b\"])(table)\n&gt;&gt;&gt; ConditionSubstring(\"col1\", [\"a\", \"b\"], any_=False)(table)\n&gt;&gt;&gt; ConditionSubstring(\"col1\", [\"a\", \"b\"], binarize_col=\"col1_bool\")(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class ConditionSubstring(QueryOp):\n    \"\"\"Filter rows on based on having substrings.\n\n    Can be specified whether it must have any or all of the specified substrings.\n    This makes no difference when only one substring is provided\n\n    Parameters\n    ----------\n    col\n        Column name on which to condition.\n    substrings\n        Substrings.\n    any_\n        If true, the row must have just one of the substrings. If false, it must\n        have all of the substrings.\n    not_\n        Take negation of condition.\n    binarize_col\n        If specified, create a Boolean column of name binarize_col instead of filtering.\n    **cond_kwargs\n        Optional keyword arguments for processing the condition.\n\n    Examples\n    --------\n    &gt;&gt;&gt; ConditionSubstring(\"col1\", [\"a\", \"b\"])(table)\n    &gt;&gt;&gt; ConditionSubstring(\"col1\", [\"a\", \"b\"], any_=False)(table)\n    &gt;&gt;&gt; ConditionSubstring(\"col1\", [\"a\", \"b\"], binarize_col=\"col1_bool\")(table)\n\n    \"\"\"\n\n    def __init__(\n        self,\n        col: str,\n        substrings: typing.Union[str, typing.List[str]],\n        any_: bool = True,\n        not_: bool = False,\n        binarize_col: typing.Optional[str] = None,\n        **cond_kwargs: typing.Any,\n    ) -&gt; None:\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.col = col\n        self.substrings = to_list(substrings)\n        self.any_ = any_\n        self.not_ = not_\n        self.binarize_col = binarize_col\n        self.cond_kwargs = cond_kwargs\n\n    def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n        return_cond\n            Return the condition instead of filtering.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        if return_cond and self.binarize_col:\n            raise ValueError(\n                \"Cannot return condition and binarize column simultaneously.\",\n            )\n        table = _process_checks(table, cols=self.col, cols_not_in=self.binarize_col)\n        conds = [\n            has_substring(get_column(table, self.col), sub, True, **self.cond_kwargs)\n            for sub in self.substrings\n        ]\n        cond = or_(*conds) if self.any_ else and_(*conds)\n        if self.not_:\n            cond = cond._negate()\n        if return_cond:\n            return cond  # type: ignore[return-value]\n        if self.binarize_col is not None:\n            return select(\n                table,\n                cast(cond, Boolean).label(self.binarize_col),\n            ).subquery()\n\n        return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionSubstring.__init__","title":"__init__","text":"<pre><code>__init__(\n    col,\n    substrings,\n    any_=True,\n    not_=False,\n    binarize_col=None,\n    **cond_kwargs\n)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    col: str,\n    substrings: typing.Union[str, typing.List[str]],\n    any_: bool = True,\n    not_: bool = False,\n    binarize_col: typing.Optional[str] = None,\n    **cond_kwargs: typing.Any,\n) -&gt; None:\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.col = col\n    self.substrings = to_list(substrings)\n    self.any_ = any_\n    self.not_ = not_\n    self.binarize_col = binarize_col\n    self.cond_kwargs = cond_kwargs\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionSubstring.__call__","title":"__call__","text":"<pre><code>__call__(table, return_cond=False)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <code>return_cond</code> <code>bool</code> <p>Return the condition instead of filtering.</p> <code>False</code> <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n    return_cond\n        Return the condition instead of filtering.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    if return_cond and self.binarize_col:\n        raise ValueError(\n            \"Cannot return condition and binarize column simultaneously.\",\n        )\n    table = _process_checks(table, cols=self.col, cols_not_in=self.binarize_col)\n    conds = [\n        has_substring(get_column(table, self.col), sub, True, **self.cond_kwargs)\n        for sub in self.substrings\n    ]\n    cond = or_(*conds) if self.any_ else and_(*conds)\n    if self.not_:\n        cond = cond._negate()\n    if return_cond:\n        return cond  # type: ignore[return-value]\n    if self.binarize_col is not None:\n        return select(\n            table,\n            cast(cond, Boolean).label(self.binarize_col),\n        ).subquery()\n\n    return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionStartsWith","title":"ConditionStartsWith","text":"<p>               Bases: <code>QueryOp</code></p> <p>Filter rows based on starting with some string.</p> <p>Parameters:</p> Name Type Description Default <code>col</code> <code>str</code> <p>Column name on which to condition.</p> required <code>string</code> <code>str</code> <p>String.</p> required <code>not_</code> <code>bool</code> <p>Take negation of condition.</p> <code>False</code> <code>binarize_col</code> <code>Optional[str]</code> <p>If specified, create a Boolean column of name binarize_col instead of filtering.</p> <code>None</code> <code>**cond_kwargs</code> <code>Any</code> <p>Optional keyword arguments for processing the condition.</p> <code>{}</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; ConditionStartsWith(\"col1\", \"a\")(table)\n&gt;&gt;&gt; ConditionStartsWith(\"col1\", \"a\", binarize_col=\"col1_bool\")(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class ConditionStartsWith(QueryOp):\n    \"\"\"Filter rows based on starting with some string.\n\n    Parameters\n    ----------\n    col: str\n        Column name on which to condition.\n    string: any\n        String.\n    not_: bool, default=False\n        Take negation of condition.\n    binarize_col: str, optional\n        If specified, create a Boolean column of name binarize_col instead of filtering.\n    **cond_kwargs\n        Optional keyword arguments for processing the condition.\n\n    Examples\n    --------\n    &gt;&gt;&gt; ConditionStartsWith(\"col1\", \"a\")(table)\n    &gt;&gt;&gt; ConditionStartsWith(\"col1\", \"a\", binarize_col=\"col1_bool\")(table)\n\n    \"\"\"\n\n    def __init__(\n        self,\n        col: str,\n        string: str,\n        not_: bool = False,\n        binarize_col: typing.Optional[str] = None,\n        **cond_kwargs: typing.Any,\n    ) -&gt; None:\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.col = col\n        self.string = string\n        self.not_ = not_\n        self.binarize_col = binarize_col\n        self.cond_kwargs = cond_kwargs\n\n    def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n        return_cond\n            Return the condition instead of filtering.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        if return_cond and self.binarize_col:\n            raise ValueError(\n                \"Cannot return condition and binarize column simultaneously.\",\n            )\n        table = _process_checks(table, cols=self.col, cols_not_in=self.binarize_col)\n        cond = starts_with(\n            get_column(table, self.col),\n            self.string,\n            True,\n            True,\n            **self.cond_kwargs,\n        )\n        if self.not_:\n            cond = cond._negate()  # type: ignore[no-untyped-call]\n        if return_cond:\n            return cond  # type: ignore[return-value]\n        if self.binarize_col is not None:\n            return select(\n                table,\n                cast(cond, Boolean).label(self.binarize_col),\n            ).subquery()\n\n        return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionStartsWith.__init__","title":"__init__","text":"<pre><code>__init__(\n    col,\n    string,\n    not_=False,\n    binarize_col=None,\n    **cond_kwargs\n)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    col: str,\n    string: str,\n    not_: bool = False,\n    binarize_col: typing.Optional[str] = None,\n    **cond_kwargs: typing.Any,\n) -&gt; None:\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.col = col\n    self.string = string\n    self.not_ = not_\n    self.binarize_col = binarize_col\n    self.cond_kwargs = cond_kwargs\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionStartsWith.__call__","title":"__call__","text":"<pre><code>__call__(table, return_cond=False)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <code>return_cond</code> <code>bool</code> <p>Return the condition instead of filtering.</p> <code>False</code> <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n    return_cond\n        Return the condition instead of filtering.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    if return_cond and self.binarize_col:\n        raise ValueError(\n            \"Cannot return condition and binarize column simultaneously.\",\n        )\n    table = _process_checks(table, cols=self.col, cols_not_in=self.binarize_col)\n    cond = starts_with(\n        get_column(table, self.col),\n        self.string,\n        True,\n        True,\n        **self.cond_kwargs,\n    )\n    if self.not_:\n        cond = cond._negate()  # type: ignore[no-untyped-call]\n    if return_cond:\n        return cond  # type: ignore[return-value]\n    if self.binarize_col is not None:\n        return select(\n            table,\n            cast(cond, Boolean).label(self.binarize_col),\n        ).subquery()\n\n    return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionEndsWith","title":"ConditionEndsWith","text":"<p>               Bases: <code>QueryOp</code></p> <p>Filter rows based on ending with some string.</p> <p>Parameters:</p> Name Type Description Default <code>col</code> <code>str</code> <p>Column name on which to condition.</p> required <code>string</code> <code>str</code> <p>String to end with.</p> required <code>not_</code> <code>bool</code> <p>Take negation of condition.</p> <code>False</code> <code>binarize_col</code> <code>Optional[str]</code> <p>If specified, create a Boolean column of name binarize_col instead of filtering.</p> <code>None</code> <code>**cond_kwargs</code> <code>Any</code> <p>Optional keyword arguments for processing the condition.</p> <code>{}</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; ConditionEndsWith(\"col1\", \"a\")(table)\n&gt;&gt;&gt; ConditionEndsWith(\"col1\", \"a\", binarize_col=\"col1_bool\")(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class ConditionEndsWith(QueryOp):\n    \"\"\"Filter rows based on ending with some string.\n\n    Parameters\n    ----------\n    col\n        Column name on which to condition.\n    string\n        String to end with.\n    not_\n        Take negation of condition.\n    binarize_col\n        If specified, create a Boolean column of name binarize_col instead of filtering.\n    **cond_kwargs\n        Optional keyword arguments for processing the condition.\n\n    Examples\n    --------\n    &gt;&gt;&gt; ConditionEndsWith(\"col1\", \"a\")(table)\n    &gt;&gt;&gt; ConditionEndsWith(\"col1\", \"a\", binarize_col=\"col1_bool\")(table)\n\n    \"\"\"\n\n    def __init__(\n        self,\n        col: str,\n        string: str,\n        not_: bool = False,\n        binarize_col: typing.Optional[str] = None,\n        **cond_kwargs: typing.Any,\n    ) -&gt; None:\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.col = col\n        self.string = string\n        self.not_ = not_\n        self.binarize_col = binarize_col\n        self.cond_kwargs = cond_kwargs\n\n    def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n        return_cond\n            Return the condition instead of filtering.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        if return_cond and self.binarize_col:\n            raise ValueError(\n                \"Cannot return condition and binarize column simultaneously.\",\n            )\n        table = _process_checks(table, cols=self.col, cols_not_in=self.binarize_col)\n        cond = ends_with(\n            get_column(table, self.col),\n            self.string,\n            True,\n            True,\n            **self.cond_kwargs,\n        )\n        if self.not_:\n            cond = cond._negate()  # type: ignore[no-untyped-call]\n        if return_cond:\n            return cond  # type: ignore[return-value]\n        if self.binarize_col is not None:\n            return select(\n                table,\n                cast(cond, Boolean).label(self.binarize_col),\n            ).subquery()\n\n        return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionEndsWith.__init__","title":"__init__","text":"<pre><code>__init__(\n    col,\n    string,\n    not_=False,\n    binarize_col=None,\n    **cond_kwargs\n)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    col: str,\n    string: str,\n    not_: bool = False,\n    binarize_col: typing.Optional[str] = None,\n    **cond_kwargs: typing.Any,\n) -&gt; None:\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.col = col\n    self.string = string\n    self.not_ = not_\n    self.binarize_col = binarize_col\n    self.cond_kwargs = cond_kwargs\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionEndsWith.__call__","title":"__call__","text":"<pre><code>__call__(table, return_cond=False)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <code>return_cond</code> <code>bool</code> <p>Return the condition instead of filtering.</p> <code>False</code> <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n    return_cond\n        Return the condition instead of filtering.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    if return_cond and self.binarize_col:\n        raise ValueError(\n            \"Cannot return condition and binarize column simultaneously.\",\n        )\n    table = _process_checks(table, cols=self.col, cols_not_in=self.binarize_col)\n    cond = ends_with(\n        get_column(table, self.col),\n        self.string,\n        True,\n        True,\n        **self.cond_kwargs,\n    )\n    if self.not_:\n        cond = cond._negate()  # type: ignore[no-untyped-call]\n    if return_cond:\n        return cond  # type: ignore[return-value]\n    if self.binarize_col is not None:\n        return select(\n            table,\n            cast(cond, Boolean).label(self.binarize_col),\n        ).subquery()\n\n    return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionInYears","title":"ConditionInYears","text":"<p>               Bases: <code>QueryOp</code></p> <p>Filter rows based on a timestamp column being in a list of years.</p> <p>Parameters:</p> Name Type Description Default <code>timestamp_col</code> <code>str</code> <p>Timestamp column name.</p> required <code>years</code> <code>Union[int, List[int]]</code> <p>Years in which the timestamps must be.</p> required <code>not_</code> <code>bool</code> <p>Take negation of condition.</p> <code>False</code> <code>binarize_col</code> <code>Optional[str]</code> <p>If specified, create a Boolean column of name binarize_col instead of filtering.</p> <code>None</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; ConditionInYears(\"col1\", [2019, 2020])(table)\n&gt;&gt;&gt; ConditionInYears(\"col1\", 2019)(table)\n&gt;&gt;&gt; ConditionInYears(\"col1\", 2019, binarize_col=\"col1_bool\")(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class ConditionInYears(QueryOp):\n    \"\"\"Filter rows based on a timestamp column being in a list of years.\n\n    Parameters\n    ----------\n    timestamp_col\n        Timestamp column name.\n    years\n        Years in which the timestamps must be.\n    not_\n        Take negation of condition.\n    binarize_col\n        If specified, create a Boolean column of name binarize_col instead of filtering.\n\n    Examples\n    --------\n    &gt;&gt;&gt; ConditionInYears(\"col1\", [2019, 2020])(table)\n    &gt;&gt;&gt; ConditionInYears(\"col1\", 2019)(table)\n    &gt;&gt;&gt; ConditionInYears(\"col1\", 2019, binarize_col=\"col1_bool\")(table)\n\n    \"\"\"\n\n    def __init__(\n        self,\n        timestamp_col: str,\n        years: typing.Union[int, typing.List[int]],\n        not_: bool = False,\n        binarize_col: typing.Optional[str] = None,\n    ):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.timestamp_col = timestamp_col\n        self.years = years\n        self.not_ = not_\n        self.binarize_col = binarize_col\n\n    def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n        return_cond\n            Return the condition instead of filtering.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        if return_cond and self.binarize_col:\n            raise ValueError(\n                \"Cannot return condition and binarize column simultaneously.\",\n            )\n        table = _process_checks(\n            table,\n            cols=self.timestamp_col,\n            cols_not_in=self.binarize_col,\n        )\n        cond = in_(\n            extract(\"year\", get_column(table, self.timestamp_col)),  # type: ignore[arg-type]\n            to_list(self.years),\n        )\n        if self.not_:\n            cond = cond._negate()  # type: ignore[no-untyped-call]\n        if return_cond:\n            return cond  # type: ignore[return-value]\n        if self.binarize_col is not None:\n            return select(\n                table,\n                cast(cond, Boolean).label(self.binarize_col),\n            ).subquery()\n\n        return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionInYears.__init__","title":"__init__","text":"<pre><code>__init__(\n    timestamp_col, years, not_=False, binarize_col=None\n)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    timestamp_col: str,\n    years: typing.Union[int, typing.List[int]],\n    not_: bool = False,\n    binarize_col: typing.Optional[str] = None,\n):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.timestamp_col = timestamp_col\n    self.years = years\n    self.not_ = not_\n    self.binarize_col = binarize_col\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionInYears.__call__","title":"__call__","text":"<pre><code>__call__(table, return_cond=False)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <code>return_cond</code> <code>bool</code> <p>Return the condition instead of filtering.</p> <code>False</code> <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n    return_cond\n        Return the condition instead of filtering.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    if return_cond and self.binarize_col:\n        raise ValueError(\n            \"Cannot return condition and binarize column simultaneously.\",\n        )\n    table = _process_checks(\n        table,\n        cols=self.timestamp_col,\n        cols_not_in=self.binarize_col,\n    )\n    cond = in_(\n        extract(\"year\", get_column(table, self.timestamp_col)),  # type: ignore[arg-type]\n        to_list(self.years),\n    )\n    if self.not_:\n        cond = cond._negate()  # type: ignore[no-untyped-call]\n    if return_cond:\n        return cond  # type: ignore[return-value]\n    if self.binarize_col is not None:\n        return select(\n            table,\n            cast(cond, Boolean).label(self.binarize_col),\n        ).subquery()\n\n    return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionInMonths","title":"ConditionInMonths","text":"<p>               Bases: <code>QueryOp</code></p> <p>Filter rows based on a timestamp being in a list of years.</p> <p>Parameters:</p> Name Type Description Default <code>timestamp_col</code> <code>str</code> <p>Timestamp column name.</p> required <code>months</code> <code>Union[int, List[int]]</code> <p>Months in which the timestamps must be.</p> required <code>not_</code> <code>bool</code> <p>Take negation of condition.</p> <code>False</code> <code>binarize_col</code> <code>Optional[str]</code> <p>If specified, create a Boolean column of name binarize_col instead of filtering.</p> <code>None</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; ConditionInMonths(\"col1\", [1, 2])(table)\n&gt;&gt;&gt; ConditionInMonths(\"col1\", 1)(table)\n&gt;&gt;&gt; ConditionInMonths(\"col1\", 1, binarize_col=\"col1_bool\")(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class ConditionInMonths(QueryOp):\n    \"\"\"Filter rows based on a timestamp being in a list of years.\n\n    Parameters\n    ----------\n    timestamp_col\n        Timestamp column name.\n    months\n        Months in which the timestamps must be.\n    not_\n        Take negation of condition.\n    binarize_col\n        If specified, create a Boolean column of name binarize_col instead of filtering.\n\n    Examples\n    --------\n    &gt;&gt;&gt; ConditionInMonths(\"col1\", [1, 2])(table)\n    &gt;&gt;&gt; ConditionInMonths(\"col1\", 1)(table)\n    &gt;&gt;&gt; ConditionInMonths(\"col1\", 1, binarize_col=\"col1_bool\")(table)\n\n    \"\"\"\n\n    def __init__(\n        self,\n        timestamp_col: str,\n        months: typing.Union[int, typing.List[int]],\n        not_: bool = False,\n        binarize_col: typing.Optional[str] = None,\n    ):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.timestamp_col = timestamp_col\n        self.months = months\n        self.not_ = not_\n        self.binarize_col = binarize_col\n\n    def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n        return_cond\n            Return the condition instead of filtering.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        if return_cond and self.binarize_col:\n            raise ValueError(\n                \"Cannot return condition and binarize column simultaneously.\",\n            )\n        table = _process_checks(\n            table,\n            cols=self.timestamp_col,\n            cols_not_in=self.binarize_col,\n        )\n        cond = in_(\n            extract(\"month\", get_column(table, self.timestamp_col)),  # type: ignore[arg-type]\n            to_list(self.months),\n        )\n        if self.not_:\n            cond = cond._negate()  # type: ignore[no-untyped-call]\n        if return_cond:\n            return cond  # type: ignore[return-value]\n        if self.binarize_col is not None:\n            return select(\n                table,\n                cast(cond, Boolean).label(self.binarize_col),\n            ).subquery()\n\n        return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionInMonths.__init__","title":"__init__","text":"<pre><code>__init__(\n    timestamp_col, months, not_=False, binarize_col=None\n)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    timestamp_col: str,\n    months: typing.Union[int, typing.List[int]],\n    not_: bool = False,\n    binarize_col: typing.Optional[str] = None,\n):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.timestamp_col = timestamp_col\n    self.months = months\n    self.not_ = not_\n    self.binarize_col = binarize_col\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionInMonths.__call__","title":"__call__","text":"<pre><code>__call__(table, return_cond=False)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <code>return_cond</code> <code>bool</code> <p>Return the condition instead of filtering.</p> <code>False</code> <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n    return_cond\n        Return the condition instead of filtering.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    if return_cond and self.binarize_col:\n        raise ValueError(\n            \"Cannot return condition and binarize column simultaneously.\",\n        )\n    table = _process_checks(\n        table,\n        cols=self.timestamp_col,\n        cols_not_in=self.binarize_col,\n    )\n    cond = in_(\n        extract(\"month\", get_column(table, self.timestamp_col)),  # type: ignore[arg-type]\n        to_list(self.months),\n    )\n    if self.not_:\n        cond = cond._negate()  # type: ignore[no-untyped-call]\n    if return_cond:\n        return cond  # type: ignore[return-value]\n    if self.binarize_col is not None:\n        return select(\n            table,\n            cast(cond, Boolean).label(self.binarize_col),\n        ).subquery()\n\n    return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionBeforeDate","title":"ConditionBeforeDate","text":"<p>               Bases: <code>QueryOp</code></p> <p>Filter rows based on a timestamp being before some date.</p> <p>Parameters:</p> Name Type Description Default <code>timestamp_col</code> <code>str</code> <p>Timestamp column name.</p> required <code>timestamp</code> <code>Union[str, datetime]</code> <p>A datetime object or str in YYYY-MM-DD format.</p> required <code>not_</code> <code>bool</code> <p>Take negation of condition.</p> <code>False</code> <code>binarize_col</code> <code>Optional[str]</code> <p>If specified, create a Boolean column of name binarize_col instead of filtering.</p> <code>None</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; ConditionBeforeDate(\"col1\", \"2020-01-01\")(table)\n&gt;&gt;&gt; ConditionBeforeDate(\"col1\", datetime.datetime(2020, 1, 1))(table)\n&gt;&gt;&gt; ConditionBeforeDate(\"col1\", \"2020-01-01\", binarize_col=\"col1_bool\")(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class ConditionBeforeDate(QueryOp):\n    \"\"\"Filter rows based on a timestamp being before some date.\n\n    Parameters\n    ----------\n    timestamp_col\n        Timestamp column name.\n    timestamp\n        A datetime object or str in YYYY-MM-DD format.\n    not_\n        Take negation of condition.\n    binarize_col\n        If specified, create a Boolean column of name binarize_col instead of filtering.\n\n    Examples\n    --------\n    &gt;&gt;&gt; ConditionBeforeDate(\"col1\", \"2020-01-01\")(table)\n    &gt;&gt;&gt; ConditionBeforeDate(\"col1\", datetime.datetime(2020, 1, 1))(table)\n    &gt;&gt;&gt; ConditionBeforeDate(\"col1\", \"2020-01-01\", binarize_col=\"col1_bool\")(table)\n\n    \"\"\"\n\n    def __init__(\n        self,\n        timestamp_col: str,\n        timestamp: typing.Union[str, datetime],\n        not_: bool = False,\n        binarize_col: typing.Optional[str] = None,\n    ):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.timestamp_col = timestamp_col\n        self.timestamp = timestamp\n        self.not_ = not_\n        self.binarize_col = binarize_col\n\n    def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n        return_cond\n            Return the condition instead of filtering.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        if return_cond and self.binarize_col:\n            raise ValueError(\n                \"Cannot return condition and binarize column simultaneously.\",\n            )\n        table = _process_checks(table, timestamp_cols=self.timestamp_col)\n        if isinstance(self.timestamp, str):\n            timestamp = to_datetime_format(self.timestamp)\n        else:\n            timestamp = self.timestamp\n        cond = get_column(table, self.timestamp_col) &lt;= timestamp\n        if self.not_:\n            cond = cond._negate()\n        if return_cond:\n            return cond\n        if self.binarize_col is not None:\n            return select(\n                table,\n                cast(cond, Boolean).label(self.binarize_col),\n            ).subquery()\n\n        return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionBeforeDate.__init__","title":"__init__","text":"<pre><code>__init__(\n    timestamp_col, timestamp, not_=False, binarize_col=None\n)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    timestamp_col: str,\n    timestamp: typing.Union[str, datetime],\n    not_: bool = False,\n    binarize_col: typing.Optional[str] = None,\n):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.timestamp_col = timestamp_col\n    self.timestamp = timestamp\n    self.not_ = not_\n    self.binarize_col = binarize_col\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionBeforeDate.__call__","title":"__call__","text":"<pre><code>__call__(table, return_cond=False)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <code>return_cond</code> <code>bool</code> <p>Return the condition instead of filtering.</p> <code>False</code> <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n    return_cond\n        Return the condition instead of filtering.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    if return_cond and self.binarize_col:\n        raise ValueError(\n            \"Cannot return condition and binarize column simultaneously.\",\n        )\n    table = _process_checks(table, timestamp_cols=self.timestamp_col)\n    if isinstance(self.timestamp, str):\n        timestamp = to_datetime_format(self.timestamp)\n    else:\n        timestamp = self.timestamp\n    cond = get_column(table, self.timestamp_col) &lt;= timestamp\n    if self.not_:\n        cond = cond._negate()\n    if return_cond:\n        return cond\n    if self.binarize_col is not None:\n        return select(\n            table,\n            cast(cond, Boolean).label(self.binarize_col),\n        ).subquery()\n\n    return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionAfterDate","title":"ConditionAfterDate","text":"<p>               Bases: <code>QueryOp</code></p> <p>Filter rows based on a timestamp being after some date.</p> <p>Parameters:</p> Name Type Description Default <code>timestamp_col</code> <code>str</code> <p>Timestamp column name.</p> required <code>timestamp</code> <code>Union[str, datetime]</code> <p>A datetime object or str in YYYY-MM-DD format.</p> required <code>not_</code> <code>bool</code> <p>Take negation of condition.</p> <code>False</code> <code>binarize_col</code> <code>Optional[str]</code> <p>If specified, create a Boolean column of name binarize_col instead of filtering.</p> <code>None</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; ConditionAfterDate(\"col1\", \"2020-01-01\")(table)\n&gt;&gt;&gt; ConditionAfterDate(\"col1\", datetime.datetime(2020, 1, 1))(table)\n&gt;&gt;&gt; ConditionAfterDate(\"col1\", \"2020-01-01\", binarize_col=\"col1_bool\")(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class ConditionAfterDate(QueryOp):\n    \"\"\"Filter rows based on a timestamp being after some date.\n\n    Parameters\n    ----------\n    timestamp_col\n        Timestamp column name.\n    timestamp\n        A datetime object or str in YYYY-MM-DD format.\n    not_\n        Take negation of condition.\n    binarize_col\n        If specified, create a Boolean column of name binarize_col instead of filtering.\n\n    Examples\n    --------\n    &gt;&gt;&gt; ConditionAfterDate(\"col1\", \"2020-01-01\")(table)\n    &gt;&gt;&gt; ConditionAfterDate(\"col1\", datetime.datetime(2020, 1, 1))(table)\n    &gt;&gt;&gt; ConditionAfterDate(\"col1\", \"2020-01-01\", binarize_col=\"col1_bool\")(table)\n\n    \"\"\"\n\n    def __init__(\n        self,\n        timestamp_col: str,\n        timestamp: typing.Union[str, datetime],\n        not_: bool = False,\n        binarize_col: typing.Optional[str] = None,\n    ):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.timestamp_col = timestamp_col\n        self.timestamp = timestamp\n        self.not_ = not_\n        self.binarize_col = binarize_col\n\n    def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n        return_cond\n            Return the condition instead of filtering.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        if return_cond and self.binarize_col:\n            raise ValueError(\n                \"Cannot return condition and binarize column simultaneously.\",\n            )\n        table = _process_checks(table, timestamp_cols=self.timestamp_col)\n        if isinstance(self.timestamp, str):\n            timestamp = to_datetime_format(self.timestamp)\n        else:\n            timestamp = self.timestamp\n        cond = get_column(table, self.timestamp_col) &gt;= timestamp\n        if self.not_:\n            cond = cond._negate()\n        if return_cond:\n            return cond\n        if self.binarize_col is not None:\n            return select(\n                table,\n                cast(cond, Boolean).label(self.binarize_col),\n            ).subquery()\n\n        return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionAfterDate.__init__","title":"__init__","text":"<pre><code>__init__(\n    timestamp_col, timestamp, not_=False, binarize_col=None\n)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    timestamp_col: str,\n    timestamp: typing.Union[str, datetime],\n    not_: bool = False,\n    binarize_col: typing.Optional[str] = None,\n):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.timestamp_col = timestamp_col\n    self.timestamp = timestamp\n    self.not_ = not_\n    self.binarize_col = binarize_col\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionAfterDate.__call__","title":"__call__","text":"<pre><code>__call__(table, return_cond=False)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <code>return_cond</code> <code>bool</code> <p>Return the condition instead of filtering.</p> <code>False</code> <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n    return_cond\n        Return the condition instead of filtering.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    if return_cond and self.binarize_col:\n        raise ValueError(\n            \"Cannot return condition and binarize column simultaneously.\",\n        )\n    table = _process_checks(table, timestamp_cols=self.timestamp_col)\n    if isinstance(self.timestamp, str):\n        timestamp = to_datetime_format(self.timestamp)\n    else:\n        timestamp = self.timestamp\n    cond = get_column(table, self.timestamp_col) &gt;= timestamp\n    if self.not_:\n        cond = cond._negate()\n    if return_cond:\n        return cond\n    if self.binarize_col is not None:\n        return select(\n            table,\n            cast(cond, Boolean).label(self.binarize_col),\n        ).subquery()\n\n    return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionLike","title":"ConditionLike","text":"<p>               Bases: <code>QueryOp</code></p> <p>Filter rows by a LIKE condition.</p> <p>Parameters:</p> Name Type Description Default <code>col</code> <code>str</code> <p>Column to filter on.</p> required <code>pattern</code> <code>str</code> <p>Pattern to filter on.</p> required <code>not_</code> <code>bool</code> <p>Take negation of condition.</p> <code>False</code> <code>binarize_col</code> <code>Optional[str]</code> <p>If specified, create a Boolean column of name binarize_col instead of filtering.</p> <code>None</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; ConditionLike(\"lab_name\", \"HbA1c\")(table)\n&gt;&gt;&gt; ConditionLike(\"lab_name\", \"HbA1c\", not_=True)(table)\n&gt;&gt;&gt; ConditionLike(\"lab_name\", \"HbA1c\", binarize_col=\"lab_name_bool\")(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class ConditionLike(QueryOp):\n    \"\"\"Filter rows by a LIKE condition.\n\n    Parameters\n    ----------\n    col\n        Column to filter on.\n    pattern\n        Pattern to filter on.\n    not_\n        Take negation of condition.\n    binarize_col\n        If specified, create a Boolean column of name binarize_col instead of filtering.\n\n    Examples\n    --------\n    &gt;&gt;&gt; ConditionLike(\"lab_name\", \"HbA1c\")(table)\n    &gt;&gt;&gt; ConditionLike(\"lab_name\", \"HbA1c\", not_=True)(table)\n    &gt;&gt;&gt; ConditionLike(\"lab_name\", \"HbA1c\", binarize_col=\"lab_name_bool\")(table)\n\n    \"\"\"\n\n    def __init__(\n        self,\n        col: str,\n        pattern: str,\n        not_: bool = False,\n        binarize_col: typing.Optional[str] = None,\n    ):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.col = col\n        self.pattern = pattern\n        self.not_ = not_\n        self.binarize_col = binarize_col\n\n    def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n        return_cond\n            Return the condition instead of filtering.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        if return_cond and self.binarize_col:\n            raise ValueError(\n                \"Cannot return condition and binarize column simultaneously.\",\n            )\n        table = _process_checks(table, cols=self.col)\n        cond = get_column(table, self.col).like(self.pattern)\n        if self.not_:\n            cond = cond._negate()\n        if return_cond:\n            return cond\n        if self.binarize_col is not None:\n            return select(\n                table,\n                cast(cond, Boolean).label(self.binarize_col),\n            ).subquery()\n\n        return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionLike.__init__","title":"__init__","text":"<pre><code>__init__(col, pattern, not_=False, binarize_col=None)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    col: str,\n    pattern: str,\n    not_: bool = False,\n    binarize_col: typing.Optional[str] = None,\n):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.col = col\n    self.pattern = pattern\n    self.not_ = not_\n    self.binarize_col = binarize_col\n</code></pre>"},{"location":"api/#cycquery.ops.ConditionLike.__call__","title":"__call__","text":"<pre><code>__call__(table, return_cond=False)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <code>return_cond</code> <code>bool</code> <p>Return the condition instead of filtering.</p> <code>False</code> <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n    return_cond\n        Return the condition instead of filtering.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    if return_cond and self.binarize_col:\n        raise ValueError(\n            \"Cannot return condition and binarize column simultaneously.\",\n        )\n    table = _process_checks(table, cols=self.col)\n    cond = get_column(table, self.col).like(self.pattern)\n    if self.not_:\n        cond = cond._negate()\n    if return_cond:\n        return cond\n    if self.binarize_col is not None:\n        return select(\n            table,\n            cast(cond, Boolean).label(self.binarize_col),\n        ).subquery()\n\n    return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.Or","title":"Or","text":"<p>               Bases: <code>QueryOp</code></p> <p>Combine multiple condition query ops using an OR.</p> <p>Parameters:</p> Name Type Description Default <code>cond_ops</code> <code>Union[QueryOp, List[QueryOp]]</code> <p>Condition Query ops to combine.</p> <code>()</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; Or(ConditionLike(\"lab_name\", \"HbA1c\"), ConditionIn(\"name\", [\"John\", \"Jane\"]))\n&gt;&gt;&gt; Or([ConditionLike(\"lab_name\", \"HbA1c\"), ConditionIn(\"name\", [\"John\", \"Jane\"])])\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class Or(QueryOp):\n    \"\"\"Combine multiple condition query ops using an OR.\n\n    Parameters\n    ----------\n    cond_ops\n        Condition Query ops to combine.\n\n    Examples\n    --------\n    &gt;&gt;&gt; Or(ConditionLike(\"lab_name\", \"HbA1c\"), ConditionIn(\"name\", [\"John\", \"Jane\"]))\n    &gt;&gt;&gt; Or([ConditionLike(\"lab_name\", \"HbA1c\"), ConditionIn(\"name\", [\"John\", \"Jane\"])])\n\n    \"\"\"\n\n    def __init__(self, *cond_ops: typing.Union[QueryOp, typing.List[QueryOp]]):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.cond_ops = cond_ops\n\n    def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n        return_cond\n            Return the condition instead of filtering.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        ops = []\n        for cond_op in self.cond_ops:\n            if isinstance(cond_op, list):\n                if len(self.cond_ops) != 1:\n                    raise ValueError(\"Cannot combine multiple lists of conditions.\")\n                ops = [op(table, return_cond=True) for op in cond_op]\n            if isinstance(cond_op, QueryOp):\n                if len(self.cond_ops) == 1:\n                    return cond_op(table, return_cond=return_cond)\n                ops.append(cond_op(table, return_cond=True))\n        cond = or_(*ops)  # type: ignore[arg-type]\n        if return_cond:\n            return cond  # type: ignore[return-value]\n\n        return select(table).where(cond).subquery()  # type: ignore[arg-type]\n</code></pre>"},{"location":"api/#cycquery.ops.Or.__init__","title":"__init__","text":"<pre><code>__init__(*cond_ops)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(self, *cond_ops: typing.Union[QueryOp, typing.List[QueryOp]]):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.cond_ops = cond_ops\n</code></pre>"},{"location":"api/#cycquery.ops.Or.__call__","title":"__call__","text":"<pre><code>__call__(table, return_cond=False)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <code>return_cond</code> <code>bool</code> <p>Return the condition instead of filtering.</p> <code>False</code> <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n    return_cond\n        Return the condition instead of filtering.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    ops = []\n    for cond_op in self.cond_ops:\n        if isinstance(cond_op, list):\n            if len(self.cond_ops) != 1:\n                raise ValueError(\"Cannot combine multiple lists of conditions.\")\n            ops = [op(table, return_cond=True) for op in cond_op]\n        if isinstance(cond_op, QueryOp):\n            if len(self.cond_ops) == 1:\n                return cond_op(table, return_cond=return_cond)\n            ops.append(cond_op(table, return_cond=True))\n    cond = or_(*ops)  # type: ignore[arg-type]\n    if return_cond:\n        return cond  # type: ignore[return-value]\n\n    return select(table).where(cond).subquery()  # type: ignore[arg-type]\n</code></pre>"},{"location":"api/#cycquery.ops.And","title":"And","text":"<p>               Bases: <code>QueryOp</code></p> <p>Combine multiple condition query ops using an And.</p> <p>Parameters:</p> Name Type Description Default <code>*cond_ops</code> <code>Union[QueryOp, List[QueryOp]]</code> <p>Query ops to combine.</p> <code>()</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; And([ConditionLike(\"lab_name\", \"HbA1c\"), ConditionIn(\"name\", [\"John\", \"Jane\"])])\n&gt;&gt;&gt; And(ConditionLike(\"lab_name\", \"HbA1c\"), ConditionIn(\"name\", [\"John\", \"Jane\"]))\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class And(QueryOp):\n    \"\"\"Combine multiple condition query ops using an And.\n\n    Parameters\n    ----------\n    *cond_ops\n        Query ops to combine.\n\n    Examples\n    --------\n    &gt;&gt;&gt; And([ConditionLike(\"lab_name\", \"HbA1c\"), ConditionIn(\"name\", [\"John\", \"Jane\"])])\n    &gt;&gt;&gt; And(ConditionLike(\"lab_name\", \"HbA1c\"), ConditionIn(\"name\", [\"John\", \"Jane\"]))\n\n    \"\"\"\n\n    def __init__(self, *cond_ops: typing.Union[QueryOp, typing.List[QueryOp]]):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.cond_ops = cond_ops\n\n    def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n        return_cond\n            Return the condition instead of filtering.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        ops = []\n        for cond_op in self.cond_ops:\n            if isinstance(cond_op, list):\n                if len(self.cond_ops) != 1:\n                    raise ValueError(\"Cannot combine multiple lists of conditions.\")\n                ops = [op(table, return_cond=True) for op in cond_op]\n            if isinstance(cond_op, QueryOp):\n                if len(self.cond_ops) == 1:\n                    return cond_op(table, return_cond=return_cond)\n                ops.append(cond_op(table, return_cond=True))\n        cond = and_(*ops)  # type: ignore[arg-type]\n        if return_cond:\n            return cond  # type: ignore[return-value]\n\n        return select(table).where(cond).subquery()  # type: ignore[arg-type]\n</code></pre>"},{"location":"api/#cycquery.ops.And.__init__","title":"__init__","text":"<pre><code>__init__(*cond_ops)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(self, *cond_ops: typing.Union[QueryOp, typing.List[QueryOp]]):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.cond_ops = cond_ops\n</code></pre>"},{"location":"api/#cycquery.ops.And.__call__","title":"__call__","text":"<pre><code>__call__(table, return_cond=False)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <code>return_cond</code> <code>bool</code> <p>Return the condition instead of filtering.</p> <code>False</code> <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes, return_cond: bool = False) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n    return_cond\n        Return the condition instead of filtering.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    ops = []\n    for cond_op in self.cond_ops:\n        if isinstance(cond_op, list):\n            if len(self.cond_ops) != 1:\n                raise ValueError(\"Cannot combine multiple lists of conditions.\")\n            ops = [op(table, return_cond=True) for op in cond_op]\n        if isinstance(cond_op, QueryOp):\n            if len(self.cond_ops) == 1:\n                return cond_op(table, return_cond=return_cond)\n            ops.append(cond_op(table, return_cond=True))\n    cond = and_(*ops)  # type: ignore[arg-type]\n    if return_cond:\n        return cond  # type: ignore[return-value]\n\n    return select(table).where(cond).subquery()  # type: ignore[arg-type]\n</code></pre>"},{"location":"api/#cycquery.ops.Limit","title":"Limit","text":"<p>               Bases: <code>QueryOp</code></p> <p>Limit the number of rows returned in a query.</p> <p>Parameters:</p> Name Type Description Default <code>number</code> <code>int</code> <p>Number of rows to return in the limit.</p> required <p>Examples:</p> <pre><code>&gt;&gt;&gt; Limit(10)(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class Limit(QueryOp):\n    \"\"\"Limit the number of rows returned in a query.\n\n    Parameters\n    ----------\n    number\n        Number of rows to return in the limit.\n\n    Examples\n    --------\n    &gt;&gt;&gt; Limit(10)(table)\n\n    \"\"\"\n\n    def __init__(self, number: int):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.number = number\n\n    @table_params_to_type(Select)  # type: ignore[arg-type]\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        return table.limit(self.number).subquery()  # type: ignore\n</code></pre>"},{"location":"api/#cycquery.ops.Limit.__init__","title":"__init__","text":"<pre><code>__init__(number)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(self, number: int):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.number = number\n</code></pre>"},{"location":"api/#cycquery.ops.Limit.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>@table_params_to_type(Select)  # type: ignore[arg-type]\ndef __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    return table.limit(self.number).subquery()  # type: ignore\n</code></pre>"},{"location":"api/#cycquery.ops.RandomizeOrder","title":"RandomizeOrder","text":"<p>               Bases: <code>QueryOp</code></p> <p>Randomize order of table rows.</p> <p>Useful when the data is ordered, so certain rows cannot be seen or analyzed when limited.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; RandomizeOrder()(table)\n</code></pre> Warnings <p>Becomes quite slow on large tables.</p> Source code in <code>cycquery/ops.py</code> <pre><code>class RandomizeOrder(QueryOp):\n    \"\"\"Randomize order of table rows.\n\n    Useful when the data is ordered, so certain rows cannot\n    be seen or analyzed when limited.\n\n    Examples\n    --------\n    &gt;&gt;&gt; RandomizeOrder()(table)\n\n    Warnings\n    --------\n    Becomes quite slow on large tables.\n\n    \"\"\"\n\n    @table_params_to_type(Subquery)  # type: ignore[arg-type]\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        return select(table).order_by(func.random()).subquery()  # type: ignore[arg-type]\n</code></pre>"},{"location":"api/#cycquery.ops.RandomizeOrder.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>@table_params_to_type(Subquery)  # type: ignore[arg-type]\ndef __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    return select(table).order_by(func.random()).subquery()  # type: ignore[arg-type]\n</code></pre>"},{"location":"api/#cycquery.ops.DropNulls","title":"DropNulls","text":"<p>               Bases: <code>QueryOp</code></p> <p>Remove rows with null values in some specified columns.</p> <p>Parameters:</p> Name Type Description Default <code>cols</code> <code>Union[str, List[str]]</code> <p>Columns in which, if a value is null, the corresponding row is removed.</p> required <p>Examples:</p> <pre><code>&gt;&gt;&gt; DropNulls(\"col1\")(table)\n&gt;&gt;&gt; DropNulls([\"col1\", \"col2\"])(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class DropNulls(QueryOp):\n    \"\"\"Remove rows with null values in some specified columns.\n\n    Parameters\n    ----------\n    cols\n        Columns in which, if a value is null, the corresponding row is removed.\n\n    Examples\n    --------\n    &gt;&gt;&gt; DropNulls(\"col1\")(table)\n    &gt;&gt;&gt; DropNulls([\"col1\", \"col2\"])(table)\n\n    \"\"\"\n\n    def __init__(self, cols: typing.Union[str, typing.List[str]]):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.cols = cols\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        self.cols = to_list(self.cols)\n        table = _process_checks(table, cols=self.cols)\n\n        cond = and_(*[not_equals(get_column(table, col), None) for col in self.cols])\n        return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.DropNulls.__init__","title":"__init__","text":"<pre><code>__init__(cols)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(self, cols: typing.Union[str, typing.List[str]]):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.cols = cols\n</code></pre>"},{"location":"api/#cycquery.ops.DropNulls.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    self.cols = to_list(self.cols)\n    table = _process_checks(table, cols=self.cols)\n\n    cond = and_(*[not_equals(get_column(table, col), None) for col in self.cols])\n    return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.DropEmpty","title":"DropEmpty","text":"<p>               Bases: <code>QueryOp</code></p> <p>Remove rows with empty values in some specified columns.</p> <p>Parameters:</p> Name Type Description Default <code>cols</code> <code>Union[str, List[str]]</code> <p>Columns in which, if a value is empty, the corresponding row is removed.</p> required <p>Examples:</p> <pre><code>&gt;&gt;&gt; DropEmpty(\"col1\")(table)\n&gt;&gt;&gt; DropEmpty([\"col1\", \"col2\"])(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class DropEmpty(QueryOp):\n    \"\"\"Remove rows with empty values in some specified columns.\n\n    Parameters\n    ----------\n    cols\n        Columns in which, if a value is empty, the corresponding row is removed.\n\n    Examples\n    --------\n    &gt;&gt;&gt; DropEmpty(\"col1\")(table)\n    &gt;&gt;&gt; DropEmpty([\"col1\", \"col2\"])(table)\n\n    \"\"\"\n\n    def __init__(self, cols: typing.Union[str, typing.List[str]]):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.cols = cols\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        self.cols = to_list(self.cols)\n        table = _process_checks(table, cols=self.cols)\n\n        cond = and_(*[not_equals(get_column(table, col), \"\") for col in self.cols])\n        return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.DropEmpty.__init__","title":"__init__","text":"<pre><code>__init__(cols)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(self, cols: typing.Union[str, typing.List[str]]):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.cols = cols\n</code></pre>"},{"location":"api/#cycquery.ops.DropEmpty.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    self.cols = to_list(self.cols)\n    table = _process_checks(table, cols=self.cols)\n\n    cond = and_(*[not_equals(get_column(table, col), \"\") for col in self.cols])\n    return select(table).where(cond).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.Apply","title":"Apply","text":"<p>               Bases: <code>QueryOp</code></p> <p>Apply function(s) to column(s).</p> <p>The function can take a sqlalchemy column object and also return a column object. It can also take multiple columns and return a single column or multiple columns. If multiple functions are provided, it is assumed that each function is applied to each input column.</p> <p>Parameters:</p> Name Type Description Default <code>cols</code> <code>Union[str, List[str]]</code> <p>Column(s) to apply the function to.</p> required <code>funcs</code> <code>Union[Callable[[Column], Column], List[Callable[[Column], Column]]]</code> <p>Function(s) that takes in sqlalchemy column(s) object and returns column(s) after applying the function or list of functions to apply to each column.</p> required <code>new_cols</code> <code>Optional[Union[str, List[str]]]</code> <p>New column name(s) after function is applied to the specified column(s).</p> <code>None</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; Apply(\"col1\", lambda x: x + 1)(table)\n&gt;&gt;&gt; Apply([\"col1\", \"col2\"], [lambda x: x + 1, lambda x: x + 2])(table)\n&gt;&gt;&gt; Apply(\"col1\", lambda x: x + 1, new_cols=\"col1_new\")(table)\n&gt;&gt;&gt; Apply([\"col1\", \"col2\"], lambda x, y: x + y, new_cols=\"col1_new\")(table)\n&gt;&gt;&gt; Apply(\n...     [\"col1\", \"col2\"],\n...     lambda x, y: (x + y, x - y),\n...     new_cols=[\"col1_new\", \"col2_new\"],\n... )(table)  # noqa: E501, pylint: disable=line-too-long\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class Apply(QueryOp):\n    \"\"\"Apply function(s) to column(s).\n\n    The function can take a sqlalchemy column object and also return a column object.\n    It can also take multiple columns and return a single column or multiple columns.\n    If multiple functions are provided, it is assumed that each function is applied to\n    each input column.\n\n    Parameters\n    ----------\n    cols\n        Column(s) to apply the function to.\n    funcs\n        Function(s) that takes in sqlalchemy column(s) object and returns column(s)\n        after applying the function or list of functions to apply to each column.\n    new_cols\n        New column name(s) after function is applied to the specified column(s).\n\n    Examples\n    --------\n    &gt;&gt;&gt; Apply(\"col1\", lambda x: x + 1)(table)\n    &gt;&gt;&gt; Apply([\"col1\", \"col2\"], [lambda x: x + 1, lambda x: x + 2])(table)\n    &gt;&gt;&gt; Apply(\"col1\", lambda x: x + 1, new_cols=\"col1_new\")(table)\n    &gt;&gt;&gt; Apply([\"col1\", \"col2\"], lambda x, y: x + y, new_cols=\"col1_new\")(table)\n    &gt;&gt;&gt; Apply(\n    ...     [\"col1\", \"col2\"],\n    ...     lambda x, y: (x + y, x - y),\n    ...     new_cols=[\"col1_new\", \"col2_new\"],\n    ... )(table)  # noqa: E501, pylint: disable=line-too-long\n\n    \"\"\"\n\n    def __init__(\n        self,\n        cols: typing.Union[str, typing.List[str]],\n        funcs: typing.Union[\n            typing.Callable[\n                [sqlalchemy.sql.schema.Column],\n                sqlalchemy.sql.schema.Column,\n            ],\n            typing.List[\n                typing.Callable[\n                    [sqlalchemy.sql.schema.Column],\n                    sqlalchemy.sql.schema.Column,\n                ]\n            ],\n        ],\n        new_cols: typing.Optional[typing.Union[str, typing.List[str]]] = None,\n    ):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.cols = cols\n        self.funcs = funcs\n        self.new_cols = new_cols\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        self.new_cols = to_list(self.new_cols)\n        if isinstance(self.funcs, list):\n            if len(self.funcs) != len(self.cols):\n                raise ValueError(\n                    \"Number of functions must be equal to number of columns.\",\n                )\n            if len(self.new_cols) != len(self.cols):\n                raise ValueError(\n                    \"Number of new columns must be equal to number of columns.\",\n                )\n        if callable(self.funcs):\n            cols = get_columns(table, self.cols)\n            result_cols = [\n                self.funcs(*cols).label(new_col) for new_col in self.new_cols\n            ]  # noqa: E501\n\n            return select(table).add_columns(*result_cols).subquery()  # type: ignore[arg-type]\n\n        return apply_to_columns(table, self.cols, self.funcs, self.new_cols)\n</code></pre>"},{"location":"api/#cycquery.ops.Apply.__init__","title":"__init__","text":"<pre><code>__init__(cols, funcs, new_cols=None)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    cols: typing.Union[str, typing.List[str]],\n    funcs: typing.Union[\n        typing.Callable[\n            [sqlalchemy.sql.schema.Column],\n            sqlalchemy.sql.schema.Column,\n        ],\n        typing.List[\n            typing.Callable[\n                [sqlalchemy.sql.schema.Column],\n                sqlalchemy.sql.schema.Column,\n            ]\n        ],\n    ],\n    new_cols: typing.Optional[typing.Union[str, typing.List[str]]] = None,\n):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.cols = cols\n    self.funcs = funcs\n    self.new_cols = new_cols\n</code></pre>"},{"location":"api/#cycquery.ops.Apply.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    self.new_cols = to_list(self.new_cols)\n    if isinstance(self.funcs, list):\n        if len(self.funcs) != len(self.cols):\n            raise ValueError(\n                \"Number of functions must be equal to number of columns.\",\n            )\n        if len(self.new_cols) != len(self.cols):\n            raise ValueError(\n                \"Number of new columns must be equal to number of columns.\",\n            )\n    if callable(self.funcs):\n        cols = get_columns(table, self.cols)\n        result_cols = [\n            self.funcs(*cols).label(new_col) for new_col in self.new_cols\n        ]  # noqa: E501\n\n        return select(table).add_columns(*result_cols).subquery()  # type: ignore[arg-type]\n\n    return apply_to_columns(table, self.cols, self.funcs, self.new_cols)\n</code></pre>"},{"location":"api/#cycquery.ops.OrderBy","title":"OrderBy","text":"<p>               Bases: <code>QueryOp</code></p> <p>Order, or sort, the rows of a table by some columns.</p> <p>Parameters:</p> Name Type Description Default <code>cols</code> <code>Union[str, List[str]]</code> <p>Columns by which to order.</p> required <code>ascending</code> <code>Optional[Union[bool, List[bool]]]</code> <p>Whether to order each columns by ascending (True) or descending (False). If not provided, orders all by ascending.</p> <code>None</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; OrderBy(\"col1\")(table)\n&gt;&gt;&gt; OrderBy([\"col1\", \"col2\"])(table)\n&gt;&gt;&gt; OrderBy([\"col1\", \"col2\"], [True, False])(table)\n&gt;&gt;&gt; OrderBy([\"col1\", \"col2\"], True)(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class OrderBy(QueryOp):\n    \"\"\"Order, or sort, the rows of a table by some columns.\n\n    Parameters\n    ----------\n    cols\n        Columns by which to order.\n    ascending\n        Whether to order each columns by ascending (True) or descending (False).\n        If not provided, orders all by ascending.\n\n    Examples\n    --------\n    &gt;&gt;&gt; OrderBy(\"col1\")(table)\n    &gt;&gt;&gt; OrderBy([\"col1\", \"col2\"])(table)\n    &gt;&gt;&gt; OrderBy([\"col1\", \"col2\"], [True, False])(table)\n    &gt;&gt;&gt; OrderBy([\"col1\", \"col2\"], True)(table)\n\n    \"\"\"\n\n    def __init__(\n        self,\n        cols: typing.Union[str, typing.List[str]],\n        ascending: typing.Optional[typing.Union[bool, typing.List[bool]]] = None,\n    ):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.cols = cols\n        self.ascending = ascending\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        self.cols = to_list(self.cols)\n        ascending = to_list_optional(self.ascending)\n        table = _process_checks(table, cols=self.cols)\n        if ascending is None:\n            ascending = [True] * len(self.cols)\n        elif len(ascending) != len(self.cols):\n            raise ValueError(\n                \"If ascending is specified. Must specify for all columns.\",\n            )\n        order_cols = [\n            col if ascending[i] else col.desc()\n            for i, col in enumerate(get_columns(table, self.cols))\n        ]\n\n        return select(table).order_by(*order_cols).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.OrderBy.__init__","title":"__init__","text":"<pre><code>__init__(cols, ascending=None)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    cols: typing.Union[str, typing.List[str]],\n    ascending: typing.Optional[typing.Union[bool, typing.List[bool]]] = None,\n):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.cols = cols\n    self.ascending = ascending\n</code></pre>"},{"location":"api/#cycquery.ops.OrderBy.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    self.cols = to_list(self.cols)\n    ascending = to_list_optional(self.ascending)\n    table = _process_checks(table, cols=self.cols)\n    if ascending is None:\n        ascending = [True] * len(self.cols)\n    elif len(ascending) != len(self.cols):\n        raise ValueError(\n            \"If ascending is specified. Must specify for all columns.\",\n        )\n    order_cols = [\n        col if ascending[i] else col.desc()\n        for i, col in enumerate(get_columns(table, self.cols))\n    ]\n\n    return select(table).order_by(*order_cols).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.GroupByAggregate","title":"GroupByAggregate","text":"<p>               Bases: <code>QueryOp</code></p> <p>Aggregate over a group by object.</p> <p>Parameters:</p> Name Type Description Default <code>groupby_cols</code> <code>Union[str, List[str]]</code> <p>Columns by which to group.</p> required <code>aggfuncs</code> <code>Union[Dict[str, Sequence[str]], Dict[str, str]]</code> <p>Specify a dictionary of key-value pairs: column name: aggfunc string or column name: (aggfunc string, new column label) This labelling prevents the aggregation of the same column using multiple aggregation functions.</p> required <code>aggseps</code> <code>Optional[Dict[str, str]]</code> <p>Specify a dictionary of key-value pairs: column name: string_aggfunc separator If string_agg used as aggfunc for a column, then a separator must be provided for the same column.</p> <code>None</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; GroupByAggregate(\"person_id\", {\"person_id\": \"count\"})(table)\n&gt;&gt;&gt; GroupByAggregate(\"person_id\", {\"person_id\": (\"count\", \"visit_count\")})(table)\n&gt;&gt;&gt; GroupByAggregate(\"person_id\", {\"lab_name\": \"string_agg\"}, {\"lab_name\": \", \"})(\n...     table\n... )\n&gt;&gt;&gt; GroupByAggregate(\"person_id\", {\"lab_name\": (\"string_agg\", \"lab_name_agg\"}, {\"lab_name\": \", \"})(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class GroupByAggregate(QueryOp):\n    \"\"\"Aggregate over a group by object.\n\n    Parameters\n    ----------\n    groupby_cols\n        Columns by which to group.\n    aggfuncs\n        Specify a dictionary of key-value pairs:\n        column name: aggfunc string or\n        column name: (aggfunc string, new column label)\n        This labelling prevents the aggregation of the same column using multiple\n        aggregation functions.\n    aggseps\n        Specify a dictionary of key-value pairs:\n        column name: string_aggfunc separator\n        If string_agg used as aggfunc for a column, then a separator must be provided\n        for the same column.\n\n    Examples\n    --------\n    &gt;&gt;&gt; GroupByAggregate(\"person_id\", {\"person_id\": \"count\"})(table)\n    &gt;&gt;&gt; GroupByAggregate(\"person_id\", {\"person_id\": (\"count\", \"visit_count\")})(table)\n    &gt;&gt;&gt; GroupByAggregate(\"person_id\", {\"lab_name\": \"string_agg\"}, {\"lab_name\": \", \"})(\n    ...     table\n    ... )\n    &gt;&gt;&gt; GroupByAggregate(\"person_id\", {\"lab_name\": (\"string_agg\", \"lab_name_agg\"}, {\"lab_name\": \", \"})(table)\n\n    \"\"\"\n\n    def __init__(\n        self,\n        groupby_cols: typing.Union[str, typing.List[str]],\n        aggfuncs: typing.Union[\n            typing.Dict[str, typing.Sequence[str]],\n            typing.Dict[str, str],\n        ],\n        aggseps: typing.Optional[typing.Dict[str, str]] = None,\n    ):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.groupby_cols = groupby_cols\n        self.aggfuncs = aggfuncs\n        if aggseps is None:\n            aggseps = {}\n        self.aggseps = aggseps\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        str_to_aggfunc = {\n            \"sum\": func.sum,\n            \"average\": func.avg,\n            \"min\": func.min,\n            \"max\": func.max,\n            \"count\": func.count,\n            \"median\": func.percentile_cont(0.5).within_group,\n            \"string_agg\": func.string_agg,\n        }\n\n        aggfunc_tuples = list(self.aggfuncs.items())\n        aggfunc_cols = [item[0] for item in aggfunc_tuples]\n        aggfunc_strs = [\n            item[1] if isinstance(item[1], str) else item[1][0]\n            for item in aggfunc_tuples\n        ]\n\n        # If not specified, aggregate column names default to that of\n        # the column being aggregated over\n        aggfunc_names = [\n            aggfunc_cols[i] if isinstance(item[1], str) else item[1][1]\n            for i, item in enumerate(aggfunc_tuples)\n        ]\n\n        groupby_names = to_list(self.groupby_cols)\n        table = _process_checks(table, cols=groupby_names + aggfunc_cols)\n\n        # Error checking\n        for i, aggfunc_str in enumerate(aggfunc_strs):\n            if aggfunc_str not in str_to_aggfunc:\n                allowed_strs = \", \".join(list(str_to_aggfunc.keys()))\n                raise ValueError(\n                    f\"Invalid aggfuncs specified. Allowed values are {allowed_strs}.\",\n                )\n            if aggfunc_str == \"string_agg\" and (\n                not bool(self.aggseps) or aggfunc_cols[i] not in self.aggseps\n            ):\n                raise ValueError(\n                    f\"\"\"Column {aggfunc_cols[i]} needs to be aggregated as string, must specify a separator!\"\"\",  # noqa: E501\n                )\n\n        all_names = groupby_names + aggfunc_names\n        if len(all_names) != len(set(all_names)):\n            raise ValueError(\n                \"\"\"Duplicate column names were found. Try naming aggregated columns\n                to avoid this issue.\"\"\",\n            )\n\n        # Perform group by\n        groupby_cols = get_columns(table, groupby_names)\n        to_agg_cols = get_columns(table, aggfunc_cols)\n        agg_cols = []\n        for i, to_agg_col in enumerate(to_agg_cols):\n            if aggfunc_strs[i] == \"string_agg\":\n                agg_col = str_to_aggfunc[aggfunc_strs[i]](  # type: ignore[operator]\n                    to_agg_col,\n                    literal_column(f\"'{self.aggseps[aggfunc_cols[i]]}'\"),\n                )\n            else:\n                agg_col = str_to_aggfunc[aggfunc_strs[i]](to_agg_col)  # type: ignore[operator]\n            agg_cols.append(agg_col.label(aggfunc_names[i]))\n\n        return select(*groupby_cols, *agg_cols).group_by(*groupby_cols).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.GroupByAggregate.__init__","title":"__init__","text":"<pre><code>__init__(groupby_cols, aggfuncs, aggseps=None)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(\n    self,\n    groupby_cols: typing.Union[str, typing.List[str]],\n    aggfuncs: typing.Union[\n        typing.Dict[str, typing.Sequence[str]],\n        typing.Dict[str, str],\n    ],\n    aggseps: typing.Optional[typing.Dict[str, str]] = None,\n):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.groupby_cols = groupby_cols\n    self.aggfuncs = aggfuncs\n    if aggseps is None:\n        aggseps = {}\n    self.aggseps = aggseps\n</code></pre>"},{"location":"api/#cycquery.ops.GroupByAggregate.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    str_to_aggfunc = {\n        \"sum\": func.sum,\n        \"average\": func.avg,\n        \"min\": func.min,\n        \"max\": func.max,\n        \"count\": func.count,\n        \"median\": func.percentile_cont(0.5).within_group,\n        \"string_agg\": func.string_agg,\n    }\n\n    aggfunc_tuples = list(self.aggfuncs.items())\n    aggfunc_cols = [item[0] for item in aggfunc_tuples]\n    aggfunc_strs = [\n        item[1] if isinstance(item[1], str) else item[1][0]\n        for item in aggfunc_tuples\n    ]\n\n    # If not specified, aggregate column names default to that of\n    # the column being aggregated over\n    aggfunc_names = [\n        aggfunc_cols[i] if isinstance(item[1], str) else item[1][1]\n        for i, item in enumerate(aggfunc_tuples)\n    ]\n\n    groupby_names = to_list(self.groupby_cols)\n    table = _process_checks(table, cols=groupby_names + aggfunc_cols)\n\n    # Error checking\n    for i, aggfunc_str in enumerate(aggfunc_strs):\n        if aggfunc_str not in str_to_aggfunc:\n            allowed_strs = \", \".join(list(str_to_aggfunc.keys()))\n            raise ValueError(\n                f\"Invalid aggfuncs specified. Allowed values are {allowed_strs}.\",\n            )\n        if aggfunc_str == \"string_agg\" and (\n            not bool(self.aggseps) or aggfunc_cols[i] not in self.aggseps\n        ):\n            raise ValueError(\n                f\"\"\"Column {aggfunc_cols[i]} needs to be aggregated as string, must specify a separator!\"\"\",  # noqa: E501\n            )\n\n    all_names = groupby_names + aggfunc_names\n    if len(all_names) != len(set(all_names)):\n        raise ValueError(\n            \"\"\"Duplicate column names were found. Try naming aggregated columns\n            to avoid this issue.\"\"\",\n        )\n\n    # Perform group by\n    groupby_cols = get_columns(table, groupby_names)\n    to_agg_cols = get_columns(table, aggfunc_cols)\n    agg_cols = []\n    for i, to_agg_col in enumerate(to_agg_cols):\n        if aggfunc_strs[i] == \"string_agg\":\n            agg_col = str_to_aggfunc[aggfunc_strs[i]](  # type: ignore[operator]\n                to_agg_col,\n                literal_column(f\"'{self.aggseps[aggfunc_cols[i]]}'\"),\n            )\n        else:\n            agg_col = str_to_aggfunc[aggfunc_strs[i]](to_agg_col)  # type: ignore[operator]\n        agg_cols.append(agg_col.label(aggfunc_names[i]))\n\n    return select(*groupby_cols, *agg_cols).group_by(*groupby_cols).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.Distinct","title":"Distinct","text":"<p>               Bases: <code>QueryOp</code></p> <p>Get distinct rows.</p> <p>Parameters:</p> Name Type Description Default <code>cols</code> <code>Union[str, List[str]]</code> <p>Columns to use for distinct.</p> required <p>Examples:</p> <pre><code>&gt;&gt;&gt; Distinct(\"person_id\")(table)\n&gt;&gt;&gt; Distinct([\"person_id\", \"visit_id\"])(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class Distinct(QueryOp):\n    \"\"\"Get distinct rows.\n\n    Parameters\n    ----------\n    cols\n        Columns to use for distinct.\n\n    Examples\n    --------\n    &gt;&gt;&gt; Distinct(\"person_id\")(table)\n    &gt;&gt;&gt; Distinct([\"person_id\", \"visit_id\"])(table)\n\n    \"\"\"\n\n    def __init__(self, cols: typing.Union[str, typing.List[str]]):\n        \"\"\"Initialize the operation.\"\"\"\n        super().__init__()\n        self.cols = cols\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        cols = to_list(self.cols)\n        table = _process_checks(table, cols=cols)\n\n        return select(table).distinct(*get_columns(table, cols)).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.Distinct.__init__","title":"__init__","text":"<pre><code>__init__(cols)\n</code></pre> <p>Initialize the operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(self, cols: typing.Union[str, typing.List[str]]):\n    \"\"\"Initialize the operation.\"\"\"\n    super().__init__()\n    self.cols = cols\n</code></pre>"},{"location":"api/#cycquery.ops.Distinct.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    cols = to_list(self.cols)\n    table = _process_checks(table, cols=cols)\n\n    return select(table).distinct(*get_columns(table, cols)).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.Count","title":"Count","text":"<p>               Bases: <code>QueryOp</code></p> <p>Count the number of rows.</p> <p>Parameters:</p> Name Type Description Default <code>col</code> <code>str</code> <p>Column to count.</p> required <p>Examples:</p> <pre><code>&gt;&gt;&gt; Count(\"person_id\")(table)\n</code></pre> Source code in <code>cycquery/ops.py</code> <pre><code>class Count(QueryOp):\n    \"\"\"Count the number of rows.\n\n    Parameters\n    ----------\n    col\n        Column to count.\n\n    Examples\n    --------\n    &gt;&gt;&gt; Count(\"person_id\")(table)\n\n    \"\"\"\n\n    def __init__(self, col: str):\n        \"\"\"Initialize the count operation.\"\"\"\n        super().__init__()\n        self.col = col\n\n    def __call__(self, table: TableTypes) -&gt; Subquery:\n        \"\"\"Process the table.\n\n        Parameters\n        ----------\n        table\n            Table on which to perform the operation.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Processed table.\n\n        \"\"\"\n        table = _process_checks(table, cols=self.col)\n        count = func.count(get_column(table, self.col))\n\n        return select(count).subquery()\n</code></pre>"},{"location":"api/#cycquery.ops.Count.__init__","title":"__init__","text":"<pre><code>__init__(col)\n</code></pre> <p>Initialize the count operation.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __init__(self, col: str):\n    \"\"\"Initialize the count operation.\"\"\"\n    super().__init__()\n    self.col = col\n</code></pre>"},{"location":"api/#cycquery.ops.Count.__call__","title":"__call__","text":"<pre><code>__call__(table)\n</code></pre> <p>Process the table.</p> <p>Parameters:</p> Name Type Description Default <code>table</code> <code>TableTypes</code> <p>Table on which to perform the operation.</p> required <p>Returns:</p> Type Description <code>Subquery</code> <p>Processed table.</p> Source code in <code>cycquery/ops.py</code> <pre><code>def __call__(self, table: TableTypes) -&gt; Subquery:\n    \"\"\"Process the table.\n\n    Parameters\n    ----------\n    table\n        Table on which to perform the operation.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Processed table.\n\n    \"\"\"\n    table = _process_checks(table, cols=self.col)\n    count = func.count(get_column(table, self.col))\n\n    return select(count).subquery()\n</code></pre>"},{"location":"api/#base-module","title":"Base Module","text":""},{"location":"api/#cycquery.base","title":"cycquery.base","text":"<p>Base querier class.</p>"},{"location":"api/#cycquery.base.DatasetQuerier","title":"DatasetQuerier","text":"<p>Base class to query EHR datasets.</p> <p>Attributes:</p> Name Type Description <code>db</code> <p>ORM Database used to run queries.</p> <p>Parameters:</p> Name Type Description Default <code>dbms</code> <code>str</code> <p>The database management system type (e.g., 'postgresql', 'mysql', 'sqlite').</p> required <code>user</code> <code>str</code> <p>The username for the database, by default empty. Not used for SQLite.</p> <code>''</code> <code>password</code> <code>str</code> <p>The password for the database, by default empty. Not used for SQLite.</p> <code>''</code> <code>host</code> <code>str</code> <p>The host address of the database, by default empty. Not used for SQLite.</p> <code>''</code> <code>port</code> <code>int</code> <p>The port number for the database, by default None. Not used for SQLite.</p> <code>None</code> <code>database</code> <code>str</code> <p>The name of the database or the path to the database file (for SQLite), by default empty.</p> <code>''</code> <code>schemas</code> <code>Union[str, List[str]]</code> <p>The schema(s) to query, by default None.</p> <code>None</code> Notes <p>This class is intended to be subclassed to provide methods for querying tables in the database. This class automatically creates methods for querying tables in the database. The methods are named after the schema and table name, i.e. <code>self.schema_name.table_name()</code>. The methods are created when the class is instantiated. The subclass can provide custom methods for querying tables in the database which can build on the methods created by this class.</p> Source code in <code>cycquery/base.py</code> <pre><code>class DatasetQuerier:\n    \"\"\"Base class to query EHR datasets.\n\n    Attributes\n    ----------\n    db\n        ORM Database used to run queries.\n\n    Parameters\n    ----------\n    dbms : str\n        The database management system type (e.g., 'postgresql', 'mysql', 'sqlite').\n    user : str, optional\n        The username for the database, by default empty. Not used for SQLite.\n    password : str, optional\n        The password for the database, by default empty. Not used for SQLite.\n    host : str, optional\n        The host address of the database, by default empty. Not used for SQLite.\n    port : int, optional\n        The port number for the database, by default None. Not used for SQLite.\n    database : str, optional\n        The name of the database or the path to the database file (for SQLite),\n        by default empty.\n    schemas : Union[str, List[str]], optional\n        The schema(s) to query, by default None.\n\n    Notes\n    -----\n    This class is intended to be subclassed to provide methods for querying tables in\n    the database. This class automatically creates methods for querying tables in the\n    database. The methods are named after the schema and table name, i.e.\n    `self.schema_name.table_name()`. The methods are created when the class is\n    instantiated. The subclass can provide custom methods for querying tables in the\n    database which can build on the methods created by this class.\n\n    \"\"\"\n\n    def __init__(\n        self,\n        dbms: str,\n        user: str = \"\",\n        password: str = \"\",\n        host: str = \"\",\n        port: int | None = None,\n        database: str = \"\",\n        schemas: list[str] | None = None,\n    ) -&gt; None:\n        \"\"\"Initialize the querier.\"\"\"\n        config = DatasetQuerierConfig(\n            database=database,\n            user=user,\n            password=password,\n            dbms=dbms,\n            host=host,\n            port=port,\n            schemas=schemas,\n        )\n        self.db = Database(config)\n        if not self.db.is_connected:\n            LOGGER.error(\"Database is not connected, cannot run queries.\")\n            return\n        self._setup_table_methods()\n\n    def list_schemas(self) -&gt; list[str]:\n        \"\"\"List schemas in the database to query.\n\n        Returns\n        -------\n        List[str]\n            List of schema names.\n\n        \"\"\"\n        all_schemas = list(self.db.inspector.get_schema_names())\n        if self.db.config.schemas is None:\n            return all_schemas\n        if isinstance(self.db.config.schemas, str):\n            schemas_to_use = [self.db.config.schemas]\n        else:\n            schemas_to_use = self.db.config.schemas\n\n        return [schema for schema in all_schemas if schema in schemas_to_use]\n\n    def list_tables(self, schema_name: str | None = None) -&gt; list[str]:\n        \"\"\"List table methods that can be queried using the database.\n\n        Parameters\n        ----------\n        schema_name\n            Name of schema in the database.\n\n        Returns\n        -------\n        List[str]\n            List of table names.\n\n        \"\"\"\n        if schema_name:\n            table_names = []\n            for table in self.db.list_tables():\n                schema_name_, _ = table.split(\".\")\n                if schema_name_ == schema_name:\n                    table_names.append(table)\n        else:\n            table_names = self.db.list_tables()\n\n        return table_names\n\n    def list_columns(self, schema_name: str, table_name: str) -&gt; list[str]:\n        \"\"\"List columns in a table.\n\n        Parameters\n        ----------\n        schema_name\n            Name of schema in the database.\n        table_name\n            Name of table in the database.\n\n        Returns\n        -------\n        List[str]\n            List of column names.\n\n        \"\"\"\n        return list(\n            getattr(getattr(self.db, schema_name), table_name).data_.columns.keys(),\n        )\n\n    def list_custom_tables(self) -&gt; list[str]:\n        \"\"\"List custom tables methods provided by the dataset API.\n\n        Returns\n        -------\n        List[str]\n            List of custom table names.\n\n        \"\"\"\n        method_list = dir(self)\n        custom_tables = []\n        for method in method_list:\n            if (\n                not method.startswith(\n                    \"__\",\n                )\n                and not method.startswith(\"_\")\n                and method not in self.list_schemas()\n                and not method.startswith(\"list_\")\n                and not method.startswith(\"get_\")\n                and method not in [\"db\"]\n            ):\n                custom_tables.append(method)\n\n        return custom_tables\n\n    def get_table(\n        self,\n        schema_name: str,\n        table_name: str,\n        cast_timestamp_cols: bool = True,\n    ) -&gt; Subquery:\n        \"\"\"Get a table and possibly map columns to have standard names.\n\n        Standardizing column names allows for columns to be\n        recognized in downstream processing.\n\n        Parameters\n        ----------\n        schema_name\n            Name of schema in the database.\n        table_name\n            Name of table in the database.\n        cast_timestamp_cols\n            Whether to cast timestamp columns to datetime.\n\n        Returns\n        -------\n        sqlalchemy.sql.selectable.Subquery\n            Table with mapped columns.\n\n        \"\"\"\n        table = _create_get_table_lambdafn(schema_name, table_name)(self.db).data_\n\n        if cast_timestamp_cols:\n            table = _cast_timestamp_cols(table)\n\n        return _to_subquery(table)\n\n    def _template_table_method(\n        self,\n        schema_name: str,\n        table_name: str,\n    ) -&gt; QueryInterface:\n        \"\"\"Template method for table methods.\n\n        Parameters\n        ----------\n        schema_name\n            Name of schema in the database.\n        table_name\n            Name of table in the database.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            A query interface object.\n\n        \"\"\"\n        table = getattr(getattr(self.db, schema_name), table_name).data_\n        table = _to_subquery(table)\n\n        return QueryInterface(self.db, table)\n\n    def _setup_table_methods(self) -&gt; None:\n        \"\"\"Add table methods.\n\n        This method adds methods to the querier class that allow querying of tables in\n        the database. The methods are named after the table names.\n\n        \"\"\"\n        schemas = self.list_schemas()\n        meta: dict[str, MetaData] = {}\n        for schema_name in schemas:\n            metadata = MetaData(schema=schema_name)\n            with self.db.engine.connect() as conn:\n                metadata.reflect(bind=conn)\n            meta[schema_name] = metadata\n            schema = DBSchema(schema_name, meta[schema_name])\n            for table_name in meta[schema_name].tables:\n                setattr(\n                    schema,\n                    get_attr_name(table_name),\n                    partial(\n                        self._template_table_method,\n                        schema_name=schema_name,\n                        table_name=get_attr_name(table_name),\n                    ),\n                )\n            setattr(self, schema_name, schema)\n</code></pre>"},{"location":"api/#cycquery.base.DatasetQuerier.__init__","title":"__init__","text":"<pre><code>__init__(\n    dbms,\n    user=\"\",\n    password=\"\",\n    host=\"\",\n    port=None,\n    database=\"\",\n    schemas=None,\n)\n</code></pre> <p>Initialize the querier.</p> Source code in <code>cycquery/base.py</code> <pre><code>def __init__(\n    self,\n    dbms: str,\n    user: str = \"\",\n    password: str = \"\",\n    host: str = \"\",\n    port: int | None = None,\n    database: str = \"\",\n    schemas: list[str] | None = None,\n) -&gt; None:\n    \"\"\"Initialize the querier.\"\"\"\n    config = DatasetQuerierConfig(\n        database=database,\n        user=user,\n        password=password,\n        dbms=dbms,\n        host=host,\n        port=port,\n        schemas=schemas,\n    )\n    self.db = Database(config)\n    if not self.db.is_connected:\n        LOGGER.error(\"Database is not connected, cannot run queries.\")\n        return\n    self._setup_table_methods()\n</code></pre>"},{"location":"api/#cycquery.base.DatasetQuerier.list_schemas","title":"list_schemas","text":"<pre><code>list_schemas()\n</code></pre> <p>List schemas in the database to query.</p> <p>Returns:</p> Type Description <code>List[str]</code> <p>List of schema names.</p> Source code in <code>cycquery/base.py</code> <pre><code>def list_schemas(self) -&gt; list[str]:\n    \"\"\"List schemas in the database to query.\n\n    Returns\n    -------\n    List[str]\n        List of schema names.\n\n    \"\"\"\n    all_schemas = list(self.db.inspector.get_schema_names())\n    if self.db.config.schemas is None:\n        return all_schemas\n    if isinstance(self.db.config.schemas, str):\n        schemas_to_use = [self.db.config.schemas]\n    else:\n        schemas_to_use = self.db.config.schemas\n\n    return [schema for schema in all_schemas if schema in schemas_to_use]\n</code></pre>"},{"location":"api/#cycquery.base.DatasetQuerier.list_tables","title":"list_tables","text":"<pre><code>list_tables(schema_name=None)\n</code></pre> <p>List table methods that can be queried using the database.</p> <p>Parameters:</p> Name Type Description Default <code>schema_name</code> <code>str | None</code> <p>Name of schema in the database.</p> <code>None</code> <p>Returns:</p> Type Description <code>List[str]</code> <p>List of table names.</p> Source code in <code>cycquery/base.py</code> <pre><code>def list_tables(self, schema_name: str | None = None) -&gt; list[str]:\n    \"\"\"List table methods that can be queried using the database.\n\n    Parameters\n    ----------\n    schema_name\n        Name of schema in the database.\n\n    Returns\n    -------\n    List[str]\n        List of table names.\n\n    \"\"\"\n    if schema_name:\n        table_names = []\n        for table in self.db.list_tables():\n            schema_name_, _ = table.split(\".\")\n            if schema_name_ == schema_name:\n                table_names.append(table)\n    else:\n        table_names = self.db.list_tables()\n\n    return table_names\n</code></pre>"},{"location":"api/#cycquery.base.DatasetQuerier.list_columns","title":"list_columns","text":"<pre><code>list_columns(schema_name, table_name)\n</code></pre> <p>List columns in a table.</p> <p>Parameters:</p> Name Type Description Default <code>schema_name</code> <code>str</code> <p>Name of schema in the database.</p> required <code>table_name</code> <code>str</code> <p>Name of table in the database.</p> required <p>Returns:</p> Type Description <code>List[str]</code> <p>List of column names.</p> Source code in <code>cycquery/base.py</code> <pre><code>def list_columns(self, schema_name: str, table_name: str) -&gt; list[str]:\n    \"\"\"List columns in a table.\n\n    Parameters\n    ----------\n    schema_name\n        Name of schema in the database.\n    table_name\n        Name of table in the database.\n\n    Returns\n    -------\n    List[str]\n        List of column names.\n\n    \"\"\"\n    return list(\n        getattr(getattr(self.db, schema_name), table_name).data_.columns.keys(),\n    )\n</code></pre>"},{"location":"api/#cycquery.base.DatasetQuerier.list_custom_tables","title":"list_custom_tables","text":"<pre><code>list_custom_tables()\n</code></pre> <p>List custom tables methods provided by the dataset API.</p> <p>Returns:</p> Type Description <code>List[str]</code> <p>List of custom table names.</p> Source code in <code>cycquery/base.py</code> <pre><code>def list_custom_tables(self) -&gt; list[str]:\n    \"\"\"List custom tables methods provided by the dataset API.\n\n    Returns\n    -------\n    List[str]\n        List of custom table names.\n\n    \"\"\"\n    method_list = dir(self)\n    custom_tables = []\n    for method in method_list:\n        if (\n            not method.startswith(\n                \"__\",\n            )\n            and not method.startswith(\"_\")\n            and method not in self.list_schemas()\n            and not method.startswith(\"list_\")\n            and not method.startswith(\"get_\")\n            and method not in [\"db\"]\n        ):\n            custom_tables.append(method)\n\n    return custom_tables\n</code></pre>"},{"location":"api/#cycquery.base.DatasetQuerier.get_table","title":"get_table","text":"<pre><code>get_table(\n    schema_name, table_name, cast_timestamp_cols=True\n)\n</code></pre> <p>Get a table and possibly map columns to have standard names.</p> <p>Standardizing column names allows for columns to be recognized in downstream processing.</p> <p>Parameters:</p> Name Type Description Default <code>schema_name</code> <code>str</code> <p>Name of schema in the database.</p> required <code>table_name</code> <code>str</code> <p>Name of table in the database.</p> required <code>cast_timestamp_cols</code> <code>bool</code> <p>Whether to cast timestamp columns to datetime.</p> <code>True</code> <p>Returns:</p> Type Description <code>Subquery</code> <p>Table with mapped columns.</p> Source code in <code>cycquery/base.py</code> <pre><code>def get_table(\n    self,\n    schema_name: str,\n    table_name: str,\n    cast_timestamp_cols: bool = True,\n) -&gt; Subquery:\n    \"\"\"Get a table and possibly map columns to have standard names.\n\n    Standardizing column names allows for columns to be\n    recognized in downstream processing.\n\n    Parameters\n    ----------\n    schema_name\n        Name of schema in the database.\n    table_name\n        Name of table in the database.\n    cast_timestamp_cols\n        Whether to cast timestamp columns to datetime.\n\n    Returns\n    -------\n    sqlalchemy.sql.selectable.Subquery\n        Table with mapped columns.\n\n    \"\"\"\n    table = _create_get_table_lambdafn(schema_name, table_name)(self.db).data_\n\n    if cast_timestamp_cols:\n        table = _cast_timestamp_cols(table)\n\n    return _to_subquery(table)\n</code></pre>"},{"location":"api/#dataset-apis","title":"Dataset APIs","text":""},{"location":"api/#mimic-iii","title":"MIMIC-III","text":""},{"location":"api/#cycquery.mimiciii","title":"cycquery.mimiciii","text":"<p>MIMIC-III query module.</p> <p>Supports querying of MIMIC-III.</p>"},{"location":"api/#cycquery.mimiciii.MIMICIIIQuerier","title":"MIMICIIIQuerier","text":"<p>               Bases: <code>DatasetQuerier</code></p> <p>MIMIC-III dataset querier.</p> Source code in <code>cycquery/mimiciii.py</code> <pre><code>class MIMICIIIQuerier(DatasetQuerier):\n    \"\"\"MIMIC-III dataset querier.\"\"\"\n\n    def diagnoses(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query MIMICIII diagnosis data.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed query, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(\"mimiciii\", \"diagnoses_icd\")\n\n        # Join with diagnoses dimension table.\n        table = qo.Join(\n            join_table=self.get_table(\"mimiciii\", \"d_icd_diagnoses\"),\n            on=[\"icd9_code\"],\n            on_to_type=[\"str\"],\n        )(table)\n\n        return QueryInterface(self.db, table)\n\n    def labevents(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query MIMICIII labevents data.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed query, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(\"mimiciii\", \"labevents\")\n\n        # Join with lab dimension table.\n        table = qo.Join(\n            join_table=self.get_table(\"mimiciii\", \"d_labitems\"),\n            on=[\"itemid\"],\n            on_to_type=[\"str\"],\n        )(table)\n\n        return QueryInterface(self.db, table)\n\n    def chartevents(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query MIMICIII chartevents data.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed query, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(\"mimiciii\", \"chartevents\")\n\n        # Join with dimension table.\n        table = qo.Join(\n            join_table=self.get_table(\"mimiciii\", \"d_items\"),\n            on=[\"itemid\"],\n            on_to_type=[\"str\"],\n        )(table)\n\n        return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.mimiciii.MIMICIIIQuerier.diagnoses","title":"diagnoses","text":"<pre><code>diagnoses()\n</code></pre> <p>Query MIMICIII diagnosis data.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed query, wrapped in an interface object.</p> Source code in <code>cycquery/mimiciii.py</code> <pre><code>def diagnoses(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query MIMICIII diagnosis data.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed query, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(\"mimiciii\", \"diagnoses_icd\")\n\n    # Join with diagnoses dimension table.\n    table = qo.Join(\n        join_table=self.get_table(\"mimiciii\", \"d_icd_diagnoses\"),\n        on=[\"icd9_code\"],\n        on_to_type=[\"str\"],\n    )(table)\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.mimiciii.MIMICIIIQuerier.labevents","title":"labevents","text":"<pre><code>labevents()\n</code></pre> <p>Query MIMICIII labevents data.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed query, wrapped in an interface object.</p> Source code in <code>cycquery/mimiciii.py</code> <pre><code>def labevents(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query MIMICIII labevents data.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed query, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(\"mimiciii\", \"labevents\")\n\n    # Join with lab dimension table.\n    table = qo.Join(\n        join_table=self.get_table(\"mimiciii\", \"d_labitems\"),\n        on=[\"itemid\"],\n        on_to_type=[\"str\"],\n    )(table)\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.mimiciii.MIMICIIIQuerier.chartevents","title":"chartevents","text":"<pre><code>chartevents()\n</code></pre> <p>Query MIMICIII chartevents data.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed query, wrapped in an interface object.</p> Source code in <code>cycquery/mimiciii.py</code> <pre><code>def chartevents(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query MIMICIII chartevents data.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed query, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(\"mimiciii\", \"chartevents\")\n\n    # Join with dimension table.\n    table = qo.Join(\n        join_table=self.get_table(\"mimiciii\", \"d_items\"),\n        on=[\"itemid\"],\n        on_to_type=[\"str\"],\n    )(table)\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#mimic-iv","title":"MIMIC-IV","text":""},{"location":"api/#cycquery.mimiciv","title":"cycquery.mimiciv","text":"<p>MIMIC-IV query module.</p> <p>Supports querying of MIMICIV-2.0.</p>"},{"location":"api/#cycquery.mimiciv.MIMICIVQuerier","title":"MIMICIVQuerier","text":"<p>               Bases: <code>DatasetQuerier</code></p> <p>MIMICIV dataset querier.</p> Source code in <code>cycquery/mimiciv.py</code> <pre><code>class MIMICIVQuerier(DatasetQuerier):\n    \"\"\"MIMICIV dataset querier.\"\"\"\n\n    def patients(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query MIMIC patient data.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed query, wrapped in an interface object.\n\n        Notes\n        -----\n        The function infers the approximate year a patient received care, using the\n        `anchor_year` and `anchor_year_group` columns. The `join` and `ops` supplied\n        are applied after the approximate year is inferred. `dod` is\n        adjusted based on the inferred approximate year of care.\n\n        \"\"\"\n        table = self.get_table(\"mimiciv_hosp\", \"patients\")\n\n        # Process and include patient's anchor year.\n        table = select(\n            *list(table.columns),\n            (\n                func.substr(get_column(table, \"anchor_year_group\"), 1, 4).cast(Integer)\n            ).label(\"anchor_year_group_start\"),\n            (\n                func.substr(get_column(table, \"anchor_year_group\"), 8, 12).cast(Integer)\n            ).label(\"anchor_year_group_end\"),\n        ).subquery()\n\n        # Select the middle of the anchor year group as the anchor year\n        table = select(\n            *list(table.c),\n            (\n                get_column(table, \"anchor_year_group_start\")\n                + (\n                    get_column(table, \"anchor_year_group_end\")\n                    - get_column(table, \"anchor_year_group_start\")\n                )\n                / 2\n            ).label(\"anchor_year_group_middle\"),\n        ).subquery()\n\n        table = select(\n            *list(table.c),\n            (\n                get_column(table, \"anchor_year_group_middle\")\n                - get_column(table, \"anchor_year\")\n            ).label(\"anchor_year_difference\"),\n        ).subquery()\n\n        # Shift relevant columns by anchor year difference\n        table = qo.AddDeltaColumn(\"dod\", years=\"anchor_year_difference\")(table)\n        table = qo.Drop(\n            [\n                \"anchor_year_group_start\",\n                \"anchor_year_group_end\",\n                \"anchor_year_group_middle\",\n            ],\n        )(table)\n\n        return QueryInterface(self.db, table)\n\n    def diagnoses_icd(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query MIMIC diagnosis data.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed query, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(\"mimiciv_hosp\", \"diagnoses_icd\")\n\n        # Join with diagnoses dimension table.\n        table = qo.Join(\n            join_table=self.get_table(\"mimiciv_hosp\", \"d_icd_diagnoses\"),\n            on=[\"icd_code\", \"icd_version\"],\n            on_to_type=[\"str\", \"int\"],\n        )(table)\n\n        return QueryInterface(self.db, table)\n\n    def procedures_icd(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query MIMIC procedures data.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed query, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(\"mimiciv_hosp\", \"procedures_icd\")\n\n        # Join with procedures dimension table.\n        table = qo.Join(\n            join_table=self.get_table(\"mimiciv_hosp\", \"d_icd_procedures\"),\n            on=[\"icd_code\", \"icd_version\"],\n            on_to_type=[\"str\", \"int\"],\n        )(table)\n\n        return QueryInterface(self.db, table)\n\n    def labevents(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query lab events from the hospital module.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed query, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(\"mimiciv_hosp\", \"labevents\")\n        dim_items_table = self.get_table(\"mimiciv_hosp\", \"d_labitems\")\n\n        # Join with lab items dimension table.\n        table = qo.Join(\n            join_table=dim_items_table,\n            on=[\"itemid\"],\n        )(table)\n\n        return QueryInterface(self.db, table)\n\n    def chartevents(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query ICU chart events from the ICU module.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed table, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(\"mimiciv_icu\", \"chartevents\")\n        dim_items_table = self.get_table(\"mimiciv_icu\", \"d_items\")\n\n        # Join with items dimension table.\n        table = qo.Join(\n            dim_items_table,\n            on=\"itemid\",\n        )(table)\n\n        return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.mimiciv.MIMICIVQuerier.patients","title":"patients","text":"<pre><code>patients()\n</code></pre> <p>Query MIMIC patient data.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed query, wrapped in an interface object.</p> Notes <p>The function infers the approximate year a patient received care, using the <code>anchor_year</code> and <code>anchor_year_group</code> columns. The <code>join</code> and <code>ops</code> supplied are applied after the approximate year is inferred. <code>dod</code> is adjusted based on the inferred approximate year of care.</p> Source code in <code>cycquery/mimiciv.py</code> <pre><code>def patients(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query MIMIC patient data.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed query, wrapped in an interface object.\n\n    Notes\n    -----\n    The function infers the approximate year a patient received care, using the\n    `anchor_year` and `anchor_year_group` columns. The `join` and `ops` supplied\n    are applied after the approximate year is inferred. `dod` is\n    adjusted based on the inferred approximate year of care.\n\n    \"\"\"\n    table = self.get_table(\"mimiciv_hosp\", \"patients\")\n\n    # Process and include patient's anchor year.\n    table = select(\n        *list(table.columns),\n        (\n            func.substr(get_column(table, \"anchor_year_group\"), 1, 4).cast(Integer)\n        ).label(\"anchor_year_group_start\"),\n        (\n            func.substr(get_column(table, \"anchor_year_group\"), 8, 12).cast(Integer)\n        ).label(\"anchor_year_group_end\"),\n    ).subquery()\n\n    # Select the middle of the anchor year group as the anchor year\n    table = select(\n        *list(table.c),\n        (\n            get_column(table, \"anchor_year_group_start\")\n            + (\n                get_column(table, \"anchor_year_group_end\")\n                - get_column(table, \"anchor_year_group_start\")\n            )\n            / 2\n        ).label(\"anchor_year_group_middle\"),\n    ).subquery()\n\n    table = select(\n        *list(table.c),\n        (\n            get_column(table, \"anchor_year_group_middle\")\n            - get_column(table, \"anchor_year\")\n        ).label(\"anchor_year_difference\"),\n    ).subquery()\n\n    # Shift relevant columns by anchor year difference\n    table = qo.AddDeltaColumn(\"dod\", years=\"anchor_year_difference\")(table)\n    table = qo.Drop(\n        [\n            \"anchor_year_group_start\",\n            \"anchor_year_group_end\",\n            \"anchor_year_group_middle\",\n        ],\n    )(table)\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.mimiciv.MIMICIVQuerier.diagnoses_icd","title":"diagnoses_icd","text":"<pre><code>diagnoses_icd()\n</code></pre> <p>Query MIMIC diagnosis data.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed query, wrapped in an interface object.</p> Source code in <code>cycquery/mimiciv.py</code> <pre><code>def diagnoses_icd(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query MIMIC diagnosis data.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed query, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(\"mimiciv_hosp\", \"diagnoses_icd\")\n\n    # Join with diagnoses dimension table.\n    table = qo.Join(\n        join_table=self.get_table(\"mimiciv_hosp\", \"d_icd_diagnoses\"),\n        on=[\"icd_code\", \"icd_version\"],\n        on_to_type=[\"str\", \"int\"],\n    )(table)\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.mimiciv.MIMICIVQuerier.procedures_icd","title":"procedures_icd","text":"<pre><code>procedures_icd()\n</code></pre> <p>Query MIMIC procedures data.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed query, wrapped in an interface object.</p> Source code in <code>cycquery/mimiciv.py</code> <pre><code>def procedures_icd(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query MIMIC procedures data.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed query, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(\"mimiciv_hosp\", \"procedures_icd\")\n\n    # Join with procedures dimension table.\n    table = qo.Join(\n        join_table=self.get_table(\"mimiciv_hosp\", \"d_icd_procedures\"),\n        on=[\"icd_code\", \"icd_version\"],\n        on_to_type=[\"str\", \"int\"],\n    )(table)\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.mimiciv.MIMICIVQuerier.labevents","title":"labevents","text":"<pre><code>labevents()\n</code></pre> <p>Query lab events from the hospital module.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed query, wrapped in an interface object.</p> Source code in <code>cycquery/mimiciv.py</code> <pre><code>def labevents(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query lab events from the hospital module.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed query, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(\"mimiciv_hosp\", \"labevents\")\n    dim_items_table = self.get_table(\"mimiciv_hosp\", \"d_labitems\")\n\n    # Join with lab items dimension table.\n    table = qo.Join(\n        join_table=dim_items_table,\n        on=[\"itemid\"],\n    )(table)\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.mimiciv.MIMICIVQuerier.chartevents","title":"chartevents","text":"<pre><code>chartevents()\n</code></pre> <p>Query ICU chart events from the ICU module.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed table, wrapped in an interface object.</p> Source code in <code>cycquery/mimiciv.py</code> <pre><code>def chartevents(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query ICU chart events from the ICU module.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed table, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(\"mimiciv_icu\", \"chartevents\")\n    dim_items_table = self.get_table(\"mimiciv_icu\", \"d_items\")\n\n    # Join with items dimension table.\n    table = qo.Join(\n        dim_items_table,\n        on=\"itemid\",\n    )(table)\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#eicu","title":"eICU","text":""},{"location":"api/#cycquery.eicu","title":"cycquery.eicu","text":"<p>EICU-CRD query module.</p> <p>Supports querying of eICU.</p>"},{"location":"api/#cycquery.eicu.EICUQuerier","title":"EICUQuerier","text":"<p>               Bases: <code>DatasetQuerier</code></p> <p>EICU dataset querier.</p> Source code in <code>cycquery/eicu.py</code> <pre><code>class EICUQuerier(DatasetQuerier):\n    \"\"\"EICU dataset querier.\"\"\"\n</code></pre>"},{"location":"api/#omop","title":"OMOP","text":""},{"location":"api/#cycquery.omop","title":"cycquery.omop","text":"<p>OMOP query API.</p>"},{"location":"api/#cycquery.omop.OMOPQuerier","title":"OMOPQuerier","text":"<p>               Bases: <code>DatasetQuerier</code></p> <p>OMOP querier.</p> Source code in <code>cycquery/omop.py</code> <pre><code>class OMOPQuerier(DatasetQuerier):\n    \"\"\"OMOP querier.\"\"\"\n\n    def __init__(\n        self,\n        database: str,\n        user: str,\n        password: str,\n        dbms: str = \"postgresql\",\n        host: str = \"localhost\",\n        port: int = 5432,\n        schema_name: str = \"omop\",\n    ) -&gt; None:\n        \"\"\"Initialize OMOP querier.\"\"\"\n        super().__init__(\n            database=database,\n            user=user,\n            password=password,\n            dbms=dbms,\n            host=host,\n            port=port,\n        )\n        self.schema_name = schema_name\n\n    def map_concept_ids_to_name(\n        self,\n        src_table: Union[Subquery, QueryInterface],\n        src_cols: Union[str, List[str]],\n        dst_cols: Optional[Union[str, List[str]]] = None,\n    ) -&gt; QueryInterface:\n        \"\"\"Map concept IDs in a source table to concept names from concept table.\n\n        For each concept ID column with a name like `somecol_concept_ID`, the mapped\n        concept name column will be named `somecol_concept_name`. If `dst_cols` is\n        specified, the mapped concept name column will be named according to the\n        corresponding name in `dst_cols`.\n\n        Parameters\n        ----------\n        src_table\n            Source table with concept IDs.\n        src_cols\n            Column name(s) to consider as concept IDs for mapping.\n        dst_cols\n            Column name(s) to assign for the mapped concept name columns.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Query with mapped columns from concept table.\n\n        \"\"\"\n        if isinstance(src_table, QueryInterface):\n            src_table = src_table.query\n        concept_table = self.get_table(self.schema_name, \"concept\")\n        src_cols = to_list(src_cols)\n        if dst_cols:\n            dst_cols = to_list(dst_cols)\n            if len(dst_cols) != len(src_cols):\n                raise ValueError(\"dst_cols must be same length as src_cols\")\n\n        for i, col in enumerate(src_cols):\n            if ID not in col:\n                raise ValueError(\"Specified column not a concept ID column!\")\n            src_table = qo.Join(\n                concept_table,\n                on=(col, CONCEPT_ID),\n                join_table_cols=[CONCEPT_NAME],\n                isouter=True,\n            )(src_table)\n            dst_col_name = dst_cols[i] if dst_cols else col.replace(ID, NAME)\n            src_table = qo.Rename({CONCEPT_NAME: dst_col_name})(src_table)  # type: ignore[arg-type]\n\n        return QueryInterface(self.db, src_table)\n\n    def _map_care_site_id(\n        self,\n        source_table: Union[Subquery, QueryInterface],\n    ) -&gt; QueryInterface:\n        \"\"\"Map care_site_id in a source table to care_site table.\n\n        Parameters\n        ----------\n        source_table\n            Source table with care_site_id.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Query with mapped columns from care_site table.\n\n        \"\"\"\n        if isinstance(source_table, QueryInterface):\n            source_table = source_table.query\n        care_site_table = self.get_table(self.schema_name, \"care_site\")\n        table = qo.Join(\n            care_site_table,\n            on=CARE_SITE_ID,\n            join_table_cols=[CARE_SITE_NAME, CARE_SITE_SOURCE_VALUE],\n            isouter=True,\n        )(source_table)\n\n        return QueryInterface(self.db, table)\n\n    def visit_occurrence(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query OMOP visit_occurrence table.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed query, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(self.schema_name, \"visit_occurrence\")\n        table = self.map_concept_ids_to_name(  # type: ignore[assignment]\n            table,\n            [\n                \"visit_concept_id\",\n                \"visit_type_concept_id\",\n            ],\n        )\n        table = self._map_care_site_id(table)  # type: ignore[assignment]\n\n        return QueryInterface(self.db, table)\n\n    def visit_detail(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query OMOP visit_detail table.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed query, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(self.schema_name, \"visit_detail\")\n        table = self.map_concept_ids_to_name(  # type: ignore[assignment]\n            table,\n            [\"visit_detail_concept_id\", \"visit_detail_type_concept_id\"],\n        )\n\n        return QueryInterface(self.db, table)\n\n    def person(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query OMOP person table.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed query, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(self.schema_name, \"person\")\n        table = self.map_concept_ids_to_name(  # type: ignore[assignment]\n            table,\n            [\"gender_concept_id\", \"race_concept_id\", \"ethnicity_concept_id\"],\n        )\n\n        return QueryInterface(self.db, table)\n\n    def observation(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query OMOP observation table.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed query, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(self.schema_name, \"observation\")\n        table = self.map_concept_ids_to_name(  # type: ignore[assignment]\n            table,\n            [OBSERVATION_CONCEPT_ID, OBSERVATION_TYPE_CONCEPT_ID],\n        )\n\n        return QueryInterface(self.db, table)\n\n    def measurement(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query OMOP measurement table.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed query, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(self.schema_name, \"measurement\")\n        # Cast value_as_concept_id to int.\n        table = qo.Cast([VALUE_AS_CONCEPT_ID], \"int\")(table)\n        table = self.map_concept_ids_to_name(  # type: ignore[assignment]\n            table,\n            [MEASUREMENT_CONCEPT_ID, MEASUREMENT_TYPE_CONCEPT_ID, UNIT_CONCEPT_ID],\n        )\n\n        return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.omop.OMOPQuerier.__init__","title":"__init__","text":"<pre><code>__init__(\n    database,\n    user,\n    password,\n    dbms=\"postgresql\",\n    host=\"localhost\",\n    port=5432,\n    schema_name=\"omop\",\n)\n</code></pre> <p>Initialize OMOP querier.</p> Source code in <code>cycquery/omop.py</code> <pre><code>def __init__(\n    self,\n    database: str,\n    user: str,\n    password: str,\n    dbms: str = \"postgresql\",\n    host: str = \"localhost\",\n    port: int = 5432,\n    schema_name: str = \"omop\",\n) -&gt; None:\n    \"\"\"Initialize OMOP querier.\"\"\"\n    super().__init__(\n        database=database,\n        user=user,\n        password=password,\n        dbms=dbms,\n        host=host,\n        port=port,\n    )\n    self.schema_name = schema_name\n</code></pre>"},{"location":"api/#cycquery.omop.OMOPQuerier.map_concept_ids_to_name","title":"map_concept_ids_to_name","text":"<pre><code>map_concept_ids_to_name(src_table, src_cols, dst_cols=None)\n</code></pre> <p>Map concept IDs in a source table to concept names from concept table.</p> <p>For each concept ID column with a name like <code>somecol_concept_ID</code>, the mapped concept name column will be named <code>somecol_concept_name</code>. If <code>dst_cols</code> is specified, the mapped concept name column will be named according to the corresponding name in <code>dst_cols</code>.</p> <p>Parameters:</p> Name Type Description Default <code>src_table</code> <code>Union[Subquery, QueryInterface]</code> <p>Source table with concept IDs.</p> required <code>src_cols</code> <code>Union[str, List[str]]</code> <p>Column name(s) to consider as concept IDs for mapping.</p> required <code>dst_cols</code> <code>Optional[Union[str, List[str]]]</code> <p>Column name(s) to assign for the mapped concept name columns.</p> <code>None</code> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Query with mapped columns from concept table.</p> Source code in <code>cycquery/omop.py</code> <pre><code>def map_concept_ids_to_name(\n    self,\n    src_table: Union[Subquery, QueryInterface],\n    src_cols: Union[str, List[str]],\n    dst_cols: Optional[Union[str, List[str]]] = None,\n) -&gt; QueryInterface:\n    \"\"\"Map concept IDs in a source table to concept names from concept table.\n\n    For each concept ID column with a name like `somecol_concept_ID`, the mapped\n    concept name column will be named `somecol_concept_name`. If `dst_cols` is\n    specified, the mapped concept name column will be named according to the\n    corresponding name in `dst_cols`.\n\n    Parameters\n    ----------\n    src_table\n        Source table with concept IDs.\n    src_cols\n        Column name(s) to consider as concept IDs for mapping.\n    dst_cols\n        Column name(s) to assign for the mapped concept name columns.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Query with mapped columns from concept table.\n\n    \"\"\"\n    if isinstance(src_table, QueryInterface):\n        src_table = src_table.query\n    concept_table = self.get_table(self.schema_name, \"concept\")\n    src_cols = to_list(src_cols)\n    if dst_cols:\n        dst_cols = to_list(dst_cols)\n        if len(dst_cols) != len(src_cols):\n            raise ValueError(\"dst_cols must be same length as src_cols\")\n\n    for i, col in enumerate(src_cols):\n        if ID not in col:\n            raise ValueError(\"Specified column not a concept ID column!\")\n        src_table = qo.Join(\n            concept_table,\n            on=(col, CONCEPT_ID),\n            join_table_cols=[CONCEPT_NAME],\n            isouter=True,\n        )(src_table)\n        dst_col_name = dst_cols[i] if dst_cols else col.replace(ID, NAME)\n        src_table = qo.Rename({CONCEPT_NAME: dst_col_name})(src_table)  # type: ignore[arg-type]\n\n    return QueryInterface(self.db, src_table)\n</code></pre>"},{"location":"api/#cycquery.omop.OMOPQuerier.visit_occurrence","title":"visit_occurrence","text":"<pre><code>visit_occurrence()\n</code></pre> <p>Query OMOP visit_occurrence table.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed query, wrapped in an interface object.</p> Source code in <code>cycquery/omop.py</code> <pre><code>def visit_occurrence(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query OMOP visit_occurrence table.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed query, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(self.schema_name, \"visit_occurrence\")\n    table = self.map_concept_ids_to_name(  # type: ignore[assignment]\n        table,\n        [\n            \"visit_concept_id\",\n            \"visit_type_concept_id\",\n        ],\n    )\n    table = self._map_care_site_id(table)  # type: ignore[assignment]\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.omop.OMOPQuerier.visit_detail","title":"visit_detail","text":"<pre><code>visit_detail()\n</code></pre> <p>Query OMOP visit_detail table.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed query, wrapped in an interface object.</p> Source code in <code>cycquery/omop.py</code> <pre><code>def visit_detail(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query OMOP visit_detail table.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed query, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(self.schema_name, \"visit_detail\")\n    table = self.map_concept_ids_to_name(  # type: ignore[assignment]\n        table,\n        [\"visit_detail_concept_id\", \"visit_detail_type_concept_id\"],\n    )\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.omop.OMOPQuerier.person","title":"person","text":"<pre><code>person()\n</code></pre> <p>Query OMOP person table.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed query, wrapped in an interface object.</p> Source code in <code>cycquery/omop.py</code> <pre><code>def person(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query OMOP person table.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed query, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(self.schema_name, \"person\")\n    table = self.map_concept_ids_to_name(  # type: ignore[assignment]\n        table,\n        [\"gender_concept_id\", \"race_concept_id\", \"ethnicity_concept_id\"],\n    )\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.omop.OMOPQuerier.observation","title":"observation","text":"<pre><code>observation()\n</code></pre> <p>Query OMOP observation table.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed query, wrapped in an interface object.</p> Source code in <code>cycquery/omop.py</code> <pre><code>def observation(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query OMOP observation table.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed query, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(self.schema_name, \"observation\")\n    table = self.map_concept_ids_to_name(  # type: ignore[assignment]\n        table,\n        [OBSERVATION_CONCEPT_ID, OBSERVATION_TYPE_CONCEPT_ID],\n    )\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.omop.OMOPQuerier.measurement","title":"measurement","text":"<pre><code>measurement()\n</code></pre> <p>Query OMOP measurement table.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed query, wrapped in an interface object.</p> Source code in <code>cycquery/omop.py</code> <pre><code>def measurement(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query OMOP measurement table.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed query, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(self.schema_name, \"measurement\")\n    # Cast value_as_concept_id to int.\n    table = qo.Cast([VALUE_AS_CONCEPT_ID], \"int\")(table)\n    table = self.map_concept_ids_to_name(  # type: ignore[assignment]\n        table,\n        [MEASUREMENT_CONCEPT_ID, MEASUREMENT_TYPE_CONCEPT_ID, UNIT_CONCEPT_ID],\n    )\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#gemini","title":"Gemini","text":""},{"location":"api/#cycquery.gemini","title":"cycquery.gemini","text":"<p>GEMINI query module.</p>"},{"location":"api/#cycquery.gemini.GEMINIQuerier","title":"GEMINIQuerier","text":"<p>               Bases: <code>DatasetQuerier</code></p> <p>GEMINI dataset querier.</p> Source code in <code>cycquery/gemini.py</code> <pre><code>class GEMINIQuerier(DatasetQuerier):\n    \"\"\"GEMINI dataset querier.\"\"\"\n\n    def ip_admin(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query GEMINI patient encounters.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed query, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(\"public\", \"ip_administrative\")\n\n        # Possibly cast string representations to timestamps\n        table = qo.Cast([\"admit_date_time\", \"discharge_date_time\"], \"timestamp\")(table)\n\n        # Get the discharge disposition code descriptions\n        lookup_table = self.get_table(\"public\", \"lookup_ip_administrative\")\n        lookup_table = qo.ConditionEquals(\"variable\", \"discharge_disposition\")(\n            lookup_table,\n        )\n        table = qo.Join(\n            lookup_table,\n            on=(\"discharge_disposition\", \"value\"),\n            on_to_type=\"int\",\n            join_table_cols=\"description\",\n            isouter=True,\n        )(table)\n        table = qo.Rename({\"description\": \"discharge_description\"})(table)\n        table = qo.Drop(\"value\")(table)\n\n        return QueryInterface(self.db, table)\n\n    def diagnoses(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query diagnosis data.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed table, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(\"public\", \"diagnosis\")\n\n        lookup_table = self.get_table(\"public\", \"lookup_diagnosis\")\n        lookup_table = qo.ConditionEquals(\"variable\", \"diagnosis_type\")(lookup_table)\n        table = qo.Join(\n            lookup_table,\n            on=(\"diagnosis_type\", \"value\"),\n            join_table_cols=\"description\",\n            isouter=True,\n        )(table)\n        table = qo.Drop(\"value\")(table)\n        table = qo.Rename({\"description\": \"diagnosis_type_description\"})(table)\n        table = qo.ReorderAfter(\"diagnosis_type_description\", \"diagnosis_type\")(table)\n\n        # Trim whitespace from ICD codes.\n        table = qo.Trim(\"diagnosis_code\")(table)\n\n        return QueryInterface(self.db, table)\n\n    def room_transfer(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query room transfer data.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed table, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(\"public\", \"room_transfer\")\n\n        # Join with lookup to get transfer description.\n        lookup_table = self.get_table(\"public\", \"lookup_room_transfer\")\n        lookup_table = qo.ConditionEquals(\"variable\", \"medical_service\")(lookup_table)\n\n        table = qo.Join(\n            lookup_table,\n            on=(\"medical_service\", \"value\"),\n            join_table_cols=\"description\",\n            isouter=True,\n        )(table)\n        table = qo.Rename({\"description\": \"transfer_description\"})(table)\n\n        return QueryInterface(self.db, table)\n\n    def care_units(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query care unit data, fetches transfer info from multiple tables.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed table, wrapped in an interface object.\n\n        \"\"\"\n        filter_care_unit_cols = qo.Keep(\n            [\n                \"genc_id\",\n                \"admit\",\n                \"discharge\",\n                CARE_UNIT,\n            ],\n        )\n\n        # In-patient table.\n        ip_table = self.get_table(\"public\", \"ip_administrative\")\n        ip_table = qo.Rename(\n            {\n                \"admit_date_time\": \"admit\",\n                \"discharge_date_time\": \"discharge\",\n            },\n        )(ip_table)\n        ip_table = qo.Literal(\"IP\", CARE_UNIT)(ip_table)\n        ip_table = filter_care_unit_cols(ip_table)\n\n        # Special care unit table.\n        scu_table = self.get_table(\"public\", \"ip_scu\")\n        scu_table = qo.Rename(\n            {\n                \"scu_admit_date_time\": \"admit\",\n                \"scu_discharge_date_time\": \"discharge\",\n            },\n        )(scu_table)\n        scu_table = qo.Literal(\"SCU\", CARE_UNIT)(scu_table)\n        scu_table = filter_care_unit_cols(scu_table)\n\n        # Emergency room/department table.\n        er_table = self.get_table(\"public\", \"er_administrative\")\n        er_table = qo.Rename(\n            {\n                \"er_admit_timestamp\": \"admit\",\n                \"er_discharge_timestamp\": \"discharge\",\n            },\n        )(er_table)\n        er_table = qo.Literal(\"ER\", CARE_UNIT)(er_table)\n        er_table = filter_care_unit_cols(er_table)\n\n        # Room transfer table.\n        rt_table = self.get_table(\"public\", \"room_transfer\")\n        rt_table = qo.Rename(\n            {\n                \"checkin_date_time\": \"admit\",\n                \"checkout_date_time\": \"discharge\",\n            },\n        )(rt_table)\n        rt_table = qo.Rename({\"transfer_description\": CARE_UNIT})(rt_table)\n        rt_table = filter_care_unit_cols(rt_table)\n\n        # Combine.\n        table = union_all(\n            select(er_table),\n            select(scu_table),\n            select(ip_table),\n            select(rt_table),\n        ).subquery()\n\n        return QueryInterface(self.db, table)\n\n    def imaging(\n        self,\n    ) -&gt; QueryInterface:\n        \"\"\"Query imaging reports data.\n\n        Returns\n        -------\n        cycquery.interface.QueryInterface\n            Constructed table, wrapped in an interface object.\n\n        \"\"\"\n        table = self.get_table(\"public\", \"imaging\")\n\n        # Get imaging test description\n        lookup_table = self.get_table(\"public\", \"lookup_imaging\")\n        lookup_table = qo.ConditionEquals(\"variable\", \"imaging_test_name_mapped\")(\n            lookup_table,\n        )\n\n        table = qo.Join(\n            lookup_table,\n            on=(\"imaging_test_name_mapped\", \"value\"),\n            on_to_type=\"str\",\n            join_table_cols=\"description\",\n        )(table)\n        table = qo.Drop(\"value\")(table)\n        table = qo.Rename({\"description\": \"imaging_test_description\"})(table)\n        table = qo.ReorderAfter(\"imaging_test_description\", \"imaging_test_name_mapped\")(\n            table,\n        )\n\n        return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.gemini.GEMINIQuerier.ip_admin","title":"ip_admin","text":"<pre><code>ip_admin()\n</code></pre> <p>Query GEMINI patient encounters.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed query, wrapped in an interface object.</p> Source code in <code>cycquery/gemini.py</code> <pre><code>def ip_admin(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query GEMINI patient encounters.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed query, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(\"public\", \"ip_administrative\")\n\n    # Possibly cast string representations to timestamps\n    table = qo.Cast([\"admit_date_time\", \"discharge_date_time\"], \"timestamp\")(table)\n\n    # Get the discharge disposition code descriptions\n    lookup_table = self.get_table(\"public\", \"lookup_ip_administrative\")\n    lookup_table = qo.ConditionEquals(\"variable\", \"discharge_disposition\")(\n        lookup_table,\n    )\n    table = qo.Join(\n        lookup_table,\n        on=(\"discharge_disposition\", \"value\"),\n        on_to_type=\"int\",\n        join_table_cols=\"description\",\n        isouter=True,\n    )(table)\n    table = qo.Rename({\"description\": \"discharge_description\"})(table)\n    table = qo.Drop(\"value\")(table)\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.gemini.GEMINIQuerier.diagnoses","title":"diagnoses","text":"<pre><code>diagnoses()\n</code></pre> <p>Query diagnosis data.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed table, wrapped in an interface object.</p> Source code in <code>cycquery/gemini.py</code> <pre><code>def diagnoses(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query diagnosis data.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed table, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(\"public\", \"diagnosis\")\n\n    lookup_table = self.get_table(\"public\", \"lookup_diagnosis\")\n    lookup_table = qo.ConditionEquals(\"variable\", \"diagnosis_type\")(lookup_table)\n    table = qo.Join(\n        lookup_table,\n        on=(\"diagnosis_type\", \"value\"),\n        join_table_cols=\"description\",\n        isouter=True,\n    )(table)\n    table = qo.Drop(\"value\")(table)\n    table = qo.Rename({\"description\": \"diagnosis_type_description\"})(table)\n    table = qo.ReorderAfter(\"diagnosis_type_description\", \"diagnosis_type\")(table)\n\n    # Trim whitespace from ICD codes.\n    table = qo.Trim(\"diagnosis_code\")(table)\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.gemini.GEMINIQuerier.room_transfer","title":"room_transfer","text":"<pre><code>room_transfer()\n</code></pre> <p>Query room transfer data.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed table, wrapped in an interface object.</p> Source code in <code>cycquery/gemini.py</code> <pre><code>def room_transfer(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query room transfer data.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed table, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(\"public\", \"room_transfer\")\n\n    # Join with lookup to get transfer description.\n    lookup_table = self.get_table(\"public\", \"lookup_room_transfer\")\n    lookup_table = qo.ConditionEquals(\"variable\", \"medical_service\")(lookup_table)\n\n    table = qo.Join(\n        lookup_table,\n        on=(\"medical_service\", \"value\"),\n        join_table_cols=\"description\",\n        isouter=True,\n    )(table)\n    table = qo.Rename({\"description\": \"transfer_description\"})(table)\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.gemini.GEMINIQuerier.care_units","title":"care_units","text":"<pre><code>care_units()\n</code></pre> <p>Query care unit data, fetches transfer info from multiple tables.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed table, wrapped in an interface object.</p> Source code in <code>cycquery/gemini.py</code> <pre><code>def care_units(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query care unit data, fetches transfer info from multiple tables.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed table, wrapped in an interface object.\n\n    \"\"\"\n    filter_care_unit_cols = qo.Keep(\n        [\n            \"genc_id\",\n            \"admit\",\n            \"discharge\",\n            CARE_UNIT,\n        ],\n    )\n\n    # In-patient table.\n    ip_table = self.get_table(\"public\", \"ip_administrative\")\n    ip_table = qo.Rename(\n        {\n            \"admit_date_time\": \"admit\",\n            \"discharge_date_time\": \"discharge\",\n        },\n    )(ip_table)\n    ip_table = qo.Literal(\"IP\", CARE_UNIT)(ip_table)\n    ip_table = filter_care_unit_cols(ip_table)\n\n    # Special care unit table.\n    scu_table = self.get_table(\"public\", \"ip_scu\")\n    scu_table = qo.Rename(\n        {\n            \"scu_admit_date_time\": \"admit\",\n            \"scu_discharge_date_time\": \"discharge\",\n        },\n    )(scu_table)\n    scu_table = qo.Literal(\"SCU\", CARE_UNIT)(scu_table)\n    scu_table = filter_care_unit_cols(scu_table)\n\n    # Emergency room/department table.\n    er_table = self.get_table(\"public\", \"er_administrative\")\n    er_table = qo.Rename(\n        {\n            \"er_admit_timestamp\": \"admit\",\n            \"er_discharge_timestamp\": \"discharge\",\n        },\n    )(er_table)\n    er_table = qo.Literal(\"ER\", CARE_UNIT)(er_table)\n    er_table = filter_care_unit_cols(er_table)\n\n    # Room transfer table.\n    rt_table = self.get_table(\"public\", \"room_transfer\")\n    rt_table = qo.Rename(\n        {\n            \"checkin_date_time\": \"admit\",\n            \"checkout_date_time\": \"discharge\",\n        },\n    )(rt_table)\n    rt_table = qo.Rename({\"transfer_description\": CARE_UNIT})(rt_table)\n    rt_table = filter_care_unit_cols(rt_table)\n\n    # Combine.\n    table = union_all(\n        select(er_table),\n        select(scu_table),\n        select(ip_table),\n        select(rt_table),\n    ).subquery()\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#cycquery.gemini.GEMINIQuerier.imaging","title":"imaging","text":"<pre><code>imaging()\n</code></pre> <p>Query imaging reports data.</p> <p>Returns:</p> Type Description <code>QueryInterface</code> <p>Constructed table, wrapped in an interface object.</p> Source code in <code>cycquery/gemini.py</code> <pre><code>def imaging(\n    self,\n) -&gt; QueryInterface:\n    \"\"\"Query imaging reports data.\n\n    Returns\n    -------\n    cycquery.interface.QueryInterface\n        Constructed table, wrapped in an interface object.\n\n    \"\"\"\n    table = self.get_table(\"public\", \"imaging\")\n\n    # Get imaging test description\n    lookup_table = self.get_table(\"public\", \"lookup_imaging\")\n    lookup_table = qo.ConditionEquals(\"variable\", \"imaging_test_name_mapped\")(\n        lookup_table,\n    )\n\n    table = qo.Join(\n        lookup_table,\n        on=(\"imaging_test_name_mapped\", \"value\"),\n        on_to_type=\"str\",\n        join_table_cols=\"description\",\n    )(table)\n    table = qo.Drop(\"value\")(table)\n    table = qo.Rename({\"description\": \"imaging_test_description\"})(table)\n    table = qo.ReorderAfter(\"imaging_test_description\", \"imaging_test_name_mapped\")(\n        table,\n    )\n\n    return QueryInterface(self.db, table)\n</code></pre>"},{"location":"api/#orm-module","title":"ORM Module","text":""},{"location":"api/#cycquery.orm","title":"cycquery.orm","text":"<p>Object Relational Mapper (ORM) using sqlalchemy.</p>"},{"location":"api/#cycquery.orm.DatasetQuerierConfig","title":"DatasetQuerierConfig  <code>dataclass</code>","text":"<p>Configuration for the dataset querier.</p> <p>Attributes:</p> Name Type Description <code>dbms</code> <code>str</code> <p>The database management system type (e.g., 'postgresql', 'mysql', 'sqlite').</p> <code>user</code> <code>(str, optional)</code> <p>The username for the database, by default empty. Not used for SQLite.</p> <code>pwd</code> <code>(str, optional)</code> <p>The password for the database, by default empty. Not used for SQLite.</p> <code>host</code> <code>(str, optional)</code> <p>The host address of the database, by default empty. Not used for SQLite.</p> <code>port</code> <code>(int, optional)</code> <p>The port number for the database, by default None. Not used for SQLite.</p> <code>database</code> <code>(str, optional)</code> <p>The name of the database or the path to the database file (for SQLite), by default empty.</p> <code>schemas</code> <code>(Union[str, List[str]], optional)</code> <p>The schema(s) to be used for querying, by default None.</p> Source code in <code>cycquery/orm.py</code> <pre><code>@dataclass\nclass DatasetQuerierConfig:\n    \"\"\"Configuration for the dataset querier.\n\n    Attributes\n    ----------\n    dbms : str\n        The database management system type (e.g., 'postgresql', 'mysql', 'sqlite').\n    user : str, optional\n        The username for the database, by default empty. Not used for SQLite.\n    pwd : str, optional\n        The password for the database, by default empty. Not used for SQLite.\n    host : str, optional\n        The host address of the database, by default empty. Not used for SQLite.\n    port : int, optional\n        The port number for the database, by default None. Not used for SQLite.\n    database : str, optional\n        The name of the database or the path to the database file (for SQLite),\n        by default empty.\n    schemas : Union[str, List[str]], optional\n        The schema(s) to be used for querying, by default None.\n\n    \"\"\"\n\n    dbms: str\n    user: str = \"\"\n    password: str = \"\"\n    host: str = \"\"\n    port: int | None = None\n    database: str = \"\"\n    schemas: str | list[str] | None = None\n</code></pre>"},{"location":"api/#cycquery.orm.Database","title":"Database","text":"<p>Database class.</p> <p>Attributes:</p> Name Type Description <code>config</code> <p>Configuration stored in a dataclass.</p> <code>engine</code> <p>SQL extraction engine.</p> <code>inspector</code> <p>Module for schema inspection.</p> <code>session</code> <p>Session for ORM.</p> <code>is_connected</code> <p>Whether the database is setup, connected and ready to run queries.</p> Source code in <code>cycquery/orm.py</code> <pre><code>class Database:\n    \"\"\"Database class.\n\n    Attributes\n    ----------\n    config\n        Configuration stored in a dataclass.\n    engine\n        SQL extraction engine.\n    inspector\n        Module for schema inspection.\n    session\n        Session for ORM.\n    is_connected\n        Whether the database is setup, connected and ready to run queries.\n\n    \"\"\"\n\n    def __init__(self, config: DatasetQuerierConfig) -&gt; None:\n        \"\"\"Instantiate.\n\n        Parameters\n        ----------\n        config\n            Path to directory with config file, for overrides.\n\n        \"\"\"\n        self.config = config\n        self.is_connected = False\n\n        # Check if server is up or database file exists.\n        if self.config.dbms.lower() == \"sqlite\":\n            if not os.path.exists(self.config.database):\n                LOGGER.error(\n                    f\"\"\"Database file '{self.config.database}' does not exist!\"\"\",\n                )\n                return\n        else:\n            if not self.config.host:\n                LOGGER.error(\"\"\"No server host provided!\"\"\")\n                return\n            if not self.config.port:\n                LOGGER.error(\"\"\"No server port provided!\"\"\")\n                return\n            sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)\n            sock.settimeout(SOCKET_CONNECTION_TIMEOUT)\n            try:\n                is_port_open = sock.connect_ex((self.config.host, self.config.port))\n            except socket.gaierror:\n                LOGGER.error(\"\"\"Server name not known, cannot establish connection!\"\"\")\n                return\n            if is_port_open:\n                LOGGER.error(\n                    \"\"\"Valid server host but port seems open, check if server is up!\"\"\",\n                )\n                return\n\n        self.engine = self._create_engine()\n        self.session = self._create_session()\n        self._tables: list[str] = []\n        self._setup()\n        self.is_connected = True\n        LOGGER.info(\"Database setup, ready to run queries!\")\n\n    def _create_engine(self) -&gt; Engine:\n        \"\"\"Create an engine.\"\"\"\n        db_url = _get_db_url(\n            self.config.dbms,\n            self.config.user,\n            self.config.password,\n            self.config.host,\n            self.config.port,\n            self.config.database,\n        )\n        self.conn = db_url\n        return create_engine(db_url)\n\n    def _create_session(self) -&gt; Session:\n        \"\"\"Create session.\"\"\"\n        self.inspector = inspect(self.engine)\n\n        # Create a session for using ORM.\n        session_factory = sessionmaker(bind=self.engine)\n\n        return session_factory()\n\n    def list_tables(self) -&gt; list[str]:\n        \"\"\"List tables in a schema.\n\n        Returns\n        -------\n        list[str]\n            List of table names.\n\n        \"\"\"\n        return self._tables\n\n    def _setup(self) -&gt; None:\n        \"\"\"Prepare ORM DB.\"\"\"\n        meta: dict[str, MetaData] = {}\n        schemas = self.inspector.get_schema_names()\n        if self.config.schemas is None:\n            schemas_to_use = schemas\n        elif isinstance(self.config.schemas, str):\n            schemas_to_use = [self.config.schemas]\n        else:\n            schemas_to_use = self.config.schemas\n        for schema_name in schemas:\n            if schemas_to_use is None or schema_name in schemas_to_use:\n                metadata = MetaData(schema=schema_name)\n                with self.engine.connect() as conn:\n                    metadata.reflect(bind=conn)\n                meta[schema_name] = metadata\n                schema = DBSchema(schema_name, meta[schema_name])\n                for table_name in meta[schema_name].tables:\n                    table = DBTable(table_name, meta[schema_name].tables[table_name])  # type: ignore[arg-type]\n                    for column in meta[schema_name].tables[table_name].columns:\n                        setattr(table, column.name, column)\n                    if not isinstance(table.name_, str):\n                        table.name_ = str(table.name_)\n                    self._tables.append(table.name_)\n                    setattr(schema, get_attr_name(table.name_), table)\n                setattr(self, schema_name, schema)\n\n    @time_function\n    @table_params_to_type(Select)  # type: ignore[arg-type]\n    def run_query(\n        self,\n        query: TableTypes | str,\n        dtype_backend: str = \"pyarrow\",\n        limit: int | None = None,\n        index_col: str | None = None,\n    ) -&gt; pd.DataFrame:\n        \"\"\"Run query.\n\n        Parameters\n        ----------\n        query\n            Query to run.\n        dtype_backend\n            Backend for dtype conversion.\n        limit\n            Limit query result to limit.\n        index_col\n            Column which becomes the index, and defines the partitioning.\n            Should be a indexed column in the SQL server, and any orderable type.\n\n        Returns\n        -------\n        pandas.DataFrame\n            Extracted data from query.\n\n        \"\"\"\n        if isinstance(query, str) and limit is not None:\n            raise ValueError(\n                \"Cannot use limit argument when running raw SQL string query!\",\n            )\n        # Limit the results returned.\n        if limit is not None:\n            query = query.limit(limit)  # type: ignore\n\n        # Wrap raw SQL strings for SA 2.0 compatibility.\n        sql = text(query) if isinstance(query, str) else query\n\n        with self.engine.connect() as conn:\n            data = pd.read_sql_query(\n                sql,\n                conn,\n                index_col=index_col,\n                dtype_backend=dtype_backend,\n            )\n        LOGGER.info(\"Query returned successfully!\")\n\n        return data\n\n    @time_function\n    @table_params_to_type(Select)  # type: ignore[arg-type]\n    def save_query_to_csv(self, query: TableTypes, path: str) -&gt; str:\n        \"\"\"Save query in a .csv format.\n\n        Parameters\n        ----------\n        query\n            Query to save.\n        path\n            Save path.\n\n        Returns\n        -------\n        str\n            Processed save path for upstream use.\n\n        \"\"\"\n        path = process_file_save_path(path, \"csv\")\n\n        with self.engine.connect() as conn:\n            result = conn.execute(query)  # type: ignore\n            with open(path, \"w\", encoding=\"utf-8\") as file_descriptor:\n                outcsv = csv.writer(file_descriptor)\n                outcsv.writerow(result.keys())\n                outcsv.writerows(result)\n\n        return path\n\n    @time_function\n    @table_params_to_type(Select)  # type: ignore[arg-type]\n    def save_query_to_parquet(self, query: TableTypes, path: str) -&gt; str:\n        \"\"\"Save query in a .parquet format.\n\n        Parameters\n        ----------\n        query\n            Query to save.\n        path\n            Save path.\n\n        Returns\n        -------\n        str\n            Processed save path for upstream use.\n\n        \"\"\"\n        path = process_file_save_path(path, \"parquet\")\n\n        # Save to CSV, load with pyarrow, save to Parquet\n        csv_path = exchange_extension(path, \"csv\")\n        self.save_query_to_csv(query, csv_path)\n        table = pv.read_csv(csv_path)\n        os.remove(csv_path)\n        pq.write_table(table, path)\n\n        return path\n\n    def _query_batch_conditions(\n        self,\n        query: TableTypes,\n        index_col: str,\n        batch_size: int,\n    ) -&gt; list[BinaryExpression | BooleanClauseList]:\n        \"\"\"Return a list of WHERE conditions to segment a query into batches.\n\n        Batches are created via SQL windowing, based on segmenting the values in a\n        given column, such as an ID column, into intervals.\n\n        Requires a database that supports window functions.\n\n        Parameters\n        ----------\n        query\n            Query to run.\n        index_col\n            Name of the sample ID column by which to batch.\n        batch_size\n            Batch size for the query.\n\n        Returns\n        -------\n        list of sqlalchemy.sql.elements.BinaryExpression or\n        sqlalchemy.sql.elements.BooleanClauseList\n            The window conditions on which to filter.\n\n        \"\"\"\n\n        def _compute_query_dividers(\n            query: Subquery,\n            index_col: str,\n            maximum: int,\n        ) -&gt; list[int]:\n            # Compute the row count for each unique value\n            col = get_column(query, index_col)\n            table = select(col, func.count(col).label(\"count\")).group_by(col)\n            count_data = self.run_query(table)\n\n            # Check that all values can actually fit into the maximum batch size\n            max_count = count_data[\"count\"].max()\n            if maximum &lt; max_count:\n                raise ValueError(f\"Maximum must be at least {max_count}.\")\n\n            # Sort and create a cumulative sum of row counts\n            count_data = count_data.sort_values(index_col)\n            count_data[\"cumsum\"] = count_data[\"count\"].cumsum()\n\n            # Create query dividers\n            last_sum = 0\n\n            if len(count_data) == 0:\n                raise ValueError(\"Query is empty. Cannot return batched results.\")\n\n            dividers = [int(count_data[index_col].iloc[0])]\n            for i, cumsum in enumerate(count_data[\"cumsum\"].values[1:]):\n                # If adding the next value will put the sum over the max,\n                # then add another divider on the previous value\n                if cumsum - last_sum &gt; maximum:\n                    dividers.append(int(count_data[index_col].iloc[i]))\n                    last_sum = count_data[\"cumsum\"].iloc[i]\n\n            return dividers\n\n        def _range_condition(\n            start_id: int,\n            end_id: int | None = None,\n        ) -&gt; BinaryExpression | BooleanClauseList:\n            if end_id:\n                return and_(column &gt;= start_id, column &lt; end_id)  # type: ignore[return-value]\n\n            return column &gt;= start_id\n\n        # Create interval dividers\n        dividers = _compute_query_dividers(query, index_col, batch_size)  # type: ignore[arg-type]\n\n        # Create filtering conditions\n        column = get_column(query, index_col)\n        conditions = []\n        while dividers:\n            # Create interval ranges\n            start = dividers.pop(0)\n            end = dividers[0] if dividers else None\n\n            # Create condition\n            conditions.append(_range_condition(start, end))\n\n        return conditions\n\n    @table_params_to_type(Subquery)  # type: ignore[arg-type]\n    def run_query_batch(\n        self,\n        query: TableTypes,\n        index_col: str,\n        batch_size: int,\n        dtype_backend: str = \"pyarrow\",\n    ) -&gt; Generator[pd.DataFrame, None, None]:\n        \"\"\"Generate query batches with complete sets of IDs in a batch.\n\n        Queries are sorted and grouped such that the rows for a given sample ID are kept\n        together in a single batch.\n\n        Parameters\n        ----------\n        query\n            Query to run.\n        index_col\n            Name of the sample ID column by which to batch.\n        batch_size\n            Batch size for the query. Since the partitioning happens on the index\n            column, the batch size is the approximate number of rows that will\n            be returned in a batch.\n        dtype_backend\n            Backend for dtype conversion.\n\n        Yields\n        ------\n        pandas.DataFrame\n            A query batch with complete sets of sample IDs.\n\n        \"\"\"\n        if \"limit\" in str(query).lower():\n            raise NotImplementedError(\n                \"Currently not supporting batching for queries with a LIMIT.\",\n            )\n\n        conditions = self._query_batch_conditions(query, index_col, batch_size)\n\n        # Opportunity for easy multi-processing/parallelization here!\n        with self.engine.connect() as conn:\n            for condition in conditions:\n                run = select(query).where(condition)  # type: ignore[arg-type]\n                yield pd.read_sql_query(run, conn, dtype_backend=dtype_backend)\n</code></pre>"},{"location":"api/#cycquery.orm.Database.__init__","title":"__init__","text":"<pre><code>__init__(config)\n</code></pre> <p>Instantiate.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>DatasetQuerierConfig</code> <p>Path to directory with config file, for overrides.</p> required Source code in <code>cycquery/orm.py</code> <pre><code>def __init__(self, config: DatasetQuerierConfig) -&gt; None:\n    \"\"\"Instantiate.\n\n    Parameters\n    ----------\n    config\n        Path to directory with config file, for overrides.\n\n    \"\"\"\n    self.config = config\n    self.is_connected = False\n\n    # Check if server is up or database file exists.\n    if self.config.dbms.lower() == \"sqlite\":\n        if not os.path.exists(self.config.database):\n            LOGGER.error(\n                f\"\"\"Database file '{self.config.database}' does not exist!\"\"\",\n            )\n            return\n    else:\n        if not self.config.host:\n            LOGGER.error(\"\"\"No server host provided!\"\"\")\n            return\n        if not self.config.port:\n            LOGGER.error(\"\"\"No server port provided!\"\"\")\n            return\n        sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)\n        sock.settimeout(SOCKET_CONNECTION_TIMEOUT)\n        try:\n            is_port_open = sock.connect_ex((self.config.host, self.config.port))\n        except socket.gaierror:\n            LOGGER.error(\"\"\"Server name not known, cannot establish connection!\"\"\")\n            return\n        if is_port_open:\n            LOGGER.error(\n                \"\"\"Valid server host but port seems open, check if server is up!\"\"\",\n            )\n            return\n\n    self.engine = self._create_engine()\n    self.session = self._create_session()\n    self._tables: list[str] = []\n    self._setup()\n    self.is_connected = True\n    LOGGER.info(\"Database setup, ready to run queries!\")\n</code></pre>"},{"location":"api/#cycquery.orm.Database.list_tables","title":"list_tables","text":"<pre><code>list_tables()\n</code></pre> <p>List tables in a schema.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>List of table names.</p> Source code in <code>cycquery/orm.py</code> <pre><code>def list_tables(self) -&gt; list[str]:\n    \"\"\"List tables in a schema.\n\n    Returns\n    -------\n    list[str]\n        List of table names.\n\n    \"\"\"\n    return self._tables\n</code></pre>"},{"location":"api/#cycquery.orm.Database.run_query","title":"run_query","text":"<pre><code>run_query(\n    query,\n    dtype_backend=\"pyarrow\",\n    limit=None,\n    index_col=None,\n)\n</code></pre> <p>Run query.</p> <p>Parameters:</p> Name Type Description Default <code>query</code> <code>TableTypes | str</code> <p>Query to run.</p> required <code>dtype_backend</code> <code>str</code> <p>Backend for dtype conversion.</p> <code>'pyarrow'</code> <code>limit</code> <code>int | None</code> <p>Limit query result to limit.</p> <code>None</code> <code>index_col</code> <code>str | None</code> <p>Column which becomes the index, and defines the partitioning. Should be a indexed column in the SQL server, and any orderable type.</p> <code>None</code> <p>Returns:</p> Type Description <code>DataFrame</code> <p>Extracted data from query.</p> Source code in <code>cycquery/orm.py</code> <pre><code>@time_function\n@table_params_to_type(Select)  # type: ignore[arg-type]\ndef run_query(\n    self,\n    query: TableTypes | str,\n    dtype_backend: str = \"pyarrow\",\n    limit: int | None = None,\n    index_col: str | None = None,\n) -&gt; pd.DataFrame:\n    \"\"\"Run query.\n\n    Parameters\n    ----------\n    query\n        Query to run.\n    dtype_backend\n        Backend for dtype conversion.\n    limit\n        Limit query result to limit.\n    index_col\n        Column which becomes the index, and defines the partitioning.\n        Should be a indexed column in the SQL server, and any orderable type.\n\n    Returns\n    -------\n    pandas.DataFrame\n        Extracted data from query.\n\n    \"\"\"\n    if isinstance(query, str) and limit is not None:\n        raise ValueError(\n            \"Cannot use limit argument when running raw SQL string query!\",\n        )\n    # Limit the results returned.\n    if limit is not None:\n        query = query.limit(limit)  # type: ignore\n\n    # Wrap raw SQL strings for SA 2.0 compatibility.\n    sql = text(query) if isinstance(query, str) else query\n\n    with self.engine.connect() as conn:\n        data = pd.read_sql_query(\n            sql,\n            conn,\n            index_col=index_col,\n            dtype_backend=dtype_backend,\n        )\n    LOGGER.info(\"Query returned successfully!\")\n\n    return data\n</code></pre>"},{"location":"api/#cycquery.orm.Database.save_query_to_csv","title":"save_query_to_csv","text":"<pre><code>save_query_to_csv(query, path)\n</code></pre> <p>Save query in a .csv format.</p> <p>Parameters:</p> Name Type Description Default <code>query</code> <code>TableTypes</code> <p>Query to save.</p> required <code>path</code> <code>str</code> <p>Save path.</p> required <p>Returns:</p> Type Description <code>str</code> <p>Processed save path for upstream use.</p> Source code in <code>cycquery/orm.py</code> <pre><code>@time_function\n@table_params_to_type(Select)  # type: ignore[arg-type]\ndef save_query_to_csv(self, query: TableTypes, path: str) -&gt; str:\n    \"\"\"Save query in a .csv format.\n\n    Parameters\n    ----------\n    query\n        Query to save.\n    path\n        Save path.\n\n    Returns\n    -------\n    str\n        Processed save path for upstream use.\n\n    \"\"\"\n    path = process_file_save_path(path, \"csv\")\n\n    with self.engine.connect() as conn:\n        result = conn.execute(query)  # type: ignore\n        with open(path, \"w\", encoding=\"utf-8\") as file_descriptor:\n            outcsv = csv.writer(file_descriptor)\n            outcsv.writerow(result.keys())\n            outcsv.writerows(result)\n\n    return path\n</code></pre>"},{"location":"api/#cycquery.orm.Database.save_query_to_parquet","title":"save_query_to_parquet","text":"<pre><code>save_query_to_parquet(query, path)\n</code></pre> <p>Save query in a .parquet format.</p> <p>Parameters:</p> Name Type Description Default <code>query</code> <code>TableTypes</code> <p>Query to save.</p> required <code>path</code> <code>str</code> <p>Save path.</p> required <p>Returns:</p> Type Description <code>str</code> <p>Processed save path for upstream use.</p> Source code in <code>cycquery/orm.py</code> <pre><code>@time_function\n@table_params_to_type(Select)  # type: ignore[arg-type]\ndef save_query_to_parquet(self, query: TableTypes, path: str) -&gt; str:\n    \"\"\"Save query in a .parquet format.\n\n    Parameters\n    ----------\n    query\n        Query to save.\n    path\n        Save path.\n\n    Returns\n    -------\n    str\n        Processed save path for upstream use.\n\n    \"\"\"\n    path = process_file_save_path(path, \"parquet\")\n\n    # Save to CSV, load with pyarrow, save to Parquet\n    csv_path = exchange_extension(path, \"csv\")\n    self.save_query_to_csv(query, csv_path)\n    table = pv.read_csv(csv_path)\n    os.remove(csv_path)\n    pq.write_table(table, path)\n\n    return path\n</code></pre>"},{"location":"api/#cycquery.orm.Database.run_query_batch","title":"run_query_batch","text":"<pre><code>run_query_batch(\n    query, index_col, batch_size, dtype_backend=\"pyarrow\"\n)\n</code></pre> <p>Generate query batches with complete sets of IDs in a batch.</p> <p>Queries are sorted and grouped such that the rows for a given sample ID are kept together in a single batch.</p> <p>Parameters:</p> Name Type Description Default <code>query</code> <code>TableTypes</code> <p>Query to run.</p> required <code>index_col</code> <code>str</code> <p>Name of the sample ID column by which to batch.</p> required <code>batch_size</code> <code>int</code> <p>Batch size for the query. Since the partitioning happens on the index column, the batch size is the approximate number of rows that will be returned in a batch.</p> required <code>dtype_backend</code> <code>str</code> <p>Backend for dtype conversion.</p> <code>'pyarrow'</code> <p>Yields:</p> Type Description <code>DataFrame</code> <p>A query batch with complete sets of sample IDs.</p> Source code in <code>cycquery/orm.py</code> <pre><code>@table_params_to_type(Subquery)  # type: ignore[arg-type]\ndef run_query_batch(\n    self,\n    query: TableTypes,\n    index_col: str,\n    batch_size: int,\n    dtype_backend: str = \"pyarrow\",\n) -&gt; Generator[pd.DataFrame, None, None]:\n    \"\"\"Generate query batches with complete sets of IDs in a batch.\n\n    Queries are sorted and grouped such that the rows for a given sample ID are kept\n    together in a single batch.\n\n    Parameters\n    ----------\n    query\n        Query to run.\n    index_col\n        Name of the sample ID column by which to batch.\n    batch_size\n        Batch size for the query. Since the partitioning happens on the index\n        column, the batch size is the approximate number of rows that will\n        be returned in a batch.\n    dtype_backend\n        Backend for dtype conversion.\n\n    Yields\n    ------\n    pandas.DataFrame\n        A query batch with complete sets of sample IDs.\n\n    \"\"\"\n    if \"limit\" in str(query).lower():\n        raise NotImplementedError(\n            \"Currently not supporting batching for queries with a LIMIT.\",\n        )\n\n    conditions = self._query_batch_conditions(query, index_col, batch_size)\n\n    # Opportunity for easy multi-processing/parallelization here!\n    with self.engine.connect() as conn:\n        for condition in conditions:\n            run = select(query).where(condition)  # type: ignore[arg-type]\n            yield pd.read_sql_query(run, conn, dtype_backend=dtype_backend)\n</code></pre>"},{"location":"api/#utilities","title":"Utilities","text":""},{"location":"api/#cycquery.utils.common","title":"cycquery.utils.common","text":"<p>Common utility functions that can be used across multiple cycquery packages.</p>"},{"location":"api/#cycquery.utils.common.to_timestamp","title":"to_timestamp","text":"<pre><code>to_timestamp(data)\n</code></pre> <p>Convert a Pandas series or NumPy array to a datetime/timestamp type.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>Series | NDArray[Any]</code> <p>Data to be converted.</p> required <p>Returns:</p> Type Description <code>Series</code> <p>The converted data.</p> Source code in <code>cycquery/utils/common.py</code> <pre><code>def to_timestamp(data: pd.Series | npt.NDArray[Any]) -&gt; pd.Series:\n    \"\"\"Convert a Pandas series or NumPy array to a datetime/timestamp type.\n\n    Parameters\n    ----------\n    data: pandas.Series or numpy.ndarray\n        Data to be converted.\n\n    Returns\n    -------\n    pandas.Series\n        The converted data.\n\n    \"\"\"\n    if isinstance(data, pd.Series):\n        return pd.to_datetime(data)\n\n    if isinstance(data, np.ndarray):\n        return pd.to_datetime(pd.Series(data))\n\n    raise ValueError(f\"Type of data argument ({type(data)}) not supported.\")\n</code></pre>"},{"location":"api/#cycquery.utils.common.add_years_approximate","title":"add_years_approximate","text":"<pre><code>add_years_approximate(timestamp_series, years_series)\n</code></pre> <p>Approximately add together a timestamp series with a years series row-by-row.</p> <p>Approximates are typically exact or incorrect by one day, e.g., on leap days.</p> <p>Parameters:</p> Name Type Description Default <code>timestamp_series</code> <code>Series</code> <p>The series of timestamps to which to add.</p> required <code>years_series</code> <code>Series</code> <p>The series of years to add.</p> required <p>Returns:</p> Type Description <code>Series</code> <p>The timestamp series with the approximately added years.</p> Source code in <code>cycquery/utils/common.py</code> <pre><code>def add_years_approximate(\n    timestamp_series: pd.Series,\n    years_series: pd.Series,\n) -&gt; pd.Series:\n    \"\"\"Approximately add together a timestamp series with a years series row-by-row.\n\n    Approximates are typically exact or incorrect by one day, e.g., on leap days.\n\n    Parameters\n    ----------\n    timestamp_series: pandas.Series\n        The series of timestamps to which to add.\n    years_series: panadas.Series\n        The series of years to add.\n\n    Returns\n    -------\n    pandas.Series\n        The timestamp series with the approximately added years.\n\n    \"\"\"\n    # Add to the years column\n    year = timestamp_series.dt.year + years_series\n\n    # Handle the other columns\n    month = timestamp_series.dt.month\n    day = timestamp_series.dt.day\n    hour = timestamp_series.dt.hour\n    minute = timestamp_series.dt.minute\n\n    # Create new timestamp column\n    data = pd.DataFrame(\n        {\"year\": year, \"month\": month, \"day\": day, \"hour\": hour, \"minute\": minute},\n    )\n\n    # Subtract 1 from potentially invalid leap days to avoid issues\n    leap_days = (month == 2) &amp; (day == 29)\n    data.loc[leap_days, \"day\"] -= 1\n\n    return pd.to_datetime(data)\n</code></pre>"},{"location":"api/#cycquery.utils.common.add_years_exact","title":"add_years_exact","text":"<pre><code>add_years_exact(timestamp_series, years_series)\n</code></pre> <p>Add together a timestamp series with a years series row-by-row.</p> <p>Warning: Very slow. It is worth using the add_years_approximate function even moderately large data.</p> <p>Parameters:</p> Name Type Description Default <code>timestamp_series</code> <code>Series</code> <p>The series of timestamps to which to add.</p> required <code>years_series</code> <code>Series</code> <p>The series of years to add.</p> required <p>Returns:</p> Type Description <code>Series</code> <p>The timestamp series with the approximately added years.</p> Source code in <code>cycquery/utils/common.py</code> <pre><code>def add_years_exact(timestamp_series: pd.Series, years_series: pd.Series) -&gt; pd.Series:\n    \"\"\"Add together a timestamp series with a years series row-by-row.\n\n    Warning: Very slow. It is worth using the add_years_approximate function even\n    moderately large data.\n\n    Parameters\n    ----------\n    timestamp_series: pandas.Series\n        The series of timestamps to which to add.\n    years_series: panadas.Series\n        The series of years to add.\n\n    Returns\n    -------\n    pandas.Series\n        The timestamp series with the approximately added years.\n\n    \"\"\"\n    warnings.warn(\n        (\n            \"Computing the exact addition cannot be vectorized and is very slow. \"\n            \"Consider using the quick, approximate calculation.\"\n        ),\n        PerformanceWarning,\n        stacklevel=1,\n    )\n    return timestamp_series + years_series.apply(lambda x: pd.DateOffset(years=x))\n</code></pre>"},{"location":"api/#cycquery.utils.common.to_list","title":"to_list","text":"<pre><code>to_list(obj)\n</code></pre> <p>Convert some object to a list of object(s) unless already one.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>any</code> <p>The object to convert to a list.</p> required <p>Returns:</p> Type Description <code>list</code> <p>The processed object.</p> Source code in <code>cycquery/utils/common.py</code> <pre><code>def to_list(obj: Any) -&gt; list[Any]:\n    \"\"\"Convert some object to a list of object(s) unless already one.\n\n    Parameters\n    ----------\n    obj : any\n        The object to convert to a list.\n\n    Returns\n    -------\n    list\n        The processed object.\n\n    \"\"\"\n    if isinstance(obj, list):\n        return obj\n\n    if isinstance(obj, (np.ndarray, set, dict)):\n        return list(obj)\n\n    return [obj]\n</code></pre>"},{"location":"api/#cycquery.utils.common.to_list_optional","title":"to_list_optional","text":"<pre><code>to_list_optional(obj, none_to_empty=False)\n</code></pre> <p>Convert some object to a list of object(s) unless already None or a list.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>any</code> <p>The object to convert to a list.</p> required <code>none_to_empty</code> <code>bool</code> <p>If true, return a None obj as an empty list. Otherwise, return as None.</p> <code>False</code> <p>Returns:</p> Type Description <code>list or None</code> <p>The processed object.</p> Source code in <code>cycquery/utils/common.py</code> <pre><code>def to_list_optional(\n    obj: Any | None,\n    none_to_empty: bool = False,\n) -&gt; list[Any] | None:\n    \"\"\"Convert some object to a list of object(s) unless already None or a list.\n\n    Parameters\n    ----------\n    obj : any\n        The object to convert to a list.\n    none_to_empty: bool, default = False\n        If true, return a None obj as an empty list. Otherwise, return as None.\n\n    Returns\n    -------\n    list or None\n        The processed object.\n\n    \"\"\"\n    if obj is None:\n        if none_to_empty:\n            return []\n        return None\n\n    return to_list(obj)\n</code></pre>"},{"location":"api/#cycquery.utils.common.to_datetime_format","title":"to_datetime_format","text":"<pre><code>to_datetime_format(date, fmt='%Y-%m-%d')\n</code></pre> <p>Convert string date to datetime.</p> <p>Parameters:</p> Name Type Description Default <code>date</code> <code>str</code> <p>Input date in string format.</p> required <code>fmt</code> <code>str</code> <p>Date formatting string.</p> <code>'%Y-%m-%d'</code> <p>Returns:</p> Type Description <code>datetime</code> <p>Date in datetime format.</p> Source code in <code>cycquery/utils/common.py</code> <pre><code>def to_datetime_format(date: str, fmt: str = \"%Y-%m-%d\") -&gt; datetime:\n    \"\"\"Convert string date to datetime.\n\n    Parameters\n    ----------\n    date: str\n        Input date in string format.\n    fmt: str, optional\n        Date formatting string.\n\n    Returns\n    -------\n    datetime\n        Date in datetime format.\n\n    \"\"\"\n    return datetime.strptime(date, fmt)\n</code></pre>"},{"location":"api/#cycquery.utils.common.list_swap","title":"list_swap","text":"<pre><code>list_swap(lst, index1, index2)\n</code></pre> <p>Swap items in a list given the item index and new item index.</p> <p>Parameters:</p> Name Type Description Default <code>lst</code> <code>list[Any]</code> <p>List in which elements will be swapped.</p> required <code>index1</code> <code>int</code> <p>Index of first item to swap.</p> required <code>index2</code> <code>int</code> <p>Index of second item to swap.</p> required <p>Returns:</p> Type Description <code>list</code> <p>List with elements swapped.</p> Source code in <code>cycquery/utils/common.py</code> <pre><code>def list_swap(lst: list[Any], index1: int, index2: int) -&gt; list[Any]:\n    \"\"\"Swap items in a list given the item index and new item index.\n\n    Parameters\n    ----------\n    lst: list\n        List in which elements will be swapped.\n    index1: int\n        Index of first item to swap.\n    index2: int\n        Index of second item to swap.\n\n    Returns\n    -------\n    list\n        List with elements swapped.\n\n    \"\"\"\n    if not 0 &lt;= index1 &lt; len(lst):\n        raise ValueError(\"index 1 is out of range.\")\n\n    if not 0 &lt;= index2 &lt; len(lst):\n        raise ValueError(\"index 2 is out of range.\")\n\n    lst[index1], lst[index2] = lst[index2], lst[index1]\n\n    return lst\n</code></pre>"},{"location":"api/#cycquery.utils.file","title":"cycquery.utils.file","text":"<p>Utility functions for saving/loading files.</p>"},{"location":"api/#cycquery.utils.file.join","title":"join","text":"<pre><code>join(*paths)\n</code></pre> <p>Robustly join paths.</p> <p>os.path.join only may cause problems with some filepaths (especially on Windows).</p> <p>Parameters:</p> Name Type Description Default <code>paths</code> <code>str</code> <p>file paths</p> <code>()</code> <p>Returns:</p> Type Description <code>str</code> <p>The joined path of all input paths.</p> Source code in <code>cycquery/utils/file.py</code> <pre><code>def join(*paths: str) -&gt; str:\n    \"\"\"Robustly join paths.\n\n    os.path.join only may cause problems with some filepaths (especially on Windows).\n\n    Parameters\n    ----------\n    paths: str\n        file paths\n\n    Returns\n    -------\n    str\n        The joined path of all input paths.\n\n    \"\"\"\n    return os.path.join(*paths).replace(\"\\\\\", \"/\")\n</code></pre>"},{"location":"api/#cycquery.utils.file.exchange_extension","title":"exchange_extension","text":"<pre><code>exchange_extension(file_path, new_ext)\n</code></pre> <p>Exchange one file extension for another.</p> <p>Parameters:</p> Name Type Description Default <code>file_path</code> <code>str</code> <p>File path in which to exchange the extension.</p> required <code>new_ext</code> <code>str</code> <p>New extension to replace the existing extension.</p> required <p>Returns:</p> Type Description <code>str</code> <p>File path with the new extension.</p> Source code in <code>cycquery/utils/file.py</code> <pre><code>def exchange_extension(file_path: str, new_ext: str) -&gt; str:\n    \"\"\"Exchange one file extension for another.\n\n    Parameters\n    ----------\n    file_path: str\n        File path in which to exchange the extension.\n    new_ext: str\n        New extension to replace the existing extension.\n\n    Returns\n    -------\n    str\n        File path with the new extension.\n\n    \"\"\"\n    # Remove a leading dot\n    new_ext = new_ext.strip(\".\")\n    _, old_ext = os.path.splitext(file_path)\n    return file_path[: -len(old_ext)] + \".\" + new_ext\n</code></pre>"},{"location":"api/#cycquery.utils.file.process_file_save_path","title":"process_file_save_path","text":"<pre><code>process_file_save_path(\n    save_path, file_format, create_dir=True\n)\n</code></pre> <p>Process file save path, perform checks, and possibly create a parent directory.</p> <p>Parameters:</p> Name Type Description Default <code>save_path</code> <code>str</code> <p>Path where the file will be saved.</p> required <code>file_format</code> <code>str</code> <p>File format of the file to save.</p> required <code>create_dir</code> <code>bool</code> <p>If True, create the parent directory path if needed.</p> <code>True</code> <p>Returns:</p> Type Description <code>str</code> <p>The processed save path.</p> Source code in <code>cycquery/utils/file.py</code> <pre><code>def process_file_save_path(\n    save_path: str,\n    file_format: str,\n    create_dir: bool = True,\n) -&gt; str:\n    \"\"\"Process file save path, perform checks, and possibly create a parent directory.\n\n    Parameters\n    ----------\n    save_path: str\n        Path where the file will be saved.\n    file_format: str\n        File format of the file to save.\n    create_dir: bool\n        If True, create the parent directory path if needed.\n\n    Returns\n    -------\n    str\n        The processed save path.\n\n    \"\"\"\n    # Create the directory if it doesn't already exist.\n    directory, _ = os.path.split(save_path)\n\n    if create_dir and directory != \"\":\n        os.makedirs(directory, exist_ok=True)\n\n    # Add the .parquet extension if it isn't there.\n    _, ext = os.path.splitext(save_path)\n\n    if ext == \"\":\n        save_path = save_path + \".\" + file_format\n    elif ext != \".\" + file_format:\n        raise ValueError(\n            f\"\"\"The file extension on the save path must be {file_format}.\n            Alternatively, consider changing the file format.\"\"\",\n        )\n\n    return save_path\n</code></pre>"},{"location":"api/#cycquery.utils.file.process_dir_save_path","title":"process_dir_save_path","text":"<pre><code>process_dir_save_path(save_path, create_dir=True)\n</code></pre> <p>Process directory save path, perform checks, and possibly create the directory.</p> <p>Parameters:</p> Name Type Description Default <code>save_path</code> <code>str</code> <p>Path where the file will be saved.</p> required <code>create_dir</code> <code>bool</code> <p>If True, create the directory if needed.</p> <code>True</code> <p>Returns:</p> Type Description <code>str</code> <p>The processed save path.</p> Source code in <code>cycquery/utils/file.py</code> <pre><code>def process_dir_save_path(save_path: str, create_dir: bool = True) -&gt; str:\n    \"\"\"Process directory save path, perform checks, and possibly create the directory.\n\n    Parameters\n    ----------\n    save_path: str\n        Path where the file will be saved.\n    create_dir: bool\n        If True, create the directory if needed.\n\n    Returns\n    -------\n    str\n        The processed save path.\n\n    \"\"\"\n    if os.path.exists(save_path):\n        if os.path.isdir(save_path):\n            return save_path\n        raise ValueError(\"If save path exists, it must be a directory.\")\n\n    if create_dir:\n        os.makedirs(save_path)\n        return save_path\n\n    raise ValueError(\"Directory does not exist.\")\n</code></pre>"},{"location":"api/#cycquery.utils.file.save_dataframe","title":"save_dataframe","text":"<pre><code>save_dataframe(\n    data, save_path, file_format=\"parquet\", log=True\n)\n</code></pre> <p>Save a pandas.DataFrame or dask.DataFrame object to file.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>DataFrame</code> <p>Dataframe to save.</p> required <code>save_path</code> <code>str</code> <p>Path where the file will be saved.</p> required <code>file_format</code> <code>str</code> <p>File format of the file to save.</p> <code>'parquet'</code> <code>log</code> <code>bool</code> <p>Whether to log the occurence.</p> <code>True</code> <p>Returns:</p> Type Description <code>str</code> <p>Processed save path for upstream use.</p> Source code in <code>cycquery/utils/file.py</code> <pre><code>def save_dataframe(\n    data: pd.DataFrame,\n    save_path: str,\n    file_format: str = \"parquet\",\n    log: bool = True,\n) -&gt; str:\n    \"\"\"Save a pandas.DataFrame or dask.DataFrame object to file.\n\n    Parameters\n    ----------\n    data: pandas.DataFrame\n        Dataframe to save.\n    save_path: str\n        Path where the file will be saved.\n    file_format: str\n        File format of the file to save.\n    log: bool\n        Whether to log the occurence.\n\n    Returns\n    -------\n    str\n        Processed save path for upstream use.\n\n    \"\"\"\n    if not isinstance(data, pd.DataFrame):\n        raise ValueError(\"Input data is not a DataFrame.\")\n    save_path = process_file_save_path(save_path, file_format)\n    if log:\n        LOGGER.info(\"Saving dataframe to %s\", save_path)\n    if file_format == \"parquet\":\n        if isinstance(data, pd.DataFrame):\n            data.to_parquet(save_path, schema=None)\n    elif file_format == \"csv\":\n        data.to_csv(save_path)\n    else:\n        raise ValueError(\n            \"Invalid file formated provided. Currently supporting 'parquet' and 'csv'.\",\n        )\n\n    return save_path\n</code></pre>"},{"location":"api/#cycquery.utils.file.load_dataframe","title":"load_dataframe","text":"<pre><code>load_dataframe(load_path, file_format='parquet', log=True)\n</code></pre> <p>Load file to a pandas.DataFrame object.</p> <p>Parameters:</p> Name Type Description Default <code>load_path</code> <code>str</code> <p>Path where the file to load.</p> required <code>file_format</code> <code>str</code> <p>File format of the file to load.</p> <code>'parquet'</code> <code>log</code> <code>bool</code> <p>Whether to log the occurence.</p> <code>True</code> <p>Returns:</p> Type Description <code>DataFrame or DataFrame</code> <p>Loaded data.</p> Source code in <code>cycquery/utils/file.py</code> <pre><code>def load_dataframe(\n    load_path: str,\n    file_format: str = \"parquet\",\n    log: bool = True,\n) -&gt; pd.DataFrame:\n    \"\"\"Load file to a pandas.DataFrame object.\n\n    Parameters\n    ----------\n    load_path: str\n        Path where the file to load.\n    file_format: str\n        File format of the file to load.\n    log: bool\n        Whether to log the occurence.\n\n    Returns\n    -------\n    pandas.DataFrame or dask.DataFrame\n        Loaded data.\n\n    \"\"\"\n    load_path = process_file_save_path(load_path, file_format)\n    if log:\n        LOGGER.info(\"Loading DataFrame from %s\", load_path)\n    if file_format == \"parquet\":\n        data = pd.read_parquet(load_path)\n    elif file_format == \"csv\":\n        data = pd.read_csv(load_path, index_col=[0])\n    else:\n        raise ValueError(\n            \"Invalid file formated provided. Currently supporting 'parquet' and 'csv'.\",\n        )\n\n    return data\n</code></pre>"},{"location":"api/#cycquery.utils.file.save_array","title":"save_array","text":"<pre><code>save_array(data, save_path, file_format='npy', log=True)\n</code></pre> <p>Save a numpy.ndarray object to file.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>ArrayLike</code> <p>Array to save.</p> required <code>save_path</code> <code>str</code> <p>Path where the file will be saved.</p> required <code>file_format</code> <code>str</code> <p>File format of the file to save.</p> <code>'npy'</code> <code>log</code> <code>bool</code> <p>Whether to log the occurence.</p> <code>True</code> <p>Returns:</p> Type Description <code>str</code> <p>Processed save path for upstream use.</p> Source code in <code>cycquery/utils/file.py</code> <pre><code>def save_array(\n    data: np.typing.ArrayLike,\n    save_path: str,\n    file_format: str = \"npy\",\n    log: bool = True,\n) -&gt; str:\n    \"\"\"Save a numpy.ndarray object to file.\n\n    Parameters\n    ----------\n    data: numpy.ndarray\n        Array to save.\n    save_path: str\n        Path where the file will be saved.\n    file_format: str\n        File format of the file to save.\n    log: bool\n        Whether to log the occurence.\n\n    Returns\n    -------\n    str\n        Processed save path for upstream use.\n\n    \"\"\"\n    save_path = process_file_save_path(save_path, file_format)\n\n    if not isinstance(data, np.ndarray):\n        raise ValueError(\"Input data is not an array.\")\n\n    if log:\n        LOGGER.info(\"Saving array to %s\", save_path)\n\n    if file_format == \"npy\":\n        np.save(save_path, data)\n    else:\n        raise ValueError(\"Invalid file formated provided. Currently supporting 'npy'.\")\n\n    return save_path\n</code></pre>"},{"location":"api/#cycquery.utils.file.load_array","title":"load_array","text":"<pre><code>load_array(load_path, file_format='npy', log=True)\n</code></pre> <p>Load file to a numpy.ndarray object.</p> <p>Parameters:</p> Name Type Description Default <code>load_path</code> <code>str</code> <p>Path where the file to load.</p> required <code>file_format</code> <code>str</code> <p>File format of the file to load.</p> <code>'npy'</code> <code>log</code> <code>bool</code> <p>Whether to log the occurence.</p> <code>True</code> <p>Returns:</p> Type Description <code>ndarray</code> <p>Loaded data.</p> Source code in <code>cycquery/utils/file.py</code> <pre><code>def load_array(\n    load_path: str,\n    file_format: str = \"npy\",\n    log: bool = True,\n) -&gt; Any:\n    \"\"\"Load file to a numpy.ndarray object.\n\n    Parameters\n    ----------\n    load_path: str\n        Path where the file to load.\n    file_format: str\n        File format of the file to load.\n    log: bool\n        Whether to log the occurence.\n\n    Returns\n    -------\n    numpy.ndarray\n        Loaded data.\n\n    \"\"\"\n    load_path = process_file_save_path(load_path, file_format)\n\n    if log:\n        LOGGER.info(\"Loading array from %s\", load_path)\n\n    if file_format == \"npy\":\n        data = np.load(load_path)\n    else:\n        raise ValueError(\"Invalid file formated provided. Currently supporting 'npy'.\")\n\n    if not isinstance(data, np.ndarray):\n        raise ValueError(\"Loaded data is not an array.\")\n\n    return data\n</code></pre>"},{"location":"api/#cycquery.utils.file.save_pickle","title":"save_pickle","text":"<pre><code>save_pickle(data, save_path, log=True)\n</code></pre> <p>Save a object to pickle file.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>Any</code> <p>Data to save.</p> required <code>save_path</code> <code>str</code> <p>Path where the file will be saved.</p> required <code>log</code> <code>bool</code> <p>Whether to log the occurence.</p> <code>True</code> <p>Returns:</p> Type Description <code>str</code> <p>Processed save path for upstream use.</p> Source code in <code>cycquery/utils/file.py</code> <pre><code>def save_pickle(\n    data: Any,\n    save_path: str,\n    log: bool = True,\n) -&gt; str:\n    \"\"\"Save a object to pickle file.\n\n    Parameters\n    ----------\n    data: any\n        Data to save.\n    save_path: str\n        Path where the file will be saved.\n    log: bool\n        Whether to log the occurence.\n\n    Returns\n    -------\n    str\n        Processed save path for upstream use.\n\n    \"\"\"\n    save_path = process_file_save_path(save_path, \"pkl\")\n\n    if log:\n        LOGGER.info(\"Pickling data to %s\", save_path)\n\n    with open(save_path, \"wb\") as handle:\n        pickle.dump(data, handle)\n\n    return save_path\n</code></pre>"},{"location":"api/#cycquery.utils.file.load_pickle","title":"load_pickle","text":"<pre><code>load_pickle(load_path, log=True)\n</code></pre> <p>Load an object from a pickle file.</p> <p>Parameters:</p> Name Type Description Default <code>load_path</code> <code>str</code> <p>Path where the file to load.</p> required <code>log</code> <code>bool</code> <p>Whether to log the occurence.</p> <code>True</code> <p>Returns:</p> Type Description <code>any</code> <p>Loaded data.</p> Source code in <code>cycquery/utils/file.py</code> <pre><code>def load_pickle(\n    load_path: str,\n    log: bool = True,\n) -&gt; Any:\n    \"\"\"Load an object from a pickle file.\n\n    Parameters\n    ----------\n    load_path: str\n        Path where the file to load.\n    log: bool\n        Whether to log the occurence.\n\n    Returns\n    -------\n    any\n        Loaded data.\n\n    \"\"\"\n    load_path = process_file_save_path(load_path, \"pkl\")\n\n    if log:\n        LOGGER.info(\"Loading pickled data from %s\", load_path)\n\n    with open(load_path, \"rb\") as handle:\n        return pickle.load(handle)\n</code></pre>"},{"location":"api/#cycquery.utils.file.listdir_nonhidden","title":"listdir_nonhidden","text":"<pre><code>listdir_nonhidden(path)\n</code></pre> <p>List the non-hidden files of a directory.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Directory path.</p> required <p>Returns:</p> Type Description <code>list</code> <p>List of non-hidden files.</p> Source code in <code>cycquery/utils/file.py</code> <pre><code>def listdir_nonhidden(path: str) -&gt; list[str]:\n    \"\"\"List the non-hidden files of a directory.\n\n    Parameters\n    ----------\n    path: str\n        Directory path.\n\n    Returns\n    -------\n    list\n        List of non-hidden files.\n\n    \"\"\"\n    return [f for f in os.listdir(path) if not f.startswith(\".\")]\n</code></pre>"},{"location":"api/#cycquery.utils.file.yield_dataframes","title":"yield_dataframes","text":"<pre><code>yield_dataframes(\n    dir_path, sort=True, skip_n=None, log=True\n)\n</code></pre> <p>Yield DataFrames loaded from a directory.</p> <p>Any non-hidden files in the directory must be loadable as a DataFrame.</p> <p>Parameters:</p> Name Type Description Default <code>dir_path</code> <code>str</code> <p>Directory path of files.</p> required <code>sort</code> <code>bool</code> <p>Whether to sort the files and yield them in an ordered manner.</p> <code>True</code> <code>skip_n</code> <code>int | None</code> <p>If specified, skip the first n files when yielding the files. This is especially useful in lieu of the execution being interrupted.</p> <code>None</code> <code>log</code> <code>bool</code> <p>Whether to log the occurence.</p> <code>True</code> <p>Yields:</p> Type Description <code>DataFrame</code> <p>A DataFrame.</p> Source code in <code>cycquery/utils/file.py</code> <pre><code>def yield_dataframes(\n    dir_path: str,\n    sort: bool = True,\n    skip_n: int | None = None,\n    log: bool = True,\n) -&gt; Generator[pd.DataFrame, None, None]:\n    \"\"\"Yield DataFrames loaded from a directory.\n\n    Any non-hidden files in the directory must be loadable as a DataFrame.\n\n    Parameters\n    ----------\n    dir_path: str\n        Directory path of files.\n    sort: bool, default = True\n        Whether to sort the files and yield them in an ordered manner.\n    skip_n: int, optional\n        If specified, skip the first n files when yielding the files.\n        This is especially useful in lieu of the execution being interrupted.\n    log: bool\n        Whether to log the occurence.\n\n    Yields\n    ------\n    pandas.DataFrame\n        A DataFrame.\n\n    \"\"\"\n    files = list(listdir_nonhidden(dir_path))\n\n    if sort:\n        files.sort()\n\n    if skip_n:\n        files = files[skip_n:]\n\n    for file in files:\n        yield load_dataframe(join(dir_path, file), log=log)\n</code></pre>"},{"location":"api/#cycquery.utils.file.yield_pickled_files","title":"yield_pickled_files","text":"<pre><code>yield_pickled_files(\n    dir_path, sort=True, skip_n=None, log=True\n)\n</code></pre> <p>Yield pickled files loaded from a directory.</p> <p>Any non-hidden files in the directory must be loadable with pickle.</p> <p>Parameters:</p> Name Type Description Default <code>dir_path</code> <code>str</code> <p>Directory path of files.</p> required <code>sort</code> <code>bool</code> <p>Whether to sort the files and yield them in an ordered manner.</p> <code>True</code> <code>skip_n</code> <code>int | None</code> <p>If specified, skip the first n files when yielding the files. This is especially useful in lieu of the execution being interrupted.</p> <code>None</code> <code>log</code> <code>bool</code> <p>Whether to log the occurence.</p> <code>True</code> <p>Yields:</p> Type Description <code>any</code> <p>Previously pickled data.</p> Source code in <code>cycquery/utils/file.py</code> <pre><code>def yield_pickled_files(\n    dir_path: str,\n    sort: bool = True,\n    skip_n: int | None = None,\n    log: bool = True,\n) -&gt; Generator[pd.DataFrame, None, None]:\n    \"\"\"Yield pickled files loaded from a directory.\n\n    Any non-hidden files in the directory must be loadable with pickle.\n\n    Parameters\n    ----------\n    dir_path: str\n        Directory path of files.\n    sort: bool, default = True\n        Whether to sort the files and yield them in an ordered manner.\n    skip_n: int, optional\n        If specified, skip the first n files when yielding the files.\n        This is especially useful in lieu of the execution being interrupted.\n    log: bool\n        Whether to log the occurence.\n\n    Yields\n    ------\n    any\n        Previously pickled data.\n\n    \"\"\"\n    files = list(listdir_nonhidden(dir_path))\n\n    if sort:\n        files.sort()\n\n    if skip_n:\n        files = files[skip_n:]\n\n    for file in files:\n        yield load_pickle(join(dir_path, file), log=log)\n</code></pre>"},{"location":"api/#cycquery.utils.log","title":"cycquery.utils.log","text":"<p>Python logging function.</p>"},{"location":"api/#cycquery.utils.log.Formatter","title":"Formatter","text":"<p>               Bases: <code>Formatter</code></p> <p>Formatter.</p> <p>Attributes:</p> Name Type Description <code>use_color</code> <code>bool</code> <p>Flag to toggle use of color.</p> Source code in <code>cycquery/utils/log.py</code> <pre><code>class Formatter(logging.Formatter):\n    \"\"\"Formatter.\n\n    Attributes\n    ----------\n    use_color: bool\n        Flag to toggle use of color.\n\n    \"\"\"\n\n    def __init__(\n        self,\n        msg: Optional[str] = None,\n        use_color: bool = True,\n    ) -&gt; None:\n        \"\"\"Instantiate.\n\n        Parameters\n        ----------\n        msg: str\n            Message format.\n        use_color: bool\n            Flag to set using colored formatting.\n\n        \"\"\"\n        if msg is None:\n            msg = formatter_message(LOG_FORMAT, True)\n        logging.Formatter.__init__(self, msg)\n        self.use_color = use_color\n\n    def format(self, record: logging.LogRecord) -&gt; str:  # noqa: A003\n        \"\"\"Apply formatting.\n\n        Parameters\n        ----------\n        record: logging.LogRecord\n            Record object for logging.\n\n        Returns\n        -------\n        str\n            Formatted string for log.\n\n        \"\"\"\n        levelname = record.levelname\n        if self.use_color and levelname in COLORS:\n            levelname_color = (\n                COLOR_SEQ % (30 + COLORS[levelname]) + levelname + RESET_SEQ\n            )\n            record.levelname = levelname_color\n        return logging.Formatter.format(self, record)\n</code></pre>"},{"location":"api/#cycquery.utils.log.Formatter.__init__","title":"__init__","text":"<pre><code>__init__(msg=None, use_color=True)\n</code></pre> <p>Instantiate.</p> <p>Parameters:</p> Name Type Description Default <code>msg</code> <code>Optional[str]</code> <p>Message format.</p> <code>None</code> <code>use_color</code> <code>bool</code> <p>Flag to set using colored formatting.</p> <code>True</code> Source code in <code>cycquery/utils/log.py</code> <pre><code>def __init__(\n    self,\n    msg: Optional[str] = None,\n    use_color: bool = True,\n) -&gt; None:\n    \"\"\"Instantiate.\n\n    Parameters\n    ----------\n    msg: str\n        Message format.\n    use_color: bool\n        Flag to set using colored formatting.\n\n    \"\"\"\n    if msg is None:\n        msg = formatter_message(LOG_FORMAT, True)\n    logging.Formatter.__init__(self, msg)\n    self.use_color = use_color\n</code></pre>"},{"location":"api/#cycquery.utils.log.Formatter.format","title":"format","text":"<pre><code>format(record)\n</code></pre> <p>Apply formatting.</p> <p>Parameters:</p> Name Type Description Default <code>record</code> <code>LogRecord</code> <p>Record object for logging.</p> required <p>Returns:</p> Type Description <code>str</code> <p>Formatted string for log.</p> Source code in <code>cycquery/utils/log.py</code> <pre><code>def format(self, record: logging.LogRecord) -&gt; str:  # noqa: A003\n    \"\"\"Apply formatting.\n\n    Parameters\n    ----------\n    record: logging.LogRecord\n        Record object for logging.\n\n    Returns\n    -------\n    str\n        Formatted string for log.\n\n    \"\"\"\n    levelname = record.levelname\n    if self.use_color and levelname in COLORS:\n        levelname_color = (\n            COLOR_SEQ % (30 + COLORS[levelname]) + levelname + RESET_SEQ\n        )\n        record.levelname = levelname_color\n    return logging.Formatter.format(self, record)\n</code></pre>"},{"location":"api/#cycquery.utils.log.formatter_message","title":"formatter_message","text":"<pre><code>formatter_message(message, use_color=True)\n</code></pre> <p>Format message.</p> <p>Parameters:</p> Name Type Description Default <code>message</code> <code>str</code> <p>Message format.</p> required <code>use_color</code> <code>Optional[bool]</code> <p>Use colored logging.</p> <code>True</code> <p>Returns:</p> Type Description <code>str</code> <p>Formatted message.</p> Source code in <code>cycquery/utils/log.py</code> <pre><code>def formatter_message(message: str, use_color: Optional[bool] = True) -&gt; str:\n    \"\"\"Format message.\n\n    Parameters\n    ----------\n    message: str\n        Message format.\n    use_color: bool, optional\n        Use colored logging.\n\n    Returns\n    -------\n    str\n        Formatted message.\n\n    \"\"\"\n    if use_color:\n        message = message.replace(\"$RESET\", RESET_SEQ).replace(\"$BOLD\", BOLD_SEQ)\n    else:\n        message = message.replace(\"$RESET\", \"\").replace(\"$BOLD\", \"\")\n    return message\n</code></pre>"},{"location":"api/#cycquery.utils.log.setup_logging","title":"setup_logging","text":"<pre><code>setup_logging(\n    log_path=None,\n    log_level=\"DEBUG\",\n    print_level=\"INFO\",\n    logger=None,\n    use_color=True,\n)\n</code></pre> <p>Create logger, and set it up.</p> <p>Parameters:</p> Name Type Description Default <code>log_path</code> <code>str</code> <p>Path to output log file.</p> <code>None</code> <code>log_level</code> <code>str</code> <p>Log level for logging, defaults to DEBUG.</p> <code>'DEBUG'</code> <code>print_level</code> <code>str</code> <p>Print level for logging, defaults to INFO.</p> <code>'INFO'</code> <code>logger</code> <code>Logger</code> <p>Pass logger if already exists, else a new logger object is created.</p> <code>None</code> <code>use_color</code> <code>bool</code> <p>Use colored logging.</p> <code>True</code> Source code in <code>cycquery/utils/log.py</code> <pre><code>def setup_logging(\n    log_path: Optional[str] = None,\n    log_level: Union[int, str] = \"DEBUG\",\n    print_level: Union[int, str] = \"INFO\",\n    logger: Optional[logging.Logger] = None,\n    use_color: bool = True,\n) -&gt; None:\n    \"\"\"Create logger, and set it up.\n\n    Parameters\n    ----------\n    log_path : str, optional\n        Path to output log file.\n    log_level : str, optional\n        Log level for logging, defaults to DEBUG.\n    print_level : str, optional\n        Print level for logging, defaults to INFO.\n    logger : logging.Logger, optional\n        Pass logger if already exists, else a new logger object is created.\n    use_color: bool, optional\n        Use colored logging.\n\n    \"\"\"\n    fmt = formatter_message(LOG_FORMAT, use_color)\n    logger = logger if logger else logging.getLogger()\n    logger.setLevel(log_level)\n    logger.handlers = []\n\n    stream_handler = logging.StreamHandler()\n    stream_handler.setFormatter(Formatter(fmt, use_color=use_color))\n    stream_handler.setLevel(print_level)\n    logger.addHandler(stream_handler)\n\n    if log_path:\n        file_handler = logging.FileHandler(log_path)\n        file_handler.setFormatter(Formatter(fmt, use_color=use_color))\n        logger.addHandler(file_handler)\n</code></pre>"},{"location":"api/#cycquery.utils.profile","title":"cycquery.utils.profile","text":"<p>Useful functions for timing, profiling.</p>"},{"location":"api/#cycquery.utils.profile.time_function","title":"time_function","text":"<pre><code>time_function(func)\n</code></pre> <p>Time decorator function.</p> <p>Parameters:</p> Name Type Description Default <code>func</code> <code>Callable[..., Any]</code> <p>Function to apply decorator.</p> required <p>Returns:</p> Type Description <code>Callable</code> <p>Wrapper function to apply as decorator.</p> Source code in <code>cycquery/utils/profile.py</code> <pre><code>def time_function(func: Callable[..., Any]) -&gt; Callable[..., Any]:\n    \"\"\"Time decorator function.\n\n    Parameters\n    ----------\n    func: function\n        Function to apply decorator.\n\n    Returns\n    -------\n    Callable\n        Wrapper function to apply as decorator.\n\n    \"\"\"\n\n    def wrapper_func(*args: List[Any], **kwargs: Dict[str, Any]) -&gt; Any:\n        start_time = time.time()\n        result = func(*args, **kwargs)\n        time_taken = time.time() - start_time\n        LOGGER.info(\"Finished executing function %s in %f s\", func.__name__, time_taken)\n        return result\n\n    return wrapper_func\n</code></pre>"},{"location":"user_guide/","title":"User Guide","text":""},{"location":"user_guide/#installation","title":"Installation","text":""},{"location":"user_guide/#installing-cycquery-using-pip","title":"Installing cycquery using pip","text":"<pre><code>python3 -m pip install cycquery\n</code></pre>"},{"location":"user_guide/#development-setup-using-uv","title":"Development setup using uv","text":"<p>The development environment can be set up using uv. Hence, make sure it is installed and then run:</p> <pre><code>uv sync\nsource .venv/bin/activate\n</code></pre> <p>In order to install dependencies for testing (codestyle, unit tests, integration tests), run:</p> <pre><code>uv sync --dev\nsource .venv/bin/activate\n</code></pre>"},{"location":"user_guide/#documentation","title":"Documentation","text":"<p>This project uses MkDocs with the Material for MkDocs theme.</p> <p>To build the documentation locally, install the documentation dependencies:</p> <pre><code>uv sync --all-extras --group docs\n</code></pre> <p>Then build or serve the docs:</p> <pre><code># Build\nuv run mkdocs build\n\n# Serve locally with live reload at http://127.0.0.1:8000\nuv run mkdocs serve\n</code></pre>"},{"location":"user_guide/#github-pages-setup","title":"GitHub Pages Setup","text":"<p>To serve documentation on GitHub Pages:</p> <ol> <li>Go to your repository's Settings tab</li> <li>Navigate to Pages in the left sidebar</li> <li>Under Source, select Deploy from a branch</li> <li>Choose the gh-pages branch and / (root) folder</li> <li>Click Save</li> </ol> <p>The documentation is automatically built and deployed to GitHub Pages whenever changes are pushed to the main branch (via the <code>.github/workflows/docs.yml</code> workflow).</p>"},{"location":"user_guide/#tutorials","title":"Tutorials","text":"<p>The cycquery tool allows you to query EHR databases using a Python API that communicates with PostgreSQL databases. It is a wrapper around the SQLAlchemy ORM and uses SQLAlchemy query objects and functions to build queries.</p> <p>The following tutorials are available in the tutorials directory of the repository:</p> <ul> <li>MIMIC-III \u2014 Querying the MIMIC-III EHR database</li> <li>MIMIC-IV \u2014 Querying the MIMIC-IV EHR database</li> <li>eICU \u2014 Querying the eICU Collaborative Research Database</li> <li>OMOP \u2014 Querying OMOP CDM databases</li> <li>Gemini \u2014 Querying the Gemini database</li> </ul>"},{"location":"user_guide/#contributing","title":"Contributing","text":"<p>Thanks for your interest in contributing to cycquery!</p> <p>To submit PRs, please fill out the PR template along with the PR. If the PR fixes an issue, don't forget to link the PR to the issue!</p>"},{"location":"user_guide/#pre-commit-hooks","title":"Pre-commit hooks","text":"<p>Once the python virtual environment is set up, you can run pre-commit hooks using:</p> <pre><code>pre-commit run --all-files\n</code></pre>"},{"location":"user_guide/#coding-guidelines","title":"Coding guidelines","text":"<p>For code style, we recommend the Google style guide.</p> <p>Pre-commit hooks apply the black code formatting.</p> <p>For docstrings we use numpy format.</p> <p>We use ruff for further static code analysis. The pre-commit hooks show errors which you need to fix before submitting a PR.</p> <p>We use type hints in our code which is then checked using mypy.</p>"},{"location":"user_guide/#github-actions","title":"GitHub Actions","text":"<p>The repository includes several GitHub Actions workflows:</p> <ul> <li>code checks: Static code analysis, code formatting and unit tests</li> <li>documentation: Project documentation build and deploy</li> <li>integration tests: Integration tests</li> <li>publish: Publishing the python package to PyPI</li> </ul> <p>codecov</p> <p>The codecov tool is subscribed under the free tier which makes it usable only for public open-source repos. Hence, if you would like to develop in a private repo, it is recommended to remove the codecov actions from the github workflow files.</p>"}]}